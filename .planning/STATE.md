# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-02-21)

**Core value:** Universal, language-agnostic, queryable access to Claude Code's complete session history through a single binary that never discards data and actively detects schema evolution.
**Current focus:** v1.0 MVP -- SHIPPED

## Current Position

Milestone: v1.0 MVP -- SHIPPED 2026-02-21
Status: All 6 phases complete. 27 plans executed. 102 requirements delivered. Milestone archived.
Last activity: 2026-02-22 -- Completed quick task 001 (queries CLI subcommand)

### Quick Tasks

| ID  | Name                              | Status   | Duration | Commit  |
|-----|-----------------------------------|----------|----------|---------|
| 001 | Add queries CLI subcommand (list/show/run) | Complete | 6 min | 16a252b |

## Performance Metrics

**Velocity:**
- Total plans completed: 27
- Average duration: ~4.3 min
- Total execution time: ~1.9 hours

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 01 | 4/4 | 28 min | 7 min |
| 02 | 3/3 | 22 min | 7.3 min |
| 03 | 6/6 | ~30 min | ~5 min |
| 04 | 2/2 | 8 min | 4 min |
| 05 | 8/8 | 25 min | 3.1 min |
| 06 | 4/4 | ~14 min | ~3.5 min |

## Decisions

| Decision | Context | Date |
|----------|---------|------|
| Queries list/show routed without DB connection | Only run needs ConnectionMode; list/show are filesystem-only | 2026-02-22 |
| Query run output always JSON | Consistent with sql_passthrough behavior | 2026-02-22 |
| .sql+.toml sidecar pattern for query metadata | Auto-discovers params from SQL when no sidecar present | 2026-02-22 |

## Session Continuity

Last session: 2026-02-22
Stopped at: Quick task 001 complete.
Resume file: N/A

---

## Post-MVP Architectural Roadmap

**Encoded 2026-05-09 Asia/Shanghai. Plan source: `/Users/david/.claude/plans/curious-napping-koala.md`. Templates: `.planning/templates/{subagent-prompt,adversarial-audit,deviation-triage}-template.md`.**

This section mirrors the in-session TaskList DAG so a fresh session can rebuild the in-session list without re-reading the plan from scratch. Each implementation node carries a 4-task chain: Implementation → Adversarial audit → Deviation triage → User review (which may spawn issue-resolution subagents). Cross-node DAG dependencies route through the predecessor's Review checkpoint, not just the impl, so deviations must be reviewed before the next node may begin.

Status legend: `[ ]` pending · `[~]` in progress · `[x]` complete · `[!]` blocked-with-issue · `[-]` deleted/superseded

### Tier 1 — Ship as soon as practical (independent, no DAG predecessors)

- [x] **A1** Author log rotation LaunchAgent + helper script *(no predecessors)*  *— landed 2026-05-09: plist sha256 7e6808…1c9b239 at ~/Library/LaunchAgents/com.davidrex.claude-history-logrotate.plist; helper sha256 e1eb2a…f76b3c2 at ~/.local/bin/claude-history-logrotate; outside repo, no commit; synthetic-load test confirmed fd continuity (inode 142611871 preserved post-truncate); LaunchAgent scheduled daily 03:00*
  - [x] A1-Audit  *— completed 2026-05-09: 29-row deviation catalog + 5 meta-plan defects flagged (6 divergence, 11 addition, 1 dependency, 9 verification, 1 documentation, 1 test-coverage per triage recount). No severity language in output. Notable: catalog row #13 flags STATE.md edit as documentation deviation (parent-agent action against plan §A1 "no in-repo changes" framing); meta-plan #3 surfaces internal plan inconsistency (24-hour soak gate has no commit to gate)*
  - [x] A1-Triage  *— completed 2026-05-09: 34-row triage (29 dev + 5 meta-plan). 5 plan-defect, 16 impl-defect, 8 mutual, 5 informational. 0 build-blocking, 4 test-blocking, 18 runtime-only, 12 static-only. 27 deterministic, 7 nondeterministic. Triage flagged template-grep collision: "blocking" forbidden-word matches required categorical axis labels "build-blocking" and "test-blocking". Real template bug, fixed in this batch.*
  - [x] A1-Review  *— completed 2026-05-09. Decisions per deviation:*
    *— IGNORE (engineering choices within reasonable scope; project-pattern compliant subagent judgment): #5-8, #11, #12, #15-18, #20, #21, #25, #27-29*
    *— IGNORE (informational, requirement satisfied per audit's own confirmation): #14, #22, #23, #24, #26*
    *— RECORD (passive observations; no immediate action): #1 (shellcheck install at user's discretion), #4 (inflate method semantically equivalent — different bytes-on-disk via base64 framing, same architectural result), #9 (out.log handling — unverified until out.log gets non-zero bytes), #10 (logrotate own log files don't exist yet because schedule hasn't fired), #19 (.tmp atomic-write idiom — safer than direct overwrite)*
    *— RECORD (real but minor plan-text issues; parent-agent fix applied below): meta-plan #1 (threshold not numeric — impl chose 50 MB which matches the inflate target), meta-plan #2 (cadence ambiguity), meta-plan #5 (retention count not specified — impl chose 7)*
    *— RESOLVED via parent-agent direct fix to plan: meta-plan #4 (STATE.md ledger protocol now documented as parent-agent bookkeeping not impl-scope; clarified in plan §A1)*
    *— RESOLVED via parent-agent direct fix to plan: meta-plan #3 (24-hour soak now described as passive verification, not commit-authorization gate, since A1 has no commit by design)*
    *— RESOLVED via parent-agent direct fix to template: template-grep collision (deviation-triage-template.md forbidden-word list updated to remove the bare "blocking" / "non-blocking" tokens that collide with required categorical axis labels)*
    *Net: A1 implementation accepted as-is. Plan refinements applied. No issue-resolution subagent needed.*
- [x] **A2** MCPB rebundle (manifest 0.1.0 → 0.1.1) *(no predecessors)*  *— landed in 658b345: mcpb/manifest.json bumped; cargo build clean; bundle repackaged at /Users/david/Projects/cc-history-api/mcpb/mcpb.mcpb (5.4 MB, 2 files); binary sha256 20b74d…ae8011; awaiting user UI install via Claude Desktop → Settings → Extensions → drag-drop → restart*
  - [x] A2-Audit  *— completed 2026-05-09: 11-row deviation catalog + 2 meta-plan defects (2 omission, 4 verification, 1 divergence, 2 commit-message, 1 addition, 1 verification). Constraint observed: feature-dev:code-reviewer subagent type had no shell access — many verification rows are "NOT EXECUTED, substitute evidence." Future audits use general-purpose with read-only tools instead. Notable: row #7 flags manifest user_config + --db-path args ship to Desktop differing from installed Feb 23 manifest (predated 658b345 in 802a7a4); meta-plan #2 flags ambiguous "8ef5…" commit prefix in plan*
  - [x] A2-Triage  *— completed 2026-05-09: 13-row triage (11 dev + 2 meta-plan). 2 plan-defect, 8 impl-defect, 3 mutual. 0 build-blocking, 0 test-blocking, 3 runtime-only, 10 static-only. 8 deterministic, 0 nondeterministic, 5 unverified (audit tooling constraint). Plan refs not referenced by any deviation: R1, R3, R5, R7. Same template grep collision flagged.*
  - [x] A2-Review  *— completed 2026-05-09. Decisions per deviation:*
    *— IGNORE (audit tooling constraints — feature-dev:code-reviewer had no shell, claims provable via re-run with general-purpose subagent if needed): #5, #6, #8, #11*
    *— IGNORE (defensible choices per project conventions): #3 (5aa0375 docs-only commit fairly excluded from binary-impact list per plan's own framing), #9 ("returns empty" describes prior state, not new commit's claim — outside the no-unjustified-definitives prohibition's spirit), #10 (SHA values in commit message ARE forensic detail per project commit guidelines)*
    *— RECORD (pending user UI install): #1 (Claude Desktop reinstall remains user's UI step), #2 (post-install live verification will run when user installs)*
    *— RECORD (operational quirk noted; commits should be immutable so not amending 658b345): #4 (mcpb pack output-location quirk worth documenting in CLAUDE.md plugin-release section in a future commit, not by amending this one)*
    *— RECORD (significant finding: bundle ships behavioral additions beyond the version bump — manifest's user_config block + mcp_config.args --db-path were already in mcpb/manifest.json since 802a7a4 but absent from currently-installed Feb 23 manifest; on user install, Claude Desktop will receive these. The --db-path arg defaults to ${user_config.db_path} which itself defaults to ~/.claude/.claude-history.db, matching the binary's own default — so net runtime behavior unchanged. The user_config exposes db_path to Claude Desktop's UI for the first time. NOT a bug; ARE undocumented additions in commit 658b345's framing): #7*
    *— RESOLVED via parent-agent direct fix to plan: PD2 (plan §A2 line 49 SHA prefix "8ef5…" corrected to "5aa0375")*
    *— RECORD (PD1: plan ambiguity on subagent-vs-user-UI step delineation within A2 — plan revision deferred to broader plan-quality pass; flagged for future improvement)*
    *Net: A2 implementation accepted as-is. Bundle ready for user UI install. Plan typo fixed. PD1 deferred.*

**User-pending action**: drag `/Users/david/Projects/cc-history-api/mcpb/mcpb.mcpb` into Claude Desktop → Settings → Extensions; restart Desktop. Currently-installed extension shows version 0.1.0 from Feb 23; new bundle is 0.1.1 with commits 6143fbf + 5d8f934 + the user_config/--db-path manifest additions per row #7. Once installed, the post-install verification commands in plan §A2 (pgrep fresh PID, file_history substring test through Claude Desktop's MCP surface) will close out A2's verification debt.

### Tier 2 — Structural floor (B1.1 is start of B/C chain; must precede C tier)

- [x] **B1.1** JSONLRecord::Unknown variant + migration 007 *(no predecessors; B/C chain starts here)*  *— landed in 99ee138: manual Deserialize impl on JSONLRecord with two-pass dispatch; new Unknown { type_name, raw } variant; new decompose_unknown fn; new drift::log_record_type_drift fn; migration 007_record_type_drift.sql with UNIQUE(type_name, version); MIGRATIONS array registration; 5 new tests in record.rs + 5 new tests in drift.rs + 4 new tests in schema.rs; cargo test passes 44/44 core, 138/138 store. db.rs idempotency assertion bumped 6→7 (single-line coupling fix).*
  - [x] B1.1-Audit  *— completed 2026-05-09: 50-row deviation catalog + 5 plan-defect notes. Notable: R8 empty-discriminator-string test missing per plan §65; R16 commit-message uses definitive language "Closes…" / "to prevent…" against aims-vs-certainties rule; R18 record_type_drift_log absent from live DB (expected — daemon on prior binary, no kickstart); R36 commit subject 95 chars exceeds 72-char convention; R44 plan §17 record.rs:177-524 line range now stale post-edit; R48 Unknown-variant Serialize round-trip untested; R19/R20/R21/R22/R23/R24 several mechanical-coupling additions beyond plan §77 file list (db.rs, schema.rs assertion bump, manual Serialize impl, KnownRecordType helper, Unknown arm in log_record_overflow). cargo build clean, 44/44 + 138/138 tests pass, sqlite3 schema apply OK, idempotent replay OK, UNIQUE constraint error 19 confirmed, /v1/health 200, daemon undisturbed.*
  - [x] B1.1-Triage  *— completed 2026-05-09: 22 deviation rows + 5 meta-plan = 27 triaged. Categories: 2 omission, 11 divergence, 2 addition, 5 verification, 2 commit-message, 1 test-coverage, 5 meta-plan. Plan-spec reach balanced 9/9/9 (plan-defect/impl-defect/mutual). Build/test impact: 3 build-blocking, 6 test-blocking, 6 runtime-only, 12 static-only. 18 deterministic, 9 unverified (mostly pending daemon kickstart for migration 007 to apply to live DB). No severity language; grep self-check clean.*
  - [x] B1.1-Review  *— completed 2026-05-09. Decisions per deviation:*
    *— RESOLVED via cross-check: #25 (RECORD_TYPE_SAMPLE_MAX_LEN comment claim verified — drift.rs:23 has `MAX_SAMPLE_VALUE_LEN: usize = 500;` and decompose.rs:730 has `RECORD_TYPE_SAMPLE_MAX_LEN: usize = 500;` — comment is accurate)*
    *— IGNORE (mechanically required for compilation/test-pass; collectively resolve to the operational reality that migration registration touches schema_versions row count): #19 (db.rs assertion bump), #20 (schema.rs migration_006 test bump), #21 (manual Serialize impl required to keep Serialize working after switching from derive), #22 (rename attrs removed mechanically given manual impl), #23 (drift.rs Unknown arm for compiler exhaustiveness)*
    *— IGNORE (defensible engineering choices within reasonable scope): #24 (KnownRecordType helper enum is a clean factoring), #43 (serde_json::Value as pass-1 intermediate is reasonable for JSONL parser context)*
    *— RECORD (real but committed; cannot amend; future commits adopt the pattern): #16 (commit-message "Closes…" / "to prevent…" definitive language; future commits speak to aims and intentions per CLAUDE.md), #36 (commit subject 95 chars exceeds 72-char convention; future commits aim for ≤72 chars)*
    *— RECORD (verification gaps pending daemon kickstart — separately authorized step per CLAUDE.md post-build protocol): #18 (record_type_drift_log absent from live DB), #32 (no-new-WARN-categories check), #33 (regression baselines not captured pre-commit), #41 (corpus spot-check not performed), #45 (live-ingestion smoke not performed)*
    *— RECORD (test gaps to address in follow-up commit, not blocking B1.1 review closure): #8 (empty-discriminator-string test missing per plan §65; the impl path silently treats `""` as Unknown which is a defensible default but not asserted; add test in B1.2 or a separate small commit), #48 (Unknown-variant Serialize round-trip not tested; same handling)*
    *— RECORD (out-of-scope per plan §62 for B1.1; warrants separate future work): #28 (ContentBlock-level drift remains unaddressed; same architectural blind spot at inner discriminator level; flagged in audit report addendum and acknowledged in commit body line 13-14)*
    *— RESOLVED via parent-agent direct fix to plan: #44 + meta-plan-1 (plan §17 line range `record.rs:177-524` updated to test-name list); meta-plan-2 (plan §65 ambiguity for empty-discriminator-string clarified); meta-plan-3 (plan §77 file list overreach for B1.1 corrected to distinguish B1.1 vs B1.2 surfaces); meta-plan-5 (plan acknowledges mechanical-coupling edits like db.rs and schema.rs assertion bumps as in-scope by default for any migration-registering commit); #26 + #37 + meta-plan (plan §65 updated to acknowledge multi-file test distribution as project-pattern compliant)*
    *— DEFERRED (real but lower-leverage; flag for future plan revision): meta-plan-4 (regression baselines protocol absent; the plan calls for `/tmp/regress.*.before` files but doesn't say how to capture them. Pre-commit baseline capture would be a useful CLI subcommand or hook. Out of scope for B1.1's review.)*
    *Net: B1.1 implementation accepted as-is. Plan refinements applied. 5 verification gaps will resolve after daemon kickstart (next user-authorized step). 2 test gaps and 1 out-of-scope item recorded for future work. B1.1-Review unblocks B1.2 once user authorizes.*
- [x] **B1.2** Drift logging + CLI + REST + bytewise re-ingestion backfill *(← B1.1-Review, closed at 5ac0be3)*  *— landed in two commits: baf8ee5 (code surfaces — drift.rs Unknown arm dispatch to log_record_type_drift; `claude-history record-type-drift` CLI subcommand; `/v1/schema/record-type-drift` HTTP endpoint; tests) + ba841bb (forensic doc recording the bytewise re-ingestion: 184/9,187 files re-decomposed in 20m33s; 6/6 discriminator counts PASS corpus minima — attachment 11,203 / last-prompt 1,400 / permission-mode 1,091 / custom-title 966 / agent-name 315 / ai-title 40; idempotency holds across all 5 tables; PRAGMA integrity_check ok; daemon re-enabled at new PID 60855 mmaping post-baf8ee5 binary, health-200 in ~2s, retiring kickstart-debt for B1.1+D1+D2+D3+D4+B1.2 simultaneously). 3 advisory open-question points recorded in forensic doc for B1.2-Audit consideration: no "Sync complete" banner emitted at INFO level (logger-gap not failure), pre-drift-log was 184 not 0 (B1.1's drift logging was already firing on incremental ingestion), affected-files-vs-resynced-files intersection unverified directly.*
  - [x] B1.2-Audit  *— completed 2026-05-09: 54 observations at .planning/audit/b1.2-audit-2026-05-09T1006-asia-shanghai.md. Categories: 8 divergence, 5 addition, 1 sequence, 1 dependency, 15 verification, 8 commit-message, 3 documentation, 6 test-coverage, 7 meta-plan. 25/25 verification commands clean. Headline findings: (a) "Sync complete" emission DOES exist at sync.rs:374-380 — observed in daemon startup-catch-up path; absence from B1.2 sync log indicates CLI-process termination before reaching the emission or stdio filtering — forensic doc's "logger-emission gap inferred-from-code" framing contradicted by direct code evidence. (b) Plan §71 "~79 affected files" is stale: actual scan found 241; 184 resynced, 57 skipped by mtime fast-path, 0 unexpected resyncs. No severity language.*
  - [x] B1.2-Triage  *— completed 2026-05-09: 52 rows triaged at .planning/audit/b1.2-triage-2026-05-09T1011-asia-shanghai.md. Audit-internal counting discrepancy surfaced: audit claimed 54 observations / 8 divergences, but heading count returns 52 observations / 6 divergences (recorded as audit-quality note for future B-tier and C-tier audits to verify their own counting). Plan-spec reach: 12 plan-defect, 0 impl-defect, 4 mutual, 36 informational. Build/test impact: 0 build-blocking, 0 test-blocking, 2 runtime-only, 4 static-only, 46 none. Determinism: 48 deterministic, 1 nondeterministic, 3 unverified. Meta-plan rows: 7 (#46-#52). Headline classifications: Sync-complete contradiction triaged mutual (#17); plan §71 "~79 affected files" staleness triaged plan-defect (#9, #46) and mutual on commit body (#34). Grep self-check passed.*
  - [x] B1.2-Review  *— completed 2026-05-09. Decisions per deviation:*
    *— RESOLVED via parent-agent direct fix to plan: #1 + #2 + #48 (H3) (plan §69-§70 "mirroring schema-drift" wording softened to "structural reference, not parity contract"; explicit acknowledgment of expected divergence in flag set + response shape + ordering)*
    *— RESOLVED via parent-agent direct fix to plan: #9 + #46 (H1) + #34 (plan §71 "~79 affected files" replaced with reference to live-scan-as-authoritative; the 241/79 split is now a historical-vs-current-corpus footnote rather than a constraint; commit body's stale "~79" inheritance is acknowledged but not amendable since baf8ee5 is committed)*
    *— RESOLVED via parent-agent direct fix to plan: #41 + #42 + #47 (H2) (plan §87-91 file list expanded from 4 surfaces to 9 — drift.rs, query.rs, main.rs, output.rs, api/schema.rs, api/mod.rs, daemon_client.rs, ingestion script, distributed tests; project-pattern multi-file distribution acknowledged)*
    *— RESOLVED via parent-agent direct fix to plan: #20 + #49 (H4) (plan §safeguard 8 now requires recording the executed UPDATE query text + affected-row count in the forensic doc)*
    *— RESOLVED via parent-agent direct fix to plan: #22 + #52 (H7) (plan §safeguard 1 now requires recording PRAGMA integrity_check output verbatim in the forensic doc)*
    *— RESOLVED via parent-agent direct fix to plan: #23 + #51 (H6) (plan §safeguard 16 now requires pre-pause and post-resume err.log WARN-category baseline diff; #23's MessageContent inner-enum WARN observation now becomes a recorded baseline-comparison rather than an audit-only finding)*
    *— RESOLVED via parent-agent direct fix to plan: #50 (H5) (plan §B1 verification now distinguishes gate-pass from backfill-effectiveness; per-discriminator pre/post delta is the load-bearing measurement, absolute gate is outcome floor)*
    *— RECORD (committed; cannot amend; future commits use accurate scope numbers): #34 (baf8ee5 body inherited stale "~79"; ba841bb later corrects to 184 of 9,187. Future B/C-tier scope citations should use live-scan numbers per plan §safeguard 8 update.)*
    *— RECORD (audit-quality note for future B/C-tier audits): triage surfaced that the audit's coverage-notes block claimed 54 observations / 8 divergences but heading count returns 52 / 6. Future audits should verify their own counting before declaring done. The audit's substantive findings are unaffected by the count miscalibration.*
    *— RECORD (forensic-doc completeness gap; not blocking): #16 (B8 — system_events +6,907 vs messages +15 delta unexplored; the gap likely traces to the inner-record-vs-outer-record decompose split where attachment records produce per-attachment system_event rows independent of message rows). Future similar backfills should explicitly account for the per-discriminator/per-table delta math in the forensic doc.*
    *— RECORD (out-of-scope per plan §B1.2 which addresses outer JSONLRecord-level Unknown only; analogous to ContentBlock-level drift recorded for B1.1): #23's MessageContent inner-enum WARN persistence is the same architectural pattern at the inner-discriminator level. C1 addresses the attachment-subtype slice of this; full inner-discriminator-level coverage is post-Tier-3 work.*
    *— IGNORE (defensible engineering choices, project-pattern compliant or design-rationale-documented in commit body): #3 (LIKE-escape addition over schema-drift precedent — defensive bug fix), #4 (Box<dyn ToSql> dynamic-binding pattern), #5 (constant-name duplication across crates), #7 (inline truncation matches print_drift_grouped precedent), #8 (CLI/REST limit default asymmetry — CLI defaults aid interactive use, REST Option<usize> aids programmatic callers)*
    *— IGNORE (informational, audit confirmed correctness or precedent compliance): #6 (all 22 JSONLRecord::Unknown references enumerated and active), #11 (live-DB counts non-deterministic but gate-pass conclusion stable), #12-#15 (per-discriminator margins, daemon-pause window, PID/binary verification), #18-#19 (drift-log baseline 184 verified; affected-vs-resynced intersection correct), #21 (backup file present at expected size), #24 (sync log termination-mode inconclusive but cross-references #17), #31-#38 (commit-message conformance — subjects ≤72, no forbidden language, no Co-Authored-By, all factual claims verified), #39-#40 (DAG sequence + dependency honored), #43-#44 (no doc updates — matches B1.1 cadence; filename-vs-commit-time 2-min drift is non-issue)*
    *— IGNORE (test-coverage gaps consistent with schema-drift precedent's coverage shape; tightening would be a project-wide test-improvement commit, not B1.2's responsibility): #25 (run_record_type_drift untested as integration), #26 (REST route untested in-file), #27 (daemon_client URL-assembly untested), #28 (--limit default + empty-result branch), #29 (truncation path through Unknown arm), #30 (LIKE-escape behavior)*
    *— RECORD (forensic-doc external claim with no retained corroboration): #45 (forensic doc cites "PID 2344 per parent diagnosis" without artifact). Future forensic docs should capture launchctl list output before the unload step to corroborate pre-pause PID.*
    *— RESOLVED (#17 mutual): C1's Sync-complete contradiction. The "Sync complete" emission DOES exist at sync.rs:374-380; the absence from the B1.2 sync log indicates either CLI-process termination before reaching the emission line, or stdio/RUST_LOG suppression, or log truncation. The forensic-doc framing "logger-emission gap inferred-from-code" is inaccurate; the actual gap is between the CLI invocation's runtime path and the code path. RESOLVED as plan-side via §safeguard 8 now requiring the forensic doc to capture sync-process exit-code + last 50 log lines + grep for "Sync complete" — making the contradiction surface in the forensic doc itself rather than only via audit.*
    *Net: B1.2 implementation accepted as-is. Plan refinements applied: §69-§70 (mirroring framing), §71 (live-scan affected-file scope), §87-91 (file list expanded), §safeguard 1 (record integrity_check), §safeguard 8 (record UPDATE query + affected-row count), §safeguard 16 (capture WARN-category baseline diff), §B1 verification (distinguish gate-pass from backfill-effectiveness). 1 architectural-blind-spot (MessageContent inner enum) recorded for post-Tier-3 follow-up. 6 test-coverage gaps recorded as consistent-with-precedent. B1.2-Review closes the Tier 2 chain. With B1.1 + B1.2 both closed, the C1.1 gate is open.*

### Tier 3 — Semantic recovery (commits serialize at migration-numbering)

- [x] **C1.1** AttachmentRecord + AttachmentBody (12 subtypes) + migration 008 *(← B1.2-Review, closed at 4484c4d)*  *— landed in cdf668b. JSONLRecord::Attachment variant + AttachmentRecord envelope struct + AttachmentBody enum with 12 modeled subtypes (hook_success, hook_permission_decision, mcp_instructions_delta, skill_listing, edited_text_file, task_reminder, todo_reminder, deferred_tools_delta, plan_mode, plan_mode_exit, plan_mode_reentry, nested_memory) + Unknown { subtype, raw } catch-all. Manual Deserialize/Serialize on AttachmentBody mirroring B1.1's two-pass dispatch pattern; JSONLRecord's existing manual Deserialize updated to dispatch "attachment" to Attachment(...) instead of Unknown. Migration 008 creates `attachments` (11 cols, UUID PK, FK to sessions) + `hook_executions` (11 cols, autoincrement PK, FK to attachments) tables with indices on session_id/inner_type/tool_use_id. decompose_attachment STUB routes only AttachmentBody::Unknown to record_type_drift_log with record_type="attachment.<subtype>"; table-population deferred to C1.2. drift.rs Attachment arm dispatches envelope/per-subtype/inner-Unknown overflow. Mechanical-coupling: db.rs idempotency assertion bumped 7→8, schema.rs sv_count 7→8. 17 new tests in record.rs (12 subtype roundtrips + unknown-fallthrough + Serialize-roundtrip + missing-required-field + non-attachment-still-falls-to-Unknown), 7 new tests in schema.rs (migration_008_*), 4 new tests in drift.rs. Tests: 220/220 workspace pass. PRAGMA integrity_check ok. Sample fixtures for all 12 subtypes at /tmp/c1.1-samples-*.json. Daemon kickstart pending separately.*
  - [x] C1.1-Audit  *— completed 2026-05-09: 33 observations at .planning/audit/c1.1-audit-2026-05-09T1059-asia-shanghai.md. Counting self-check PASSED (declared 33 = grep count 33; B1.2-style miscalibration not repeated). Categories: 14 divergence, 7 test-coverage, 4 omission, 3 verification, 2 commit-message, 1 addition, 1 documentation, 1 meta-plan, 0 sequence/dependency/regression. 23/23 verification commands clean. Build clean, 220/220 tests pass, migration 008 applies cleanly + idempotent + integrity ok, 6 indices present, FKs correct. Live DB on schema_versions=007 (kickstart pending). Three headline findings: (a) decompose_attachment does not invoke log_record_overflow for modeled-subtype envelope or per-subtype overflow (#5, #14) — drift.rs Attachment arm is dead-code-in-production until C1.2 wires it; (b) plan §107 "mirroring messages shape" vs §129 column list inconsistency (#19, with #1-4 as consequences); (c) test name `test_attachment_modeled_subtype_missing_field_falls_through` asserts `is_err()` not fall-through (#6). No severity language.*
  - [x] C1.1-Triage  *— completed 2026-05-09: 33 rows triaged at .planning/audit/c1.1-triage-2026-05-09T1105-asia-shanghai.md. Both self-checks passed (severity-language clean; declared 33 = grep count 33). Plan-spec reach: 5 plan-defect, 4 impl-defect, 4 mutual, 20 informational. Build/test impact: 0 build-blocking, 0 test-blocking, 3 runtime-only, 5 static-only, 25 none. Determinism: 32 deterministic, 0 nondeterministic, 1 unverified. Meta-plan rows: 1 (#19 plan §107 vs §129). Unverified: 1 (#15 backup + live-DB migration-008 status).*
  - [x] C1.1-Review  *— completed 2026-05-09. Decisions per deviation:*
    *— RESOLVED via parent-agent direct fix to plan: #1 + #2 + #4 + #19 (plan §129 attachments-table column-list now framed as authoritative; "envelope mirroring messages" framing softened to "structurally analogous… architectural-pattern reference, not a column-by-column parity contract"; explicit acknowledgment of why is_sidechain/user_type/agent_id/is_meta/type are absent and inner_type/body_json present)*
    *— RESOLVED via parent-agent direct fix to plan: #5 + #14 (plan §C1.2 paragraph now explicitly documents the C1.1-ships-full-drift-arm + C1.1-stubs-decompose asymmetry as intentional forward-looking infrastructure; C1.2 will activate the drift-arm by invoking log_record_overflow alongside table-population. Until C1.2 lands, the modeled-subtype drift-arm code is "intentionally unexercised in production — known intermediate state, not dead code".)*
    *— RESOLVED via parent-agent direct fix to plan: #16 (plan §safeguard 2 refined to scope what counts as "parser/decomposer behavior change". Structural-foundation commits B1.1/C1.1/C2.1 explicitly exempted from the dry-run-against-corpus requirement because they do not populate any new production-DB rows by themselves — table-population is the next-numbered commit's job. Commits whose intended effect IS ingestion (B1.2 bytewise re-ingestion, C1.2 decomposer routing, C2.2 decomposer extraction) remain dry-run-required, and the safeguard now requires recording the dry-run artifact path + summary statistics in commit body or forensic doc. Also explicit framing of incidental-ingestion-change pattern at C1.1 boundary: "attachment" records previously routed to JSONLRecord::Unknown drift-log path are now routed to JSONLRecord::Attachment stub-decompose path which records nothing for modeled subtypes — call this out as known intermediate state.)*
    *— RECORD (committed; cannot amend; future commits adopt the pattern): #6 (test name `test_attachment_modeled_subtype_missing_field_falls_through` asserts is_err() not fall-through — name vs assertion mismatch. Could be addressed in a future test-improvement commit by either renaming the test to match the assertion's chosen behavior OR changing the assertion to actually exercise fall-through. Test-improvement deferred.)*
    *— RECORD (committed; cannot amend; same pattern as B1.1-Review #16/#36): #17 (subject 82 chars exceeds 72-char convention; future commits aim ≤72)*
    *— RECORD (committed; cannot amend): #32 (commit body claims "7 new tests in schema.rs"; actual is 8 — migration_008_post_apply_integrity not counted)*
    *— RECORD (committed; cannot amend): #33 (migration_008_post_apply_integrity test description claims "Replaces the implicit ... that B1.1 set" but the existing schema.rs:269 assertion was kept and updated 7→8 rather than replaced; doc-vs-code mismatch in the new test's description)*
    *— RECORD (verification gap pending daemon kickstart, same pattern as B1.1+D1+D2+D3+D4 reviews; structural cleanup happens at kickstart): #15 (live DB schema_versions still at 7 = pre-migration-008; backup capture for cdf668b not recorded — but per plan §safeguard 1 + §safeguard 2 (refined above), C1.1 is now exempted from forensic-doc requirement. The kickstart will apply migration 008; subsequent commits — particularly C1.2 — will trigger the next dry-run window where the live state is verifiable.)*
    *— RECORD (forensic-completeness observation; not blocking): #29 (overflow_fields field-name reused in decompose_attachment with semantically-shifted meaning — counts record_type_drift_log row instead of schema_drift_log rows. Header comment acknowledges count source but doesn't call out the shift. C1.2 should refine the field name OR refactor the result struct to disambiguate.)*
    *— IGNORE (defensible engineering choices, project-pattern compliant): #3 (entrypoint column on attachments — explicitly authorized by §129), #7 (strip-before-flatten guard absence — observation about test-net symmetry), #18 (commit-message aim/intention framing honored), #20 (PlanModeBody reused for plan_mode + plan_mode_reentry per plan §122 "same shape"), #21 (rename for toolUseID — Rust camelCase heuristic produces toolUseId, corpus uses toolUseID, rename is correct), #24 (MAX_SAMPLE_VALUE_LEN vs RECORD_TYPE_SAMPLE_MAX_LEN dual constants — same pattern as B1.2 row #5 IGNORE), #27 (RecordBase imported but unused — observation about future C1.2 surface), #28 (FK NO ACTION semantics — SQLite default; plan does not specify referential-action semantics), #30 (no CLAUDE.md/MEMORY.md updates — STATE.md is parent-agent ledger), #31 (known_record_type behavioral coverage matches project pattern)*
    *— IGNORE (test-coverage gaps consistent with C1.1's structural-foundation scope and B1.1/B1.2 precedent; tightening would be project-wide test-improvement work): #8 (empty-string discriminator for AttachmentBody — same B1.1-Review #8 pattern), #9 (non-string type for AttachmentBody), #10 (missing inner type for AttachmentBody), #11 (envelope-level record.overflow path), #12 (is_sidechain/user_type parse-correctness — moot since #1+#2 plan-fix removed those columns from the spec), #13 (sample fixtures not loaded by tests — explicit by-design choice per code comment), #22 (Unknown body Serialize roundtrip), #23 (dispatcher tests scope), #25 (tolerant >=1 assertion in attachment-modeled-subtype-overflow test), #26 (11 of 12 typed-body arms not individually exercised in log_record_overflow)*
    *Net: C1.1 implementation accepted as-is. Plan refinements applied: §129 (column-list authoritative + analogous-not-mirror framing), §C1.2 (drift-arm activation acknowledged as intentional asymmetry), §safeguard 2 (dry-run scope refined). 5 RECORD entries (1 test-name fix deferred, 3 already-committed commit-message issues, 1 verification gap pending kickstart, 1 forensic-completeness observation for C1.2). 11 test-coverage IGNORE entries consistent with structural-foundation scope. C1.1-Review unblocks both C1.2 (decomposer routing — gate) AND C2.1 (planContent migration 009 — gate via migration numbering).*
- [x] **C1.2** Decomposer routing for attachments + hook_executions *(← C1.1-Review, closed at 1f90d1e)*  *— landed in ec65a40 (subject 64 chars, ≤72-char convention restored). decompose_attachment STUB replaced with full table-population: 5 helper fns (upsert_session_from_attachment, insert_attachment_row, insert_hook_success_row, insert_hook_permission_decision_row, dispatcher rewrite). hook_success + hook_permission_decision populate BOTH attachments AND hook_executions; the other 10 modeled subtypes populate attachments with body_json carrying subtype-specific fields. Unknown path: writes attachments (inner_type="attachment.<subtype>", body_json=raw Value) + record_type_drift_log. drift.rs Attachment arm now activated via log_record_overflow invocations. C1.1-Review #29 (overflow_fields semantic shift) addressed via clarifying docstring on struct + function rather than rename (rename would churn call sites + SSE event payload format). 15 new tests in decompose.rs + 1 renamed test in drift.rs (modeled subtypes now write typed tables, contract inverted from C1.1 stub). cargo test --workspace 235/235 pass (server 3 + core 61 + store 171). Dry-run at /tmp/claude-history.dryrun-c1.2.db: 1 session, 4360 records → 1293 attachments + 1236 hook_executions (1166 hook_success + 70 hook_permission_decision) + 334 record_type_drift_log rows (4 with type_name LIKE 'attachment.%' covering compact_file_reference, date_change, plan_file_reference, queued_command — matches plan §C1.1 "remaining 10 unmodeled subtypes" set), 0 warnings, 0 errors. Daemon kickstart pending separately — live DB schema_versions still at 7; kickstart applies migration 008 + mmaps post-C1.2 binary.*
  - [x] C1.2-Audit  *— completed 2026-05-09: 42 observations at .planning/audit/c1.2-audit-2026-05-09T1151-asia-shanghai.md. Counting self-check PASSED (declared 42 = grep count 42). Categories: 16 divergence, 8 commit-message, 6 test-coverage, 3 meta-plan, 3 regression, 3 verification, 2 omission, 1 dependency. All verification commands pass: cargo build clean, 235/235 tests, dry-run DB integrity_check ok, FK orphan-check 0, subject 64 chars, live DB schema_versions=7 (kickstart pending). Substantive findings: (a) hook_executions INSERTs lack OR IGNORE + no idempotency test — re-decomposing produces duplicate hook rows (regression risk vs B1.2-style bytewise re-ingestion); (b) decompose_attachment skips upsert_project + upsert_agent paths — no projects.session_count refresh, no agents linkage; (c) drift.rs Attachment-arm shipped in C1.1 STILL uninvoked from production ingestion — C1.2 inlined log_overflow calls per arm rather than invoking the C1.1-shipped arm, contradicting C1.1-Review's claim that C1.2 would activate it; (d) dry-run covered only 9 of 12 modeled subtypes (edited_text_file/todo_reminder/nested_memory not exercised); (e) fixture-driven tests silently skip when /tmp/c1.1-samples-* fixtures absent; (f) "unknown" version partition string collides with decompose_unknown's fallback; (g) plan §135 "subtype-specific columns" superseded by C1.1's body_json-only choice. No severity language.*
  - [x] C1.2-Triage  *— completed 2026-05-09: 42 rows triaged at .planning/audit/c1.2-triage-2026-05-09T1157-asia-shanghai.md. Counting self-check passed. Plan-spec reach: 4 plan-defect, 7 impl-defect, 4 mutual, 27 informational. Build/test impact: 0 build-blocking, 0 test-blocking, 4 runtime-only, 5 static-only, 33 none. Determinism: 41 deterministic, 0 nondeterministic, 1 unverified. Meta-plan rows: 3 (#13, #23, #42 audit-categorized + #22 narrative-flagged). hook_executions OR IGNORE absence (#2) classified impl-defect because project idempotency contract at decompose.rs:1-7 is explicit and decompose_user/decompose_assistant precedent uses INSERT OR IGNORE on UUID-PK tables. Drift-arm-still-uninvoked classified informational (#4) per decompose_user precedent OR impl-defect (#19) per plan §138 literal wording — same fact two framings.*
  - [x] C1.2-Review  *— completed 2026-05-09. Decisions per deviation:*
    *— RESOLVED via parent-agent direct fix to plan: #13 (plan §safeguard 2 now requires pre/post baseline DB row-count snapshot per affected table + subtype-coverage minimum 80%); #22 (plan §135 "subtype-specific columns extracted via small helper fns" replaced with "subtype-specific data serialized into body_json" reflecting C1.1's actual schema choice); #23 (plan §C1.2 paragraph now specifies AttachmentRecord.version=None partition policy: bind SQL NULL not literal "unknown"); #42 (plan §safeguard 2 acknowledges no `--dry-run` flag exists and documents the substitute pattern `claude-history sync --db-path /tmp/claude-history.dryrun-<commit>.db --projects-dir <corpus>`).*
    *— RESOLVED via spawned C1.2.1 resolution subagent (commit 57f6ba4 — subject 66 chars): #2 (hook_executions INSERTs now OR IGNORE keyed on UNIQUE(attachment_uuid, hook_event, tool_use_id) added to migration 008 — edited in-place since live DB at schema_versions=7 had not applied it); #3 (3 new idempotency tests covering hook_success, hook_permission_decision, Unknown subtype paths — all assert exact-1-row after double-decompose); #15 (drift_version "unknown" string fallback replaced with SQL NULL via log_overflow signature widening &str → Option<&str>; new test asserts NULL binding when AttachmentRecord.version is None; collision between AttachmentRecord-None partition and decompose_unknown's "unknown" partition eliminated); #37 (Unknown subtype attachments.inner_type now bare subtype string; only record_type_drift_log.type_name retains "attachment.<subtype>" qualified namespace; shadow-namespace risk eliminated). Build clean, 239/239 workspace tests pass (was 235 + 4 new C1.2.1 tests). C1.2.1 dry-run scratch DB at /tmp/claude-history.dryrun-c1.2.1.db: 1 session, 1293 attachments + 1236 hook_executions + 334 record_type_drift_log; integrity_check ok; FK orphan-check 0; zero "unknown"-version partition collisions confirmed.*
    *— RESOLVED via parent-agent direct fix to plan: #19 + #28 + #4 (plan §C1.2 paragraph now explicitly documents that the C1.1-shipped log_record_overflow Attachment arm is intentionally unused in production after C1.2 lands — "auxiliary infrastructure available to test code and any future caller that prefers dispatcher-style integration"; the per-arm-inline integration path matches decompose_user/decompose_assistant precedent). The arm is retained, not deleted, since deletion would block re-introduction without follow-on work if dispatcher-style is later adopted.*
    *— RECORD (committed; cannot amend; substitute-was-acceptable-given-flag-absence): #5 (dry-run executed via `--db-path` + curated `--projects-dir` rather than literal `--dry-run` flag — resolved at plan-side by #42 plan-edit acknowledging the substitute pattern is canonical until a `--dry-run` flag is built)*
    *— RECORD (mutual; non-actionable observation about session-scope choice): #6 (dry-run covered 9 of 12 modeled subtypes; future C-tier dry-runs honor §safeguard 2 refinement requiring 80% coverage minimum), #36 (curated vs cross-corpus dry-run scope — same observation framed differently)*
    *— RECORD (mutual; commit-body factual claims accurate per audit verification): #14 (334 row-count vs 341 occurrence-sum — both interpretations correct; commit accurately uses row count), #18 (sessions_total=1 confirmed accurate)*
    *— IGNORE (informational, defensible engineering choices or project-pattern compliant): #1 (Unknown subtypes populate attachments AND drift per plan §136), #7 (decompose_record maybe_base arm omits Attachment — AttachmentRecord has cwd via envelope, no plan mandate), #8 (no upsert_agent — AttachmentRecord intentionally doesn't promote agentId, plan does not mandate), #9 (subject 64 chars compliant), #10 (aim/intention framing), #11 (no Co-Authored-By), #12 (per-subtype body_json shape enumeration covers all 12), #16 (FK integrity ok), #17 (PRAGMA integrity_check ok), #20 (Unknown-subtype attachments idempotent — known asymmetry now closed via #2 fix), #21 (log_record_type_drift UPDATE-on-conflict matches decompose_unknown precedent), #24 (overflow_fields docstring path matches C1.1-Review #29 closeout choice), #25 (commit body explicitly states C1.2 boundary), #26 (zero changes to crates/core/ and migrations/ outside the C1.2.1 fix), #27 (decompose_attachment doesn't accept session_id_from_file fallback — AttachmentRecord.session_id is non-Optional per C1.1), #29 (15 new tests count accurate), #31 + #32 (per-subtype field-level + hook_permission_decision FK-shape coverage gaps recorded), #33 (renamed test exercises new path substantively), #35 (verification stats accurate), #38 (Unknown arm doesn't log to schema_drift_log — defensible asymmetry), #39 (commit-message conformance), #40 (Unknown-arm coverage by-inspection for 6 unsurfaced subtypes), #41 (commit body forensic detail satisfied)*
    *— IGNORE (test-coverage gaps now closed by C1.2.1 or recorded for future test-improvement): #30 (fixture-driven tests silent-pass on /tmp/c1.1-samples-* absence — could be tightened in future test-improvement commit; not blocking), #34 (live regression net unverified pending kickstart — same pattern as B1.1+D1+D2+D3+D4+B1.2+C1.1 reviews)*
    *Net: C1.2 implementation accepted with C1.2.1 resolution. Plan refinements applied to §135 (body_json choice acknowledged), §C1.2 paragraph (drift-arm policy + version-None policy + Unknown-subtype inner_type policy + per-table idempotency contract pinned), §safeguard 2 (substitute-pattern acknowledged + baseline-diff + 80% subtype coverage minimum). 4 architectural-debt impl-defects RESOLVED via C1.2.1 (commit 57f6ba4). 1 verification gap pending kickstart. 5 mutual rows RECORDed. C1.2-Review closes the C1.2 chain (impl ec65a40 + audit + triage + review + resolution 57f6ba4); C1.3 (FTS5 attachment-text-content) and C1.4 (CLI/MCP/REST surfacing) gates are now both open per spawn-order DAG hard rule 4.*
- [x] **C1.3** FTS5 fts_attachment_text_content + watcher rebuild integration *(← C1.2-Review, closed at e501f94)*  *— landed in 76bcdde (subject 69 chars per `awk '{print length}'` — impl-report context cited "60 chars" in error; 69 is still ≤72-char convention compliant; STATE.md amended at C1.3-Review per audit row #43). Migration 009 (009_attachment_fts.sql) creates fts_attachment_text_content FTS5 contentless virtual table. fts.rs adds SearchResultSource enum (Message default + Attachment(String) carrying inner_type) backwards-compatibly via #[serde(default)] additive field on SearchResult; rebuild_fts_attachment_text_content + search_attachment_text_content + search_messages_and_attachments fns; 7 new tests. watcher.rs wires the 3rd FTS rebuild call into the 30s loop inside the same conn.call() closure (preserves atomicity + data_ingested_since_fts_rebuild gate + tokio::select! cancellation). main.rs/api/search.rs/mcp/tools.rs/output.rs all call the union path; output.rs adds `[attachment]` prefix for attachment-source rows. db.rs idempotency 8→9, schema.rs sv_count 8→9 + 3 new migration_009_* tests. Workspace tests 249/249 (was 239 + 10 new). Dry-run at /tmp/claude-history.dryrun-c1.3.db: rebuild produced 11 FTS rows (10 mcp_instructions_delta + 1 skill_listing; edited_text_file/nested_memory absent from scratch corpus — live-DB verification of those subtypes deferred to post-kickstart against corpus with workflowsPiExtension's ~73 edited_text_file rows). MATCH "MCP" → 4 results, "claude" → 9. PRAGMA integrity_check ok. **Migration-numbering side-effect**: C2.1 now takes 010, not 009 — plan §hard-rule-3 statement "C1.1 takes 008, C2.1 takes 009" is now stale; future C2.1 spawn must use migration 010.*
  - [x] C1.3-Audit  *— completed 2026-05-09: 44 observations at .planning/audit/c1.3-audit-2026-05-09T1334-asia-shanghai.md. Counting self-check PASSED (declared 44 = grep count 44). Categories: 15 divergence, 7 test-coverage, 6 meta-plan, 5 commit-message, 4 verification, 3 regression, 2 addition, 1 sequence, 1 dependency, 0 omission, 0 documentation. All verification commands clean (cargo build, 249/249 tests, migration 009 apply + idempotent replay, integrity_check ok). Headline findings: (a) C1.3 consumed migration 009 reserved by plan §hard-rule-3 for C2.1 (plan-defect already flagged); (b) CLI search surface adopts default-on union without `--include-attachments` flag named in plan §296 (impl divergence); (c) mcp_instructions_delta.added_blocks flatten via json_each produces N FTS docs per row rather than plan's "joined as one document" wording; (d) commit subject is 69 chars (impl-report claimed 60 — miscount but still ≤72 compliant); (e) kickstart deferred so live edited_text_file and nested_memory paths remain unverified end-to-end. No severity language.*
  - [x] C1.3-Triage  *— completed 2026-05-09: 44 rows triaged at .planning/audit/c1.3-triage-2026-05-09T1340-asia-shanghai.md. Counting self-check passed. Plan-spec reach: 5 plan-defect, 6 impl-defect, 4 mutual, 29 informational. Build/test impact: 0 build-blocking, 0 test-blocking, 4 runtime-only, 7 static-only, 33 none. Determinism: 39 deterministic, 0 nondeterministic, 5 unverified. Meta-plan rows: 6 (#2, #9, #37, #41, #42, #43). Headline classifications: --include-attachments flag missing classified impl-defect (#1 — plan §296 typed flag in literal verification command, reading as normative); json_each flatten classified plan-defect (#4 — plan said "joined as one document" but both shapes work and impl's choice is documented).*
  - [x] C1.3-Review  *— completed 2026-05-09. Decisions per deviation:*
    *— RESOLVED via parent-agent direct fix to plan: #2 + #42 (plan §hard-rule 3 line 210 updated to acknowledge C1.3 took 009 and C2.1 takes next-available 010; STATE.md and plan-file now consistent on migration-numbering)*
    *— RESOLVED via parent-agent direct fix to plan: #4 (plan §144 "joined as one document" replaced with "flattened via json_each into one FTS row per block, preserving per-block search granularity" — ratifies impl's choice with explicit alternative-rejection rationale)*
    *— RESOLVED via parent-agent direct fix to plan: #6 (plan §144 `added_blocks` snake_case replaced with `addedBlocks` camelCase matching AttachmentBody serde)*
    *— RESOLVED via parent-agent direct fix to plan: #3 (plan §145 `crates/server/src/watcher.rs:689-711` line range replaced with symbol-name reference `find by symbol name rebuild_fts_*` — line numbers shift across commits)*
    *— RESOLVED via parent-agent direct fix to plan: #1 (plan §296 `--include-attachments` flag wording replaced with default-on-union framing — ratifies impl's UX choice; the rejected opt-in flag is explicitly documented as rejected. The §296 verification command is now runnable as `claude-history search "MCP"` without the flag.)*
    *— RESOLVED via parent-agent direct fix to plan: #9 (plan §safeguard 2 now classifies FTS-only commits as dry-run-encouraged-but-not-required, with corpus-scope-relaxation for subtypes the FTS rebuild doesn't index; live-DB end-to-end verification deferred to next kickstart is acceptable)*
    *— RESOLVED via parent-agent direct fix to plan: #32 + #44 (plan §148-152 C1.4 scope refined to clarify that C1.3 already extended the SEARCH surface across CLI/REST/MCP — C1.4's surfacing is the NEW list/show/query surfaces specific to attachments and hook_executions, not search-extension which is already done)*
    *— RESOLVED via parent-agent direct fix to plan: §250 Tier C1 file-list expanded to enumerate the 9 files C1.3 actually touched plus the new 009_attachment_fts.sql migration*
    *— RESOLVED via parent-agent direct fix to plan: #37 (§safeguard 2 corpus-scope ambiguity acknowledged via the FTS-only-commit relaxation clause — scratch DB clone-from-c1.2.1 pattern is now permitted for FTS-only verification)*
    *— RESOLVED via STATE.md amendment in this commit: #43 (STATE.md line previously read "subject 60 chars"; corrected to "subject 69 chars per `awk '{print length}'` — impl-report context cited 60 in error; 69 is still ≤72-char compliant")*
    *— RECORD (committed; cannot amend impl-report; subject 69 ≤ 72 still compliant): #10 (impl-report's claimed 60 chars vs actual 69 — propagation halted at STATE.md per #43 fix; impl-report committed and untouched)*
    *— RECORD (real but recoverable): #17 (three rebuild_* calls in 30s loop share one conn.call without enclosing BEGIN/COMMIT — partial-rebuild state reachable on rebuild_* failure mid-closure; data_ingested_since_fts_rebuild reset only on full success means the next rebuild cycle catches up. Worth fixing in a future test-improvement commit but not architectural debt — system functions correctly under normal flow; concern is recovery-from-failure-mid-rebuild quality.)*
    *— RECORD (real but minor; depends on usage): #25 (cross-table BM25 ranking semantics — search_messages_and_attachments unions both pools and sorts by raw rank, but BM25 IDF/avgdl differ across tables so ranks are not directly comparable. Concern is search-result ordering quality when matches span sources. User impact depends on how often users issue queries that match both message and attachment content. Could be addressed via percentile-normalization in a future test-improvement commit; not currently blocking.)*
    *— RECORD (commit-message factual claim re: substitute pattern; informational): #12 (commit body provides post-state row counts only; pre-state baseline-diff against live DB not literally satisfied per §safeguard 2 line 364 since scratch DB cloned from c1.2.1; substitute is reasonable but not literal)*
    *— RECORD (verification gaps pending kickstart, same pattern as B1.1+D1+D2+D3+D4+B1.2+C1.1+C1.2 reviews): #7 + #8 + #14 + #30 + #39 (live-DB end-to-end verification of edited_text_file + nested_memory paths; subtype-coverage 80% minimum end-to-end; merged search-path against dry-run; daemon binary refresh)*
    *— RECORD (mutual; non-actionable observation about session-scope choice): same pattern as C1.2-Review #6/#36*
    *— IGNORE (informational, defensible engineering choices or project-pattern compliant): #5 (nested_memory.content.content path correct), #11 (commit body forensic + 95 lines + aim/intention framing), #13 (10-test count accurate), #15 (empty-extract test breadth — SQL guard length()>0 uniform), #16 (SearchResult #[serde(default)] correct by construction), #18 (REST source field shape), #19 (CLI [attachment] prefix), #20 (synthetic message_type="attachment" choice), #21 (block_type+source duplication), #22 (tracing-log update), #23 (idempotency test single-row), #24 (pagination by-inspection), #26 (no C1.1/C1.2 surfaces touched), #27 + #28 + #29 (cargo + integrity + dry-run regression checks ok), #31 (commit body documents migration-numbering side-effect), #33 (commit body forensic accuracy), #34 (non-indexed subtype test breadth), #35 (no block_index column — consumer-side disambiguation gap acceptable), #36 (defensive IS NOT NULL guards), #38 (orphan FTS rows pattern), #40 (verification stats cross-reference), #41 (scratch DB persistence — operational observation)*
    *Net: C1.3 implementation accepted as-is with comprehensive plan refinements. Plan refinements applied to §144 (json_each row-shape + addedBlocks camelCase), §145 (line-range → symbol-name), §148-152 (C1.4 scope clarified), §210 (hard-rule 3 migration numbering), §250 (Tier C1 file list expanded), §296 (default-on-union ratification + flag-removal), §safeguard 2 (FTS-only commit classification + corpus-scope relaxation). STATE.md amended for subject-char miscount. 2 design-quality concerns RECORDed for potential future test-improvement commits (#17 partial-rebuild atomicity, #25 cross-table BM25 ranking — neither blocks system function). 5 verification gaps pending kickstart. 1 commit-message accuracy issue RECORDed. C1.3-Review unblocks C1.4 (CLI/MCP/REST list/show surfaces — search-extension done in C1.3); C2.1 (planContent migration 010 — note migration-number shift) gate is also open since C1.1-Review closed earlier.*
- [x] **C1.4** CLI / MCP / REST surfacing for attachments + hook_executions *(← C1.2-Review, closed at e501f94; C1.3 already extended SEARCH surface so C1.4's scope is the NEW list/show/query surfaces only)*  *— landed in 30c709c (subject 71 chars, verified by both `wc -c` and `awk` to break the C1.3-style miscount pattern). 8 files +1134/-18 LOC. Workspace tests 256/256 (was 249 + 7 new query.rs tests). 3 new CLI subcommands (`attachments list/show`, `hook-executions`); 3 new REST endpoints (GET /v1/attachments, /v1/attachments/{uuid}, /v1/hook-executions); 2 new MCP tools (list_attachments, get_hook_executions — bringing total to 15). Surface shape mirrors record-type-drift precedent (B1.2): --json flag for machine output, --project/--inner-type/--since/--limit filters, daemon_client HTTP-over-UDS wrappers, output.rs formatters. AttachmentsAction enum for the show subcommand subtree. 2 documented caveats: (a) attachments_list --project LIKE on NULL project_path omits the row (matches list_sessions precedent); (b) hook_executions_list doesn't auto-join attachments.inner_type to recover hook_success vs hook_permission_decision discriminator (flat-surface choice; callers use attachments show or execute_sql for the join). Dry-run against /tmp/claude-history.dryrun-c1.2.1.db: attachments list returned 3 hook_success rows timestamp DESC; attachments show pretty-printed body_json; hook-executions --hook-event PreToolUse returned 5 rows; show-on-miss returned exit 1 with "Attachment not found". Daemon kickstart pending separately.*
  - [x] C1.4-Audit  *— completed 2026-05-09: 37 observations at .planning/audit/c1.4-audit-2026-05-09T1401-asia-shanghai.md. Counting self-check PASSED. Categories: 8 divergence, 8 test-coverage, 5 addition, 4 commit-message, 4 documentation, 3 verification, 2 omission, 1 regression, 1 sequence + 4 plan-defect notes in separate section. Verification commands all clean: subject 71 chars verified; cargo build clean; 256/256 tests; daemon /v1/health 200; daemon /v1/attachments returns 404 (expected pre-kickstart); dry-run DB exercise of list/show/hook-executions/miss-uuid all correct; 15 #[tool] attributes verified in mcp/tools.rs; existing surfaces (search/fts/watcher/migrations) unchanged. Headline finding: stale count strings across multiple help-text/doc surfaces — MAIN_HELP "MCP TOOLS (13)" actual 15; main.rs:3 "15 subcommands" actual 20; api/mod.rs:6 "32 routes" actual 36; CLAUDE.md "10 tools". Documentation drift; not architectural debt. No severity language.*
  - [x] C1.4-Triage  *— completed 2026-05-09: 37 rows triaged at .planning/audit/c1.4-triage-2026-05-09T1408-asia-shanghai.md. Counting self-check passed. Plan-spec reach: 0 plan-defect, 5 impl-defect, 0 mutual, 32 informational. Build/test impact: 0 build-blocking, 0 test-blocking, 2 runtime-only, 8 static-only, 27 none. Determinism: 35 deterministic, 0 nondeterministic, 2 unverified. Meta-plan: 0 in main catalog; 4 plan-defect notes (P1-P4) folded into meta-plan subsection separately. Count-string drift cluster classified impl-defect because production-source string literals with observably inaccurate counts are refreshable by impl; CLAUDE.md drift kept informational as project-doc layer.*
  - [x] C1.4-Review  *— completed 2026-05-09. Decisions per deviation:*
    *— RESOLVED via parent-agent direct fix to plan: P1 + P2 + P3 + P4 (plan §C1.4 expanded — flag enumeration adds --limit and --json per project precedent; MCP tool params enumerated; REST query-param shapes documented snake_case)*
    *— RESOLVED via spawned C1.4.1 resolution subagent (commit b471491 — subject 64 chars): #1 + #30 (MAIN_HELP "MCP TOOLS (13)" → "MCP TOOLS (15)" at main.rs:608); #2 (crate-doc "15 subcommands" → "20 subcommands" at main.rs:3); #36 (api/mod.rs:6 module-doc "32 routes" → "36 routes"; companion line :34 "32 endpoints across 14 resource groups" → "36 endpoints across 16 resource groups"); +2 sections in build_router catalog (Attachments + Hook executions). Pre-edit grep verified counts matched audit claims (#[tool]=15, Commands variants=20, route lines=36 — no audit-stale condition triggered). 256/256 tests pass post-fix.*
    *— RECORD (informational reading; row #10's "impl-defect" tag is a strict reading of audit-row pairing with C1.3 miscount precedent — strict reading of as-shipped is informational since 71 ≤ 72): #10 (subject 71 chars verified accurate; impl-report's claim accurate this time vs C1.3's 60-vs-69 miscount)*
    *— RECORD (verification gaps pending kickstart, same pattern as B1.1+D1+D2+D3+D4+B1.2+C1.1+C1.2+C1.3 reviews): #13 + #14 (live-DB verification of attachments-list project filter against workflowsPiExtension corpus + dry-run-vs-live baseline pattern; resolved at next kickstart)*
    *— RECORD (CLAUDE.md drift triaged informational; project-doc layer): #3 (CLAUDE.md line 49 "10 tools" enumerates pre-B1.2/C1.3/C1.4 tool names — natural drift between commit windows; will be refreshed in a future CLAUDE.md cleanup pass)*
    *— RECORD (test-coverage gaps consistent with C1.4's read-side scope and project-pattern compliance with existing API/MCP test surfaces; tightening would be project-wide test-improvement work): #16-#23 (NULL-LIKE semantic, all-four-filter combinations, LIKE ESCAPE wildcard inputs, hook_executions ordering rigor, daemon_client 404 mapping, api/* HTTP integration, mcp/* handler tests — all are project-pattern-compliant gaps inherited from existing surfaces, not C1.4-specific test omissions)*
    *— IGNORE (informational, defensible engineering choices or project-pattern compliant): #4 (plan §154 "Optionally extend query_messages" is plan-tagged optional; impl honored skip), #5 + #6 + #7 + #8 + #28 (CLI/REST/MCP additions beyond plan §152's enumerated flags — project-precedent additions per list_sessions/record_type_drift, all RESOLVED via P1-P4 plan refinements above), #9 (MCP tool naming asymmetry list_*/get_* — plan §154 named both literally per current convention; existing precedents mix both prefixes), #11 + #12 + #34 (commit-message conformance — aim/intention framing, no Co-Authored-By, forensic detail), #15 (cargo test workspace verification), #24 (search-extension scope-disjoint rule honored), #25 + #29 (output.rs UTF-8-mid-codepoint truncation + uuid 8-char display — project precedent in print_record_type_drift; future Unicode-safety improvement could refactor), #26 (fixed-string dashes — project precedent), #27 (AttachmentRow.body_json: Option<String> raw JSON not typed core::record::AttachmentBody — design intent documented), #31 (dry-run substitute exercise complete), #32 (Path<String> extractor with no UUID-format validation — matches existing /v1/sessions/{id} precedent), #33 (DAG sequence honored), #35 (MEMORY.md not updated — not required for impl-tier commits), #37 (build_router catalog now extended via C1.4.1)*
    *Net: C1.4 implementation accepted with C1.4.1 resolution. Plan refinements applied to §C1.4 (flag/param/REST query-param explicit enumeration). 4 count-string drift sites RESOLVED via C1.4.1 (commit b471491). 0 architectural-debt impl-defects. 8 test-coverage gaps RECORDed as project-pattern-compliant. 2 verification gaps pending kickstart. C1.4-Review closes the C1.4 chain. C1 Tier complete (C1.1 + C1.2 + C1.3 + C1.4 + their resolutions all closed). Remaining work: C2.1 + C2.3-C2.6 (planContent promotion chain). C2.1 takes migration 010 per refined §hard-rule 3.*
- [x] **C2.1** planContent migration **010** + decomposer extraction *(← C1.1-Review for migration numbering — actual number 010 since C1.3 consumed 009)*  *— landed in 6d54e88 (subject 67 chars). Migration 010 (010_plan_content.sql) ALTER messages ADD COLUMN plan_content TEXT + idempotent backfill UPDATE pulling from json_extract(extra_json, '$.planContent') + cleanup UPDATE via json_remove + partial index idx_messages_plan_content_present ON messages(session_id) WHERE plan_content IS NOT NULL. decompose_user extended to extract planContent overflow key directly into plan_content column for future ingestion. db.rs idempotency assertion 9→10; schema.rs sv_count 9→10 + 5 new migration_010_* tests + 3 new decompose_user tests. Workspace tests 264/264 (was 256 + 8 new). Dry-run scratch DB at /tmp/claude-history.dryrun-c2.1.db (cloned from c1.2.1): 81 rows with $.planContent pre-migration → 81 plan_content IS NOT NULL + 0 residual post-migration. Plan-content LENGTH distribution: min 607, max 30,261, avg ~7,550 bytes. PRAGMA integrity_check ok. Idempotent replay confirmed at runner-level (schema_versions guard) AND UPDATE-level (WHERE clauses no longer match after first run). C2.1 + C2.2 combined per encoded TaskList framing. Daemon kickstart pending separately — live DB schema_versions=7; kickstart applies 008/009/010 in sequence and backfills ~81 plan_content rows.*
  - [x] C2.1-Audit  *— completed 2026-05-09: 52 observations at .planning/audit/c2.1-audit-2026-05-09T1430-asia-shanghai.md. Counting self-check PASSED (declared 52 = grep count 52). Categories: 12 divergence, 14 verification, 5 meta-plan, 4 test-coverage, 4 sequence, 3 commit-message, 1 regression, 1 omission, 1 documentation, 1 addition. All verification commands pass: cargo build clean, 264/264 tests, dry-run integrity_check ok, partial index used per EXPLAIN, 81 plan_content rows / 0 residual. Headline findings: (a) migration 010 ALTER TABLE not independently replay-safe at DDL level — relies on schema_versions runner gate (same SQLite limitation as B1.1's mig 007 pattern); (b) cleanup UPDATE strips planContent unconditionally — theoretical data-loss path for non-string Values not exercised by tests; (c) §safeguard-2-mandated dry-run for decomposer-changing C2.2 work was performed as bare-SQL replay against DB-clone, NOT via `claude-history sync --db-path …` exercising the new Rust decomposer code path; (d) plan §157+§161+§163+§474 contain stale references post-renumbering + post-C2.1+C2.2 fold; (e) C2.1+C2.2 combined-commit shape derives from encoded TaskList #9 not §157's "six commits" framing. No severity language.*
  - [x] C2.1-Triage  *— completed 2026-05-09: 52 rows triaged at .planning/audit/c2.1-triage-2026-05-09T1433-asia-shanghai.md. Counting self-check passed. Plan-spec reach: 8 plan-defect, 9 impl-defect, 1 mutual, 34 informational. Build/test impact: 0 build-blocking, 0 test-blocking, 7 runtime-only, 6 static-only, 39 none. Determinism: 45 deterministic, 0 nondeterministic, 7 unverified. Meta-plan rows: 5 (#11, #48, #49, #50, #51). Mig 010 ALTER replay-safety classified informational (project-precedent compliant). Cleanup UPDATE non-string risk classified impl-defect (theoretical against today's all-string corpus). Dry-run shape (bare-SQL vs Rust path) classified mutual.*
  - [x] C2.1-Review  *— completed 2026-05-09. Decisions per deviation:*
    *— RESOLVED via parent-agent direct fix to plan: #1 + #11 + #37 + #48 + #49 + #50 + #51 (plan §157 "six commits" → "five commits — C2.1 folds the original C2.2 decomposer extraction"; §159 "Migration 009" → "Migration 010 (NOT 009 — C1.3 consumed 009)" with explicit all-strings contract documented; §161 line-range `decompose.rs:393-447` → symbol-name reference per C1.3-Review precedent + conditional-removal contract noted; §163 stale "Migration 009 includes a backfill insert" → C2.3 takes own migration with verify-next-available framing; §474 operational-decisions "009=C2.1 in tier order" → chronological actual landing 007=B1.1, 008=C1.1, 009=C1.3, 010=C2.1, future C2.3 takes 011)*
    *— RESOLVED via parent-agent direct fix to plan: #6 (plan §161 line-range stale; replaced with symbol-name reference per C1.3-Review precedent)*
    *— RESOLVED via spawned C2.1.1 resolution subagent (commit 60bd7f4 — subject 70 chars): #25 + #39 + #40 cluster (non-string planContent defensiveness — `remaining.remove("planContent")` now gated on `plan_content.is_some()`; new regression test `test_decompose_user_preserves_non_string_plan_content` asserts non-string Value is preserved in extra_json AND plan_content stays NULL); #45 (schema.rs `MIGRATIONS.iter().take(9)` → `take_while(|(v, _)| *v < "010")` — eliminates hard-coded count brittleness against future migration insertion). 265/265 workspace tests pass (was 264 + 1 new).*
    *— RECORD (forensic-completeness gaps in commit body; cannot amend already-committed): #8 + #9 (§safeguard-2 line 367 pre/post baseline-DB row-count snapshot + line 369 per-subtype-row breakdown not literally in commit body — substitute statistics provided but not the literal §safeguard 2 shape; future C-tier commits should explicitly capture both)*
    *— RECORD (real but minor; same-pattern as B1.1 mig 007 ALTER limitation accepted previously): #2 (migration 010 ALTER TABLE not independently replay-safe at DDL level — relies on schema_versions runner gate per project-precedent compliance with B1.1's mig 007 pattern)*
    *— RECORD (test-coverage gap; corpus is all-strings so manifestation is theoretical; resolution closure on the decompose path was applied via C2.1.1 #25 fix, but the migration-side cleanup UPDATE remains unconditional on extracted-typeof — could be tightened in a future test-improvement commit by adding `WHERE typeof(json_extract(...)) = 'text'` guard to migration 010's cleanup; not currently blocking since corpus has only string values per MEMORY.md snapshot): #24 (test-coverage gap for non-string Values — partially closed by C2.1.1's regression test for decompose path; migration-cleanup-side test coverage not added)*
    *— RECORD (dry-run shape concern, mutual): #7 + #30 + #31 (§safeguard-2 dry-run for ingestion-changing commits should exercise Rust code path via `claude-history sync --db-path …` per §369 prescription; impl substitute was bare-SQL replay against DB-clone bypassing the migration runner's chained-application path. Plan ambiguity around how the requirement transfers when commits are folded; impl substitute does not satisfy the spirit. Future C2.x ingestion-changing commits should use the literal `claude-history sync --db-path` substitute pattern documented in refined §safeguard 2.)*
    *— RECORD (verification gaps pending kickstart, same pattern as B1.1+D1+D2+D3+D4+B1.2+C1.1+C1.2+C1.3+C1.4 reviews): #15 + #16 + #17 + #26 + #34 + #46 + #47 (live-DB backfill parity, C2.4-bounded plans list verification, C2.3-bounded FTS5 search verification, MEMORY.md-snapshot-vs-live drift check, live PRAGMA integrity_check, schema_versions=7 → 10 cascade on next kickstart, chained-application end-to-end exercise)*
    *— IGNORE (informational, defensible engineering choices or project-pattern compliant): #3 (no DEFAULT clause — design intent), #4 + #5 (overflow extraction shape correct), #10 (DAG sequence honored), #12 + #13 + #14 (commit-message conformance), #18 + #19 + #20 (cleanup parity + integrity_check + index plan all verified on dry-run DB), #21 + #22 + #23 (test coverage of idempotency layers), #27 (re-extraction-on-each-sync idempotent same-value), #28 (UPDATE statement expansion 3→4 SET clauses), #29 (symmetric extraction across compactSummary/sourceToolUseID/planContent), #32 (MEMORY.md drift not required for impl-tier commits), #33 (cargo workspace verification), #35 (no orphaned planContent readers found in code), #36 (DAG sequence — no C1.* surfaces touched), #38 (no BEGIN/COMMIT wrappers in migration file — runner wraps each migration in tx per project precedent), #41 (migration docstring forensic detail satisfies mandate), #42 (decompose_user docstring updated to mention planContent), #43 (drift.rs unchanged — drift detection remains independent of decomposer promotions per commit body framing), #44 (MIGRATIONS array index 9 = 010), #52 (CREATE INDEX IF NOT EXISTS replay-safe at DDL level)*
    *Net: C2.1 implementation accepted with C2.1.1 resolution. Plan refinements applied to §157 (commit count + fold rationale), §159 (mig 010 + all-strings contract), §161 (symbol-name reference + conditional-removal contract), §163 (C2.3 takes own migration), §474 (chronological migration numbering). 4 architectural-debt impl-defects RESOLVED via C2.1.1 (commit 60bd7f4). 7 verification gaps pending kickstart. 5 RECORD-only items (forensic-completeness gaps, mig 010 ALTER limitation, dry-run substitute shape, theoretical non-string risks). C2.1-Review unblocks C2.3 + C2.4 + C2.5 + C2.6 per the spawn-order DAG (these may interleave freely).*
- [x] **C2.3** FTS5 coverage for plan_content via synthetic message_content rows *(← C2.1-Review, closed at 0b37e00; takes migration 011)*  *— landed in 7c6048b (subject 69 chars). Migration 011 (011_plan_content_fts.sql): INSERT OR IGNORE INTO message_content (message_uuid, block_index, block_type, text_content) SELECT uuid, -1, 'plan_content', plan_content FROM messages WHERE plan_content IS NOT NULL. UNIQUE(message_uuid, block_index) pre-existed in migration 001 — no new index added; idempotent on replay. decompose_user extended: when plan_content extraction returns Some, INSERT OR IGNORE synthetic row at decompose-time so future ingestion is searchable without FTS rebuild lag. db.rs idempotency 10→11; schema.rs sv_count 10→11 + 3 new migration_011_* tests + 1 new decompose_user idempotency test. Workspace tests 269/269 (was 265 + 4 new). Dry-run at /tmp/claude-history.dryrun-c2.3.db: pre message_content=739,123 → post=739,204 (delta 81); synthetic rows=81; JOIN 1:1 with plan_content; PRAGMA integrity_check ok. **FTS5 search gate**: after INSERT INTO fts_message_content('rebuild'), MATCH "JSON-First Artifact Invariant" returns 193 total hits with 2 restricted to block_type='plan_content' at block_index=-1 (vs 0 pre-C2.3 — closes the high-value semantic gap recorded in MEMORY.md). Block_index=-1 sentinel verified non-colliding (pre-migration MIN=0, MAX=7). Daemon kickstart pending separately — kickstart applies migrations 008+009+010+011 in sequence.*
  - [x] C2.3-Audit  *— completed 2026-05-09: 52 observations at .planning/audit/c2.3-audit-2026-05-09T1455-asia-shanghai.md. Counting self-check PASSED (declared 52 = grep count 52). Categories: 14 verified, 10 divergence, 8 verification, 7 test-coverage, 3 sequence, 3 documentation, 3 commit-message, 2 addition, 1 regression, 1 meta-plan. All verification commands pass: subject 69 chars; cargo build clean; 269/269 tests; dry-run integrity_check ok; backfill 81=81 1:1; block_index sentinel non-colliding (MIN=-1 only from synthetic, real MAX=7); FTS5 MATCH "JSON-First Artifact Invariant" pre-rebuild=191 → post-rebuild=193 with 2 in block_type='plan_content'; block_index range-filter audit clean (no production filters on >=0/BETWEEN/<0); view-filter immunity confirmed (existing views filter block_type='text' so synthetic plan_content rows don't pollute). Headline finding: markdown export ordering quirk — synthetic block_index=-1 rows sort first under ORDER BY block_index ASC and export.rs `_` arm renders them as `*[plan_content]* <text>` prefix line BEFORE the user's actual text block (rows #28 + #29). No severity language.*
  - [x] C2.3-Triage  *— completed 2026-05-09: 52 rows triaged at .planning/audit/c2.3-triage-2026-05-09T1501-asia-shanghai.md. Counting self-check passed. Plan-spec reach: 2 plan-defect, 6 impl-defect, 4 mutual, 40 informational. Build/test impact: 0 build-blocking, 0 test-blocking, 9 runtime-only, 5 static-only, 38 none. Determinism: 47 deterministic, 0 nondeterministic, 5 unverified. Meta-plan: 1 (#48 — plan §163 stale "Migration 009"). Export-ordering issue (#28+#29) classified mutual — plan §163 silent on export-rendering semantics; impl chose preexisting block_index ASC ordering + render-all-block-types defaulting.*
  - [x] C2.3-Review  *— completed 2026-05-09. Decisions per deviation:*
    *— RESOLVED via parent-agent direct fix to plan: #1 + #48 (plan §163 stale "Migration 009" → "migration 011" with explicit dry-run-verified count); #5 (plan §163 now explicitly mentions decompose_user as the live-ingestion locus); #43 (plan §163 now specifies the 30s rebuild window: synthetic rows tokenized within ≤30s of next daemon-startup OR immediately on next sync_all where files_synced > 0); + comprehensive export-side rendering contract (synthetic plan_content rows are FTS-only, NOT user-facing message content, MUST be filtered from export output)*
    *— RESOLVED via spawned C2.3.1 resolution subagent (commit 113190e — subject 69 chars): #28 + #29 (export-rendering leak — three ORDER BY block_index ASC paths in query.rs (session_messages_for_export, session_conversation, get_message) now filter `WHERE block_type != 'plan_content'`; regression test export_markdown_filters_synthetic_plan_content_rows asserts plan_content text never appears in markdown export, real text content still surfaces, synthetic row remains in message_content for FTS); #26 (QUERIES_SCHEMA_HELP block_type enumeration extended with `plan_content` + 2 prose lines explaining FTS-only synthetic-row contract + recommending `block_type != 'plan_content'` filter for ad-hoc SQL on real blocks). 270/270 workspace tests pass (was 269 + 1 new export regression).*
    *— RECORD (operational shortcut; minor): #10 (dry-run hand-edited schema_versions row for 010 because C2.1's bare-sqlite3 application of 010 left column present without runner-tracking row; clean-lineage 001→011 from fresh DB on production-corpus mass not exercised — unit tests cover idempotency, dry-run is the corpus-mass exercise on hand-edited base)*
    *— RECORD (test-coverage gap; small): #24 (no test asserts synthetic-row absence when plan_content extraction returns None; the existing non-string preservation test indirectly covers but doesn't assert message_content row absence — add to future test-improvement commit)*
    *— RECORD (real but bounded by 30s window per refined plan §163): #44 (sync_all rebuilds FTS only when files_synced > 0; backfilled synthetic rows un-tokenized until watcher 30s timer fires OR next sync_all with files. Plan-side contract now explicitly documents the 30s window as acceptable.)*
    *— RECORD (verification gaps pending kickstart, same pattern as prior commits): #12 + #15 + #18 + #45 + #50 (live-ingestion 2s parity, Co-Authored-By absence-of-pattern, live integrity_check, chained-migration sequence on populated live DB, live count snapshot for ratification)*
    *— RECORD (mutual; non-actionable observation about pattern preservation): #43 (plan §163 phrasing structurally accurate but eventual-consistency window now specified; refinement applied above)*
    *— IGNORE (informational, defensible engineering choices or project-pattern compliant): #2-#9 (— verified rows confirming structural correctness), #11 (C2.4-bounded verification not in C2.3 scope), #13 (DAG sequence honored), #14 + #16 (commit-message conformance), #17 (cargo workspace verification), #19-#23 (test coverage of idempotency layers + FTS gate), #25 (rows_inserted metric semantics shift), #27 (no MCP/CLAUDE.md tool count change required), #30 (first_text_content filter incidentally compatible — synthetic rows excluded), #31 (analytical views immune via existing block_type='text' filter), #32 (canned queries pick up synthetic rows post-rebuild without off-by-one drift), #33 (MEMORY.md drift — natural between commits), #34-#42 + #46-#47 + #49 + #51-#52 (drift unchanged, MIGRATIONS array correct, sv_count assertions bumped, dry-run safeguard exercised, test-improvement pattern adopted, idempotency-via-IGNORE consistent, sentinel structurally unique)*
    *Net: C2.3 implementation accepted with C2.3.1 resolution. Plan refinements applied to §163 (migration number current + decompose_user locus + 30s rebuild window + export-filter contract). 3 audit-flagged UX/discoverability findings RESOLVED via C2.3.1 (commit 113190e). 5 verification gaps pending kickstart. 4 RECORD-only items (operational shortcut, test gap, FTS rebuild window, mutual pattern observations). C2.3-Review unblocks remaining Tier C2 work (C2.4, C2.5, C2.6 may interleave freely per §hard-rule-5).*
- [x] **C2.4** CLI plans subcommand *(← C2.1-Review, closed at 0b37e00)*  *— landed in ab0971a (subject 71 chars). PlanRow + PlanFullRow types in query.rs; plans_list + plan_show fns; list_sessions extended with has_plan: Option<bool> using EXISTS subquery engaging migration 010's idx_messages_plan_content_present partial index. Commands::Plans + PlansAction subtree (List, Show); --has-plan tri-state flag on Sessions via clap::ArgAction::Set. main.rs run_plans always direct-to-DB; daemon_client HTTP wrappers for /v1/plans deferred to C2.6. output.rs print_plans_list (table) + print_plan_show (block-per-message) with multibyte-safe preview truncation. api/sessions.rs SessionsParams gains has_plan query param; daemon_client.sessions(..., has_plan) forwards it; mcp/tools.rs passes None for has_plan (MCP exposure deferred to C2.5). 10 new query.rs tests; 279/279 workspace tests pass (was 270 + 9 net new). Dry-run against /tmp/claude-history.dryrun-c2.3.db (81 plan rows): plans list --limit 5 returned 5 rows; plans list --project smak --limit 5 filtered correctly; plans show <smak-session> rendered 8,972-byte plan body; sessions --has-plan --limit 5 in direct mode returned plan-bearing sessions. **Open caveat**: supervised launchd daemon at PID 60855 predates this commit — --has-plan is silently dropped on daemon-routed sessions calls until kickstart; CLI's direct mode (CLAUDE_HISTORY_SOCKET=/nonexistent) verifies SQL correctness. Daemon kickstart pending separately.*
  - [x] C2.4-Audit  *— completed 2026-05-09: 30 observations at .planning/audit/c2.4-audit-2026-05-09T1527-asia-shanghai.md. Counting self-check PASSED (declared 30 = grep count 30). Categories: 8 divergence, 7 addition, 4 test-coverage, 2 documentation, 2 verification, 2 regression, 2 commit-message, 1 sequence, 1 dependency, 1 omission. All verification commands pass: cargo build clean, 279/279 tests, PRAGMA integrity_check ok, EXPLAIN QUERY PLAN confirms idx_messages_plan_content_present engaged for --has-plan, dry-run samples (list / list+project+json / show-missing / sessions tri-state) behave as documented, live daemon at PID 60855 silently drops has_plan per kickstart-pending pattern. Headline findings: (a) commit body declares "10 new unit tests" but diff adds 9 — internal breakdown sums to 9, only headline overcounts; (b) --has-plan docstring at main.rs:113 references nonexistent --no-has-plan flag; (c) module doc at main.rs:3 still says "20 subcommands" — actual is 21 post-Plans (count-string drift recurring); (d) commit message cites plan §310 verification clauses but that anchor lacks plan-list-specific text; (e) plan_show LENGTH (Rust .len() byte count) vs plans_list plan_content_length (SQLite length() char count) report different values for non-ASCII plans (semantic inconsistency); (f) project-path left-truncation in print_plans_list byte-indexes not multibyte-safe while PREVIEW field IS multibyte-safe via char_indices().nth(). No severity language.*
  - [x] C2.4-Triage  *— completed 2026-05-09: 30 rows triaged at .planning/audit/c2.4-triage-2026-05-09T1531-asia-shanghai.md. Counting self-check passed. Plan-spec reach: 1 plan-defect, 12 impl-defect, 2 mutual, 15 informational. Build/test impact: 0 build-blocking, 0 test-blocking, 7 runtime-only, 5 static-only, 18 none. Determinism: 30 deterministic, 0 nondeterministic, 0 unverified. Meta-plan: 0. Six headline classifications: #3 test-count miscount → impl-defect static-only; #1 --no-has-plan docstring → impl-defect runtime-only; #2 module-doc 20→21 count drift → impl-defect static-only; #23 commit-body §310 anchor → plan-defect static-only; #15 byte-vs-char LENGTH → impl-defect runtime-only; #13 partial multibyte safety → impl-defect runtime-only.*
  - [x] C2.4-Review  *— completed 2026-05-09. Decisions per deviation:*
    *— RESOLVED via spawned C2.4.1 resolution subagent (commit b1a0ebc — subject 64 chars): #1 (--has-plan docstring at main.rs:113-117 rewritten to describe actual tri-state: omit / --has-plan[=true] / --has-plan=false; explicitly states there is no --no-has-plan flag); #2 (main.rs:3 module-doc "20 subcommands" → "21 subcommands"; pre-edit verification revealed actual count was 21 = 20 struct-form variants + 1 unit McpStdio variant — prior C1.4.1 awk recipe matched only `\{` so undercounted); #13 (output.rs:741 project-path left-truncation now uses chars().count() + char_indices().nth(start_char) to land on UTF-8 boundary, matching the existing multibyte-safe preview pattern); #15 (LENGTH semantics unified — print_plan_show now reports chars().count() with unit label "chars" matching SQLite-side length(); doc comments corrected to clarify TEXT length() returns char count not byte count). 279/279 workspace tests pass.*
    *— RECORD (committed; cannot amend): #3 (commit body claims "10 new tests" but actual 9; internal breakdown sums to 9, only headline overcounts; future commits aim for accurate counts), #23 (commit message cites plan §310 verification clauses but anchor lacks plan-list-specific text; future commit-bodies cross-reference accurately)*
    *— IGNORE (informational, defensible engineering choices or precedent compliance): remaining 24 informational + mutual rows including subject-char accuracy (71 verified), aim/intention framing, no Co-Authored-By, dry-run sample exercises, EXPLAIN QUERY PLAN engaging idx_messages_plan_content_present, --json flag operation, plan_show on missing-session exit/message, daemon-routed --has-plan silent-drop pending kickstart per existing pattern, SessionsParams.has_plan backwards-compat, mcp/tools.rs None pass-through, all test-coverage gaps consistent with project pattern, no C1.*/C2.1/C2.1.1/C2.3/C2.3.1 surfaces touched, no migration changes*
    *Net: C2.4 implementation accepted with C2.4.1 resolution. 4 actionable impl-defects RESOLVED via C2.4.1 (commit b1a0ebc). 2 commit-body issues RECORDed (cannot amend). C2.4-Review unblocks remaining Tier C2 work — C2.5 (MCP list_plans/get_plan tools) and C2.6 (REST /v1/plans endpoints + daemon_client retrofit). C2.5 + C2.6 may interleave per §hard-rule-5; encoded order serializes them.*
- [x] **C2.5** MCP list_plans / get_plan tools + query_messages extension *(← C2.1-Review, closed at 0b37e00)*  *— landed in 678cd0f (subject 49 chars). 2 new MCP tools: list_plans (params: project, since, limit; delegates to query::plans_list, returns Vec<PlanRow> timestamp DESC with ~200-char preview), get_plan (params: session_id; delegates to query::plan_show, returns Vec<PlanFullRow> timestamp ASC with full markdown). MCP tool count 15→17 in both MCP_STDIO_HELP and MAIN_HELP. query_messages extension skipped per refined plan §167 — moot because list_plans + get_plan already cover plan discovery; a plan_content_present filter on query_messages would duplicate list_plans's discriminator without new capability. No new tests added (matches existing mcp/tools.rs pre-C2.5 pattern of no #[cfg(test)]; underlying logic covered by 6 plans_list/plan_show tests in query.rs from C2.4). 279/279 workspace tests pass (unchanged baseline). Files modified: 2 (mcp/tools.rs +57 lines, main.rs 4 hunks). Daemon kickstart pending — running launchd daemon serves prior binary; new tools reach /mcp only after kickstart.*
  - [x] C2.5-Audit  *— completed 2026-05-09: 19 observations at .planning/audit/c2.5-audit-2026-05-09T1544-asia-shanghai.md. Counting self-check PASSED (declared 19 = grep count 19). Categories: 1 omission, 1 divergence, 2 addition, 1 dependency, 1 verification, 3 commit-message, 2 documentation, 1 test-coverage, 8 mapped-clean. Verification commands all pass: subject 49 chars, cargo build clean, 279/279 tests, grep '#[tool(' returns 17, count strings at main.rs:564/649 both '17', integrity_check ok. Headline finding: Plan §167 specifies three coordinate clauses (list_plans + get_plan + query_messages has_plan extension); impl implements two and skips query_messages extension with implementer-constructed rationale not present in plan text. Daemon kickstart pending — new tools on disk but not on /mcp until kickstart. Pre-existing 10-tool stale declarations in mcpb/manifest.json + CLAUDE.md widened by C2.5's 17-tool reality. No severity language.*
  - [x] C2.5-Triage  *— completed 2026-05-09: 19 rows triaged at .planning/audit/c2.5-triage-2026-05-09T1547-asia-shanghai.md. Counting self-check passed. Plan-spec reach: 0 plan-defect, 1 impl-defect, 1 mutual, 17 informational. Build/test impact: 0 build-blocking, 0 test-blocking, 3 runtime-only, 5 static-only, 11 none. Determinism: 19 deterministic, 0 nondeterministic, 0 unverified. Meta-plan: 0. query_messages-skip classified impl-defect (#1) — refined plan §C2.5 has three coordinate clauses joined by ";" with no optionality marker; impl unilaterally adjudicated the substitutability via list_plans+get_plan rationale.*
  - [x] C2.5-Review  *— completed 2026-05-09. Decisions per deviation:*
    *— RESOLVED via spawned C2.5.1 resolution subagent (commit 28e5344 — subject 63 chars): #1 (query_messages MCP tool now accepts has_plan: Option<bool>; semantics mirror C2.4's --has-plan: Some(true)→plan_content IS NOT NULL, Some(false)→IS NULL, None→no filter; REST /v1/messages/query also extended for surface parity; regression test test_query_messages_has_plan_filter asserts the contract. Backwards-compat verified — existing callers without has_plan deserialize to None via #[serde(default)]). 280/280 workspace tests pass (was 279 + 1 new).*
    *— RECORD (mutual; non-actionable observation about plan-impl rationale): #2 (plan §C2.5 should have ratified the substitutability relationship between query_messages.has_plan and list_plans/get_plan; impl should not have unilaterally adjudicated. Resolved structurally via C2.5.1 fix; no plan edit needed since the impl now matches the literal plan wording.)*
    *— RECORD (commit-message + documentation widened by C2.5; non-architectural): #5 (commit-body "16+1=17" arithmetic miscount — actual was 15+2=17), pre-existing 10-tool stale declarations in mcpb/manifest.json + CLAUDE.md widened by C2.5's 17-tool reality (drift exists in tracked files but not in C2.5's host files; will be refreshed in a future MCPB rebundle / CLAUDE.md cleanup pass)*
    *— RECORD (verification gap pending kickstart): #19 (live /mcp endpoint won't expose new tools or new has_plan param until daemon kickstart)*
    *— RECORD (test-coverage gap; consistent with mcp/tools.rs no-test pattern): #15 (no MCP-layer unit tests for new tools; underlying logic covered by query.rs tests)*
    *— IGNORE (informational, defensible engineering choices or precedent compliance): #3 + #4 (count strings updated correctly), #6-#9 (categorized listings, JsonSchema params, delegation correctness), #10-#14 (subject length, aim/intention framing, no Co-Authored-By, no logic duplication, daemon-routed pre-kickstart silent-drop pattern), #16-#18 (mapped-clean conformance rows), all remaining informational rows*
    *Net: C2.5 implementation accepted with C2.5.1 resolution. 1 architectural-debt impl-defect (query_messages has_plan omission) RESOLVED via C2.5.1 (commit 28e5344). 1 commit-body arithmetic miscount + pre-existing MCPB/CLAUDE.md drift RECORDed (cannot amend; future cleanup). 1 verification gap pending kickstart. 1 test-coverage gap consistent with mcp/tools.rs no-test pattern. C2.5-Review unblocks C2.6 (REST /v1/plans endpoints + daemon_client retrofit) — final Tier C2 commit.*
- [x] **C2.6** REST /v1/plans endpoints *(← C2.1-Review, closed at 0b37e00; final Tier C2 commit)*  *— landed in a2defdc (subject 64 chars). 3 new REST endpoints: GET /v1/plans (project/since/limit query params); GET /v1/plans/{session_id} (404 on miss); GET /v1/plans/search (q required + limit default 20 + offset default 0). 3 new daemon_client wrappers: plans_list, plan_show (404 → Ok(None)), plans_search. main.rs run_plans retrofitted to match on ConnectionMode — daemon path calls daemon_client; direct-DB fallback calls store-layer fns; ensure_direct_conn helper from C2.4 removed. Direct-DB exercised via CLAUDE_HISTORY_SOCKET=/nonexistent against /tmp/claude-history.dryrun-c2.3.db returning expected JSON. search_plans implemented in fts.rs querying fts_message_content joined to message_content filtered by mc.block_type='plan_content'; returns existing SearchResult shape (no new PlanSearchResult type — synthetic rows live in same FTS5 index). Same query sanitization as search_messages / search_attachment_text_content. api/mod.rs "36 routes" → "39 routes" and "36 endpoints across 16 resource groups" → "39 endpoints across 17 resource groups"; build_router catalog gains Plans group. 2 new search_plans tests. Workspace tests 282/282 (was 280 + 2 new). Files: 5 (fts.rs, api/mod.rs, api/plans.rs new, daemon_client.rs, main.rs). Daemon kickstart pending — live REST exercise deferred. Optional CLI `plans search` subcommand not added (separate ergonomic question; MCP search_messages already surfaces plan-content via the union path).*
  - [x] C2.6-Audit  *— completed 2026-05-09: 28 observations at .planning/audit/c2.6-audit-2026-05-09T1603-asia-shanghai.md. Counting self-check PASSED (declared 28 = grep count 28). Categories: 8 mapped (no deviation), 8 addition, 8 divergence, 1 omission, 2 verification, 1 commit-message. All verification commands pass: cargo build clean, 282/282 tests, integrity_check ok, subject 64 chars confirmed, route count 39 (grep) matches doc string, ensure_direct_conn fully removed, CLI direct-DB exercise green. Headline finding: implementation structurally correct against §C2.6; live /v1/plans* REST verification deferred (pre-C2.6 daemon returns 404); optional CLI `plans search` omission documented (plan §C2.6 is REST-only — search-via-MCP already exists via search_messages union path). No severity language.*
  - [x] C2.6-Triage  *— completed 2026-05-09: 28 rows triaged at .planning/audit/c2.6-triage-2026-05-09T1607-asia-shanghai.md. Counting self-check passed. Plan-spec reach: 0 plan-defect, 0 impl-defect, 0 mutual, 28 informational — **cleanest triage outcome of the entire chain (no defect-axis rows)**. Build/test impact: 0 build-blocking, 0 test-blocking, 4 runtime-only, 12 static-only, 12 none. Determinism: 28 deterministic, 0 nondeterministic, 0 unverified. Meta-plan: 0. Headline classifications: 8 mapped-clean (R1-R9 satisfied), 8 addition, 8 divergence, 1 omission (#9 no CLI plans search — documented rationale), 2 verification (#11 deferred live-curl, #27 direct-DB pass), 1 commit-message (#16 doc-test-target omission).*
  - [x] C2.6-Review  *— completed 2026-05-09. Decisions per deviation:*
    *— RECORD (verification gap pending kickstart, same pattern as all prior commits): #11 (live /v1/plans* REST exercise deferred — pre-C2.6 daemon returns 404; resolved at next kickstart)*
    *— RECORD (committed; cannot amend; minor): #16 (commit-body 282-test decomposition omits doc-test 0/0 figures — minor forensic-completeness gap; future commit bodies could include the full breakdown)*
    *— IGNORE (informational, no action needed): all 28 rows triaged informational; no impl-defects, no plan-defects, no mutual; no resolution subagent required. The 8 addition rows are project-precedent-compliant (--limit/--json analogues per B1.2), the 8 divergence rows are all defensible engineering choices documented in commit body, the 1 omission (CLI plans search) explicitly out-of-scope per refined plan §C2.6 framing as REST-only, the 2 verification rows resolve at kickstart, the 1 commit-message gap is minor forensic completeness.*
    *Net: C2.6 implementation accepted as-is — zero resolution subagent needed. The cleanest review outcome of the entire 17-implementation-commit roadmap. **Tier C2 is now structurally complete**: C2.1 + C2.1.1 + C2.3 + C2.3.1 + C2.4 + C2.4.1 + C2.5 + C2.5.1 + C2.6 all closed. **The entire post-MVP architectural roadmap is structurally complete**: A1 + A2 + B1.1 + B1.2 + C1.1 + C1.2 + C1.2.1 + C1.3 + C1.4 + C1.4.1 + C2.1 + C2.1.1 + C2.3 + C2.3.1 + C2.4 + C2.4.1 + C2.5 + C2.5.1 + C2.6 + D1 + D2 + D3 + D4 — all 23 implementation commits closed via the 4-task-per-node Audit→Triage→Review→Resolution-where-needed chain. Pending operational steps: daemon kickstart to apply migrations 008+009+010+011 and mmap the post-C2.6 binary; MCPB rebundle (A2 user-pending UI install); CLAUDE.md cleanup pass for tool-count drift.*

### Tier 4 — Cleanup (technical debt, non-blocking)

- [x] **D1** Move version_history backfill into sync_all *(no predecessors)*  *— landed in 6a91894: 38-line INSERT OR IGNORE block at end of sync_all (between "Sync complete" tracing and FTS rebuild); regression test test_sync_all_backfills_version_history with 3-version fixture asserting per-version session_count from sessions table. cargo test 129/129 store.*
  - [x] D1-Audit  *— completed 2026-05-09: 22-row catalog. Notable: D1 production block ~36 LOC vs plan estimate "~15 LOC"; regression test (~110 LOC) lives in D1 but plan §167 assigns regression-test commit ownership to D4 (internal plan inconsistency); plan §164 wording "Move" vs impl additive interpretation (watcher's startup backfill kept; impl defers removal to D2); D1 runs backfill unconditionally on no-op syncs (defensible per impl: INSERT OR IGNORE makes it cheap); 5 verification gaps pending daemon kickstart; commit message conforms to all project conventions (61-char subject, no Co-Authored-By, no definitive language). Categories used: divergence, addition, sequence, verification, test-coverage, commit-message, documentation, meta-plan.*
  - [x] D1-Triage  *— completed 2026-05-09: 14 dev rows + 5 meta-plan = 19. 0 omission, 6 divergence, 1 addition, 2 verification, 2 test-coverage, 8 meta-plan. 0 build-blocking, 0 test-blocking, 3 runtime-only, 16 static-only — minimal runtime impact, mostly observational. 6 plan-defect / 8 impl-defect / 5 mutual.*
  - [x] D1-Review  *— completed 2026-05-09. Decisions per deviation:*
    *— RESOLVED via parent-agent direct fix to plan: mp-1 (test ownership ambiguity D1 vs D4 — plan §164 updated to acknowledge D1 owns the version_history-specific regression test, D4 owns the integration test exercising D1+D3 together); mp-2 (Move vs Add — plan §164 updated from "Move" to "Add" with explicit deferral of watcher's backfill removal to D2); mp-3 (no-op sync behavior — plan §164 updated to specify backfill runs unconditionally on every sync_all invocation, INSERT OR IGNORE makes it cheap)*
    *— RESOLVED via prior commit a6edb61 (operational policy): mp-4 (procedure for verifying independent commit when workspace uncompilable — the parallel-spawn batching policy in MEMORY.md and subagent-prompt-template.md addresses exactly this case)*
    *— DEFERRED (lower-leverage; future plan-text revision): 20 + mp-5 (audit doc cited as D1 input has no D1-specific content; could update plan §221 input list to remove the audit-doc reference for D1 since the plan section itself is sufficient)*
    *— IGNORE (cosmetic, behaviorally equivalent): #3 (closure-binding identifier `\|c\|` vs `\|conn\|`), #4 (log strings differ from watcher's — site-distinguishable and appropriate)*
    *— IGNORE (test is correct, conforms to plan): #7 (placement matches commit msg), #13 (test fixture invariant of session_count=1 correctly asserted)*
    *— IGNORE (correctly scoped out per plan): #14 (OR-IGNORE-pre-existing-row case is D3 territory)*
    *— RECORD (LOC estimate vs actual; minor): #1 (plan ~15 LOC vs actual 36 LOC for production block — plan estimates updated above to ~30-40 LOC)*
    *— RECORD (verification gaps pending daemon kickstart, same pattern as B1.1): #6 (live-verify procedure), #8 (regression baselines)*
    *— RECORD (intentional engineering choice; plan now reflects): #2 (regression test in D1 — resolved by mp-1 plan update), #5 (additive interpretation — resolved by mp-2 plan update), #9 (no-op sync backfill — resolved by mp-3 plan update)*
    *— IGNORE (non-actionable observation): #19 (workspace-broken-during-verification — resolved by policy commit a6edb61), #22 (test count discrepancy 129 vs 138 — explained by other commits between D1 and audit time)*
    *Net: D1 implementation accepted as-is. Plan refinements applied to §164. 2 verification gaps pending daemon kickstart (separate user-authorized step). D1-Review unblocks D4 once D3 also reaches review.*
- [x] **D2** Sequence the daemon-startup race *(no predecessors)*  *— landed in b7e18d1: tokio::sync::oneshot signaling between serve.rs's sync_all task and watcher_loop's startup backfill query. Sender fires after sync_all returns regardless of Ok/Err. Receiver awaited as first action in watcher_loop before issuing the INSERT OR IGNORE backfill SQL. Live filesystem-event arm and cancellation arm intentionally NOT gated on the signal — only the one-shot startup backfill query waits. API listener binds and serves concurrently. Regression test watcher::tests::watcher_startup_backfill_waits_for_sync_all_signal asserts no race: spawns loop, inserts sessions row AFTER spawn but BEFORE firing oneshot, sleeps, asserts version_history still empty (no race), fires oneshot, polls until non-empty with 1s deadline, asserts observation timestamp at-or-after signal-fire timestamp. Two files changed: serve.rs (+42), watcher.rs (+144). Build clean, regression test passes. Daemon kickstart pending — current PID 2344 still on pre-99ee138 binary.*
  - [x] D2-Audit  *— completed 2026-05-09: 25-row catalog. Notable: D2 production ~30 LOC matches plan estimate; +117 lines is regression test (plan §167 assigns regression-test ownership to D4 only — same scope-blur as D1 absorbing its own test); plan §D2 verification line 314 enumerates boot-log strings that do not match actual code on the cold-boot path (plan: "Filesystem watcher established", "Starting sync", "Sync complete" vs actual: "File watcher started for projects directory" at serve.rs:134, "Startup sync completed" at serve.rs:174); functional gate works correctly via tokio::sync::oneshot; race-detector test load-bearing assertion is the pre-signal assert_eq!(0) at watcher.rs:809-812; subject 73 chars (one over conventional 72); 5 verification gaps pending daemon kickstart; initial_version query at watcher.rs:487-497 has the same cold-boot race shape as the now-gated backfill but is NOT gated (out of plan scope but observable). Linear history confirmed (parent 636aa9b). Categories used: divergence, addition, sequence, verification, test-coverage, commit-message, documentation, dependency.*
  - [x] D2-Triage  *— completed 2026-05-09: 24 triaged (20 dev + 4 meta-plan). 0 omission, 5 divergence, 1 addition, 3 sequence, 1 dependency, 1 verification, 1 regression, 1 commit-message, 1 documentation, 6 test-coverage, 4 meta-plan. 1 build-blocking, 6 test-blocking, 8 runtime-only, 9 static-only. 16 deterministic, 4 nondeterministic, 4 unverified. 5 plan-defect, 12 impl-defect, 7 mutual.*
  - [x] D2-Review  *— completed 2026-05-09. Decisions per deviation:*
    *— RESOLVED via parent-agent direct fix to plan: mp-1 + mp-2 + #3 + #4 (plan §314 boot-log strings updated to match actual code: "File watcher started for projects directory" replaces "Filesystem watcher established"; "Startup sync completed" added between "Sync complete" and "version_history backfill completed on startup" to reflect the gate's release point); mp-3 (plan §244 D2 line clarified — each D-tier item may include its own focused regression test in the file it modifies; D4 owns the integration test); mp-4 (plan §244 D2 LOC clarification — ~30 LOC is production-code only; regression tests additive)*
    *— IGNORE (mechanically required for the gate; only internal callsite): #22 (public-fn signature widening on watcher_loop)*
    *— IGNORE (defensible engineering choices, audit confirmed semantics match commit-message claims): #13 (expect panic message; load-bearing invariant assertion that cannot fire under normal control flow), #21 (warn-on-Err falls through to backfill; matches message claim), #14 (commit-message describes conceptual order, runtime is the same just with additional intermediate log strings)*
    *— IGNORE (test design observations; non-blocking): #8 (PID-based test DB path — cargo serializes tests in same process), #10 + #11 (race-detector reasoning relies on init_db sequence — the LOAD-BEARING assertion is correctly the post-signal poll, pre-signal is corroborating not load-bearing), #23 + #24 (mpsc shutdown paths and wall-clock assertion semantics)*
    *— RECORD (real but minor): #9 (1s wall-clock deadline in test could flake under heavy CI load; acceptable for now), #16 (subject 73 chars, one over conventional 72; same pattern as B1.1's 95-char subject — future commits aim for ≤72)*
    *— RECORD (out-of-scope per plan §165 but worth flagging): #12 (initial_version query at watcher.rs:487-497 has the same cold-boot race shape as the now-gated backfill; not gated by D2 because plan §165 only requires gating the backfill query; recorded as future-work analogous to ContentBlock-level drift recorded for B1.1)*
    *— RECORD (verification gaps pending daemon kickstart, same pattern as B1.1 + D1): #5 (live boot-log verification), #17 (regression baselines)*
    *— IGNORE (informational, not deviations): #19 (D2 committed alone per operational policy — confirms compliance), #20 (no documentation changes — plan §D2 doesn't require any), #2 (regression test addition — resolved by mp-3 plan fix)*
    *— RECORD (LOC estimate vs actual; same pattern as D1): #1 (plan ~30 LOC vs 185 insertions — resolved by mp-4 plan fix clarifying production-code-only estimate)*
    *Net: D2 implementation accepted as-is. Plan refinements applied to §314 (boot-log strings) and §244 (LOC + regression test scope). 2 verification gaps pending daemon kickstart. 1 future-work item recorded (#12 ungated initial_version query). D2-Review unblocks D3 once D3 reaches review and D4 once both D1+D3 reviews close.*
- [x] **D3** Unify session_count semantics *(no predecessors)*  *— landed in 2e44080: SQL swap in check_version_change's ON CONFLICT clause from `session_count = version_history.session_count + 1` to `session_count = (SELECT COUNT(*) FROM sessions WHERE version = ?1)`, aligning live path with backfill paths' truth-from-sessions semantic. Regression test `check_version_change_session_count_re_derives_from_sessions_truth` seeds 2 sessions at version 2.1.99, plants stale version_history row with session_count=0, calls check_version_change, asserts post-conflict-update session_count==2 (not 1 — which would be the +1-from-stale-0 pre-D3 result). +100/-1 LOC. Subject 62 chars (well under 72). cargo build clean; 2/2 watcher tests pass.*
  - [x] D3-Audit  *— completed 2026-05-09: 24-row catalog (8 real deviations + 16 conformance rows). Notable: #1 LOC-budget ambiguity (plan ~5 LOC vs actual +100/-1 of which production is 1 SQL line + 10-line comment block; +89 is plan-permitted regression test); #3 + #22 live-verify pending daemon kickstart (same pattern as B1.1, D1, D2); #4 + #19 plan §169/§222 cite schema-drift audit doc as D3's spec but doc has no D3 content (real plan-defect about citation accuracy); #14 test cleanup best-effort (project-precedent same as D2's test); #16 test only asserts session_count==2 not all three ON CONFLICT semantic effects (row uniqueness, last_seen_at advancement, session_id preservation). Build clean; 2/2 watcher tests pass; daemon undisturbed; D2 gate region byte-identical pre/post.*
  - [x] D3-Triage  *— completed 2026-05-09: 9 triaged (7 dev + 2 meta-plan). 1 divergence (LOC budget), 2 verification (pending kickstart), 2 test-coverage, 4 meta-plan (citation accuracy + LOC ambiguity). 0 build-blocking, 0 test-blocking, 2 runtime-only, 7 static-only. 4 plan-defect, 3 impl-defect, 2 mutual.*
  - [x] D3-Review  *— completed 2026-05-09. Decisions per deviation:*
    *— RESOLVED via parent-agent direct fix to plan: #1 + mp-2 (LOC budget clarified in plan §D3 to "~5 LOC of SQL — production-code-only budget; in-file regression test additive per Tier 4's general rule")*
    *— DEFERRED (lower-leverage; future plan-quality pass): #4 + #19 + mp-1 (plan §169 + §222 cite schema-drift audit doc as D3 spec but doc has no D3 content; loose citation chain. Future plan-text fix would either remove the citation for D-tier items or add a MEMORY.md cross-reference. Not load-bearing — D3 spec is fully in plan §166 + §316-318 + STATE.md notes.)*
    *— IGNORE (project-pattern compliant per D2 precedent; non-blocking): #14 (test cleanup best-effort matches D2's test pattern at project level)*
    *— RECORD (test-coverage gap; could be tightened in follow-up): #16 (test only asserts session_count==2; doesn't assert row uniqueness, last_seen_at advancement, or session_id preservation. A SQL change breaking any of those would still pass the test. Acceptable for D3's scope which is purely the session_count semantic; tightening adjacent assertions could be a separate test-improvement commit.)*
    *— RECORD (verification gaps pending daemon kickstart, same pattern as B1.1+D1+D2): #3 (live verify), #22 (daemon binary staleness)*
    *Net: D3 implementation accepted as-is. Plan refinement applied to §D3 LOC clarification. 1 test-coverage gap recorded for potential follow-up. 2 verification gaps pending daemon kickstart. D3-Review unblocks D4 (which is also gated on D1-Review, already closed).*
- [x] **D4** Regression test for D1 / D3 *(← D1-Review AND D3-Review — both now closed)*  *— landed in 21ec5b6: integration test `d4_integration_sync_all_then_live_check_version_change` added to crates/server/src/watcher.rs's existing `#[cfg(test)] mod tests` block. Phase 1: sync_all against 3 fixture JSONL at distinct versions; asserts version_history has 3 rows with session_count=1 each (D1 invariant). Phase 2: insert 2 additional sessions at 2.1.99 (sessions truth = 3); call check_version_change; assert session_count==3 (D3 truth-from-COUNT semantic). The =3 assertion is load-bearing: pre-D3 path would compute 1+1=2, distinguishing D3's COUNT(*) re-derive from a +1-from-D1-row mathematical accident. +245 LOC test only; no production code modified. Subject 57 chars. cargo build --release clean; cargo test -p claude-history watcher::tests passes 3/3.*
  - [x] D4-Audit  *— completed 2026-05-09: 38 observations at .planning/audit/d4-audit-2026-05-09T0853-asia-shanghai.md. Categories: 2 divergence, 8 addition, 1 sequence, 1 dependency, 9 verification, 1 commit-message, 1 documentation, 12 test-coverage, 3 meta-plan. Headline: D4 ships +245 LOC vs plan ~50 LOC budget; expansion is comment-heavy with in-code rationale duplicating commit-message body. Load-bearing session_count==3 assertion correctly distinguishes D3's COUNT(*) from a hypothetical pre-D3 +1 path (Phase 2 seeds 2 additional sessions, not 1). Production code byte-identical at V7 (D1/D2/D3 surfaces empty diff). 14/14 verification commands passed. No severity language.*
  - [x] D4-Triage  *— completed 2026-05-09: 38 rows triaged at .planning/audit/d4-triage-2026-05-09T0859-asia-shanghai.md. Plan-spec reach: 4 plan-defect, 0 impl-defect, 0 mutual, 34 informational. Build/test impact: 0 build-blocking, 0 test-blocking, 0 runtime-only, 5 static-only, 33 none. Determinism: 36 deterministic, 0 nondeterministic, 2 unverified (#37 + #38 pending daemon kickstart). Meta-plan rows: 3 (#27 LOC budget ambiguity, #28 plan §167 commenting expectations, #36 D4 expanding-test-pattern precedent). Grep self-check passed.*
  - [x] D4-Review  *— completed 2026-05-09. Decisions per deviation:*
    *— RESOLVED via parent-agent direct fix to plan: #1 + #27 + #28 (plan §167 D4 line refined: removed stale ~50 LOC budget, added test-only-no-production-code framing, two-phase shape description, discriminating-math note, LOC-loose-because-D-tier-tests-are-comment-heavy explanation citing D1's ~110 / D2's ~117 / D3's ~89 LOC test-code precedents, host-file-placement rule citing workspace-dependency direction)*
    *— IGNORE (defensible engineering choices, project-pattern compliant per D1/D2/D3 precedent): #2 (~50-line doc-comment block above test fn — matches D-tier prose density), #5 (best-effort cleanup), #6 (inlined JSONL strings — cross-crate visibility forces inlining), #7 (Phase-2 raw-SQL inserts bypass decompose by design and acknowledged in code comment), #9 (test name length matches watcher.rs precedent), #10 (~39% comment density — matches D-tier precedent), #11 (CancellationToken absent — no spawn occurs, matches D3 pattern), #12 (Option A justification doc-comment), #13 (partly end-to-end + partly unit-level — plan does not mandate stricter shape), #14 (PID-scoped temp paths match D2/D3), #19 (commit body sections duplicate in-code rationale — reasonable for forensic record), #20 (single-file working set — plan doesn't mandate D4 doc edits), #21 (linear history honors DAG), #22 (sync_all already public — no Cargo.toml change), #23 (fixture JSONL realism — cargo test 3/3 confirms deserialization), #24 (test isolated from prod DB), #29 (WatcherState construction matches D3-test pattern byte-identically), #34 (FTS5 side-effect not asserted — matches D1 precedent), #35 (Phase-2 precondition is correctly load-bearing), #36 (D4-Review unblock criterion in STATE.md not plan — coherent with §192-§196 leaf-node DAG)*
    *— IGNORE (informational, audit confirmed correctness): #3 (load-bearing math distinguishes D3 from pre-D3 +1 path), #4 (D4-test-vs-D3-test setup-shape comparison — both correct under their respective scopes), #15 (plan §322 satisfied), #16 (daemon-state-inconsistency observation — D4 is test-only so out of D4's path), #18 (commit subject 57 chars, conventional, no forbidden language), #25 (PRAGMA integrity_check returns ok), #26 (sync no-op safeguard not applicable to test-only commit), #31 + #32 + #33 (D1/D2/D3 production code byte-identical pre/post D4 — desired state)*
    *— RECORD (test-coverage gap inherited from D3-Review #16; could be tightened in a future test-improvement commit but not blocking D4 closure): #8 + #17 + #30 (assertion specificity gaps — last_seen_at advancement, session_id preservation, total-row-count post-call, untouched-row invariant for non-2.1.99 versions). The discriminating session_count==3 assertion alone is sufficient for D4's stated scope of "exercising D1+D3 together"; tightening adjacent assertions would make the test more robust against unrelated SQL changes but is not the assertion D4 was specced to make.*
    *— RECORD (verification gaps pending daemon kickstart, same pattern as B1.1+D1+D2+D3): #37 (live-ingestion latency check), #38 (no-new-WARN check)*
    *Net: D4 implementation accepted as-is. Plan refinement applied to §167 D4 line. 1 test-coverage gap (assertion specificity) recorded for potential follow-up commit. 2 verification gaps pending daemon kickstart. D4-Review closes the Tier 4 chain (D1+D2+D3+D4 all complete). With B1.1 also closed, only Tier 2 B1.2 → Tier 3 work remains.*

### Post-roadmap operational addendum

**As of: 2026-05-09 16:24 Asia/Shanghai (post-kickstart, post-CLAUDE.md/README refresh).**

The 23-commit roadmap is structurally complete (last review: C2.6-Review at d8f5ca1). The C2.6-Review entry's "Pending operational steps" list has resolved as follows:

- **Daemon kickstart** — DONE 2026-05-09 16:13. PID 60855 → 11364. Migrations 008+009+010+011 all applied at 08:13:39-40 UTC (the schema_versions timestamp is the daemon's UTC-clock time; the wall-clock kickstart was 16:13 local). Backup at `/tmp/claude-history.db.bak-d8f5ca1` (3.1 GB) retained for rollback. /v1/health returned 200 in ~2s. err.log shows clean migration application + sync_all completion + version_history backfill + 0 panics + 0 ERROR-level lines.
- **CLAUDE.md cleanup pass** (tool-count drift 10 → 17) — DONE in 859af69. Same commit creates the project's first README.md (87 lines, blandly factual). Remaining drift (mcpb/manifest.json) is bundle-state and resolves at next MCPB UI install.
- **MCPB rebundle UI install** — STILL USER-PENDING. Bundle at `mcpb/mcpb.mcpb` is the post-C2.6 binary (was post-baf8ee5 + 5d8f934 + 6143fbf when 658b345 packed it; the binary at PID 11364 supersedes that bundle's binary, so the bundle is now stale relative to live daemon). To ship the post-C2.6 surface (typed Attachment + plan_content + 17 MCP tools) to Claude Desktop, the bundle needs a re-pack against `target/release/claude-history` post-d8f5ca1 + manifest version bump (0.1.1 → 0.1.2) + drag into Claude Desktop → Settings → Extensions.
- **Post-kickstart live verification** — DONE for the gates each commit's review entry recorded. Sync currency: 9232/9246 JSONL files synced, 14 0-byte empty files correctly skipped; last sync 08:19:43 UTC; watcher live. Retrieval surfaces all functional: /v1/health, /v1/schema/record-type-drift, /v1/attachments, /v1/hook-executions, /v1/plans, /v1/plans/search, /v1/search (union path); CLI `record-type-drift`, `plans list/show`, `sessions --has-plan`, `search` route through daemon. FTS5 search returns BM25-ranked snippets with `>>>highlight<<<` markers. 81 plan_content rows fully searchable post-mig-011 backfill.

**New pending architectural debt surfaced post-kickstart**:

- [ ] **C1-attachments-backfill**: typed-Attachment infrastructure (C1.1+C1.2) is live but ~13,395 historical attachment records remain in `record_type_drift_log` only — the typed `attachments` + `hook_executions` tables hold only 48 newly-ingested rows (since 16:13 kickstart). `fts_attachment_text_content` is empty. Closing the gap requires B1.2-style bytewise re-ingestion of the 241 affected files (list at `/tmp/b1.2-affected-files.txt`, still resident). Detailed mechanics + operating process documented in MEMORY.md "Attachment Table Backfill Gap" section. Expected post-backfill state: `attachments` ~13K rows; `hook_executions` ~9K rows; `fts_attachment_text_content` populated for the 4 indexed subtypes (mcp_instructions_delta + skill_listing + edited_text_file + nested_memory). User-pending authorization.
- [ ] **ContentBlock inner-discriminator blind spot**: structurally identical to the JSONLRecord-level Unknown gap that B1.1 closed, but at the inner content-block level (MessageContent + ContentBlock enums in `crates/core/src/message.rs`). Empirically observed post-resume: `data did not match any variant of untagged enum MessageContent` for word-roots project records. Resolution shape mirrors B1.1: ~150 LOC for manual two-pass Deserialize + Unknown { block_type, raw } catch-all variant + inner-discriminator drift logging via record_type_drift_log namespace `content_block.<block_type>`. No new migration; no new tables; no new CLI/MCP/REST surfacing (drift-log-side already covers via record-type-drift). Detailed framing in MEMORY.md "Architectural Blind Spot — Unknown Discriminators" section. Concrete explanation with two-layer JSONL example + cascading deserialization-failure chain + resolution shape persisted at `.planning/audit/contentblock-inner-discriminator-explanation-2026-05-10T0615-asia-shanghai.md`.

### Initial unblocked starting points (six) — historical

```
A1, A2, B1.1, D1, D2, D3
```

Any of these may be authorized first; none block any other. **All six shipped**; this enumeration is preserved as the cross-session-rebuild starting reference.

### Cross-session rebuild procedure

If a fresh Claude Code session needs to rebuild the in-session TaskList from this section:

1. Read this section's checkbox tree.
2. For each `[ ]` line, call `TaskCreate` with the matching subject and description from the plan file's Tier sections.
3. After all 68 tasks are created, call `TaskUpdate` with `addBlockedBy` to encode dependencies per the gate annotations above (each `*(← XYZ-Review)*` becomes `addBlockedBy=[XYZ-Review-task-id]`).
4. The within-node chain (Audit ← Impl, Triage ← Audit, Review ← Triage) is implicit and must be encoded for every node.
5. For any task marked `[x]` in this section, after creation call `TaskUpdate` with `status: "completed"` to skip it.
6. For any task marked `[!]`, leave it pending; the prior session left it blocked-with-issue and the user owns the unblock decision.

The plan file at `/Users/david/.claude/plans/curious-napping-koala.md` is the canonical source of per-item subject/description content; this section is the durable status mirror.

### Status update protocol

When a task transitions in the in-session TaskList, mirror the change here within the same commit so STATE.md stays synchronized:

- Task moves to `in_progress` → flip checkbox to `[~]`
- Task completes → flip to `[x]` and add a one-line note with commit SHA below the task: `  - landed in <sha>: <one-line description>`
- Task surfaces an issue that blocks progress → flip to `[!]` and add a one-line note pointing at the audit-output or triage-output file
- Task is superseded or deleted → flip to `[-]` and add a one-line note explaining

Without this protocol, STATE.md drifts from the in-session view and the cross-session rebuild produces a stale picture.
