# B1.2 Adversarial Audit

**Recorded**: 2026-05-09T10:06 Asia/Shanghai (UTC+0800)
**Commits audited**: `baf8ee5` (code) + `ba841bb` (forensic doc)
**Plan**: `/Users/david/.claude/plans/curious-napping-koala.md` §B1 / §B1.2 / §Verification per item B1 / §Regression and continuity safeguards
**Audit type**: coverage-of-finding; no triage, no severity, no remediation
**Daemon at audit time**: PID 60855, `/v1/health` 200, mmaped `target/release/claude-history` (size 14,942,784)

---

## Verification command summary

| # | Command | Status | Note |
|---|---|---|---|
| V1 | `git show --stat baf8ee5` | OK | 7 files, 553 insertions, 13 deletions |
| V2 | `git show --stat ba841bb` | OK | 1 file (audit md), 194 insertions |
| V3 | `git log -n 1 baf8ee5 --format=%s | wc -c` | OK | subject 70 chars |
| V4 | `git log -n 1 ba841bb --format=%s | wc -c` | OK | subject 55 chars |
| V5 | `cargo build --release` | OK | clean build, no warnings |
| V6 | `cargo test -p claude-history-store` | OK | 144 tests pass |
| V7 | `cargo test -p claude-history` | OK | 3 tests pass |
| V8 | `launchctl list | grep claude-history` | OK | PID 60855 status 0 |
| V9 | `curl /v1/health` | OK | `{"status":"ok","record_count":734636}` |
| V10 | `curl /v1/schema/record-type-drift` | OK | flat JSON array; first row attachment v2.1.91 |
| V11 | `claude-history record-type-drift --limit 10` | OK | table output, 10 rows |
| V12 | `claude-history record-type-drift --type-name attachment --limit 5` | OK | 5 attachment rows |
| V13 | `sqlite3 PRAGMA integrity_check; foreign_key_check` | OK | `ok`, no FK violations |
| V14 | DB drift counts SUM | OK | attachment 12,131; last-prompt 1,576; permission-mode 1,235; custom-title 978; agent-name 447; ai-title 171 (counts above forensic-doc snapshot, daemon ingested ~1h of live data after backfill) |
| V15 | `wc -l /tmp/b1.2-sync-output.log` | OK | 173,735 lines |
| V16 | grep panic-class tokens in sync log | OK | 0 matches across the three forensic-doc panic-class patterns |
| V17 | `grep -cE 'ERROR' sync log` | OK | 0 |
| V18 | `grep -E 'Sync complete' sync log` | OK | 0 occurrences (see #C1) |
| V19 | `wc -l /tmp/b1.2-affected-files.txt` | OK | 241 |
| V20 | affected-vs-resynced intersection | OK | intersection=184; affected_only=57; resynced_only=0 (see #B5) |
| V21 | `grep WARN err.log post-resume` | OK | only legacy WARN entries pre-resume; new "untagged enum MessageContent" WARN observed at 02:03:56 (see #C7) |
| V22 | `lsof -p 60855 | grep target/release/claude-history` | OK | binary mmaped at expected size |
| V23 | `grep JSONLRecord::Unknown` codebase | OK | all match sites enumerated (see #A6) |
| V24 | `claude-history schema-drift --limit 3` | OK | unchanged output shape |
| V25 | `curl /v1/schema/drift?limit=2` | OK | unchanged grouped-by-version JSON shape |

All commands executed successfully against the live system. No command errored.

---

## Deviation catalog

### Section A — Code surface

#### #A1. [divergence] CLI subcommand flag-set diverges from schema-drift precedent
- Plan-spec ref: §B1 plan §69 ("CLI subcommand mirroring schema-drift")
- Actual state: `record-type-drift` exposes `--type-name`, `--version`, `--since`, `--limit`, `--json`. The precedent `schema-drift` exposes `--record-type`, `--limit`, `--json` (per main.rs banner at line 19). Naming is distinct (type-name vs record-type) and `record-type-drift` adds two filters (`--version`, `--since`) that schema-drift lacks.
- Observation: plan said "mirroring"; actual is parity-plus rather than mirror. Consistent with the underlying table's column shape, but mirror-claim is loose.
- Determinism: deterministic
- Verification evidence: V11; main.rs lines 174-200, 195-221 of diff

#### #A2. [divergence] HTTP endpoint response shape differs from schema-drift precedent
- Plan-spec ref: §B1 plan §70
- Actual state: `/v1/schema/record-type-drift` returns flat `Vec<RecordTypeDriftEntry>`; `/v1/schema/drift` returns `Vec<VersionDriftGroup>` (grouped-by-version). Commit message acknowledges this divergence with rationale ("record_type_drift_log has no field-name layer").
- Observation: plan §70 said "mirror"; commit message provides justification. Triage may evaluate whether the mirror-claim should be relaxed in plan text.
- Determinism: deterministic
- Verification evidence: V10, V25; api/schema.rs line 224-246 of diff

#### #A3. [addition] `record_type_drift_list` uses runtime LIKE-escape that schema-drift query does not
- Plan-spec ref: N/A
- Actual state: query.rs:686 escapes `\\`, `%`, `_` in user input and appends `ESCAPE '\\'` to the LIKE clause. The existing `schema_drift_list` (read at query.rs:618-629) does not perform this escape on its own filters.
- Observation: net-positive defensive escape but inconsistent with the precedent's handling of substring filters.
- Determinism: deterministic
- Verification evidence: V20 query.rs:686-691

#### #A4. [addition] `Box<dyn ToSql>` dynamic-binding pattern introduced; commit message admits divergence from precedent
- Plan-spec ref: N/A
- Actual state: query.rs:660 uses `Vec<Box<dyn rusqlite::ToSql>>` for dynamic param binding; schema-drift's CLI handler uses a different rebuilding pattern. Commit message acknowledges this in the `crates/store/src/query.rs` bullet.
- Observation: orthogonal pattern introduction; no plan-spec reference; commit message documents the choice.
- Determinism: deterministic
- Verification evidence: query.rs:660-697

#### #A5. [divergence] Truncation constant name diverges between drift.rs and decompose.rs
- Plan-spec ref: N/A
- Actual state: drift.rs:23 declares `const MAX_SAMPLE_VALUE_LEN: usize = 500`; decompose.rs:730 declares a separately named `const RECORD_TYPE_SAMPLE_MAX_LEN: usize = 500`. Both equal 500 but are independently declared.
- Observation: two-source-of-truth pattern; the comment in decompose.rs:728 notes the mirror but does not import the drift.rs constant.
- Determinism: deterministic
- Verification evidence: V_grep on the two constants

#### #A6. [verification] All `JSONLRecord::Unknown` match sites enumerated; one stub-completion site present
- Plan-spec ref: §B1.2 plan §68 ("Extend log_record_overflow with an Unknown arm")
- Actual state: `grep JSONLRecord::Unknown` in crates/ shows 22 occurrences spanning record.rs, drift.rs, decompose.rs, query.rs, daemon_client.rs, main.rs, migrations/007, decompose tests. Only two `match` arms in production code: decompose.rs:69 and drift.rs:237. Both now active (no stubs).
- Observation: no remaining stub match arms found.
- Determinism: deterministic
- Verification evidence: V23

#### #A7. [addition] `print_record_type_drift` re-implements truncation rather than reusing `print_drift_grouped` helpers
- Plan-spec ref: N/A
- Actual state: output.rs:495-547 inline-re-implements three independent truncations (type_name to 24, version to 10, sample to 60) using direct slice indexing. `print_drift_grouped` (read at output.rs:494) uses similar inline patterns without a shared helper.
- Observation: project-pattern-consistent inline-truncation; no shared helper exists.
- Determinism: deterministic
- Verification evidence: output.rs:495-547

#### #A8. [divergence] CLI handler `--limit` defaults to 50; HTTP query param `limit` has no default
- Plan-spec ref: N/A
- Actual state: main.rs:198 sets `default_value = "50"` for the CLI `--limit`; the REST handler at api/schema.rs:235 takes `Option<usize>` with no default and the daemon_client passes `Some(limit)` always (main.rs:1568) so the CLI imposes the cap on every daemon call.
- Observation: behavioral asymmetry between CLI and direct REST callers (REST without limit returns all rows; CLI always sends 50).
- Determinism: deterministic
- Verification evidence: main.rs:198, api/schema.rs:104, daemon_client.rs:434-441

### Section B — Backfill execution / forensic-doc claims

#### #B1. [divergence] Plan §71 estimated "~79 affected files"; actual affected-files list has 241 entries
- Plan-spec ref: §B1.2 plan §71 ("the ~79 affected files")
- Actual state: `/tmp/b1.2-affected-files.txt` contains 241 entries. Forensic doc records 241 in its first paragraph. Plan estimate 79 is roughly 1/3 of actual.
- Observation: divergence between plan estimate and observed scope. Likely staleness in plan text vs. corpus growth.
- Determinism: deterministic
- Verification evidence: V19

#### #B2. [divergence] Forensic doc claims 184 of 9,187 files re-decomposed; affected list is 241
- Plan-spec ref: §B1.2 plan §71 + §safeguard 8
- Actual state: 241 affected files were pre-computed; 184 were actually re-decomposed by the sync pass. 57 affected files were not re-decomposed.
- Observation: gap between affected-list scope and resync scope. Forensic-doc open-question 3 flags this; intersection check now confirms 57 affected files were skipped by the sync's mtime/size fast-path.
- Determinism: deterministic
- Verification evidence: V20 (intersection=184, affected_only=57, resynced_only=0)

#### #B3. [verification] Forensic doc reports counts as of pre-snapshot; live DB now shows higher numbers
- Plan-spec ref: §B1 verification §274
- Actual state: forensic doc table shows attachment=11,203, last-prompt=1,400, permission-mode=1,091, custom-title=966, agent-name=315, ai-title=40. Audit-time live counts: attachment=12,131; last-prompt=1,576; permission-mode=1,235; custom-title=978; agent-name=447; ai-title=171.
- Observation: live-system has ingested additional records via watcher between forensic-doc capture (~01:55 UTC) and audit (~02:06 UTC). All discriminators still exceed corpus-minimum gates. ai-title growth from 40 → 171 is the largest proportional jump.
- Determinism: deterministic
- Verification evidence: V14 vs forensic-doc table

#### #B4. [verification] Per-discriminator gate arithmetic verified
- Plan-spec ref: §B1 verification §274
- Actual state: attachment 11,203 ≥ 10,085 (margin 1,118); last-prompt 1,400 ≥ 1,396 (margin 4); permission-mode 1,091 ≥ 868 (margin 223); custom-title 966 ≥ 884 (margin 82); agent-name 315 ≥ 296 (margin 19); ai-title 40 ≥ 22 (margin 18). All pass at forensic-doc snapshot. Last-prompt margin of 4 is the smallest.
- Observation: arithmetic correct.
- Determinism: deterministic
- Verification evidence: V14 + forensic-doc table

#### #B5. [verification] Daemon-pause window strictly larger than sync execution window
- Plan-spec ref: §safeguard 7
- Actual state: forensic doc records unload "~01:29 UTC", sync 01:30:07 → 01:50:40 UTC, load 01:55:55 UTC. Pause window ≈ 26 min; sync wall-clock ≈ 20 min 33 s. Pause strictly contains sync.
- Observation: §safeguard 7 honored. ~5-minute gap between sync end and load — daemon was idle during that window per forensic doc.
- Determinism: deterministic
- Verification evidence: forensic doc lines 154-156

#### #B6. [verification] PID 60855 alive at audit time and mmaping post-baf8ee5 binary
- Plan-spec ref: §safeguard 4 + forensic doc claim
- Actual state: `launchctl list` returns `60855 0 com.davidrex.claude-history`; `lsof -p 60855` shows `target/release/claude-history` size 14,942,784 bytes mapped as `txt`.
- Observation: forensic-doc claim verified.
- Determinism: deterministic
- Verification evidence: V8, V22

#### #B7. [verification] Idempotency assertion math internally consistent
- Plan-spec ref: §safeguard 9
- Actual state: pre-counts (734,222 / 735,083 / 263,644 / 28,376 / 427,119 / 184) → post-counts (734,237 / 735,098 / 263,650 / 35,283 / 427,129 / 3,832). All deltas non-negative.
- Observation: arithmetic correct.
- Determinism: deterministic
- Verification evidence: scratch /tmp/b1.2-pre-counts.txt + /tmp/b1.2-post-counts.txt

#### #B8. [verification] system_events delta of +6,907 disproportionately large
- Plan-spec ref: N/A
- Actual state: system_events grew by 6,907; messages grew by 15; tool_executions by 6; token_usage by 10. The system_events jump is ~460× the messages jump.
- Observation: forensic doc explains this as previously-Unknown discriminators now landing in system_events (custom-title, ai-title, agent-name, permission-mode are routed via decompose_unknown into system_events per design). Per-discriminator total of ~3,832 drift-log rows × distinct-versions does not directly equal 6,907 — gap unexplored in forensic doc.
- Determinism: deterministic
- Verification evidence: pre/post counts; forensic doc lines 99-114

### Section C — Forensic-doc open questions / claim verification

#### #C1. [verification] Forensic-doc claim "sync_all does not emit Sync complete at INFO level" is contradicted by code
- Plan-spec ref: forensic-doc lines 78-80, 191
- Actual state: `crates/store/src/sync.rs:374-380` emits `tracing::info!(... "Sync complete")` unconditionally at the end of `sync_all`. The watcher's startup-catch-up sync at 00:08:42 UTC produced this exact line in err.log: "Sync complete files_synced=1 ...". The B1.2 sync log at /tmp/b1.2-sync-output.log contains zero `Sync complete` matches across 173,735 lines.
- Observation: the emission exists in code and is emitted by the daemon's startup path. Its absence from the B1.2 sync log indicates the CLI sync process was killed before reaching line 380, OR stdio/RUST_LOG filtering suppressed it, OR the tail of the log is truncated. Forensic-doc framing as "logger-emission gap" inferred-from-code is incorrect.
- Determinism: deterministic
- Verification evidence: V18; sync.rs:374-380; err.log line `00:08:42.602740Z ... "Sync complete"`

#### #C2. [verification] Forensic-doc claim "pre-drift-log was 184 not 0" verified against pre-counts
- Plan-spec ref: forensic-doc lines 53-56
- Actual state: `/tmp/b1.2-pre-counts.txt` records `record_type_drift_log 184`. This is consistent with the watcher having logged drift entries between B1.1 deployment (when migration 007 first applied to live DB) and the pre-snapshot moment.
- Observation: claim factually correct. The 184 baseline does not invalidate the +3,648 delta but does mean "drift log was empty before backfill" cannot be asserted.
- Determinism: deterministic
- Verification evidence: V14, /tmp/b1.2-pre-counts.txt

#### #C3. [verification] Affected-files-vs-resynced intersection: 184/241 affected files were resynced
- Plan-spec ref: forensic-doc open question 3 (lines 192-194)
- Actual state: cross-check via grep against sync log shows intersection = 184; 57 affected files were not in the resync set; 0 resynced files were outside the affected list.
- Observation: 57 of the 241 affected files were skipped by the sync's mtime/size fast-path. The full coverage of "files that contain unknown discriminators" was not achieved by this sync pass alone. Forensic-doc framed this as "indirectly evidenced by corpus-minimum gates passing"; the direct cross-check now exists.
- Determinism: deterministic
- Verification evidence: V20

#### #C4. [verification] §safeguard 8 ("scoped sync_metadata reset") — scope not directly verifiable
- Plan-spec ref: §safeguard 8
- Actual state: forensic doc does not record the actual `UPDATE sync_metadata SET last_byte_offset = 0 WHERE file_path IN (<list>)` query that was run, nor whether the WHERE clause matched all 241 files or some subset. The sync's mtime/size fast-path may have re-skipped files whose offset reset did land but whose mtime did not change post-reset.
- Observation: §safeguard 8 cannot be confirmed honored from the forensic doc alone.
- Determinism: unverified
- Verification evidence: forensic-doc absence of the reset query text

#### #C5. [verification] §safeguard 1 (DB backup) honored
- Plan-spec ref: §safeguard 1
- Actual state: `/tmp/claude-history.db.bak-b1.2` exists, 3,320,553,472 bytes, retained.
- Observation: backup present.
- Determinism: deterministic
- Verification evidence: ls output

#### #C6. [verification] §safeguard 1 — backup `PRAGMA integrity_check` not recorded
- Plan-spec ref: §safeguard 1 line 346 ("sqlite3 /tmp/...db.bak-... PRAGMA integrity_check  # confirm backup")
- Actual state: forensic doc confirms only the post-sync live-DB integrity_check (line 142). Backup-file integrity_check absent from doc.
- Observation: §safeguard 1's confirm-backup step not evidenced.
- Determinism: unverified
- Verification evidence: forensic doc lines 38-39, 138-145

#### #C7. [verification] Post-resume err.log shows new WARN category
- Plan-spec ref: §safeguard 16
- Actual state: at 2026-05-09T02:03:56Z (post-resume, audit-time-adjacent), err.log has WARN entries `data did not match any variant of untagged enum MessageContent` for two lines in `-Users-david-Projects-word-roots/55b4637a-...jsonl`. The pre-resume legacy WARN category was `unknown variant 'attachment'` etc., which the forensic-doc-claim implied would stop. The B1.2 fix specifically addresses unknown record-type variants (the outer `JSONLRecord` enum); it does not address inner `MessageContent` enum failures.
- Observation: post-resume daemon emits a different WARN category (inner-enum failure) that B1.2 was not designed to address. Whether this category was present pre-resume is not directly captured by forensic doc; observation is that B1.2 narrowed but did not eliminate WARN noise.
- Determinism: deterministic
- Verification evidence: tail err.log post 02:03:56Z

#### #C8. [verification] Sync log final entries truncated mid-stream (no terminal banner)
- Plan-spec ref: forensic-doc line 80
- Actual state: last 5 log lines all `Observed N schema drift field(s)` for various record_types under version 2.1.128. Last line at 01:50:40.895597Z. No `Sync complete` line. No `Synced 184 files` summary. No SIGINT/SIGTERM trace.
- Observation: termination diagnostics inconclusive from log alone. The forensic doc's "natural EOF" inference rests on process-absence + zero panics.
- Determinism: deterministic
- Verification evidence: V18, tail of /tmp/b1.2-sync-output.log

### Section D — Tests

#### #D1. [test-coverage] No CLI handler test for `run_record_type_drift`
- Plan-spec ref: §B1.2 plan §91 ("tests in those crates")
- Actual state: tests for the CLI's `run_record_type_drift` function (main.rs:1521-1592) absent from any test module. The store-layer query has 4 unit tests; drift.rs has 2 new tests for the Unknown arm; api/schema.rs has no test for the new handler.
- Observation: no in-process integration test confirms CLI flag → handler → daemon-client → REST → store-query end-to-end behavior.
- Determinism: deterministic
- Verification evidence: grep test in main.rs has no record-type-drift coverage

#### #D2. [test-coverage] No REST route integration test for `/v1/schema/record-type-drift`
- Plan-spec ref: §B1.2 plan §91
- Actual state: api/schema.rs has no test asserting the new route returns 200 with the expected JSON shape. Existing precedent: `/v1/schema/drift` is also not directly route-tested in the file (read at api/schema.rs).
- Observation: gap consistent with precedent; not a regression but a coverage gap.
- Determinism: deterministic
- Verification evidence: grep test in api/schema.rs

#### #D3. [test-coverage] No test for `daemon_client::record_type_drift` query-string assembly
- Plan-spec ref: §B1.2 plan §91
- Actual state: daemon_client.rs:434 introduces a urlencoded query-string builder; no test asserts the produced URL for various filter combinations.
- Observation: gap consistent with precedent (`schema_drift_grouped` similarly untested in-file).
- Determinism: deterministic
- Verification evidence: grep test in daemon_client.rs

#### #D4. [test-coverage] No test for the `--limit` CLI flag's default (50) or for empty-result handling
- Plan-spec ref: N/A
- Actual state: `run_record_type_drift` returns `ExitCode::SUCCESS` after printing "No record-type drift detected." on empty (main.rs:1581-1584). No test asserts this behavior.
- Observation: empty-result path uncovered.
- Determinism: deterministic
- Verification evidence: main.rs:1581-1584

#### #D5. [test-coverage] No test for `sample_value` truncation through `log_record_overflow`'s Unknown arm
- Plan-spec ref: B1.2 plan implicit
- Actual state: drift.rs's two new B1.2 tests cover with-version and no-version paths but not the >MAX_SAMPLE_VALUE_LEN truncation path. The existing `test_decompose_unknown_truncates_large_sample` covers the decompose path but not the log_record_overflow path.
- Observation: truncation logic in the Unknown arm at drift.rs:242-250 is uncovered by direct test.
- Determinism: deterministic
- Verification evidence: drift.rs:813-902

#### #D6. [test-coverage] No test for the LIKE escape in `record_type_drift_list`
- Plan-spec ref: N/A
- Actual state: query.rs:687-689 escapes `\\`, `%`, `_` in user input. No test exercises a `type_name` filter containing `%` or `_` to confirm the escape works (i.e., a user query for `type_name=foo%bar` should match the literal substring `foo%bar`, not `foo<anything>bar`).
- Observation: escape logic uncovered.
- Determinism: deterministic
- Verification evidence: query.rs:686-691; tests at query.rs:1782-1853 do not include escape probe

### Section E — Commit-message audit

#### #E1. [commit-message] Subject of `baf8ee5` is exactly 70 chars (within 72-char convention)
- Plan-spec ref: project mandate (subject ≤72)
- Actual state: subject `feat(store,server): B1.2 record-type drift logging + CLI/REST surfaces` = 70 chars.
- Observation: within bounds. The B1.1 audit flagged a 95-char subject as a divergence; this commit corrects the trend.
- Determinism: deterministic
- Verification evidence: V3

#### #E2. [commit-message] No "ensures"/"fixes"/"guarantees" detected in either commit body
- Plan-spec ref: project mandate (no unjustified definitives)
- Actual state: grep against commit bodies returns 0 matches for `ensure|guarantee|fixes`.
- Observation: aim/intention framing honored.
- Determinism: deterministic
- Verification evidence: grep results above

#### #E3. [commit-message] No `Co-Authored-By` lines in either commit
- Plan-spec ref: project mandate (no author-credit)
- Actual state: grep returns 0 matches.
- Observation: complies.
- Determinism: deterministic
- Verification evidence: grep results

#### #E4. [commit-message] `baf8ee5` body claims "Bytewise re-ingestion of the ~79 historical files"
- Plan-spec ref: project mandate (forensic accuracy)
- Actual state: body line repeats the plan's stale "~79" estimate. The actual affected-files list is 241; 184 were re-decomposed. ba841bb later corrects to "184 of 9,187".
- Observation: factual divergence between baf8ee5's body text and ba841bb's recorded execution. baf8ee5 wrote before the backfill scan finished and inherited the stale plan number.
- Determinism: deterministic
- Verification evidence: git show baf8ee5 last paragraph

#### #E5. [commit-message] `baf8ee5` body says "144 store tests (was 138; +6 new)"; current count is 144
- Plan-spec ref: project mandate (forensic accuracy)
- Actual state: V6 reports 144 tests pass — matches.
- Observation: claim verified.
- Determinism: deterministic
- Verification evidence: V6

#### #E6. [commit-message] `baf8ee5` body says "3 watcher tests passing"; current count is 3
- Plan-spec ref: project mandate
- Actual state: V7 reports 3 tests pass.
- Observation: verified.
- Determinism: deterministic
- Verification evidence: V7

#### #E7. [commit-message] `ba841bb` body claims "no foreign_key_check rows"; verified
- Plan-spec ref: project mandate
- Actual state: V13 returns no rows.
- Observation: verified.
- Determinism: deterministic
- Verification evidence: V13

#### #E8. [commit-message] `ba841bb` body says "PID 60855 mmaped binary verified via lsof"; verified
- Plan-spec ref: project mandate
- Actual state: V22 confirms.
- Observation: verified.
- Determinism: deterministic
- Verification evidence: V22

### Section F — Sequence / dependency / scope

#### #F1. [sequence] B1.2 lands at correct DAG position (after B1.1, before C1.1)
- Plan-spec ref: §Spawn-order DAG hard-rule 1, 2
- Actual state: `git log --oneline` shows `99ee138` (B1.1) then `baf8ee5` then `ba841bb`. C1.1 not yet committed.
- Observation: DAG order honored.
- Determinism: deterministic
- Verification evidence: git log output

#### #F2. [dependency] B1.2 references no migration 008 or later
- Plan-spec ref: §Spawn-order DAG hard-rule 3
- Actual state: code touches no migration files. `git diff 18715f3..baf8ee5 -- 'crates/store/migrations/'` produces no output.
- Observation: no premature dependency.
- Determinism: deterministic
- Verification evidence: V_diff_migrations

#### #F3. [addition] `crates/server/src/output.rs` modified; not in plan §B1.2 file list
- Plan-spec ref: §B1.2 plan §88-91 (file list)
- Actual state: plan §88-91 lists "crates/server/src/main.rs", "crates/server/src/api/schema.rs", "Bytewise re-ingestion script", "Tests in those crates". Commit also modifies output.rs (table formatter), api/mod.rs (route registration), daemon_client.rs (HTTP-call helper), and crates/store/src/{drift,query}.rs.
- Observation: store crate touches were declared in plan §82 (B1.1 surfaces) but B1.2's §87-91 file list does not enumerate output.rs / api/mod.rs / daemon_client.rs explicitly. Mechanically required for the surfaces to function.
- Determinism: deterministic
- Verification evidence: V1 stat lists 7 files

#### #F4. [addition] `crates/store/src/query.rs` added but not in plan §87-91 B1.2 file list
- Plan-spec ref: §B1.2 plan §87-91
- Actual state: query.rs received the new `RecordTypeDriftEntry` struct + `record_type_drift_list` query function (+170 lines incl. tests). Plan §87-91 lists only main.rs, api/schema.rs, ingestion script, tests.
- Observation: same pattern as #F3 — plan §87-91 underspecified for the actual surface required.
- Determinism: deterministic
- Verification evidence: V1 stat

### Section G — Documentation

#### #G1. [documentation] No CLAUDE.md or MEMORY.md updates in B1.2
- Plan-spec ref: N/A explicit; project pattern allows omission
- Actual state: `git diff 18715f3..baf8ee5` modifies no `.md` files outside the forensic-audit-doc commit ba841bb.
- Observation: no documentation update for the new CLI subcommand or REST endpoint. Project pattern from B1.1 was to defer doc additions.
- Determinism: deterministic
- Verification evidence: V1 stat

#### #G2. [documentation] Forensic doc filename uses "0956" but commit at "0958"
- Plan-spec ref: N/A
- Actual state: filename `b1.2-backfill-execution-2026-05-09T0956-asia-shanghai.md`; commit timestamp `Sat May 9 09:58:25 2026`.
- Observation: 2-minute drift between filename and commit-author-time.
- Determinism: deterministic
- Verification evidence: ba841bb commit metadata + filename

#### #G3. [documentation] Forensic doc line 153 records "PID at unload = 2344" but no evidence in scratch artifacts
- Plan-spec ref: N/A
- Actual state: forensic-doc daemon-transition table cites "PID 2344 (per parent diagnosis)" with no scratch-file or err.log corroboration in the retained /tmp/* artifacts.
- Observation: external-to-this-doc claim; not directly verifiable from retained artifacts.
- Determinism: unverified
- Verification evidence: forensic doc line 153

### Section H — Meta-plan defects

#### #H1. [meta-plan] Plan §71 estimate "~79 affected files" is stale relative to current corpus
- Plan-spec ref: §B1 plan §71
- Actual state: actual scan returned 241 affected files; only 184 were re-decomposed.
- Observation: plan-text staleness. Estimate is from earlier corpus survey; growth + multi-version-emission inflated the count by ~3×.
- Determinism: deterministic
- Verification evidence: V19

#### #H2. [meta-plan] Plan §B1.2 file-list (§87-91) underspecifies the actual surface needed
- Plan-spec ref: §B1.2 plan §87-91
- Actual state: plan lists 4 surfaces; commit touches 7 files. Plan does not enumerate output.rs (table formatter), daemon_client.rs (HTTP-call helper), api/mod.rs (route registration), or crates/store/src/query.rs (new query function + struct).
- Observation: plan-text underspecification.
- Determinism: deterministic
- Verification evidence: V1 stat vs §87-91

#### #H3. [meta-plan] Plan §69 "mirroring schema-drift" claim mismatches actual divergence
- Plan-spec ref: §B1 plan §69, §70
- Actual state: implementations diverge in flag set (CLI), response shape (REST), and ordering field (last_seen_at DESC vs schema-drift's first_seen_at-bucketed grouping).
- Observation: plan word "mirroring" overstates parity.
- Determinism: deterministic
- Verification evidence: V11, V25

#### #H4. [meta-plan] Plan §safeguard 8 prescribes scoped reset query but does not require recording the executed query
- Plan-spec ref: §safeguard 8
- Actual state: forensic doc does not contain the literal `UPDATE sync_metadata ...` query that was run.
- Observation: §safeguard 8 verifiability gap; plan does not require the executor to record the actual query text.
- Determinism: deterministic
- Verification evidence: forensic doc absence

#### #H5. [meta-plan] Plan §B1 verification §274 corpus-minimum gates do not anticipate continued post-backfill ingestion
- Plan-spec ref: §B1 verification line 274
- Actual state: gates compare current totals against historical minima (e.g., `attachment ≥ 10,085`). They do not differentiate "newly recovered records by backfill" from "incremental new records ingested since corpus survey." Live counts at audit time are well above gates because of both contributions.
- Observation: passing gates is necessary but is consistent with multiple causes; backfill effectiveness alone is not isolatable from the gate-pass.
- Determinism: deterministic
- Verification evidence: V14 vs forensic-doc table

#### #H6. [meta-plan] Plan does not require post-resume WARN-category-pre/post comparison
- Plan-spec ref: §safeguard 16
- Actual state: §safeguard 16 says "the seven known JSONLRecord variants must continue to deserialize byte-identically" via `crates/core/src/record.rs` test net. Does not require comparing err.log WARN categories pre vs post resume.
- Observation: WARN-category pre/post diff would surface #C7 systematically; plan currently lacks this requirement.
- Determinism: deterministic
- Verification evidence: §safeguard 16 text

#### #H7. [meta-plan] Plan §safeguard 1 "confirm backup" step lacks evidentiary recording requirement
- Plan-spec ref: §safeguard 1 line 346
- Actual state: plan prescribes `sqlite3 ... PRAGMA integrity_check` on the backup but does not require the result to be recorded in the forensic doc. ba841bb does not record it.
- Observation: §safeguard 1 verification-evidence gap.
- Determinism: deterministic
- Verification evidence: forensic doc absence

---

## Coverage notes

- 11 categories used: omission (0), divergence (8), addition (5), sequence (1), dependency (1), verification (15), regression (0), commit-message (8), documentation (3), test-coverage (6), meta-plan (7).
- Total observations: 54.
- No severity language. No remediation language.
- Read-only audit; no source files, plan, MEMORY.md, STATE.md, or production assets modified.
- All listed verification commands executed against the live system. None errored.

---

## Self-check

A regex self-check for the prohibited severity vocabulary against this file is expected to return empty (matches inside grep-pattern code-fence literals are excluded by intent).
