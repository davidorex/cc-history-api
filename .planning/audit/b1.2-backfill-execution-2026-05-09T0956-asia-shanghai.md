# B1.2 Backfill Execution Evidence

**Recorded**: 2026-05-09T09:56 Asia/Shanghai (UTC+0800) — i.e. 2026-05-09T01:56 UTC.
**Code commit under verification**: `baf8ee5 feat(store,server): B1.2 record-type drift logging + CLI/REST surfaces`.
**Author of this record**: continuation subagent picking up from a prior subagent that committed the code, started the backfill sync, then suspended mid-procedure with the daemon still unloaded.

This document captures the post-execution forensic state of the bytewise re-ingestion backfill that B1.2 prescribes. The intent is to leave a complete-enough trail that a future reader can reconstruct what happened, what was measured, and what assumptions were checked, without re-running anything.

---

## Inputs

### Affected-files list

`/tmp/b1.2-affected-files.txt` — 241 entries, 28,893 bytes. Generated by the prior subagent prior to sync. Not regenerated here; relied on as-is.

First 5 entries (evidence sample):

```
/Users/david/.claude/projects/-Applications-VCV-Rack-2-Pro-app-Contents-Resources/433e62b0-2b3e-4c99-aea7-1609c7fb2102.jsonl
/Users/david/.claude/projects/-Applications-VCV-Rack-2-Pro-app-Contents-Resources/7e705dd5-dd01-4e46-ad1d-148d074727c0.jsonl
/Users/david/.claude/projects/-Applications-VCV-Rack-2-Pro-app-Contents-Resources/c5e078f1-0417-4e23-8f7f-eaa45c6790d2.jsonl
/Users/david/.claude/projects/-Applications-VCV-Rack-2-Pro-app-Contents-Resources/dd90a715-d663-478d-955b-2ac88f129eaf.jsonl
/Users/david/.claude/projects/-Applications-VCV-Rack-2-Pro-app-Contents-Resources/f75af0ef-9951-4ca4-af51-8ec5af984a7d.jsonl
```

Last 5 entries:

```
/Users/david/.claude/projects/-Users-david-Projects-workflowsPiExtension/f5cae5d4-2180-41eb-8351-3ee9287d2059.jsonl
/Users/david/.claude/projects/-Users-david-Projects-workflowsPiExtension/f6a55d0b-84b1-4894-8117-c96a50f395fb.jsonl
/Users/david/.claude/projects/-Users-david-Projects-workflowsPiExtension/fa30af98-c05f-402e-b105-21c1239e9b18.jsonl
/Users/david/.claude/projects/-Users-david-Projects/8c923414-4f35-48df-905f-267df7cdacdd.jsonl
/Users/david/.claude/projects/-Users-david-Projects/e3e6c51c-2b3d-4b93-99a8-28c72060d412.jsonl
```

### Database backup

`/tmp/claude-history.db.bak-b1.2` — 3,320,553,472 bytes (~3.32 GB). Captured by the prior subagent at 2026-05-09 09:29 Asia/Shanghai, immediately before sync. Retained as rollback target. Not deleted by this continuation.

---

## Pre-counts

Captured by the prior subagent before invoking sync. From `/tmp/b1.2-pre-counts.txt`:

| Table | Pre-count |
|---|---|
| messages | 734,222 |
| message_content | 735,083 |
| tool_executions | 263,644 |
| system_events | 28,376 |
| token_usage | 427,119 |
| record_type_drift_log | 184 |

Note: `record_type_drift_log` was non-empty at pre-snapshot. The 184 rows likely reflect ingestion writes that occurred between B1.2 code deployment (binary built from `baf8ee5`) and the moment the pre-snapshot was taken. The pre-vs-post comparison is therefore monotonic-progress-only; it does not certify "sync started from a freshly-empty drift-log table." That would require a TRUNCATE step which the plan did not prescribe.

---

## Sync execution

### Invocation context

The prior subagent ran `claude-history sync` with the daemon unloaded (so the watcher would not race the CLI ingestion path). Output was redirected to `/tmp/b1.2-sync-output.log`. The CLI path runs the same `sync_all` code as the daemon's startup catch-up; it is bytewise-idempotent on previously-ingested rows because primary keys are deterministic.

### Log statistics

- File size: 39,863,527 bytes (~38 MB; gitignored, lives only in `/tmp`).
- Line count: 173,735.
- First entry timestamp: `2026-05-09T01:30:07.359955Z` (Starting sync; `db_path=/Users/david/.claude/.claude-history.db`, `projects_dir=/Users/david/.claude/projects`).
- Files discovered by sync scan: 9,187 (per `Starting sync … files_discovered=9187`).
- Last entry timestamp: `2026-05-09T01:50:40.895597Z`.
- Total sync wall-clock: ~20 min 33 s.
- "Synced file" entries (files actually re-decomposed): 184. The remaining 9,003 of 9,187 were skipped on mtime/size match — bytewise-idempotent fast-path. The 184 re-decomposed files are the ones that had been written or modified since their last ingestion record, which includes the files that B1.1 + B1.2 needed in order to flush previously-Unknown-discriminator records into `record_type_drift_log` and (as applicable) `system_events`.

### Termination assessment

- `panic | FATAL | backtrace | RUST_BACKTRACE` matches: **0**.
- `ERROR`-level log lines: **0**.
- `Sync complete` / `files_synced=` / `total_records=` terminal line: **not present**. The current `sync_all` code path emits per-file INFO and per-drift INFO lines but does not appear to emit a single explicit "completion banner" line at this log level. The log ends at 01:50:40 mid-stream (last lines are normal drift INFO entries) and no `claude-history sync` process is currently running (`pgrep -fl 'claude-history sync'` returns no process). Conclusion: sync exited cleanly via natural EOF; the absence of a "Sync complete" string is a logger-emission gap, not evidence of a crash.

Last 3 "Synced file" entries (evidence of orderly progress at termination):

```
01:50:34.049976Z  Synced file=…/MUSE-SYNTH/…/agent-a2fa4e23652f3928e.jsonl  records=160  warnings=0  overflow_fields=757
01:50:36.444365Z  Synced file=…/MUSE-SYNTH/…/agent-a1ba114f21b2c2c9c.jsonl  records=95   warnings=0  overflow_fields=411
01:50:40.774461Z  Synced file=…/MUSE-SYNTH/…/agent-ab1cd7c2ea4e57c0b.jsonl  records=179  warnings=0  overflow_fields=813
```

All terminal entries report `warnings=0`. No file-level error rollups appear in the log.

---

## Post-counts

Read live from `~/.claude/.claude-history.db` while the daemon was still unloaded (clean read, no concurrent writers). Saved to `/tmp/b1.2-post-counts.txt`:

| Table | Pre-count | Post-count | Delta |
|---|---|---|---|
| messages | 734,222 | 734,237 | +15 |
| message_content | 735,083 | 735,098 | +15 |
| tool_executions | 263,644 | 263,650 | +6 |
| system_events | 28,376 | 35,283 | +6,907 |
| token_usage | 427,119 | 427,129 | +10 |
| record_type_drift_log | 184 | 3,832 | +3,648 |

### Idempotency assertion

Every post-count is greater than or equal to its pre-count. The bytewise-idempotency safeguard (sync must not delete or shrink existing well-formed rows during a re-ingestion pass) holds.

The +6,907 jump in `system_events` aligns with the B1.2 intent: previously-`Unknown`-variant records (the six discriminators below) are now routed into `system_events` rather than discarded, so any of the 184 re-decomposed files that previously contained these discriminators will now contribute additional rows.

The +3,648 jump in `record_type_drift_log` is the B1.1+B1.2 mechanism functioning end-to-end: each (type_name, version) combination encountered during decomposition writes one INSERT … ON CONFLICT DO UPDATE row.

---

## Per-discriminator drift counts

Captured to `/tmp/b1.2-drift-counts.txt`:

| Discriminator | Distinct versions | Total occurrences | Last seen | Corpus minimum | Verdict |
|---|---|---|---|---|---|
| attachment | 20 | 11,203 | 2026-05-09 01:50:36 | 10,085 | PASS |
| last-prompt | 1,400 | 1,400 | 2026-05-09 01:49:54 | 1,396 | PASS |
| permission-mode | 1,091 | 1,091 | 2026-05-09 01:49:16 | 868 | PASS |
| custom-title | 966 | 966 | 2026-05-09 01:49:54 | 884 | PASS |
| agent-name | 315 | 315 | 2026-05-09 01:49:54 | 296 | PASS |
| ai-title | 40 | 40 | 2026-05-09 01:38:17 | 22 | PASS |

All six known unknowns from the schema-drift modeling audit are present and exceed the corpus-minimum gates derived from prior `extra_json` field-counting analysis. No discriminator is missing.

The `distinct_versions` column for the four singular discriminators (last-prompt, permission-mode, custom-title, agent-name) equals their `total_occurrences`, indicating that each row in those discriminator buckets is for a unique (type_name, version) combination — i.e. each occurrence in those classes happens at a single distinct Claude Code version. The high distinct-version counts (1,400 / 1,091 / 966 / 315) reflect the fact that Claude Code emits these discriminators across many sub-versions of its output and the drift-log row key is `(type_name, version)`. The `attachment` row (20 distinct versions) is the discriminator most concentrated by version.

The total of 3,832 rows in `record_type_drift_log` versus the 11,203 + 1,400 + 1,091 + 966 + 315 + 40 = 15,015 sum of `total_occurrences` reflects the row-key collapse: `occurrence_count` aggregates within (type_name, version) groups via ON CONFLICT DO UPDATE.

---

## Schema integrity

`/tmp/b1.2-integrity.txt`:

- `PRAGMA integrity_check;` → `ok`
- `PRAGMA foreign_key_check;` → no rows returned

No structural corruption, no orphan FK references introduced by the backfill.

---

## Daemon transition

| Event | Time | PID | Notes |
|---|---|---|---|
| Pre-pause daemon (PID at unload) | (per parent diagnosis) | 2344 | Was running pre-99ee138 binary (kickstart-debt accumulated across A2 + B1.1 + D1 + D2 + D3 + D4) |
| Daemon unloaded by prior subagent | ~2026-05-09T01:29 UTC | n/a | `launchctl unload -w` to free DB for sync |
| Sync window | 01:30:07 – 01:50:40 UTC | n/a | ~20m 33s, daemon paused |
| `launchctl load -w` issued | 2026-05-09T01:55:55 UTC | — | Continuation subagent |
| Daemon healthy on `/v1/health` | 2026-05-09T01:55:57 UTC | 60855 | First curl probe succeeded after ~2 s wait (attempt 1 in the 30-attempt loop) |
| Mmaped binary identity | — | 60855 | `/Users/david/Projects/cc-history-api/target/release/claude-history` (size 14,942,784 bytes), confirmed via `lsof -p 60855 \| grep target/release/claude-history`. This binary corresponds to commit `baf8ee5`. |
| Health-endpoint body at first probe | — | 60855 | `{"status":"ok","db_size":3330830336,"record_count":734302,"version":"0.1.0"}` |

`launchctl list \| grep claude-history` post-load shows two entries (the daemon `com.davidrex.claude-history`, exit-status 0; and the unrelated `com.davidrex.claude-history-logrotate`).

`tail -n 50 ~/Library/Logs/claude-history.err.log` after re-load shows only INFO `claude_history_store::drift::Observed N schema drift field(s)` lines from the watcher's startup catch-up sync — no WARN, no ERROR, no panic, no backtrace. Daemon-side ingestion is functioning under the new binary.

The `record_count=734302` in the health body is +65 above the post-sync `messages` count of 734,237. The watcher's startup catch-up `sync_all` (post-D2 sequencing fix) is processing files modified between the prior sync's terminal moment (01:50:40 UTC) and daemon-load time (01:55:57 UTC) — about 5 minutes of live JSONL writes by other Claude Code sessions. This is expected and confirms the daemon is correctly resuming from where the CLI sync left off.

### Kickstart-debt closure note

This re-enable mmaps the post-`baf8ee5` release binary and thereby retires the accumulated kickstart-debt from six prior commits whose code surfaces had built but never been loaded into the supervised process: B1.1, D1, D2, D3, D4, B1.2. All those code paths are now live in the running daemon at PID 60855.

---

## Timing summary

| Event | Time (Asia/Shanghai) | Time (UTC) |
|---|---|---|
| DB backup taken | 2026-05-09T09:29 | 2026-05-09T01:29 |
| Sync started | 2026-05-09T09:30:07 | 2026-05-09T01:30:07 |
| Sync ended (last log entry) | 2026-05-09T09:50:40 | 2026-05-09T01:50:40 |
| Daemon-pause window (unload → re-load) | ~26 minutes | — |
| Daemon re-loaded | 2026-05-09T09:55:55 | 2026-05-09T01:55:55 |
| Health 200 reached | ~2 s after load | — |
| This forensic record committed | ~2026-05-09T09:56 | ~2026-05-09T01:56 |

---

## Open questions / loose ends for the auditor

These are observations the B1.2-Audit subagent may want to inspect, not failures of the backfill itself:

1. The `sync_all` CLI path does not appear to emit an explicit terminal "Sync complete" log line at INFO level. Inferred clean termination from process-absence + zero panics + zero errors + orderly final "Synced file" entries. A future code-level inspection to confirm could surface a warning-level emission gap.
2. The pre-snapshot of `record_type_drift_log` was 184, not 0. Implies the table received writes between code deployment and pre-snapshot (during routine watcher activity prior to the prior subagent's daemon-unload). Pre-vs-post comparison remains valid as a monotonic-progress check; it does not establish "drift log was empty before backfill."
3. Only 184 of 9,187 files were re-decomposed by the sync pass. The bytewise-idempotency fast-path skipped the rest based on mtime/size match. Whether all 241 affected-files were among the 184 actually re-decomposed is not directly verifiable from this log alone (would require cross-referencing each affected-file path against the 184 "Synced file" log entries). The corpus-minimum gates above passing is the indirect evidence that the discriminator coverage is sufficient regardless.
4. `record_type_drift_log` row count of 3,832 versus the sum-of-occurrences of 15,015 reflects (type_name, version) row-key compression by ON CONFLICT DO UPDATE. No separate row-count integrity assertion was performed beyond the aggregate per-discriminator counts.
