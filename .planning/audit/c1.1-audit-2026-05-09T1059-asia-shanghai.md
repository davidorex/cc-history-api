# C1.1 Adversarial Audit

**Recorded**: 2026-05-09T10:59 Asia/Shanghai (UTC+0800)
**Commit audited**: `cdf668b` ("feat(core,store): add Attachment variant + migration 008 attachments tables (C1.1)")
**Parent commit**: `4484c4d`
**Plan**: `/Users/david/.claude/plans/curious-napping-koala.md` §C1 / §C1.1 (lines 100–130) / §Verification per item C1 (lines 286–298) / §Regression and continuity safeguards (lines 341–442)
**Audit type**: coverage-of-finding; no triage, no severity, no remediation
**Daemon at audit time**: PID 60855, status 0; live DB at schema_versions max=007 (migration 008 not yet applied to `~/.claude/.claude-history.db`); migration 008 applied cleanly to in-memory test DB and to `/tmp/c1.1-test.db` per the test harness

---

## Verification command summary

| # | Command | Status | Note |
|---|---|---|---|
| V1 | `git show --stat cdf668b` | OK | 6 files, 1587 insertions, 29 deletions |
| V2 | `git show --format=fuller cdf668b` | OK | Author + Committer = David Ryan; no Co-Authored-By trailer |
| V3 | `git log -n 1 cdf668b --format=%s \| awk '{print length}'` | OK | subject = **82 chars** (>72 char convention) |
| V4 | `git log --oneline cdf668b -10` | OK | Linear history; parent is 4484c4d (B1.2-Review close); spawn-order DAG hard rule 2 satisfied |
| V5 | `cargo build --release` | OK | Clean (cached, "Finished" — no new warnings) |
| V6 | `cargo test -p claude-history-core` | OK | 61 passed, 0 failed |
| V7 | `cargo test -p claude-history-store` | OK | 156 passed, 0 failed |
| V8 | `cargo test --workspace` | OK | 3 + 61 + 156 = 220 passed total |
| V9 | `sqlite3 /tmp/c1.1-test.db < migrations/008_attachments.sql` | OK | First apply produced no errors |
| V10 | `PRAGMA table_info(attachments)` | OK | 11 cols matching plan §129 (uuid PK, session_id NN, parent_uuid, timestamp NN, cwd, version, git_branch, slug, entrypoint, inner_type NN, body_json) |
| V11 | `PRAGMA table_info(hook_executions)` | OK | 11 cols (id PK AUTOINC, attachment_uuid NN, hook_name, hook_event, tool_use_id, exit_code, duration_ms, stdout, stderr, command, decision) |
| V12 | `PRAGMA index_list(attachments)` | OK | 3 user indices: idx_attachments_session_id / inner_type / timestamp + sqlite_autoindex on uuid |
| V13 | `PRAGMA index_list(hook_executions)` | OK | 3 user indices: idx_hook_executions_tool_use_id / attachment_uuid / hook_event |
| V14 | `PRAGMA foreign_key_list(attachments)` | OK | session_id → sessions(session_id) |
| V15 | `PRAGMA foreign_key_list(hook_executions)` | OK | attachment_uuid → attachments(uuid) |
| V16 | `PRAGMA integrity_check` post-apply | OK | `ok` |
| V17 | `PRAGMA foreign_key_check` post-apply | OK | empty (no orphans) |
| V18 | `sqlite3 008_attachments.sql` re-apply against same DB | OK | Idempotent, no errors, integrity still `ok` |
| V19 | `launchctl list \| grep claude-history` | OK | PID 60855, status 0 |
| V20 | `sqlite3 ~/.claude/.claude-history.db "SELECT name FROM sqlite_master WHERE name IN ('attachments','hook_executions')"` | OK | empty — migration 008 NOT yet applied to the live DB (kickstart pending per audit context, expected) |
| V21 | `sqlite3 ~/.claude/.claude-history.db "SELECT MAX(version), COUNT(*) FROM schema_versions"` | OK | `007 \| 7` — confirms live DB still on B1.1's migration 007 |
| V22 | `ls -la /tmp/c1.1-samples-*.json` | OK | 12 fixture files present, sizes 451 B – 26.8 KB |
| V23 | `git diff 4484c4d..cdf668b --stat` | OK | exactly 6 files: record.rs, 008_attachments.sql, db.rs, decompose.rs, drift.rs, schema.rs — no scope creep into other crates |

All commands executed successfully against the live system. No command errored.

---

## Requirements ledger summary

Extracted ~24 numbered requirements from plan §C1.1 lines 100–130 plus §Verification per item C1 (lines 286–298) plus the spawn-order DAG hard rules (lines 203–211). Highest Rn ≈ R24. Plan §C1.1 is unambiguous on the 12 modeled subtypes (table at lines 110–123), the column list of `attachments` (line 129), and the column list of `hook_executions` (line 130). One ambiguity: plan §107 states the `attachments` table envelope "mirroring `messages` shape" but the column list at §129 differs from `messages` (no `is_sidechain`, `user_type`, `agent_id`, `is_meta`; adds `entrypoint`, `inner_type`, `body_json`) — see #M1.

---

## Deviation catalog

### #1. [divergence] `AttachmentRecord` envelope captures `is_sidechain` and `user_type`, but `attachments` table omits those columns
- Plan-spec ref: §C1.1 line 107 ("uuid-keyed envelope mirroring messages shape") + line 129 (table column list)
- Actual state: `AttachmentRecord` struct (`crates/core/src/record.rs:430-456`) has `is_sidechain: bool` and `user_type: Option<String>` fields. Migration 008 does NOT define `is_sidechain` or `user_type` columns on `attachments` (`crates/store/migrations/008_attachments.sql:46-59`). The `messages` table DOES have those columns (`crates/store/migrations/001_initial.sql`). Plan §129 column list also omits them.
- Observation: deserialized envelope fields will not have a typed-table destination in C1.2; they will need to land in `body_json`, in `extra_json` (no such column on `attachments`), or be discarded. The "mirroring messages shape" framing at §107 is in tension with the §129 column list.
- Determinism: deterministic
- Verification evidence: V10 (table_info attachments output), record.rs:430-456 inspection, plan §107/§129 read-through

### #2. [divergence] `AttachmentRecord` envelope omits `agent_id`, `is_meta` while `messages` carries them
- Plan-spec ref: §C1.1 line 107 ("mirroring messages shape")
- Actual state: `messages` table has `agent_id` and `is_meta` (`001_initial.sql`). `AttachmentRecord` struct omits both (`crates/core/src/record.rs:430-456`); migration 008 omits both.
- Observation: per the commit msg note, `agentId` is observed on some `deferred_tools_delta` records and is intended to ride in envelope overflow rather than as a typed field. The plan does not specify whether shape-mirroring extends to those columns.
- Determinism: deterministic
- Verification evidence: V10, record.rs inspection, 001_initial.sql inspection

### #3. [addition] `attachments` table adds `entrypoint` column; messages-table envelope does not have `entrypoint`
- Plan-spec ref: §C1.1 line 129 (lists `entrypoint` in attachments column list)
- Actual state: migration 008 promotes `entrypoint` to a typed column (line 55). `messages` table stores `entrypoint` only in `extra_json` (per memory entry on schema drift, "entrypoint user/assistant/system/progress, 157K combined" — currently unpromoted on messages).
- Observation: plan §129 explicitly authorizes `entrypoint` on `attachments`. This is consistent with the plan but inconsistent with the §107 "mirroring messages shape" framing — `attachments` mirrors-with-additions rather than mirrors-exactly.
- Determinism: deterministic
- Verification evidence: V10 (entrypoint at column ordinal 8 in attachments), plan §129 line read

### #4. [divergence] `attachments` table omits a `type` column (the outer JSONL discriminator)
- Plan-spec ref: §C1.1 line 129
- Actual state: `messages` table has `type` (the JSONL discriminator). Plan §129 column list does NOT include `type`. Migration 008 omits `type`.
- Observation: in `attachments`, `type` is implicitly always `"attachment"`, so a `type` column would be constant. The implementation matches the plan; the divergence is between the plan and the "mirroring messages shape" framing at §107.
- Determinism: deterministic
- Verification evidence: V10, plan §129

### #5. [divergence] `decompose_attachment` does not invoke `log_record_overflow` for modeled-subtype envelope or per-subtype overflow HashMaps
- Plan-spec ref: §C1.1 line 138 ("Drift logging at `crates/store/src/drift.rs:107-156` `log_record_overflow` gains an `Attachment` arm")
- Actual state: `drift.rs` Attachment arm at `crates/store/src/drift.rs:235-323` is fully implemented (envelope + 12 subtype arms + Unknown). However, `decompose_attachment` (`crates/store/src/decompose.rs:828-871`) only invokes `drift::log_record_type_drift` for the Unknown subtype path. For modeled subtypes the stub never invokes `drift::log_record_overflow`, so envelope overflow (e.g. `agentId` per the commit msg) and per-subtype `#[serde(flatten)] overflow` (e.g. `removedNames`, `skillCount`, `addedLines`) deserialize into the structs but are never written to `schema_drift_log` during ingestion.
- Observation: the existing precedent (`decompose_user`, `decompose_assistant`, etc. at `crates/store/src/decompose.rs:441, 489, 534, …`) calls `log_overflow` from each decomposer arm. `decompose_attachment`'s deferral of those calls means the Attachment arm of `log_record_overflow` is dead code in the live ingestion path until C1.2.
- Determinism: deterministic
- Verification evidence: `grep log_record_overflow crates/store/src/decompose.rs` returns 0; `grep log_overflow crates/store/src/decompose.rs` returns calls only in pre-existing arms (lines 441, 489, 534, 540, 571, 603, 659, 688, 718) — none in `decompose_attachment`. Drift.rs Attachment arm at lines 235-323.

### #6. [divergence] Test name contradicts test body for `test_attachment_modeled_subtype_missing_field_falls_through`
- Plan-spec ref: §Risks across tiers line 448 ("getting any single subtype's struct shape wrong on first encounter will silently dump the record into the unknown-subtype catch-all")
- Actual state: test at `crates/core/src/record.rs:1765-1792` is named `test_attachment_modeled_subtype_missing_field_falls_through` and the inline header comment block at lines 1336-1344 says "Missing required field on a modeled subtype falls through to Unknown". The test body asserts `result.is_err()` (line 1788), i.e. parse error, NOT fall-through.
- Observation: the test name and the introductory comment claim fall-through; the test body documents and locks in the opposite (error-bubble-up) behavior. Plan §448 framing implies "silently dump … into the unknown-subtype catch-all" — i.e. fall-through — but the implementation chose error-on-malformed-modeled-body. The test name is misleading; an inline comment inside the test (lines 1780-1787) acknowledges the asymmetry but the function name and the surrounding header retain the misleading wording.
- Determinism: deterministic
- Verification evidence: record.rs:1336-1344 (header comment), record.rs:1765-1792 (test body)

### #7. [divergence] `AttachmentBody::Unknown { raw }` retains the `type` field, while `JSONLRecord::Unknown { raw }` precedent also retains it — but the strip-before-flatten guard for typed dispatch could leak `type` into the `Unknown` Value through a different path
- Plan-spec ref: §C1.1 line 104 ("Path A's pattern applied at the inner discriminator level")
- Actual state: `AttachmentBody` Deserialize impl (`crates/core/src/record.rs:528-626`) reads `value`, extracts `type`, dispatches into typed variants after stripping `type` from `typed_value`. For Unknown branch (line 621-624), the original unstripped `value` is stored in `raw`. This matches `JSONLRecord::Unknown` behavior. Test `test_attachment_unknown_subtype_falls_through` (line 1687) asserts `raw.get("type")` is `Some("date_change")`, locking in that the Unknown raw retains the discriminator. No corresponding test asserts the typed path does NOT leak `type` into per-subtype `overflow`.
- Observation: B1.1 has `test_typed_dispatch_does_not_leak_type_to_overflow` (line 1305) for outer dispatch. C1.1 has no equivalent test for the inner AttachmentBody dispatch — it would be `test_attachment_typed_dispatch_does_not_leak_type_to_overflow`. The strip-before-flatten guard at AttachmentBody Deserialize lines 553-556 exists; the test for it does not.
- Determinism: deterministic
- Verification evidence: record.rs:528-626 (impl); record.rs:1305 (B1.1 precedent test) vs. attachment-test list 1357-1831 (no equivalent for AttachmentBody)

### #8. [omission] No test for `AttachmentBody` with empty-string discriminator (`"type": ""`)
- Plan-spec ref: B1.1-Review #8 inheritance — flagged in B1.1 audit chain that the empty-string discriminator case is not tested
- Actual state: `AttachmentBody` Deserialize at `crates/core/src/record.rs:542-547` invokes `as_str()` and would treat `""` as a string, then fall to `known_attachment_subtype("")` (returns None at line 508), then fall through to `AttachmentBody::Unknown { subtype: "".to_string(), raw: value }`. No test exercises this case for AttachmentBody. B1.1 had the same gap at JSONLRecord level.
- Observation: inherited gap. Empty-string subtype would log to `record_type_drift_log` as `type_name = "attachment."` (with trailing period), which is observable but not tested.
- Determinism: deterministic
- Verification evidence: grep `\"\\\"\\\"\"` /Users/david/Projects/cc-history-api/crates/core/src/record.rs in the attachment test block returns no matches; record.rs:542-547 + 508 + 621 trace

### #9. [omission] No test for `AttachmentBody` with non-string `type` (null, number, object)
- Plan-spec ref: B1.1 precedent `test_non_string_type_field_errors` (line 1284) for outer dispatch
- Actual state: `AttachmentBody::deserialize` at lines 542-547 returns an error if `type_field.as_str()` fails. There is no test asserting this behavior at the AttachmentBody level. B1.1 has the test for the outer dispatch.
- Observation: inherited gap relative to B1.1 precedent.
- Determinism: deterministic
- Verification evidence: record.rs:1357-1831 has no test exercising `attachment.type: null/42/object`

### #10. [omission] No test for missing `type` field inside `attachment` object
- Plan-spec ref: B1.1 precedent `test_missing_type_field_errors` (line 1269) for outer dispatch
- Actual state: `AttachmentBody::deserialize` at lines 539-541 returns `de::Error::missing_field("type")` if the inner `type` field is absent. No test exercises this.
- Observation: parallel gap to #9.
- Determinism: deterministic
- Verification evidence: record.rs:1357-1831 inspection

### #11. [test-coverage] `AttachmentRecord` envelope-overflow path has no dedicated test exercising `record.overflow`
- Plan-spec ref: §C1.1 + commit msg note ("agentId observed on some deferred_tools_delta records … capture by … #[serde(flatten)]")
- Actual state: `AttachmentRecord` has `#[serde(flatten)] pub overflow: HashMap<String, serde_json::Value>` at line 454. No test deserializes an attachment record with an unmodeled envelope-level field (e.g. `agentId`) and asserts it lands in `record.overflow`.
- Observation: the per-subtype overflow path IS tested (e.g. `test_attachment_mcp_instructions_delta` asserts `removedNames` in `b.overflow`). The envelope-level overflow is not.
- Determinism: deterministic
- Verification evidence: record.rs:1357-1831 has no `assert!(r.overflow.contains_key(...))` for any AttachmentRecord-level envelope overflow

### #12. [test-coverage] No test asserts that `is_sidechain` and `user_type` parse correctly on AttachmentRecord
- Plan-spec ref: §C1.1 line 107
- Actual state: `test_attachment_hook_success` (record.rs:1357-1396) includes `"userType": "external"` and `"isSidechain": false` in JSON but does not assert on `r.is_sidechain` or `r.user_type` after parse. Similarly, `parse_attachment` helper (line 1348) discards the parent `AttachmentRecord` envelope and returns only the body.
- Observation: any future regression that flipped the `#[serde(default)]` annotations or the `rename_all = "camelCase"` would not be caught.
- Determinism: deterministic
- Verification evidence: record.rs:1348-1356 (helper) + 1357-1396 (test)

### #13. [divergence] Sample fixture files at `/tmp/c1.1-samples-*.json` are not loaded by any test
- Plan-spec ref: §Verification per item C1 line 288 ("per-subtype deserialization tests using real records sampled from the corpus (one per inner subtype, minimum 12)")
- Actual state: 12 fixtures exist at `/tmp/c1.1-samples-{hook_success,hook_permission_decision,…}.json` (V22 confirms). Tests in record.rs use INLINE JSON literals at lines 1359-1813, not fixture file reads. The header comment at line 1336-1338 explicitly says "kept inline so the regression net does not depend on any out-of-tree fixture".
- Observation: this is by design (in-tree literals are deterministic), but the fixture files are extra-corpus capture that the tests do not actually exercise. The fixtures serve only as developer reference, not as the test net.
- Determinism: deterministic
- Verification evidence: V22 shows 12 fixtures present; record.rs:1336-1338 explicitly opts out of fixture loading

### #14. [divergence] `decompose_attachment` does not call `log_record_overflow` even for the Unknown body path
- Plan-spec ref: §C1.1 line 138
- Actual state: `decompose_attachment` (`decompose.rs:828-871`) for `AttachmentBody::Unknown` calls `drift::log_record_type_drift` (line 857) but does NOT call `drift::log_record_overflow`. So the envelope-overflow drift signal (#1 in the drift.rs Attachment arm comment block at lines 215-220) is never produced during live ingestion in C1.1, even for unknown subtypes.
- Observation: the drift.rs Attachment arm function is exhaustive and would log envelope overflow if invoked. `decompose_attachment` does not invoke it. This is consistent with the C1.1 stub framing in the function docstring (lines 803-825), but means three drift signals described in drift.rs lines 215-230 collapse to only signal #3 (Unknown subtype catch-all) during ingestion.
- Determinism: deterministic
- Verification evidence: `grep log_record_overflow crates/store/src/decompose.rs` returns no hits; drift.rs:215-230 comment claims three signals; `decompose_attachment` issues only signal #3

### #15. [verification] Migration 008 has not been applied to the live `~/.claude/.claude-history.db`; verification is by in-memory + `/tmp/c1.1-test.db` only
- Plan-spec ref: §Regression and continuity safeguards line 354 (DB backup before migration commit) + §Verification per item C1 (live verify gates listed at lines 290-297, none of which apply to C1.1's structural-only commit)
- Actual state: V20 confirms the live DB has no `attachments` or `hook_executions` tables; V21 confirms `MAX(version) = 007`. The audit-context paragraph in the input states "Daemon at PID 60855 is on the post-baf8ee5 binary; migration 008 is registered in MIGRATIONS but has not been applied to the live ~/.claude/.claude-history.db yet (kickstart pending separately)."
- Observation: the binary at `target/release/claude-history` includes migration 008 in MIGRATIONS, but the daemon mmaped at PID 60855 is the prior B1.2 binary (per audit-context note), so the live DB has not yet seen 008. Plan §354 requires a DB backup before any migration commit; whether that backup was captured for this commit is not verifiable from the audit (no `/tmp/claude-history.db.bak-cdf668b` was recorded in the commit message or any forensic doc).
- Determinism: deterministic for live state; backup capture is unverified.
- Verification evidence: V19, V20, V21; no forensic doc for cdf668b yet

### #16. [verification] Plan-required pre-merge dry-run against the corpus is not documented
- Plan-spec ref: §Regression and continuity safeguards safeguard #2 line 356-361 ("Pre-merge dry-run … For commits that change parser or decomposer behavior (B1.1, B1.2, C1.1, C1.2, C2.1, C2.2)")
- Actual state: the commit message has a "Verification" block listing test results (220 passed, PRAGMA integrity_check ok) but does not record a dry-run against the existing 9,113 JSONL files via a `--dry-run` mode binary against `/tmp/claude-history.dryrun.db`. The commit message also does not document a regression baseline diff against `/tmp/regress.*.before` files (safeguard #3, lines 363-376).
- Observation: pre-merge dry-run requirement applies to C1.1 per safeguard #2's enumeration. C1.1's parser behavior change (attachment now dispatches to typed Attachment variant) is in scope for that safeguard. No evidence of dry-run capture in the commit or in any forensic doc.
- Determinism: deterministic
- Verification evidence: `git show cdf668b` body inspection; no `/tmp/claude-history.dryrun.db` referenced; no forensic doc found at `.planning/audit/c1.1-*` other than this audit

### #17. [commit-message] Subject line is 82 characters, exceeding the 72-character soft limit
- Plan-spec ref: project commit conventions (≤72 chars typical)
- Actual state: V3 reports 82 chars: `feat(core,store): add Attachment variant + migration 008 attachments tables (C1.1)`
- Observation: B1.2 audit precedent (V3 in that audit: 70 chars) was within bounds. cdf668b exceeds.
- Determinism: deterministic
- Verification evidence: V3 (82 chars)

### #18. [commit-message] No `Co-Authored-By` trailer; aim/intention framing matches mandate
- Plan-spec ref: user-global CLAUDE.md ("Do not pollute the semantics of the repository with author credit claims") + ("Speak to aims and intentions, not unwarranted certainties")
- Actual state: V2 confirms Author = Committer = David Ryan; no trailer. Body uses "intent is documented", "structural foundation", "Builds on", "intentionally does NOT yet write rows" — aim/intention framing throughout. No "this ensures" or "this fixes" patterns observed.
- Observation: compliant with mandate. (Recorded as a positive verification rather than a deviation.)
- Determinism: deterministic
- Verification evidence: V2 + commit-body grep for "ensures|fixes" returns 0 matches in body proper

### #19. [meta-plan] Plan §107 says "mirroring messages shape" but plan §129 lists columns that diverge from the messages-table column set
- Plan-spec ref: plan lines 107 vs 129
- Actual state: §107 ("uuid-keyed envelope mirroring messages shape, with inner_type discriminator and body_json for unmodeled-subtype content") and §129 (column list omits `is_sidechain`, `user_type`, `agent_id`, `is_meta`, `type`; adds `entrypoint`, `inner_type`, `body_json`).
- Observation: plan-internal inconsistency. Implementation picked §129's column list; this audit cannot determine which specification was intended to govern. Findings #1, #2, #3, #4 are downstream consequences of this ambiguity.
- Determinism: deterministic; resolution requires plan author intent
- Verification evidence: plan lines 107 + 129 read-through

### #20. [divergence] `PlanModeBody` reuses one struct for both `plan_mode` and `plan_mode_reentry`; `KnownAttachmentSubtype` enumerates them as distinct variants
- Plan-spec ref: §C1.1 line 122 ("plan_mode_reentry … same shape as plan_mode")
- Actual state: `crates/core/src/record.rs:476-478` defines `PlanMode(PlanModeBody)` and `PlanModeReentry(PlanModeBody)` (two enum variants, same inner struct). `KnownAttachmentSubtype` (line 522, 524) has separate `PlanMode` / `PlanModeReentry` variants. Dispatch code (lines 598-612) constructs different `AttachmentBody` variants but from the same `PlanModeBody` parsed value.
- Observation: this matches plan §122's "same shape" framing while preserving the discriminator distinction. There is no strict deviation; it does mean future shape divergence between `plan_mode` and `plan_mode_reentry` (if Claude Code adds fields to one but not the other) would force an enum-variant restructure rather than just adding an optional field. The Serialize impl correctly emits the two distinct discriminator strings (lines 653-657).
- Determinism: deterministic
- Verification evidence: record.rs:476-478, 522-524, 598-612, 653-657

### #21. [divergence] `HookSuccessBody.tool_use_id` uses `#[serde(rename = "toolUseID")]` rather than relying on `rename_all = "camelCase"`
- Plan-spec ref: §C1.1 line 112 (`toolUseID`)
- Actual state: `HookSuccessBody` (`record.rs:666-687`) has `#[serde(rename_all = "camelCase")]` AND inside it `#[serde(rename = "toolUseID")]` on `tool_use_id`. Same on `HookPermissionDecisionBody` (line 695). The reason: `camelCase` of `tool_use_id` is `toolUseId`, NOT `toolUseID`. The corpus uses `toolUseID` (capital ID).
- Observation: the inner `rename` is necessary because Rust's `camelCase` heuristic would produce `toolUseId`. The implementation correctly handles this. Recorded as a divergence-from-default-convention so the triage stage can confirm intent. No bug.
- Determinism: deterministic
- Verification evidence: record.rs:666-700; investigation audit doc lines 88, 100 confirm corpus uses `toolUseID`

### #22. [test-coverage] `test_attachment_serialize_roundtrip` does not exercise the `Unknown` body Serialize path
- Plan-spec ref: §C1.1 (Serialize impl); commit msg "AttachmentBody Unknown serializes raw"
- Actual state: `test_attachment_serialize_roundtrip` (record.rs:1719-1757) exercises `HookSuccess` Serialize roundtrip. `AttachmentBody::Unknown` Serialize at line 659 (`raw.serialize(serializer)`) is not directly exercised by a roundtrip test.
- Observation: an Attachment with an Unknown subtype body, serialized and re-parsed, would lose nothing if the raw retained `type`. No test locks in this invariant.
- Determinism: deterministic
- Verification evidence: record.rs:1719-1757; record.rs:659

### #23. [test-coverage] No test asserts `decompose_record` Attachment dispatch handles `is_sidechain` and `user_type` envelope fields end-to-end
- Plan-spec ref: §C1.1 line 137 ("decompose_record gains an Attachment arm")
- Actual state: dispatcher tests at `crates/store/src/drift.rs:1051-1133` (`test_decompose_record_attachment_modeled_is_stub_no_op_on_typed_tables` and `test_decompose_record_attachment_unknown_subtype_writes_drift_row`) verify count=0 in attachments + hook_executions for modeled subtypes and presence of a drift row for Unknown. They do not assert on parsed `record.is_sidechain` or `record.user_type`.
- Observation: this is consistent with the C1.1 stub framing (no rows written yet, so envelope fields have nowhere to be observed). Recorded for triage to weigh.
- Determinism: deterministic
- Verification evidence: drift.rs:1051-1133

### #24. [divergence] `MAX_SAMPLE_VALUE_LEN` constant in drift.rs is reused for inner-subtype truncation; `decompose_attachment` defines its own `RECORD_TYPE_SAMPLE_MAX_LEN`
- Plan-spec ref: §C1.1 (truncation policy not specified)
- Actual state: drift.rs Attachment arm at line 308-318 uses `MAX_SAMPLE_VALUE_LEN` for truncation. `decompose_attachment` at lines 845-852 uses `RECORD_TYPE_SAMPLE_MAX_LEN`. Two different constants for the same logical concept. (Both are referenced from different module scopes; the values may or may not be identical.)
- Observation: dual-constant policy. If the two constants drift in value, the same Unknown-subtype payload could be truncated differently depending on whether it goes through `decompose_attachment` (production path) or directly through `log_record_overflow` (test-only path).
- Determinism: deterministic
- Verification evidence: drift.rs:308-318; decompose.rs:845-852; grep `RECORD_TYPE_SAMPLE_MAX_LEN\\|MAX_SAMPLE_VALUE_LEN` shows two distinct symbols

### #25. [test-coverage] Test `test_log_record_overflow_attachment_modeled_subtype_overflow` asserts `total >= 1` rather than exact count
- Plan-spec ref: §Verification per item C1 line 288
- Actual state: drift.rs test at line 1041-1043 asserts `total >= 1`. The actual return value depends on exactly how many fields are flagged (envelope overflow + per-subtype overflow). A precise assertion would lock in expected behavior more tightly.
- Observation: tolerant assertion. A future change that, say, doubled the per-field counting would still pass.
- Determinism: deterministic
- Verification evidence: drift.rs:1041-1043

### #26. [omission] No test asserts the per-subtype overflow branch for the other 11 modeled subtypes (only `mcp_instructions_delta` is exercised in drift.rs tests)
- Plan-spec ref: §C1.1 line 138
- Actual state: drift.rs's `test_log_record_overflow_attachment_modeled_subtype_overflow` exercises only `McpInstructionsDelta`'s overflow path. The other 11 typed body arms in `log_record_overflow` (lines 248-307 in the diff) are not individually exercised. Each arm is essentially mechanical (`log_overflow(version, "attachment.<subtype>", &b.overflow, tx)`), so behavior is similar, but a typo in any of the 11 hardcoded record_type strings would not be caught.
- Observation: 11 arm-specific tests would be repetitive but would lock in the record_type string per subtype. record.rs has 12 per-subtype deserialization tests (one per subtype) but drift.rs has only one.
- Determinism: deterministic
- Verification evidence: drift.rs:1015-1133 contains 4 new tests; only one exercises a typed body arm

### #27. [divergence] `RecordBase` is included in the imports for decompose.rs but not actually used by `decompose_attachment`
- Plan-spec ref: §C1.1 (no specific guidance)
- Actual state: `crates/store/src/decompose.rs:21` imports `RecordBase` (pre-existing). `decompose_attachment` does not use `RecordBase` because `AttachmentRecord` does NOT embed `RecordBase` — instead it has its own envelope fields. The `messages` decomposer arms use `RecordBase` from `UserRecord.base`, `AssistantRecord.base`, etc.
- Observation: structural divergence from the messages-record family. `AttachmentRecord` does not embed `RecordBase`. This means future code that wants to treat all "envelope-bearing" records uniformly cannot use `RecordBase` as the abstraction. C1.2 will need to either route through `AttachmentRecord`'s individual fields or refactor.
- Determinism: deterministic
- Verification evidence: record.rs:430-456 (no `pub base: RecordBase`); decompose.rs:21 import unchanged

### #28. [verification] Migration 008's foreign-key-list shows actions = `NO ACTION` for both FKs (no CASCADE / RESTRICT)
- Plan-spec ref: §C1.1 line 130 ("attachment_uuid FK → attachments.uuid")
- Actual state: V14, V15 show ON UPDATE / ON DELETE = `NO ACTION` for both FKs. Migration 008 SQL does not specify actions. SQLite default is NO ACTION.
- Observation: deleting a session with attachments (or an attachment with hook_executions) would error if FK enforcement is on; the tables become append-only in practice. Plan does not specify referential-action semantics; recorded for completeness.
- Determinism: deterministic
- Verification evidence: V14, V15 output

### #29. [divergence] `decompose_attachment`'s `result.overflow_fields` semantics are not commented as "1 = drift_log row written" vs the existing `overflow_fields` semantics elsewhere
- Plan-spec ref: §C1.1 (no spec); existing pattern at decompose.rs (overflow_fields counts schema-drift-log rows updated)
- Actual state: `decompose_attachment` returns `overflow_fields = 1` for an Unknown subtype that wrote a record_type_drift_log row (decompose.rs:861). Elsewhere in the codebase, `overflow_fields` counts `schema_drift_log` rows updated (per-field). The Attachment arm uses the same field name to count `record_type_drift_log` rows (per-record-type). Mixed semantics.
- Observation: the result field is named `overflow_fields` but in this arm it counts a different table's rows. Header comment at decompose.rs:822-825 acknowledges this with "rows_inserted: 0, overflow_fields: <changed>" but does not call out the semantic shift.
- Determinism: deterministic
- Verification evidence: decompose.rs:828-871 (function); decompose.rs:441 et al. (existing pattern); grep `result.overflow_fields` for cross-reference

### #30. [documentation] Memory.md / CLAUDE.md / .planning/STATE.md were not updated as part of cdf668b
- Plan-spec ref: §C1.1 does not mandate doc updates for the structural commit; STATE.md is parent-agent ledger, not commit-scope
- Actual state: V1 confirms only 6 files changed: 5 code + 1 SQL. No `.planning/STATE.md`, `CLAUDE.md`, or memory updates.
- Observation: STATE.md update is intentionally outside the impl subagent's working set per parent-agent protocol. Recorded for completeness; no mandate violation.
- Determinism: deterministic
- Verification evidence: V1 + V23

### #31. [test-coverage] No assertion that the dispatch table at `known_record_type` was updated to include `attachment` (regression test from B1.1's perspective)
- Plan-spec ref: §C1.1 line 207 (DAG hard rule 3 implies attachment must now route to Attachment)
- Actual state: `test_attachment_discriminator_no_longer_unknown` (record.rs:1799-1814) asserts that `attachment` parses to `JSONLRecord::Attachment(_)`. `test_unknown_variant_known_corpus_discriminators` (1244-1263) is updated to remove `attachment` from the unknown-corpus discriminator set. Both tests collectively cover the regression. No direct test calls `known_record_type("attachment")` because that helper is private (`fn`, not `pub fn`).
- Observation: the regression coverage is via behavioral test, not via direct unit test on the helper. Recorded for completeness; behavioral coverage is the project's prevailing pattern.
- Determinism: deterministic
- Verification evidence: record.rs:87-99 (`fn known_record_type`); record.rs:1799-1814 + 1244-1263

### #32. [verification] The 220-test workspace count matches the commit message claim, but the per-crate breakdown the commit message gives (`3 server + 61 core + 156 store`) — no separate "core test count under v_attachment_*" assertion
- Plan-spec ref: §C1.1 (commit message claims "17 new tests in record.rs, 7 in schema.rs, 4 in drift.rs")
- Actual state: counted 17 attachment-related tests in record.rs (12 subtype + 5 generic = 17 — verified by enumeration: hook_success, hook_permission_decision, mcp_instructions_delta, skill_listing, edited_text_file, task_reminder, todo_reminder, deferred_tools_delta, plan_mode, plan_mode_exit, plan_mode_reentry, nested_memory, unknown_subtype_falls_through, serialize_roundtrip, modeled_subtype_missing_field_falls_through, discriminator_no_longer_unknown, non_attachment_unknown_still_falls_through_post_c11). 7 new in schema.rs (migration_008_creates_attachments_table, migration_008_creates_hook_executions_table, migration_008_attachments_columns_queryable, migration_008_hook_executions_columns_queryable, migration_008_attachments_indices_present, migration_008_hook_executions_indices_present, migration_008_idempotent, migration_008_post_apply_integrity = 8). 4 new in drift.rs (test_log_record_overflow_attachment_modeled_subtype_overflow, test_log_record_overflow_attachment_unknown_subtype, test_decompose_record_attachment_modeled_is_stub_no_op_on_typed_tables, test_decompose_record_attachment_unknown_subtype_writes_drift_row).
- Observation: schema.rs has **8** new migration_008_* tests, not 7 as the commit message claims (the post_apply_integrity test was likely not counted in the "7" claim).
- Determinism: deterministic
- Verification evidence: V8 + grep `migration_008_` returns 8 hits in schema.rs

### #33. [divergence] `migration_008_post_apply_integrity` claim "Replaces the implicit 'should still have exactly 7' expectation that B1.1 set" but the migration_006_version_history_not_schema_versions test still has the count-bumped (7→8) assertion at `crates/store/src/schema.rs:266-273`
- Plan-spec ref: §C1.1 (migration_006 sv_count bump expected as mechanical-coupling edit)
- Actual state: schema.rs:269 contains `assert_eq!(sv_count, 8, "schema_versions should track all 8 applied migrations")` per the diff hunk at lines 268-273. The new `migration_008_post_apply_integrity` test (lines 481-498 of post-commit schema.rs) ALSO asserts `sv_count == 8`. Two tests now assert the same invariant.
- Observation: the new test description ("Replaces the implicit 'should still have exactly 7' expectation") is misleading — the existing assertion was kept and updated, not replaced. Recorded as a doc-vs-code mismatch.
- Determinism: deterministic
- Verification evidence: schema.rs:269 + 488-498 (post-commit numbering); commit msg description vs actual code

---

## Counting self-check

```
$ grep -cE '^### #' /Users/david/Projects/cc-history-api/.planning/audit/c1.1-audit-2026-05-09T1059-asia-shanghai.md
33
```

Declared total: **33 observations**.

Per-category breakdown:
- omission: 4 (#8, #9, #10, #26)
- divergence: 14 (#1, #2, #4, #5, #6, #7, #13, #14, #20, #21, #24, #27, #29, #33)
- addition: 1 (#3)
- verification: 3 (#15, #16, #28)
- commit-message: 2 (#17, #18)
- test-coverage: 7 (#11, #12, #22, #23, #25, #31, #32)
- documentation: 1 (#30)
- meta-plan: 1 (#19)
- sequence: 0
- dependency: 0
- regression: 0

Sum: 4+14+1+3+2+7+1+1 = 33. Matches.

---

## Severity-language self-check

```
$ grep -iE '\b(critical|severe|blocker|high.?priority|medium.?priority|low.?priority|trivial|fatal|crucial)\b' c1.1-audit-2026-05-09T1059-asia-shanghai.md
(no matches)
```
