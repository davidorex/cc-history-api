# C1.1 Triage — commit `cdf668b`

**Triage timestamp:** 2026-05-09T11:05 Asia/Shanghai (UTC+0800)
**Audit input:** `/Users/david/Projects/cc-history-api/.planning/audit/c1.1-audit-2026-05-09T1059-asia-shanghai.md`
**Plan reference:** `/Users/david/.claude/plans/curious-napping-koala.md` (§C1, §C1.1 lines 100–130, §Verification per item C1 lines 286–298, §Regression and continuity safeguards lines 341–442, §spawn-order DAG lines 203–211, §Risks across tiers line 448)
**Mode:** Structural classification along three axes — plan-spec reach, build/test impact, determinism. No severity language. No accept/reject decisions.

---

## Audit-quality note

Audit catalog declares 33 observations; `grep -cE '^### #' c1.1-audit-2026-05-09T1059-asia-shanghai.md` returns 33. Per-category breakdown self-tally in audit body (omission 4, divergence 14, addition 1, verification 3, commit-message 2, test-coverage 7, documentation 1, meta-plan 1) sums to 33. Counting matches. No rows missing evidence; all 33 carry "Verification evidence" lines.

---

## Axis totals

**Plan-spec reach:**
- plan-defect: 5 (#1, #2, #4, #16, #19)
- impl-defect: 4 (#6, #17, #32, #33)
- mutual: 4 (#5, #14, #15, #29)
- informational: 20 (#3, #7, #8, #9, #10, #11, #12, #13, #18, #20, #21, #22, #23, #24, #25, #26, #27, #28, #30, #31)

(5 + 4 + 4 + 20 = 33.)

**Build/test impact:**
- build-blocking: 0
- test-blocking: 0
- runtime-only: 3 (#5, #14, #28)
- static-only: 5 (#3, #6, #24, #27, #29)
- none: 25 (#1, #2, #4, #7, #8, #9, #10, #11, #12, #13, #15, #16, #17, #18, #19, #20, #21, #22, #23, #25, #26, #30, #31, #32, #33)

(0 + 0 + 3 + 5 + 25 = 33.)

**Determinism:**
- deterministic: 32
- nondeterministic: 0
- unverified: 1 (#15)

(32 + 0 + 1 = 33.)

**Meta-plan rows:** 1 (#19)
**Unverified rows:** 1 (#15)

---

## Triage table

| # | Category | Plan-spec reach | Build/test impact | Determinism | Rationale |
|---|---|---|---|---|---|
| 1 | divergence | plan-defect | none | deterministic | `AttachmentRecord` struct carries `is_sidechain`/`user_type` per §107 "mirror messages shape" while §129 column list omits them; downstream consequence of §107 vs §129 inconsistency (cross-references #19). |
| 2 | divergence | plan-defect | none | deterministic | Envelope omits `agent_id`/`is_meta` while messages-table carries them; same §107-vs-§129 ambiguity, opposite direction (cross-references #19). |
| 3 | addition | informational | static-only | deterministic | `entrypoint` column on `attachments` is explicitly authorized by plan §129; the divergence is between §129 and the §107 framing rather than between plan and implementation. |
| 4 | divergence | plan-defect | none | deterministic | `attachments` omits `type` discriminator column; matches §129 column list but diverges from §107 mirror-messages framing (cross-references #19). |
| 5 | divergence | mutual | runtime-only | deterministic | Plan §C1.1 paragraph explicitly defers decomposer routing to C1.2, which favors `informational`; however drift.rs Attachment arm is fully implemented (envelope + 12 subtype arms + Unknown) while `decompose_attachment` only invokes the Unknown-subtype drift call — the asymmetric stub-coherence (drift.rs full, decompose.rs stub) is an implementation choice not specified in the plan, and the plan's stub-deferral framing also did not specify the drift-arm scope, so both contribute. Headline finding. |
| 6 | divergence | impl-defect | static-only | deterministic | `test_attachment_modeled_subtype_missing_field_falls_through` test name + introductory header comment claim fall-through; body asserts `is_err()`. Plan §448 framing implies fall-through but the implementation chose error-on-malformed and the test name was not updated to match the chosen behavior — the test name is the implementation artifact at fault. Headline finding. |
| 7 | divergence | informational | none | deterministic | Strip-before-flatten guard at AttachmentBody Deserialize present; B1.1's `test_typed_dispatch_does_not_leak_type_to_overflow` precedent has no AttachmentBody equivalent — observation about test-net symmetry, not a code or plan defect. |
| 8 | omission | informational | none | deterministic | Empty-string discriminator (`"type": ""`) untested for AttachmentBody; B1.1-level same-shape gap inherited at the inner discriminator. |
| 9 | omission | informational | none | deterministic | Non-string `type` (null/number/object) untested for AttachmentBody; B1.1 precedent test exists for outer dispatch only. |
| 10 | omission | informational | none | deterministic | Missing inner `type` field untested for AttachmentBody; parallel gap to #9. |
| 11 | test-coverage | informational | none | deterministic | Envelope-level `record.overflow` path untested; per-subtype overflow path is tested. |
| 12 | test-coverage | informational | none | deterministic | `is_sidechain`/`user_type` parse-correctness untested at envelope level; helper `parse_attachment` discards envelope. |
| 13 | divergence | informational | none | deterministic | 12 sample fixtures at `/tmp/c1.1-samples-*.json` not loaded by tests; explicit by-design choice per record.rs:1336-1338 "kept inline so the regression net does not depend on any out-of-tree fixture". |
| 14 | divergence | mutual | runtime-only | deterministic | `decompose_attachment` Unknown path also does not call `log_record_overflow`, only `log_record_type_drift`; drift.rs comment claims three drift signals while ingestion produces only signal #3. Plan stub-deferral framing and impl drift-arm-fully-built asymmetry both contribute, parallel to #5. |
| 15 | verification | mutual | none | unverified | Live DB does not yet have migration 008 applied (kickstart pending); §safeguard 1 backup capture for cdf668b is unrecorded in any forensic doc. Plan §safeguard 1 prescribes the backup but does not require recording the result, and no forensic doc was authored for cdf668b — both contribute. (Mirrors B1.2-Triage rows #20, #22 pattern.) |
| 16 | verification | plan-defect | none | deterministic | §safeguard 2 enumerates C1.1 in the pre-merge dry-run requirement, but commit body and forensic record contain no dry-run capture; gap originates in the lack of a plan-side requirement to record the dry-run artifact path even though the safeguard names C1.1. |
| 17 | commit-message | impl-defect | none | deterministic | Subject 82 chars vs 72-char convention; B1.2 precedent (70 chars) was within bounds. Commit-author choice. |
| 18 | commit-message | informational | none | deterministic | Aim/intention framing honored; no `Co-Authored-By` trailer; mandate compliant. Recorded as positive verification. |
| 19 | meta-plan | plan-defect | none | deterministic | §107 "mirroring messages shape" vs §129 column list inconsistency; resolution requires plan author intent. Drives #1, #2, #3, #4. Headline finding. |
| 20 | divergence | informational | none | deterministic | `PlanModeBody` reused for `plan_mode` and `plan_mode_reentry` matches plan §122 "same shape" framing; preserves discriminator distinction in dispatch. No deviation. |
| 21 | divergence | informational | none | deterministic | `#[serde(rename = "toolUseID")]` is necessary because Rust's camelCase heuristic produces `toolUseId`, not `toolUseID`; corpus uses `toolUseID`. Implementation correct; recorded for triage to confirm intent. |
| 22 | test-coverage | informational | none | deterministic | Unknown body Serialize roundtrip not directly exercised; serialize impl at line 659 (`raw.serialize(serializer)`) covered indirectly only. |
| 23 | test-coverage | informational | none | deterministic | Dispatcher tests verify count=0 in attachments + hook_executions for modeled and presence of drift row for Unknown; do not assert on parsed envelope fields. Consistent with C1.1 stub framing. |
| 24 | divergence | informational | static-only | deterministic | `MAX_SAMPLE_VALUE_LEN` (drift.rs) vs `RECORD_TYPE_SAMPLE_MAX_LEN` (decompose.rs); two constants for the same logical concept. Source-text shape, no functional impact at present. (Mirrors B1.2 row #5 pattern.) |
| 25 | test-coverage | informational | none | deterministic | `total >= 1` tolerant assertion in `test_log_record_overflow_attachment_modeled_subtype_overflow`; would not catch regression that doubled per-field counting. |
| 26 | omission | informational | none | deterministic | 11 of 12 typed-body arms in `log_record_overflow` not individually exercised; only `mcp_instructions_delta` covered. Hardcoded record_type strings unprotected against per-arm typo. |
| 27 | divergence | informational | static-only | deterministic | `RecordBase` imported but unused by `decompose_attachment`; `AttachmentRecord` does not embed `RecordBase`. Structural divergence from messages-record family — observation about future C1.2 surface, not a present defect. |
| 28 | verification | informational | runtime-only | deterministic | Migration 008 FK actions = NO ACTION (SQLite default); plan does not specify referential-action semantics. Tables become append-only in practice if FK enforcement is on. |
| 29 | divergence | mutual | static-only | deterministic | `decompose_attachment.result.overflow_fields = 1` counts a `record_type_drift_log` row while elsewhere `overflow_fields` counts `schema_drift_log` rows; header comment acknowledges the count source but does not call out the semantic shift. Plan does not specify the field-name convention; impl reused the existing field name with shifted meaning. |
| 30 | documentation | informational | none | deterministic | No CLAUDE.md / MEMORY.md / STATE.md updates in cdf668b; STATE.md is parent-agent ledger per protocol. No mandate violation. |
| 31 | test-coverage | informational | none | deterministic | `known_record_type` regression covered behaviorally via two tests (positive: `test_attachment_discriminator_no_longer_unknown`; negative: `test_unknown_variant_known_corpus_discriminators` updated); helper itself private. Behavioral coverage is project's prevailing pattern. |
| 32 | verification | impl-defect | none | deterministic | Commit message claims "7 new tests in schema.rs"; actual count is 8 (the `migration_008_post_apply_integrity` test was not counted). Commit-body factual divergence; counted via grep. |
| 33 | divergence | impl-defect | none | deterministic | New `migration_008_post_apply_integrity` test description claims "Replaces the implicit 'should still have exactly 7' expectation that B1.1 set" but the existing assertion at schema.rs:269 was kept and updated (count 7→8) rather than replaced; doc-vs-code mismatch in the new test's own description. |

---

## Meta-plan subsection

The audit catalog contains exactly 1 `meta-plan` row:

- **#19 (plan §107 vs §129 internal inconsistency)** — Plan §107 frames the `attachments` table as "uuid-keyed envelope mirroring messages shape" while §129 lists columns that do not mirror messages exactly (omits `is_sidechain`, `user_type`, `agent_id`, `is_meta`, `type`; adds `entrypoint`, `inner_type`, `body_json`). Implementation picked the §129 column list. Drives audit rows #1, #2, #3, #4 as downstream consequences. Resolution requires plan author intent — the audit cannot determine which specification was authoritative. Classified as `plan-defect` along plan-spec reach because the inconsistency is internal to the plan text. Identified as a headline finding in the triage context.

Two further rows (#5, #14) raise plan/impl ambiguity at a different layer — the C1.1 stub-deferral boundary between drift.rs (fully implemented) and `decompose_attachment` (stub). These are not categorized `meta-plan` by the audit, but their `mutual` plan-spec-reach classification reflects that the stub-deferral framing in §C1.1 did not specify which side of the boundary the drift-arm fell on, while the implementation chose to fully build the drift arm asymmetrically with the decompose stub. The plan's silence on stub-arm scope and the implementation's chosen asymmetry both contribute.

---

## Notes

- **Headline findings:** rows #5, #6, #19 per the triage context. #5 → `mutual` / `runtime-only` / `deterministic`. #6 → `impl-defect` / `static-only` / `deterministic`. #19 → `plan-defect` / `none` / `deterministic`.
- **Decompose-stub vs full-drift-impl judgment (#5, #14):** Plan §C1.1 paragraph explicitly says decomposer routing is C1.2's job, which independently considered would favor `informational`. However the audit observes that drift.rs Attachment arm is fully implemented (envelope + 12 subtype arms + Unknown) while `decompose_attachment` only stubs — this creates dead code in production until C1.2. The asymmetry is a stub-coherence gap not specified in the plan. Both contributions classify as `mutual`.
- **Plan-defect vs impl-defect distinction:** Rows #1, #2, #4 classify `plan-defect` because in each case the plan text (§107 "mirror messages shape") is the loose party while the implementation followed the §129 column list. Row #6 classifies `impl-defect` because the test-name and header comment do not match the test body's chosen behavior — the impl artifact is internally inconsistent. Rows #17, #32, #33 classify `impl-defect` because they are commit-author choices: 82-char subject, miscounted "7 new tests" claim, misleading test-description framing.
- **Verification asymmetry (#15, #16):** mirrors B1.2-Triage rows #20, #22, #45 pattern of safeguard-recording gaps. #15 is `unverified` for the backup-capture status (plan does not require recording it; no forensic doc exists for cdf668b yet). #16 is `deterministic` because the absence of dry-run evidence is directly observable; the meta-classification is plan-defect because §safeguard 2 names C1.1 without requiring an artifact path.
- **No `build-blocking` or `test-blocking` rows.** All 220 workspace tests pass per V5–V8.
- **Three `runtime-only` rows:** #5 and #14 (drift signals collapsed during ingestion until C1.2 wires the decompose calls); #28 (FK NO ACTION semantics observable at delete time).
- **Five `static-only` rows:** #3 (additional column observable in schema only), #6 (test-name mismatch — observable only by reading test code), #24 (dual-constant source-text shape), #27 (unused import), #29 (semantic-shift in field meaning observable only by reading decomposer source).
- **One `unverified` row:** #15 — backup file existence and integrity-check capture for cdf668b not recorded; live DB has not yet had migration 008 applied (kickstart pending per audit-context note).
- C1.1-Review is the next subagent in the DAG; this triage feeds that review.
