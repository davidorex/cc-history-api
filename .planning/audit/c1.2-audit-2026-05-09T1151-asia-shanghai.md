# C1.2 Adversarial Audit — commit ec65a40

**Audit metadata.** Commit SHA: `ec65a40` ("feat(store): activate C1.2 decompose_attachment table population"). Plan section: §C1.2 (lines 132–138, plus §131 unmodeled-subtype catch-all, §safeguard 2 refined at line 356–363, §verification per item C1 lines 286–297). Plan file: `/Users/david/.claude/plans/curious-napping-koala.md`. Audit timestamp: 2026-05-09T11:51 Asia/Shanghai.

**Requirements ledger summary.** 13 plan requirements extracted from §C1.2 + §131 + §137 + §138 + §safeguard 2 + §verification per item C1: R1 dispatcher dispatches by `attachment.type`; R2 hook_success populates BOTH attachments + hook_executions; R3 hook_permission_decision populates BOTH; R4 the other 10 modeled subtypes populate attachments; R5 unmodeled subtypes serialize to attachments.body_json AND log to record_type_drift_log with `record_type = "attachment.<subtype>"`; R6 decompose_record gains an Attachment arm (already shipped C1.1); R7 drift.rs::log_record_overflow Attachment arm gets activated (envelope + per-subtype overflow logging); R8 dry-run produced + summary in commit body (§safeguard 2); R9 cargo build/test verification gates; R10 forensic commit message; R11 no migration / core-record-type changes (those were C1.1's contract); R12 ingestion-changing-commit dry-run-against-corpus; R13 helper fns extract subtype-specific columns (§135 — small helper fns).

## Deviation catalog

| # | Plan ref | Impl ref | Plan file:line | Impl file:line | Category | Description | Evidence |
|---|---|---|---|---|---|---|---|
| 1 | R5 | I-decompose | curious-napping-koala.md:136 | crates/store/src/decompose.rs:1135-1162 | divergence | Plan §136 specifies unmodeled subtypes "serialize their body to attachments.body_json AND additionally log to record_type_drift_log with record_type = 'attachment.<subtype>'". Implementation also unconditionally populates `attachments` for Unknown — observed in dry-run as 4 rows with `inner_type LIKE 'attachment.%'` (compact_file_reference, date_change, plan_file_reference, queued_command). The plan's "AND additionally" wording is satisfied; the audit observation is that this is the C1.2 affirmative behavior (preserves drift signal AND preserves body) and not a bug. Listed for completeness. | `sqlite3 /tmp/claude-history.dryrun-c1.2.db "SELECT inner_type, COUNT(*) FROM attachments WHERE inner_type LIKE 'attachment.%' GROUP BY inner_type"` returns 4 rows |
| 2 | R2/R3 | insert_hook_*_row | curious-napping-koala.md:130 | decompose.rs:899-916, 927-940 | divergence | hook_executions INSERT helpers use plain `INSERT INTO hook_executions` (no `OR IGNORE`). On re-decompose of the same record, the hook_executions table will accumulate duplicate rows because the table's primary key is `id INTEGER PRIMARY KEY AUTOINCREMENT` and there is no UNIQUE constraint on (attachment_uuid). decompose_user/decompose_assistant precedent uses INSERT OR IGNORE on UUID-PK tables. The attachments-row write is INSERT OR IGNORE and would no-op on re-decompose, so the asymmetry is: re-decomposing produces 1 attachments row (idempotent) + N hook_executions rows (not idempotent). | grep -n "INSERT INTO hook_executions" decompose.rs returns lines 900, 928 (no OR IGNORE on either); migration 008 has no UNIQUE(attachment_uuid) |
| 3 | R10 | commit body §verification | (project mandate) | commit message body | test-coverage | The 15 new tests in decompose.rs do not include an idempotency test (re-decompose of the same record asserts no duplicate rows). decompose_user/decompose_assistant precedent has `test_decompose_idempotency` at decompose.rs:1529. Given observation #2, an idempotency test would have surfaced the hook_executions duplication path. | `grep -n "fn test_" crates/store/src/decompose.rs` shows test_decompose_idempotency at 1529; the 15 attachment-tests at 2356-2727 contain no idempotency assertion |
| 4 | R7 | decompose_attachment dispatcher | curious-napping-koala.md:138 | decompose.rs:993-1166 | divergence | Plan §138 says C1.2 activates the drift.rs::log_record_overflow Attachment arm. Implementation does NOT call `drift::log_record_overflow` — instead it calls `drift::log_overflow` directly per arm, mirroring decompose_user/decompose_assistant precedent. The drift.rs Attachment arm in `log_record_overflow` (the function that takes a `&JSONLRecord` and dispatches internally) remains uninvoked from production ingestion in C1.2, contradicting the literal plan §138 wording. The commit message body acknowledges this design choice ("matches the existing decompose_user/decompose_assistant precedent"). | `grep -n "log_record_overflow" crates/store/src/decompose.rs` returns 0 hits; `grep -n "log_overflow" crates/store/src/decompose.rs` shows direct calls at lines 1012, 1027, 1038, 1053, 1064, 1071, 1082, 1089, 1096, 1107, 1114, 1121, 1132 |
| 5 | R8 | commit body | curious-napping-koala.md:356-363 (§safeguard 2) | commit body | divergence | Plan §safeguard 2 says ingestion-changing commits must produce a "binary that runs in a `--dry-run` mode against the existing JSONL corpus without writing to the production DB". The implementation used `claude-history sync --db-path /tmp/claude-history.dryrun-c1.2.db --projects-dir /tmp/dryrun-corpus` (a separate scratch DB + a curated corpus subdir, NOT a `--dry-run` flag against the existing corpus). This is a defensible pattern equivalent to the §safeguard 2 intent, but is not the literal "dry-run mode against the existing corpus" described. | commit message body cites `--db-path /tmp/claude-history.dryrun-c1.2.db --projects-dir /tmp/dryrun-corpus`; no `--dry-run` flag exists in the CLI per `claude-history sync --help` |
| 6 | R8 | dry-run scope | curious-napping-koala.md:356-363 | commit body | verification | Dry-run was against ONE session (4360 records, single cc-history-api session). Plan §C1 verification line 290 ("Volume parity: SELECT COUNT(*) FROM attachments ≥ 10,085") implies corpus-wide validation. C1.1 corpus survey identified 22 distinct subtypes (12 modeled + 10 unmodeled). Dry-run surfaced 9 modeled subtypes (hook_success, hook_permission_decision, mcp_instructions_delta, skill_listing, task_reminder, deferred_tools_delta, plan_mode, plan_mode_exit, plan_mode_reentry) — 3 modeled subtypes (edited_text_file, todo_reminder, nested_memory) did NOT appear, so the dispatcher's correctness for those subtypes is exercised only by the per-subtype tests (which load `/tmp/c1.1-samples-*.json` fixtures). The dry-run also surfaced only 4 unmodeled subtypes (compact_file_reference, date_change, plan_file_reference, queued_command) — plan §C1.1 identified ~10 unmodeled subtypes (commit body acknowledges this discrepancy). | `sqlite3 /tmp/claude-history.dryrun-c1.2.db "SELECT inner_type, COUNT(*) FROM attachments GROUP BY inner_type"` shows 13 distinct inner_type values (9 modeled + 4 unmodeled) |
| 7 | R6 | decompose_record | curious-napping-koala.md:137 | decompose.rs:78, 85-91 | divergence | `decompose_record` does NOT include Attachment in the `maybe_base` match arm (lines 85–91), so for an Attachment record the `upsert_project` call (lines 92–96) is skipped. Attachments thus never increment `projects.session_count` and never refresh `projects.last_seen`. AttachmentRecord has `cwd: Option<String>` populated, which would be sufficient to compute the project row, but the dispatcher branches it only on User/Assistant/Progress/System. Note: this matches the existing pattern for QueueOperation/Summary/FileHistorySnapshot which also lack RecordBase, but AttachmentRecord has cwd via its envelope and could be wired equivalently — the plan does not explicitly mandate this, so the divergence is from analogous decomposers rather than from a plan requirement. | decompose.rs:85-91 shows `_ => None` path for non-User/Assistant/Progress/System; AttachmentRecord has `pub cwd: Option<String>` per record.rs:438 |
| 8 | R7 | decompose_attachment | curious-napping-koala.md:138 | decompose.rs:993-1166 | omission | `decompose_attachment` does not call `upsert_agent` despite AttachmentRecord's overflow potentially containing `agentId` (per c1.1-audit observation that envelope-overflow `agentId` is observed on real records — confirmed by the envelope-overflow test at decompose.rs:2727 explicitly using `"agentId": "subagent_xyz"`). Existing `decompose_user`/`decompose_assistant` call upsert_agent off `RecordBase.agent_id`, but AttachmentRecord intentionally does not promote agentId to a struct field — it is captured only in `overflow`. The agents-table linkage from attachments is therefore not materialized. Plan does not explicitly mandate this, so this is a divergence-from-analogous-pattern observation rather than a strict plan-omission. | grep -n "upsert_agent" decompose.rs shows it called from decompose_user/decompose_assistant (lines 421, 487), not from decompose_attachment |
| 9 | R10 | commit message body | (project mandate) | commit body | commit-message | Subject is 64 chars, ≤72-char convention restored after B1.1's 95 + C1.1's 82 over-runs. Confirmed. | `git log -n 1 ec65a40 --format=%s \| awk '{print length}'` returns 64 |
| 10 | R10 | commit message body | (project mandate) | commit body | commit-message | Commit body uses no "ensures" / "fixes" / "guarantees"; uses aim/intention framing throughout ("preserves", "mirrors", "intended", "matches"). Confirmed compliant. | `git show ec65a40 \| grep -iE "\b(ensures?|guarantees?|fixes?)\b"` returns no matches in body |
| 11 | R10 | commit message body | (project mandate) | commit body | commit-message | No `Co-Authored-By:` trailer present. | `git show ec65a40 \| grep -i "co-authored-by"` returns no matches |
| 12 | R10 | commit message body | (project mandate) | commit body line "Per-subtype body_json shape" | commit-message | The "Per-subtype body_json shape" enumeration in commit body lists 9 entries covering 11 subtypes (plan_mode + plan_mode_reentry combined, edited_text_file separate, hook_success / hook_permission_decision combined). The 12th modeled subtype `nested_memory` IS listed. Two entries unaccounted-for at glance: TaskReminder + TodoReminder are explicitly listed; PlanMode + PlanModeReentry combined; PlanModeExit separate. All 12 modeled subtypes accounted for; the count is correct. Listed for forensic completeness. | commit body lines list: hook_success / hook_permission_decision, mcp_instructions_delta, skill_listing, edited_text_file, task_reminder, todo_reminder, deferred_tools_delta, plan_mode, plan_mode_reentry, plan_mode_exit, nested_memory = 11 entries covering 12 subtypes |
| 13 | R12 | dry-run output in commit | curious-napping-koala.md:356-363 | commit body counts | meta-plan | Plan §safeguard 2 line 363 lists what the dry-run report MUST contain. Commit body provides: attachments_total, hook_executions_total, record_type_drift_log_total, sessions_total, messages_total, hook decomposition (1166+70=1236), unmodeled-subtype list, "0 warnings, 0 errors". This satisfies §safeguard 2's reporting expectations. The plan however does not specify a numerical-baseline-comparison requirement (e.g. pre/post DB row count diff against an existing scratch DB); B1.2's audit precedent established such a baseline pattern. | commit body has counts but no pre/post diff against a baseline |
| 14 | R8 | dry-run total | (commit body) | dry-run scratch DB | divergence | Commit body claims "334 record_type_drift_log rows". `sqlite3 /tmp/claude-history.dryrun-c1.2.db "SELECT COUNT(*) FROM record_type_drift_log"` returns 334 distinct rows; `SELECT SUM(occurrence_count) FROM record_type_drift_log` returns 341. Commit body uses 334 (row count), which is correct against the COUNT(*) reading. | `sqlite3 /tmp/claude-history.dryrun-c1.2.db "SELECT COUNT(*) FROM record_type_drift_log; SELECT SUM(occurrence_count) FROM record_type_drift_log"` returns 334, 341 |
| 15 | R12 | drift_version "unknown" fallback | (drift.rs precedent) | decompose.rs:1006 | divergence | `drift_version = record.version.as_deref().unwrap_or("unknown")` — when AttachmentRecord.version is None, the drift-version partition uses the literal string "unknown", which collides with the partition created by `decompose_unknown` (which also uses `"unknown"` as a generic fallback for the JSONLRecord::Unknown variant). schema_drift_log's UNIQUE(field_name, record_type, version) and record_type_drift_log's UNIQUE(type_name, version) keys would conflate two distinct meanings of "unknown" in the version column. Commit body acknowledges the policy ("matches the policy in drift.rs::log_record_overflow's Attachment arm"). | decompose.rs:1006 `unwrap_or("unknown")`; drift.rs (for the other arm) has same fallback |
| 16 | R12 | hook_executions FK shape | curious-napping-koala.md:130 | dry-run + tests | regression | FK integrity check: `PRAGMA foreign_key_check` on dry-run DB returns no rows; `SELECT COUNT(*) FROM hook_executions h LEFT JOIN attachments a ON h.attachment_uuid = a.uuid WHERE a.uuid IS NULL` returns 0. FK semantics confirmed — every hook_executions row joins. | sqlite3 dry-run output `PRAGMA foreign_key_check` empty; LEFT JOIN orphan-check returns 0 |
| 17 | R12 | dry-run integrity | (project safeguard) | dry-run DB | regression | `PRAGMA integrity_check` against /tmp/claude-history.dryrun-c1.2.db returns `ok`. FK enforcement is OFF (`PRAGMA foreign_keys` returns 0) on the dry-run DB by default — so the FK constraint was not enforced at INSERT time. Tests in decompose.rs:1185 explicitly enable `PRAGMA foreign_keys = ON` before running migrations + tests, so FK enforcement IS exercised in unit tests. The dry-run path bypasses runtime FK enforcement. | `sqlite3 /tmp/claude-history.dryrun-c1.2.db "PRAGMA foreign_keys"` returns 0; decompose.rs:1185 sets ON in tests |
| 18 | R9 | dry-run sessions count | (commit body) | dry-run scratch DB | divergence | Commit body claims "sessions_total = 1". `sqlite3 /tmp/claude-history.dryrun-c1.2.db "SELECT COUNT(*) FROM sessions"` returns 1. Confirmed accurate. The 1293 attachments + 1811 messages all bind to a single session_id. | `sqlite3 /tmp/claude-history.dryrun-c1.2.db "SELECT COUNT(DISTINCT session_id) FROM attachments"` returns 1 |
| 19 | R7 | drift signal #3 path | curious-napping-koala.md:138 | decompose.rs:1135-1162 | divergence | drift.rs Attachment arm comment block describes 3 drift signals (envelope, per-subtype, Unknown subtype). C1.2 activates signal #1 (envelope) via `log_overflow` at decompose.rs:1012, signal #2 (per-subtype) per arm via `log_overflow` at lines 1027/1038/.../1132, and signal #3 (Unknown subtype) via `log_record_type_drift` at decompose.rs:1156. All three signals reachable. The drift.rs::log_record_overflow function itself, however, contains the Attachment arm wired identically (per c1.1-audit row #5) — that function path is NOT invoked from decompose_attachment, so the function-as-shipped-in-C1.1 remains dead code in the live ingestion path even after C1.2 (because C1.2 reproduces the same logic inline rather than calling through). | grep `log_record_overflow` decompose.rs returns 0 hits; the C1.1-shipped Attachment arm in drift.rs::log_record_overflow remains unexercised by ingestion |
| 20 | R6 | unknown-subtype attachments duplication | (no plan ref) | decompose.rs:1135-1144 | divergence | The `AttachmentBody::Unknown` arm calls `insert_attachment_row` (INSERT OR IGNORE — idempotent) AND `log_record_type_drift` (which is itself idempotent via INSERT OR IGNORE + UPDATE). So Unknown subtypes ARE idempotent on re-decompose, including the drift-log occurrence_count side-effect. This is a deliberate asymmetry: hook_executions duplicates on re-decompose (observation #2), but Unknown-subtype attachments do not. | decompose.rs:1142 uses insert_attachment_row (INSERT OR IGNORE); decompose.rs:1156 uses log_record_type_drift (idempotent) |
| 21 | R6 | unknown-subtype occurrence_count semantics | (B1.1 precedent) | decompose.rs:1156-1161 | divergence | `log_record_type_drift` UPDATE-on-conflict semantics imply that re-decomposing the SAME unknown-subtype record will increment `occurrence_count` (same as decompose_unknown's per-record-instance count). Whether this is correct depends on whether the bytewise re-ingestion path triggers re-decompose of the same record (B1.2 docs suggest yes). The decompose_unknown precedent at decompose.rs:802-803 has the same shape, so this is consistent — but worth noting: `occurrence_count` for the Unknown attachment subtype semantically counts re-decompose events, not distinct records. | decompose.rs:1156 calls log_record_type_drift; behavior same as decompose_unknown precedent |
| 22 | R13 | per-subtype helper fns | curious-napping-koala.md:135 | decompose.rs:823-940, 994-1166 | divergence | Plan §135 says the 10 non-hook subtypes "populate attachments with subtype-specific columns extracted via small helper fns". Implementation does NOT extract subtype-specific columns into named SQL columns — there are no per-subtype columns on the `attachments` table (per migration 008 which has only generic `inner_type` + `body_json`). All 10 non-hook subtypes use the single `insert_attachment_row` helper with `body_json = serde_json::to_string(b)` storing the entire serialized body. There are NO subtype-specific helper fns, only the 4 helpers (upsert_session_from_attachment, insert_attachment_row, insert_hook_success_row, insert_hook_permission_decision_row). The plan's "subtype-specific columns extracted via small helper fns" wording is unsatisfied by the migration 008 schema as committed in C1.1; the plan's wording was effectively superseded by the C1.1 schema choice (only `inner_type` + `body_json`). This is meta-plan ambiguity: §135 written before C1.1 froze the schema shape. | migration 008 attachments table has 11 columns total (uuid, session_id, parent_uuid, timestamp, cwd, version, git_branch, slug, entrypoint, inner_type, body_json) — no per-subtype columns; commit body confirms "body_json carries the serialized inner struct" |
| 23 | R7 | drift_version partition for None case | (B1.2 precedent) | decompose.rs:1006 | meta-plan | When AttachmentRecord.version is None, the dry-run shows envelope and inner overflow logged under version="unknown" (string). The plan does not address the None-version case for attachments. Records with version=None will all collapse into a single "unknown"-version row in schema_drift_log per (field_name, record_type) combo, masking version-evolution signal. | decompose.rs:1006; sqlite3 dry-run shows no rows with version="unknown" because all dryrun records had version 2.1.126 |
| 24 | R5 | C1.1-Review #29 resolution | (C1.1-Review STATE.md:148) | decompose.rs:31-43, 982-991 | divergence | C1.1-Review #29 closed the C1.1-audit observation about `overflow_fields` cross-table semantics with "address in C1.2 via clarifying docstring or rename". C1.2 chose the docstring path. The DecomposeResult struct field at decompose.rs:31-43 has a 9-line docstring explaining the cross-table semantics. The function-level docstring at decompose.rs:982-991 echoes this. Field name retained as `overflow_fields`. The structural concern (per-record-type drift counts mixed into the same field as per-field schema-drift counts) is documented but not resolved at the type level. Forensic clarity preserved; semantic ambiguity preserved. | decompose.rs:31-43 (struct doc); decompose.rs:982-991 (function doc); field name unchanged |
| 25 | R10 | commit body §C1.2 boundary | (project mandate) | commit body last line | commit-message | Commit body explicitly states scope: "C1.2 boundary: decomposer-routing only. FTS5 wiring is C1.3; CLI/MCP/REST surfacing is C1.4." Confirmed in line with §C1.3/§C1.4 sequencing. | commit body final paragraph |
| 26 | R11 | core / migration unchanged | curious-napping-koala.md:247 | git diff | dependency | `git diff 1f90d1e..ec65a40 -- crates/core/` returns 0 lines (no changes to core). `git diff 1f90d1e..ec65a40 -- crates/store/migrations/` returns 0 lines (no migration changes). C1.2 is decompose.rs + drift.rs only, per plan and per commit boundary statement. AttachmentBody enum, AttachmentRecord, migration 008 — all unchanged. | `git diff 1f90d1e..ec65a40 --stat` shows only crates/store/src/decompose.rs and crates/store/src/drift.rs |
| 27 | R6 | session_id_from_file fallback | (decompose.rs precedent) | decompose.rs:78, 994 | divergence | `decompose_record` accepts `session_id_from_file` as fallback for records lacking one (used by decompose_summary, decompose_file_history_snapshot). `decompose_attachment` does NOT accept this parameter — it relies on AttachmentRecord.session_id which is non-Optional. If a real-corpus AttachmentRecord arrived with a missing or empty `sessionId`, the upsert_session_from_attachment would write a row with empty session_id and the FK would technically resolve. C1.1 declared session_id as `String` (non-Optional). Plan does not address this case. | decompose.rs:994 signature `(record: &AttachmentRecord, tx: &Transaction)` — no session_id_from_file param |
| 28 | R7 | log_overflow vs log_record_overflow | curious-napping-koala.md:138 | decompose.rs vs drift.rs | divergence | The drift.rs `log_record_overflow` Attachment-arm wiring shipped in C1.1 (per c1.1-audit) accepts a `&JSONLRecord` and dispatches internally. C1.2 chose to call `log_overflow` (the lower-level fn) directly per arm instead. The drift.rs Attachment-arm code remains tested only by drift::tests (not by decompose.rs::tests), and is reachable only via direct test invocation, not via ingestion path. The two paths produce identical schema_drift_log rows for the same input but the C1.1-shipped dispatcher arm is dead code in production. | grep "log_record_overflow" returns hits only in drift.rs (the function definition + its own tests) |
| 29 | R9 | new test file count breakdown | curious-napping-koala.md:286-288 | decompose.rs:2356-2764 | test-coverage | 15 new tests claimed; verified: 12 fixture-driven per-subtype tests (lines 2356, 2386, 2408, 2431, 2451, 2471, 2491, 2510, 2534, 2553, 2563, 2582), 1 unknown-subtype test (2613), 1 FK-shape test (2672), 1 envelope-overflow test (2727) = 15. Confirmed accurate. | grep -nE "fn test_decompose_attachment_" decompose.rs returns 15 lines |
| 30 | R9 | fixture-driven test skip behavior | (no plan ref) | decompose.rs:2357-2360 et al. | test-coverage | All 12 fixture-driven tests use early-return pattern: `let Some((conn, uuid)) = decompose_fixture(...) else { eprintln!("skip: ..."); return; };`. If the fixture file at `/tmp/c1.1-samples-<subtype>.json` is absent, the test passes silently as a no-op. CI without the fixtures would see a green test suite but exercise zero per-subtype logic. The 12 fixtures DO exist in /tmp at audit time (verified via `ls /tmp/c1.1-samples-*.json`). On a fresh-machine CI run, they would not exist. C1.2 thus binds test correctness to a non-versioned, non-repo-tracked artifact. | decompose.rs:2358 `eprintln!("skip: /tmp/c1.1-samples-hook_success.json missing"); return;` — silent pass on absence |
| 31 | R9 | per-subtype test depth | (project mandate) | decompose.rs:2356-2600 | test-coverage | The 12 fixture-driven tests assert: (a) one row in attachments with correct inner_type, (b) for hook subtypes, one hook_executions row, (c) body_json round-trips through serde_json::from_str. They do NOT assert: (d) per-subtype struct field round-trip (e.g., `parsed["filename"]` == original fixture filename), (e) overflow handling per fixture, (f) hook_executions column-by-column shape (only the FK-shape test at 2672 covers cross-column completeness, and only for synthetic data). Tests are shallow on field-level semantic coverage. | decompose.rs test bodies show `assert!(parsed.get("filename").and_then(\|v\| v.as_str()).is_some())` — presence-only assertion |
| 32 | R9 | hook_permission_decision FK column-shape test | (project mandate) | decompose.rs:2672-2718 | test-coverage | The FK-shape test at 2672 uses `hook_success` body, NOT `hook_permission_decision`. There is no equivalent FK-shape test asserting that `hook_permission_decision` records correctly populate `decision` while NULL-ing `hook_name`/`exit_code`/`stdout`/`stderr`/`command`/`duration_ms`. The fixture-driven `test_decompose_attachment_hook_permission_decision` at 2386 asserts (decision present, hook_name None, exit_code None) but does NOT assert duration_ms / stdout / stderr / command are also None. | decompose.rs:2394-2403 asserts only 3 columns (decision, hook_name, exit_code); migration 008 has 6 hook-success-side columns |
| 33 | R7 | renamed test in drift.rs | (commit body) | drift.rs:1129-1180 | test-coverage | The renamed test `test_decompose_record_attachment_modeled_writes_typed_tables` (drift.rs:1129) actually exercises the new path. Test body asserts attachments_count == 1 and hook_count == 1 after dispatching a hook_success record through `decompose_record`. It does exercise table-population (not just inverted assertion direction). Confirmed substantive. | drift.rs:1158-1178 asserts row counts, not just deviation from prior assertion |
| 34 | R12 | live DB regression net | curious-napping-koala.md:286-297 | live system | regression | Daemon at PID 60855 still on post-baf8ee5 binary; live DB schema_versions=7; attachments + hook_executions tables do NOT exist on live DB. Migration 008 not applied. Kickstart pending separately, per commit body acknowledgement and per parent-agent ledger. C1.2's verification path is test suite + dry-run, not live ingestion. | `sqlite3 ~/.claude/.claude-history.db "SELECT MAX(version) FROM schema_versions"` returns 007; `SELECT name FROM sqlite_master WHERE name IN ('attachments','hook_executions')` returns empty |
| 35 | R10 | commit body verification stats | (project mandate) | commit body | commit-message | Commit body cites: `cargo build --release clean`, `cargo test -p claude-history-store: 171 passed (was 156 + 15 new)`, `cargo test --workspace: 235 total passed (server 3 + core 61 + store 171 + doc-tests 0)`. Audit re-ran: workspace 235 = 3+61+171+0+0 confirmed. 156+15 = 171 confirmed. | `cargo test --workspace 2>&1 \| grep "test result:"` returns 3, 61, 171, 0, 0 |
| 36 | R8 | cross-corpus dry-run absence | curious-napping-koala.md:356-363 | (commit body) | verification | The dry-run targeted `--projects-dir /tmp/dryrun-corpus` (one curated session), not `~/.claude/projects/` (the real corpus of all projects). A cross-corpus dry-run would have surfaced the 3 missing modeled subtypes (edited_text_file, todo_reminder, nested_memory) and additional unmodeled subtypes (auto_mode, command_permissions, etc.) per c1.1 plan §C1.1 corpus survey. The single-session approach gives runtime-error-free verification at the cost of subtype-coverage breadth. | dry-run inner_type breakdown shows 9 of 12 modeled subtypes; 3 modeled subtypes (edited_text_file, todo_reminder, nested_memory) absent |
| 37 | R5 | unmodeled-subtype-name canonicalization | (no plan ref) | decompose.rs:1140 | divergence | Unknown-subtype `inner_type` is computed as `format!("attachment.{subtype}")` where `subtype` is the raw discriminator string from the JSONL. No normalization (lowercase, trim, sanitize) — a malicious or malformed subtype name like `"hook_success"` (i.e., a subtype that should have been modeled but failed to deserialize for any reason) would land as `inner_type = "attachment.hook_success"`, distinct from a canonical `inner_type = "hook_success"` row written by the modeled path. This shadow-namespace allows the same logical subtype to appear under two different `inner_type` strings depending on whether deserialization succeeded. | decompose.rs:1140 `let inner_type = format!("attachment.{subtype}");` — no sanitization; the modeled path writes `inner_type = "hook_success"` (no `attachment.` prefix) |
| 38 | R7 | per-subtype overflow drift policy on Unknown | (no plan ref) | decompose.rs:1135-1162 | omission | The Unknown arm at decompose.rs:1135-1162 logs to record_type_drift_log via `log_record_type_drift` but does NOT call `log_overflow` for inner-body overflow under `record_type = "attachment.<subtype>"` (no struct exists yet for the unmodeled subtype). The envelope `log_overflow` at line 1012 fires for ALL paths (including Unknown). So Unknown-subtype envelope-overflow IS logged; Unknown-subtype inner-body-overflow is captured only via `body_json` raw-Value storage, not via schema_drift_log. This is a defensible asymmetry (the inner body has no schema to detect overflow against), but worth surfacing. | decompose.rs:1135-1162 has no `log_overflow` call inside the Unknown arm |
| 39 | R10 | commit body §verification subagent attribution | (project mandate) | commit body | commit-message | Commit body has no author-credit / Co-Authored-By trailer. Subject "feat(store): activate C1.2 decompose_attachment table population" follows project subject convention (≤72 chars, conventional-commit prefix, no emoji). | git show ec65a40 |
| 40 | R6 | dispatcher reachability for 6 unsurfaced unmodeled subtypes | curious-napping-koala.md:131 | decompose.rs:1135-1162 | verification | Plan §C1.1 enumerated 10 unmodeled subtypes; dry-run surfaced 4. The dispatcher's Unknown arm at decompose.rs:1135-1162 is generic (`AttachmentBody::Unknown { subtype, raw }`) and does not enumerate specific subtypes — so it would route auto_mode, auto_mode_exit, command_permissions, agent_mention, invoked_skills, file (the 6 not surfaced by dry-run) correctly via the same code path. Coverage by inspection rather than by execution. | decompose.rs:1135 `AttachmentBody::Unknown { subtype, raw }` — generic; would handle any unmodeled subtype |
| 41 | R10 | commit body length and structure | (project mandate) | commit body | commit-message | Commit body is approximately 105 lines: subject (1) + blank + foundation context (8) + per-record dispatch numbered list (54 lines covering 5 steps) + per-subtype body_json shape (12 lines) + C1.1-Review #29 resolution (10 lines) + tests (15 lines) + verification (15 lines) + production-DB risk (10 lines) + boundary statement (1). Forensic detail per project mandate satisfied. | git show ec65a40 message length |
| 42 | R8 | dry-run write semantics | curious-napping-koala.md:362 | dry-run scratch DB | meta-plan | Plan §safeguard 2 line 362 says dry-run "without writing to the production DB". Implementation wrote to /tmp/claude-history.dryrun-c1.2.db (scratch) — not production. Compliant. The dry-run scratch DB is 12.1 MB. Note: parent-agent did not delete it before audit, so it remains available for inspection. | `ls -la /tmp/claude-history.dryrun-c1.2.db` returns 12136448 bytes |

## Verification command results

```
$ git show ec65a40 --stat
crates/store/src/decompose.rs | 885 ++++++++++++++++++++++++++++++++++++++++--
crates/store/src/drift.rs     |  45 ++-
2 files changed, 864 insertions(+), 66 deletions(-)
```

```
$ git log -n 1 ec65a40 --format=%s | awk '{print length}'
64
```

```
$ cargo test --workspace 2>&1 | grep "^test result:"
test result: ok. 3 passed; 0 failed; 0 ignored; 0 measured
test result: ok. 61 passed; 0 failed; 0 ignored; 0 measured
test result: ok. 171 passed; 0 failed; 0 ignored; 0 measured
test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured
test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured
```

```
$ sqlite3 /tmp/claude-history.dryrun-c1.2.db "PRAGMA integrity_check; PRAGMA foreign_key_check"
ok
(no FK violations)
```

```
$ sqlite3 /tmp/claude-history.dryrun-c1.2.db "SELECT COUNT(*) FROM hook_executions h LEFT JOIN attachments a ON h.attachment_uuid = a.uuid WHERE a.uuid IS NULL"
0
```

```
$ sqlite3 /tmp/claude-history.dryrun-c1.2.db "SELECT inner_type, COUNT(*) FROM attachments GROUP BY inner_type ORDER BY 2 DESC"
hook_success|1166
hook_permission_decision|70
task_reminder|32
attachment.compact_file_reference|5
attachment.date_change|4
deferred_tools_delta|4
mcp_instructions_delta|4
plan_mode|2
plan_mode_exit|2
attachment.plan_file_reference|1
attachment.queued_command|1
plan_mode_reentry|1
skill_listing|1
```

```
$ sqlite3 /tmp/claude-history.dryrun-c1.2.db "SELECT a.inner_type, COUNT(*) FROM hook_executions h JOIN attachments a ON h.attachment_uuid = a.uuid GROUP BY a.inner_type"
hook_permission_decision|70
hook_success|1166
```

```
$ launchctl list | grep claude-history
60855	0	com.davidrex.claude-history
-	0	com.davidrex.claude-history-logrotate
```

```
$ sqlite3 ~/.claude/.claude-history.db "SELECT MAX(version) FROM schema_versions"
007
```

```
$ sqlite3 ~/.claude/.claude-history.db "SELECT name FROM sqlite_master WHERE type='table' AND name IN ('attachments', 'hook_executions')"
(empty — neither table exists on live DB)
```

```
$ ls -la /tmp/c1.1-samples-*.json | wc -l
12
```

## Plan-defect notes

**Meta-plan #1 (corresponds to deviation #22).** Plan §135 says "the other 10 modeled subtypes populate `attachments` with subtype-specific columns extracted via small helper fns". Migration 008 (committed in C1.1) does NOT have subtype-specific columns — it has only `inner_type` + `body_json`. The plan's §135 wording was effectively superseded by the C1.1 schema choice but was not edited to reflect the change. The implementation follows the C1.1 schema (`body_json`-only) which is correct for the as-shipped table, but is technically not what §135 specifies.

**Meta-plan #2 (corresponds to deviation #5).** Plan §safeguard 2 line 356 specifies a `--dry-run` mode flag; no such flag exists in the CLI. The implementation used `--db-path` + `--projects-dir` to scope a separate scratch run. The plan's wording assumes a dry-run capability that has not been built. The implementation's substitute pattern is reasonable but not the literal spec.

**Meta-plan #3 (corresponds to deviation #15).** Plan does not specify how to handle `AttachmentRecord.version = None` for the drift_version partition string. C1.2 chose `"unknown"`, which collides with `decompose_unknown`'s fallback for the JSONLRecord::Unknown variant. The plan should specify a partition-collision-avoidance policy or accept the conflation.

## Audit completeness self-check

1. Built requirements ledger: 13 requirements extracted from §C1.2, §131, §137, §138, §safeguard 2, §verification per item C1. Done.
2. Built implementation ledger: 5 helper fns (upsert_session_from_attachment at decompose.rs:823, insert_attachment_row at 853, insert_hook_success_row at 894, insert_hook_permission_decision_row at 922, decompose_attachment dispatcher rewrite at 993) + 15 new tests + 1 renamed test in drift.rs + DecomposeResult struct doc update. Done.
3. Cross-referenced R-list against I-list. R1–R13 all mapped to implementation. No orphaned Im (no scope-creep additions outside decompose.rs/drift.rs). Done.
4. Ran plan's verification commands: cargo build/test, sqlite3 dry-run integrity, FK check, all captured above. Done.
5. Ran regression safeguards: PRAGMA integrity_check on dry-run DB returns ok; FK check returns 0 orphans. Live DB regression net: schema_versions=7, migration 008 unapplied, kickstart pending. Done.
6. Audited commit message: subject 64 chars, no Co-Authored-By, aim/intention framing throughout, no "ensures"/"fixes"/"guarantees", forensic detail extensive, scope statement at end. Done.
7. Audited sequence/dependency: ec65a40 follows 1f90d1e (C1.1-Review checkpoint) which followed cdf668b (C1.1). Position correct in DAG. No references to migration 009 / future commits. Done.
8. Audited documentation gaps: plan does not require CLAUDE.md/MEMORY.md updates for C1.2 (those are gates for C1.4 surfacing). STATE.md C1.2 entry already populated by parent-agent (verified). Done.
9. Audited test coverage: 15 new tests + 1 renamed; 12 fixture-driven (skip on missing fixture); 3 synthetic-JSON tests (Unknown subtype, FK shape, envelope overflow). Idempotency test absent (deviation #3). Per-subtype field round-trip shallow (deviation #31). Done.
10. Compiled deviation catalog: 42 observation rows above. Done.
