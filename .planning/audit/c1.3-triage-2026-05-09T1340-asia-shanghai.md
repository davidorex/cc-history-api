# C1.3 Triage — commit `76bcdde`

**Triage timestamp:** 2026-05-09T13:40 Asia/Shanghai (UTC+0800)
**Audit input:** `/Users/david/Projects/cc-history-api/.planning/audit/c1.3-audit-2026-05-09T1334-asia-shanghai.md`
**Plan reference:** `/Users/david/.claude/plans/curious-napping-koala.md` (§C1.3 lines 143–146, §verification per item C1 lines 286–300 incl. §296 flag-named verification, §safeguard 2 lines 359–368, §spawn-order DAG hard-rule 3 line 210, §250 files-to-modify Tier C1 listing, §148–152 Tier C1.4 scope)
**Mode:** Structural classification along three axes — plan-spec reach, build/test impact, determinism. No severity language. No accept/reject decisions.

---

## Audit-quality note

Audit catalog declares 44 observations. `grep -cE '^\| [0-9]+ \|'` against the audit file returns 44 matching the deviation table. Per-category breakdown in audit context (15 divergence, 7 test-coverage, 6 meta-plan, 5 commit-message, 4 verification, 3 regression, 2 addition, 1 sequence, 1 dependency) sums to 44. Counting matches. All 44 rows carry "Evidence" lines.

---

## Axis totals

**Plan-spec reach:**
- plan-defect: 5 (#2, #3, #4, #6, #42)
- impl-defect: 6 (#1, #10, #12, #17, #25, #43)
- mutual: 4 (#9, #32, #37, #44)
- informational: 29 (#5, #7, #8, #11, #13, #14, #15, #16, #18, #19, #20, #21, #22, #23, #24, #26, #27, #28, #29, #30, #31, #33, #34, #35, #36, #38, #39, #40, #41)

(5 + 6 + 4 + 29 = 44.)

**Build/test impact:**
- build-blocking: 0
- test-blocking: 0
- runtime-only: 4 (#1, #17, #25, #38)
- static-only: 7 (#3, #10, #15, #16, #24, #34, #43)
- none: 33 (#2, #4, #5, #6, #7, #8, #9, #11, #12, #13, #14, #18, #19, #20, #21, #22, #23, #26, #27, #28, #29, #30, #31, #32, #33, #35, #36, #37, #39, #40, #41, #42, #44)

(0 + 0 + 4 + 7 + 33 = 44.)

**Determinism:**
- deterministic: 39
- nondeterministic: 0
- unverified: 5 (#7, #8, #14, #30, #39)

(39 + 0 + 5 = 44.)

**Meta-plan rows:** 6 (#2, #9, #37, #41, #42, #43 — these are the rows audit-categorized `meta-plan` in the catalog table)
**Unverified rows:** 5 (#7, #8, #14, #30, #39)

---

## Triage table

| # | Category | Plan-spec reach | Build/test impact | Determinism | Rationale |
|---|---|---|---|---|---|
| 1 | divergence | impl-defect | runtime-only | deterministic | Plan §296 literally names `--include-attachments` in a verification command; impl chose default-on union with no flag across CLI/REST/MCP. Plan-named flag absent from CLI surface. Headline finding (#2 in context): plan §296's verification syntax types the flag name as part of an executable command, which reads as normative not indicative. |
| 2 | meta-plan | plan-defect | none | deterministic | Plan §hard-rule 3 (line 210) states "C1.1 takes 008, C2.1 takes 009"; C1.3 consumed 009 by introducing 009_attachment_fts.sql. Plan §250 Tier C1 listing names only 008_attachments.sql, not anticipating C1.3's migration. Plan-side staleness flagged at parent-agent level (STATE.md line 162). Headline finding (#1 in context). |
| 3 | divergence | plan-defect | static-only | deterministic | Plan §145 cites watcher.rs:689-711 as the rebuild-loop site; current site is watcher.rs:721-755 due to D-tier line-shift. Plan line-citation stale. |
| 4 | divergence | plan-defect | none | deterministic | Plan §144 says `mcp_instructions_delta.added_blocks` "joined as one document" (singular). Impl uses `json_each(body_json, '$.addedBlocks')` to flatten into N FTS docs per row. Headline finding (#3 in context): both shapes work for FTS5 matching; plan's literal "joined as one" is contradicted by the json_each shape, but the implementation's choice is documented in commit body and migration 009 docstring. Classified `plan-defect` because the plan wording was indicative-of-target-content not normative-on-row-shape; impl's row-shape choice falls outside plan's specification scope. (Adjacent reading `mutual` is reasonable; classification falls to `plan-defect` because the plan wording is stale relative to the as-implemented shape.) |
| 5 | divergence | informational | none | deterministic | Plan §144 names `nested_memory.content.content`; impl uses `json_extract(a.body_json, '$.content.content')` matching the plan's stated path. Listed for forensic completeness. |
| 6 | divergence | plan-defect | none | deterministic | Plan §144 spells `added_blocks` (snake_case); on-disk JSON uses `addedBlocks` (camelCase) per AttachmentBody serde. Impl correctly targets `addedBlocks`. Plan-spelling stale. |
| 7 | verification | informational | none | unverified | Plan §296 verification commands cannot run against live DB (schema_versions=007; migrations 008/009 unapplied). Verification deferred to post-kickstart. Live-DB end-to-end shape unobserved. |
| 8 | verification | informational | none | unverified | Plan §safeguard 2 line 365 sets 80% subtype-coverage minimum. C1.3's 4-subtype set sees 2/4 (50%) exercised against scratch corpus (mcp_instructions_delta + skill_listing); edited_text_file + nested_memory absent from scratch source rows. Plan §safeguard 2 was authored pre-C1.3; C1.3's 4-subtype FTS-indexing scope is narrower than the C1 modeled-subtype set. End-to-end verification of the missing 2 subtypes deferred. |
| 9 | meta-plan | mutual | none | deterministic | Plan §safeguard 2 line 368 exempts parser-typing-only commits; line 359 lists ingestion-changing exemplars. C1.3 is FTS-only (neither parser-typing nor ingestion-changing). Plan ambiguity around classification of FTS-only commits. Impl chose to dry-run anyway. Both plan-side gap and impl-side substitute-shape contribute. |
| 10 | commit-message | impl-defect | static-only | deterministic | Impl-report context cites "Subject 60 chars"; actual subject is 69 chars per `awk '{print length}'`. STATE.md line 162 also records "60 chars". Mis-count propagated impl-report → STATE.md. 72-char convention satisfied; the count claim is inaccurate. Headline finding (#4 in context). |
| 11 | commit-message | informational | none | deterministic | Commit body forensic + ~95 lines. No "ensures"/"fixes"/"guarantees"/"Co-Authored-By:" trailer. Aim/intention framing throughout. Mandate compliant. |
| 12 | divergence | impl-defect | none | deterministic | Plan §safeguard 2 line 364 prescribes baseline pre/post diff against `~/.claude/.claude-history.db` with both row-count values in commit body. Impl provides post-state row counts only; scratch DB cloned from c1.2.1 (so the relevant pre-state is the cloned start, not live DB). Baseline-diff shape not literally satisfied. |
| 13 | test-coverage | informational | none | deterministic | 7 new fts.rs tests + 3 new schema.rs migration_009 tests = 10 total, declared count = grep count. Verified by `grep -nE "fn test_"` and `grep -nE "fn migration_009"`. |
| 14 | test-coverage | informational | none | unverified | 4-subtype rebuild test (fts.rs:670) exercises all four indexed subtypes plus task_reminder negative-control. Unit-test coverage of all four complete; gap is dry-run + live-DB end-to-end on edited_text_file + nested_memory (cross-references #8 + #30). |
| 15 | test-coverage | informational | static-only | deterministic | `test_search_attachment_text_content_skips_empty_extracts` covers mcp_instructions_delta empty addedBlocks + skill_listing empty content; does NOT cover edited_text_file empty snippet or nested_memory empty content.content. SQL guard `length(...) > 0` is uniform; coverage gap is in test breadth not production-code coverage. |
| 16 | divergence | informational | static-only | deterministic | `SearchResult.source` field has `#[serde(default)]`; pre-C1.3 JSON deserializes to `SearchResultSource::Message` via manual Default impl. No test deserializes pre-C1.3-shaped JSON to assert the legacy round-trip. Mechanism correct by construction; test coverage is by-inspection. |
| 17 | divergence | impl-defect | runtime-only | deterministic | Three `rebuild_*` calls in the 30s rebuild closure share one conn.call wrapper without enclosing BEGIN/COMMIT; SQLite default deferred-transaction means each execute_batch commits immediately. Partial-rebuild state reachable (e.g., fts_attachment fails after fts_message + fts_file_operations succeed). `data_ingested_since_fts_rebuild` reset only on full success — but partial writes already committed. |
| 18 | divergence | informational | none | deterministic | REST `source` field shaped as tagged-content enum (`{kind, subtype}`); `Option`/default via `#[serde(default)]` not `Option<T>`. Plan §146 does not specify the enum's wire shape. |
| 19 | divergence | informational | none | deterministic | CLI `print_search_results` prefixes attachment-source rows with `[attachment] `; message-source rows render unchanged. Plan §146 "result-source label" wording does not specify visual format. |
| 20 | addition | informational | none | deterministic | `search_attachment_text_content` sets synthetic `message_type = "attachment"` on result rows. Plan §146 does not specify; commit body documents the choice (consumer-discriminator without new branching). |
| 21 | addition | informational | none | deterministic | For attachment-source results, `block_type = inner_type`; for message-source, block_type carries text/thinking/etc. Subtype duplicated across `block_type` and `source: Attachment(inner_type)`. Plan does not specify; commit body documents. |
| 22 | divergence | informational | none | deterministic | Tracing-log message updated to "(message content + file operations + attachment text)" reflecting triple-rebuild observability. Listed for completeness. |
| 23 | test-coverage | informational | none | deterministic | `test_rebuild_fts_attachment_idempotent` asserts row-count identity across two rebuilds with no source changes (single-row case: 1 skill_listing). Multi-row idempotency relies on SQL semantics. |
| 24 | test-coverage | informational | static-only | deterministic | `test_search_messages_and_attachments_unions_both_sources` uses limit=10 (effectively unbounded on 2-row case); pagination correctness for limit<actual not exercised. fts.rs:380-388 implements limit+offset pool merging — coverage is by-inspection. |
| 25 | test-coverage | impl-defect | runtime-only | deterministic | Union test asserts both rows appear but does not assert ranking order. BM25 cross-table comparison is structurally suspect (table-relative IDF/avgdl differ). Impl sorts both pools by raw `rank` value as if comparable. Test coverage of cross-table-ranking correctness absent. Classified `impl-defect` because the cross-table sort is the implementer's choice; plan does not prescribe ranking semantics. |
| 26 | dependency | informational | none | deterministic | C1.3 modifies 9 files: store/migrations/009_attachment_fts.sql (new), store/src/fts.rs, store/src/schema.rs, store/src/db.rs, server/src/api/search.rs, server/src/main.rs, server/src/mcp/tools.rs, server/src/output.rs, server/src/watcher.rs. Does not touch core/record.rs or store/decompose.rs or migration 008. C1.1/C1.2 surfaces respected. |
| 27 | regression | informational | none | deterministic | `cargo build --release` clean. `cargo test --workspace`: 249/249 passed (3+61+185+0+0). Pre-C1.3 baseline 239; +10 confirmed. |
| 28 | regression | informational | none | deterministic | Live DB `PRAGMA integrity_check; PRAGMA foreign_key_check` returns ok / no FK violations. Live DB schema_versions=007 (kickstart pending). |
| 29 | regression | informational | none | deterministic | Dry-run scratch DB `PRAGMA integrity_check` ok. FTS rebuild produced 11 rows; MATCH 'MCP' → 4; MATCH 'claude' → 9. Confirms commit-body counts. |
| 30 | verification | informational | none | unverified | Daemon at PID 60855 still on pre-C1.3 binary; live DB schema_versions=007. Migration 008/009 unapplied. Kickstart deferred per task constraints. Live edited_text_file + nested_memory paths unverified end-to-end. Headline finding (#5 in context). |
| 31 | commit-message | informational | none | deterministic | Commit body explicitly documents migration-numbering side-effect (C2.1 must take next-available, not 009). Forensic note preserved for future C2.1 author. |
| 32 | divergence | mutual | none | deterministic | Plan §146 names CLI as the surface to extend; impl extends CLI + REST + MCP at C1.3. §148-152 lists C1.4 as the "CLI/MCP/REST surfacing" tier — C1.3 partially overlaps with C1.4's planned scope. Plan does not forbid the overlap; both plan ambiguity and impl scope-choice contribute. |
| 33 | commit-message | informational | none | deterministic | Commit body lists "edited_text_file: 0; nested_memory: 0" — verified accurate against dry-run. Forensic accuracy preserved. |
| 34 | test-coverage | informational | static-only | deterministic | 4-subtype rebuild test exercises task_reminder as non-indexed control. Coverage of additional non-indexed subtypes (hook_success, plan_mode, todo_reminder, deferred_tools_delta, etc.) is by SQL inspection, not by additional test rows. |
| 35 | divergence | informational | none | deterministic | `fts_attachment_text_content` has no block_index column; mcp_instructions_delta with N added_blocks produces N FTS rows sharing attachment_uuid with no per-block index. Plan does not specify; consumer-side analyzers cannot disambiguate which block matched from FTS row alone. |
| 36 | divergence | informational | none | deterministic | All four arms guard with `a.body_json IS NOT NULL` despite migration 008 setting body_json NOT NULL. Defensive-against-schema-evolution rather than required for correctness. |
| 37 | meta-plan | mutual | none | deterministic | Scratch DB cloned from /tmp/claude-history.dryrun-c1.2.1.db (single-session corpus, ~1293 attachments). Narrower than `~/.claude/projects/`. The 2-of-4-subtype gap follows directly from corpus narrowness. Both plan-side (no corpus-scope spec) and impl-side (clone-from-c1.2.1 choice) contribute. |
| 38 | divergence | informational | runtime-only | deterministic | `search_attachment_text_content` LEFT JOIN attachments to retrieve timestamp; contentless FTS5 has no FK cascade — DELETE FROM attachments leaves orphan FTS rows until next rebuild. Defensive-against-orphan-FTS-rows pattern. |
| 39 | verification | informational | none | unverified | Commit body cites direct `fts_attachment_text_content MATCH` counts but does not exercise merged `search_messages_and_attachments` path against dry-run DB. Live-shape merging at scale (e.g., 9 message + 11 attachment → top-20 merged) unverified end-to-end. |
| 40 | commit-message | informational | none | deterministic | Verification stats: cargo build clean; cargo test 249 passed (3+61+185); pre-C1.3 baseline 239. Re-run confirmed (cross-references #27). |
| 41 | meta-plan | informational | none | deterministic | Scratch DB at /tmp/claude-history.dryrun-c1.3.db is 12.35 MB; not deleted before audit. /tmp auto-rotates on reboot. Operational-state observation. |
| 42 | meta-plan | plan-defect | none | deterministic | STATE.md line 162 records "C2.1 now takes 010" reflecting C1.3's migration-numbering side-effect at parent-agent ledger; plan source-of-truth (curious-napping-koala.md line 210) still reads "C1.1 takes 008, C2.1 takes 009". Parent-ledger / plan-file inconsistency. |
| 43 | meta-plan | impl-defect | static-only | deterministic | STATE.md line 162 records "subject 60 chars" — same mis-count as impl-report context (cross-references #10). Error propagated impl-report → STATE.md. Actual subject 69 chars verified via awk. |
| 44 | sequence | mutual | none | deterministic | C1.4's planned scope (§148-152) names "CLI / MCP / REST surfacing" for attachments. C1.3 already extends MCP and REST search-message tools. Whether scope-creep depends on interpretation: §148-152 implies search-MCP extension is C1.4's; plan does not forbid extending search-MCP at C1.3. Both plan ambiguity and impl scope-choice contribute. (Cross-references #32 — same observation framed against §146 surface scope; #44 framed against §148-152 sequencing.) |

---

## Meta-plan subsection

The audit catalog has 6 rows category-labeled `meta-plan` (#2, #9, #37, #41, #42, #43). The audit's "Plan-defect notes" subsection enumerates 7 plan-side findings spanning meta-plan and divergence rows.

- **#2 (Migration 009 consumed by C1.3)** — Plan §hard-rule 3 line 210 states "C1.1 takes 008, C2.1 takes 009"; C1.3 consumed 009. Plan §250 Tier C1 listing names only 008_attachments.sql. Plan source-of-truth stale. Headline finding (#1 in context). Already flagged at parent-agent level for plan-edit at C1.3-Review.

- **#9 (FTS-only commit classification gap in §safeguard 2)** — Plan §safeguard 2 lines 359-368 exempts parser-typing-only commits and lists ingestion-changing exemplars. C1.3 is neither (FTS5-indexing wiring). Plan ambiguity around classification. Impl chose to dry-run anyway. Mutual.

- **#37 (Scratch DB corpus-scope ambiguity)** — Plan does not specify dry-run corpus scope. Impl cloned from c1.2.1's single-session scratch (~1293 attachments) → narrower than `~/.claude/projects/`. The 2-of-4-subtype coverage gap follows from corpus narrowness. Mutual.

- **#41 (Scratch DB persistence)** — Operational-state observation. Scratch DB at /tmp not deleted before audit; /tmp auto-rotates on reboot. Informational.

- **#42 (STATE.md vs plan-file migration-numbering inconsistency)** — STATE.md line 162 records the side-effect ("C2.1 now takes 010"); plan file line 210 still reads "C1.1 takes 008, C2.1 takes 009". Parent-ledger/plan-file inconsistency. Plan-side.

- **#43 (Subject-char miscount propagation to STATE.md)** — STATE.md line 162 records "subject 60 chars"; actual is 69. Mirrors #10 impl-report miscount; the error propagated impl-report → STATE.md. Impl-defect at parent-ledger level.

---

## Notes

- **Headline findings:** rows #2, #1, #4, #10, #30 per the triage context.
  - #1 (CLI surface adopts default-on union without `--include-attachments` flag) → `impl-defect` / `runtime-only` / `deterministic`. Classification rationale: plan §296 reads `claude-history search "MCP" --include-attachments` as a literal verification-command invocation. The flag is typed as part of an executable command, not framed as "could be added via a flag like --include-attachments". The verification-command form reads as normative on the flag's existence. Impl chose default-on union with no flag across all three surfaces (CLI/REST/MCP). Plan-named flag absent. Adjacent reading: `mutual` if verification-command flag-naming is read as indicative example; that reading is not adopted because §296 commands appear in a "Live verify" block where they are intended to be runnable as-written.
  - #2 (Migration 009 consumed) → `plan-defect` / `none` / `deterministic`. Plan-side staleness already flagged at parent-agent level.
  - #4 (mcp_instructions_delta json_each flatten) → `plan-defect` / `none` / `deterministic`. Classification rationale: plan §144 says "joined as one document" (singular); impl's json_each produces N FTS docs per row. Both shapes work for FTS5 matching. Classified `plan-defect` because the plan wording is stale relative to the as-implemented row-shape; impl's choice is documented in commit body and migration 009 docstring, and falls within reasonable indexing-target interpretation. Adjacent reading `mutual` is also defensible (plan indicative, impl chose flatten); classification falls to `plan-defect` because the plan's literal "joined as one document" wording is contradicted by the json_each shape and the plan does not address row-shape-vs-content-target tradeoff.
  - #10 (Subject 69 chars vs impl-report's 60-char claim) → `impl-defect` / `static-only` / `deterministic`. Two-row pattern: #10 captures the impl-report misclaim; the actual subject's 72-char convention compliance is informational at the surface level (no audit row classifies the as-shipped 69-char subject as a defect because 69 ≤ 72; #10 isolates the misclaim). #43 captures STATE.md propagation of the same miscount.
  - #30 (Kickstart deferred — live verification gap on edited_text_file + nested_memory paths) → `informational` / `none` / `unverified`. Same pattern as prior commits' kickstart-pending verification gaps.

- **No `build-blocking` or `test-blocking` rows.** All 249 workspace tests pass per audit row #27.

- **Four `runtime-only` rows:** #1 (default-on union — observable at search invocation across CLI/REST/MCP), #17 (partial-rebuild atomicity — observable on rebuild_* failure mid-closure), #25 (cross-table BM25 ranking — observable in merged result ordering), #38 (orphan FTS rows between rebuilds — observable post-attachments-DELETE).

- **Seven `static-only` rows:** #3 (plan line-citation stale — observable by reading), #10 (subject-char miscount — observable in impl-report text), #15 (empty-extract test breadth — observable in test code), #16 (legacy-JSON deserialize coverage by-inspection — observable in test code), #24 (pagination test coverage by-inspection — observable in test code), #34 (non-indexed-subtype exclusion test breadth — observable in test code), #43 (STATE.md miscount — observable in ledger text).

- **Five `unverified` rows:** #7 (live-DB FTS verification deferred), #8 (subtype-coverage minimum end-to-end deferred), #14 (live-DB exercise of edited_text_file + nested_memory paths deferred), #30 (kickstart deferred — same pattern as prior commits), #39 (merged search-path against dry-run not exercised).

- **#1 vs #32 vs #44 reconciliation:** the audit lists three rows touching the surface-extension theme. #1 frames it as flag-named-vs-default-on (impl-defect because plan §296 names a flag). #32 frames it as §146-prescribed-CLI-only-vs-impl-CLI+REST+MCP (mutual — plan does not forbid REST/MCP at C1.3). #44 frames it as scope-encroachment-on-C1.4 (mutual — §148-152 sequencing implies search-MCP at C1.4 but does not forbid earlier). Three distinct framings recorded per audit row-shape.

- **#10 vs #43 reconciliation:** #10 captures the impl-report subject-char misclaim ("60 chars" vs actual 69). #43 captures STATE.md line 162 propagating the same miscount. Both rows record the propagation chain at distinct locations.

- **C1.3-Review is the next subagent in the DAG;** this triage feeds that review.
