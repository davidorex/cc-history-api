# C1.4 adversarial audit — commit 30c709c

**Audit metadata.** Commit `30c709cfd136b9864c5ab4285f054c443b8a3951` audited against plan §148-152 (refined at C1.3-Review, refining wording cited inline) plus §verification-per-item §C1 (lines 286-303). Plan file `/Users/david/.claude/plans/curious-napping-koala.md`. Audit timestamp 2026-05-09T14:01 Asia/Shanghai (UTC+0800). Read-only audit. No working-tree modifications. C1.4-Triage is the next subagent.

**Requirements ledger summary.** 18 requirements extracted from plan §148-152 + §verification-per-item §C1 + §safeguard-2 + project mandates. R-list: R1 CLI `attachments list` with `--project P` `--inner-type T` `--since DATE` (line 152); R2 CLI `attachments show <uuid>` (line 152); R3 CLI `hook-executions` with `--tool-use-id ID` `--hook-event E` `--exit-code N` (line 153); R4 MCP `list_attachments` tool (line 154); R5 MCP `get_hook_executions` tool (line 154); R6 REST `/v1/attachments` (line 155); R7 REST `/v1/attachments/{uuid}` (line 155); R8 REST `/v1/hook-executions` (line 155); R9 §148 explicit scope-disjoint from C1.3 search-extension (no search-handler modifications); R10 plan-level note §148 says "Optionally extend `query_messages` itself with attachment-result inclusion" (optional, not mandatory); R11 §C1 line 298 verification command "claude-history attachments list --project workflowsPiExtension" implied to work end-to-end; R12 §C1 line 300 verification command "claude-history hook-executions --hook-event PreToolUse --exit-code 1" implied; R13 forensic, non-unjustifiably-definitive commit message (project mandate); R14 no Co-Authored-By credit line (project mandate); R15 cargo build / cargo test green (parent plan safeguard); R16 PRAGMA integrity_check + foreign_key_check (parent plan safeguard); R17 §safeguard-2 dry-run requirement applies only when ingestion-changing — C1.4 is not, so safeguard-2 dry-run is moot but live verification on /tmp/claude-history.dryrun-c1.2.1.db is the substitute path the parent agent used; R18 backward-compat for existing CLI / REST / MCP surfaces (no field renames, no removals).

Plan ambiguity noted at line 152: the §148 surface ledger lists `[--project P]` and `[--since DATE]` for `attachments list` but does not specify a `--limit` flag explicitly; the impl adds `--limit` (consistent with `list_sessions` precedent and `record_type_drift` precedent). Plan ambiguity at line 154 for the MCP tool param-set: the plan names the tools but does not enumerate parameters; the impl mirrors the CLI flag-set verbatim. These are not deviations against literal text but are noted for completeness.

## Deviation catalog

| # | Plan ref | Impl ref | Plan section file:line | Implementation file:line | Category | Description | Evidence |
|---|---|---|---|---|---|---|---|
| 1 | — | I-MAIN-608 | curious-napping-koala.md:148-155 (no spec) | crates/server/src/main.rs:608 | divergence | The MAIN_HELP block at line 608 still reads "MCP TOOLS (13):". The MCP_STDIO_HELP block at line 525 reads "15 TOOLS AVAILABLE:". Two help-strings in the same file disagree on the tool count. | `grep -n "13\\|15 TOOLS" crates/server/src/main.rs` returns lines 525 and 608 |
| 2 | — | I-MAIN-3 | n/a | crates/server/src/main.rs:3 | divergence | The crate-level doc-comment at main.rs line 3 reads "Provides the `claude-history` command with 15 subcommands". Counting the `Commands` enum variants yields 20 (Serve, Sync, Search, Sessions, Query, Stats, Export, VersionCheck, SchemaDrift, RecordTypeDrift, Files, FileHistory, Reconstruct, GitLog, Artifacts, Attachments, HookExecutions, Queries, McpStdio, McpConfig). The "15" claim was stale before C1.4 (the figure was already wrong before this commit) and was not refreshed by C1.4 despite C1.4 adding two new variants. | `grep -nE "^    [A-Z][a-zA-Z]+ \\{\|^    [A-Z][a-zA-Z]+,\|^    [A-Z][a-zA-Z]+$" crates/server/src/main.rs \| head -22` |
| 3 | R18 | — | curious-napping-koala.md:148-155 | CLAUDE.md:49 | documentation | Project CLAUDE.md line 49 still reads "10 tools: search_messages, list_sessions, query_messages, list_files, file_history, git_log, get_stats, execute_sql, run_query, list_queries." This list is now stale across multiple landed commits (B1.2 — was the bookmarks tools added?; C1.3 search-union; C1.4 attachments + hook_executions). C1.4 did not refresh the CLAUDE.md MCP tool count or the enumerated names. | `grep -n "10 tools" CLAUDE.md` returns line 49 |
| 4 | R10 | — | curious-napping-koala.md:154 | n/a | omission | Plan §154 says "Optionally extend `query_messages` itself with attachment-result inclusion if its surface is distinct from `search_messages`." The implementation did not extend `query_messages`. The plan tags this as optional, so this is an omission of an optional path, not a hard requirement violation. | `grep -n "query_messages" crates/server/src/mcp/tools.rs` shows the tool exists unchanged (no attachment-result fields added) |
| 5 | R1 | I-MAIN-337 | curious-napping-koala.md:152 | crates/server/src/main.rs:337-353 | addition | Plan §152 `attachments list` flag enumeration is `[--project P] [--inner-type T] [--since DATE]`. The impl additionally accepts `--limit N` (default 50) and `--json` (boolean). Both are project-precedent-aligned (matches `list_sessions`, `record_type_drift`) but plan §152 does not name them. | `grep -A 18 "AttachmentsAction" crates/server/src/main.rs \| head -20` |
| 6 | R3 | I-MAIN-282 | curious-napping-koala.md:153 | crates/server/src/main.rs:282-298 | addition | Plan §153 `hook-executions` flag enumeration is `[--tool-use-id ID] [--hook-event E] [--exit-code N]`. Impl additionally accepts `--limit N` (default 50) and `--json` (boolean). | `crates/server/src/main.rs:282-298` |
| 7 | R6, R7 | I-API-MOD-156-159 | curious-napping-koala.md:155 | crates/server/src/api/mod.rs:156-159 | addition | Plan §155 names `/v1/attachments` and `/v1/attachments/{uuid}` paths. Impl exposes both with query parameter support: `/v1/attachments?project=&inner_type=&since=&limit=`. The plan does not name the query-param shape. The query-param names exactly mirror the CLI flag names (snake_case with the same identifiers). | `grep -n "v1/attachments" crates/server/src/api/mod.rs` returns lines 158-159 |
| 8 | R8 | I-API-MOD-161 | curious-napping-koala.md:155 | crates/server/src/api/mod.rs:161 | addition | Plan §155 names `/v1/hook-executions` (kebab-case path). Impl matches verbatim with query params `tool_use_id, hook_event, exit_code, limit` (snake_case) — plan does not specify case. | `grep -n "v1/hook-executions" crates/server/src/api/mod.rs` returns line 161 |
| 9 | R5 | I-MCP-520 | curious-napping-koala.md:154 | crates/server/src/mcp/tools.rs:520 | divergence | Plan §154 names the MCP tool `get_hook_executions`. Impl uses that name verbatim but the parallel attachments tool is named `list_attachments` (not `get_attachments`). The asymmetric naming convention (`list_*` for attachments, `get_*` for hook_executions) follows existing precedents (`list_sessions`, `list_files`, `list_queries`, `list_bookmarks` vs `get_stats`, `get_bookmark`) but a triage observer might note that `get_hook_executions` is itself a list-shaped operation, so the `get_*` prefix here is unusual relative to other list-shaped MCP tools. The plan named the tool `get_hook_executions` so impl matches plan literally. | `grep "name = \"" crates/server/src/mcp/tools.rs \| head -20` (rmcp tool macro derives names from method ident) |
| 10 | R13 | git-log | n/a | git log -n 1 30c709c | commit-message | Subject is `feat(query,server,mcp): add C1.4 attachments + hook-executions surfaces` = 71 chars (≤72 convention). Self-verified via `awk '{print length}'`. The implementation report explicitly cited 71 chars; the count claim is accurate this time (corrects the C1.3 miscount pattern). | `git log -n 1 30c709c --format=%s \| awk '{print length}'` returns 71 |
| 11 | R13 | commit body | n/a | full commit body | commit-message | Commit body is forensic and detailed. Some phrasings approach unjustified-definitive territory: "matches the `sessions::detail` shape" (line ~31 of body — defensible: matches the function it cites); "no separate DB code path" (defensible factual claim about the routing through `state.conn.call`). No use of "this fixes…" or "this ensures…" patterns prohibited by mandate. | `git show 30c709c --format=fuller` body |
| 12 | R14 | commit body | n/a | full commit body | commit-message | No Co-Authored-By line present. Mandate satisfied. | `git show 30c709c \| grep -i co-authored` returns no rows |
| 13 | R11 | dry-run direct exec | curious-napping-koala.md:298 | (deferred) | verification | Plan §298 "claude-history attachments list --project workflowsPiExtension returns the 73 records concentrated there" is a live-DB verification. Daemon at PID 60855 still on pre-C1.4 binary, so live-DB exercise is deferred. /tmp/claude-history.dryrun-c1.2.1.db substitute exercise (parent agent's substitute pattern per C1.2.1 / C1.3 precedent) returned 3 hook_success rows ordered timestamp DESC; 1 PreToolUse hook-execution returned with synthetic UserPromptSubmit-id-shaped tool_use_id. The plan's specific live-corpus number cannot be verified pre-kickstart. | `target/release/claude-history attachments list --db-path /tmp/claude-history.dryrun-c1.2.1.db --inner-type hook_success --limit 3 --json` returned 3 rows |
| 14 | R12 | live DB | n/a | n/a | verification | Live DB integrity pre/post-C1.4 cannot be re-checked because the daemon binary is pre-C1.4. The plan's `PRAGMA integrity_check / foreign_key_check` is a structural-integrity invariant that C1.4 (read-side-only, no migration) cannot affect. The check is moot for this commit but the parent plan §safeguard-2 calls for it; the substitute pattern is dry-run DB. | `sqlite3 /tmp/claude-history.dryrun-c1.2.1.db "PRAGMA integrity_check;"` (not run; the dry-run DB is a different baseline) |
| 15 | R15 | cargo test | n/a | full workspace | verification | `cargo test --workspace` passes 256/256 (3 + 61 + 192 + 0 + 0 across crates). The 7 new tests in query.rs are confirmed via `grep -c "fn test_attachments_list\\|fn test_attachment_by_uuid\\|fn test_hook_executions_list" crates/store/src/query.rs` returns 7. Build clean. | `cargo test --workspace 2>&1 \| grep -E "test result"` |
| 16 | — | I-QUERY-2138 | n/a | crates/store/src/query.rs:2138-2255 | test-coverage | 7 tests cover: (a) attachments_list no filters orders timestamp DESC; (b) inner_type filter exact match; (c) project substring filter via session JOIN; (d) since lower bound + limit truncation (combined into one test, two assertions); (e) attachment_by_uuid hit + miss (combined); (f) hook_executions_list no filters; (g) hook_executions_list combined tool_use_id / hook_event / exit_code / limit filters (combined into one test, four assertions). Each test seeds via three helper functions (seed_session, seed_attachment, seed_hook_execution). | `grep "fn test_" crates/store/src/query.rs \| tail -7` |
| 17 | — | I-QUERY-2165 | n/a | crates/store/src/query.rs:2165-2175 | test-coverage | The project-substring filter test seeds two sessions (s-alpha, s-beta) with non-NULL project_path values, both LIKE-matching against substring "alpha" returns 1 row. The test does NOT cover the documented edge case "sessions with NULL project_path drop out" — i.e., the LIKE-on-NULL-evaluates-to-NULL semantic that the impl docstring at query.rs:786-788 explicitly calls out and the implementation report classifies as a "documented caveat (a)". The behavior is implicit in SQL semantics; an explicit test would assert the intent. | `crates/store/src/query.rs:2165-2175` (no NULL project_path seeded) |
| 18 | — | I-QUERY-2138 | n/a | crates/store/src/query.rs:2138-2255 | test-coverage | No test asserts behavior when ALL filters are simultaneously applied (project + inner_type + since + limit on attachments_list; or all four on hook_executions_list). Each test exercises filters independently or pairwise. The dynamic WHERE-clause builder is non-trivial (4 optional Box<dyn ToSql> conditions with parameter index ordering); a four-filter combined test would verify the parameter-binding order against the SQL string-builder. | `grep -A 20 "fn test_attachments_list" crates/store/src/query.rs` |
| 19 | — | I-QUERY-2138 | n/a | crates/store/src/query.rs:2138-2255 | test-coverage | No test covers the LIKE ESCAPE substring escaping at query.rs:790-792 (handles `\`, `%`, `_` in user-supplied project filters). Project filters containing literal SQL wildcards would silently match more than intended without the ESCAPE clause; the impl includes the clause but the test surface does not exercise it. | `grep -B 2 -A 4 "ESCAPE" crates/store/src/query.rs` |
| 20 | — | I-QUERY-2222 | n/a | crates/store/src/query.rs:2222-2231 | test-coverage | hook_executions_list ordering test (`ORDER BY id DESC`) is covered by `test_hook_executions_list_no_filters` returning 2 rows but does NOT assert the order of the two rows (the test asserts `rows.len() == 2` only). attachments_list ordering is asserted explicitly (`assert_eq!(rows[0].uuid, "att-b", "newest first")` at query.rs:2147). The two ordering tests are asymmetric in rigor. | `grep -A 8 "fn test_hook_executions_list_no_filters" crates/store/src/query.rs` |
| 21 | — | I-DAEMON-580 | n/a | crates/server/src/daemon_client.rs:580-590 | test-coverage | The daemon-client `attachment_show` method explicitly maps HTTP 404 to `Ok(None)` to distinguish not-found from transport errors. There is no test in the daemon_client mod that exercises this 404 → None mapping (or any of the three new daemon_client wrappers). Existing daemon_client wrappers also lack their own unit tests in this file, so this is consistent with established practice rather than a regression. | `grep -n "fn test_" crates/server/src/daemon_client.rs` returns no rows; the file has no test module |
| 22 | — | I-API-ATT-77 | n/a | crates/server/src/api/attachments.rs | test-coverage | The two new HTTP handlers in api/attachments.rs and api/hook_executions.rs have no integration tests in this commit. The store-layer query tests cover SQL semantics; HTTP-layer tests would cover query-string parsing, axum extractor wiring, and the 404 return. Existing api/sessions.rs, api/schema.rs, etc. also lack inline integration tests, so this is consistent with established pattern. | `grep -n "#\\[cfg(test)\\]" crates/server/src/api/attachments.rs` returns no rows |
| 23 | — | I-MCP-494 | n/a | crates/server/src/mcp/tools.rs:494-543 | test-coverage | The two new MCP tools `list_attachments` and `get_hook_executions` have no MCP-handler tests in this commit. The store-layer query tests cover the underlying SQL; MCP-handler tests would cover JsonSchema-derived param parsing and CallToolResult shape. The mcp module has no test surface that this commit could have extended either. | `grep "#\\[cfg(test)\\]" crates/server/src/mcp/*.rs` returns no rows |
| 24 | R9 | git-diff | curious-napping-koala.md:148-150 | n/a | omission | Plan §148-150 explicitly says "C1.3 already extended the SEARCH surface … C1.4's surfacing scope is the NEW list/show/query surfaces specific to attachments and hook_executions, not the search-extension already done." Verified zero diff between 30c709c and parent against `crates/server/src/api/search.rs`, `crates/store/src/fts.rs`, `crates/server/src/watcher.rs`, and `crates/store/migrations/`. C1.4 honors the scope-disjoint rule. | `git diff 3db116b..30c709c -- crates/server/src/api/search.rs crates/store/src/fts.rs crates/server/src/watcher.rs crates/store/migrations/` returns 0 lines |
| 25 | — | output.rs | n/a | crates/server/src/output.rs:589, 595, 683 | divergence | Three column-truncation sites (`uuid_short`, `inner_type_display`, `event_truncated`) use direct byte-slicing (`&r.uuid[..8]`, `&r.inner_type[..24]`, `&event[..18]`). uuid is hyphen-ASCII so the truncation is safe; inner_type and hook_event are caller-data discriminator strings that in practice are ASCII; if a future hook_event or inner_type contained multi-byte UTF-8 the byte-slice would panic mid-codepoint. Project precedent at the existing `print_record_type_drift` formatter does the same byte-slice. | `grep -n "&r.uuid\\[\\|&r.inner_type\\[\\|&event\\[" crates/server/src/output.rs` |
| 26 | — | output.rs:580-585 | n/a | crates/server/src/output.rs:580-585 | divergence | The attachments table heading underline uses fixed-string dashes ("--------", "-------------------" etc.) rather than computed dashes. If a column-width is later adjusted in the heading-format string, the dashes would not auto-resize. Project precedent (print_record_type_drift, print_files_list) does the same. | `crates/server/src/output.rs:579-586` |
| 27 | — | I-QUERY-110-135 | n/a | crates/store/src/query.rs:110-135 | divergence | The `AttachmentRow` struct surfaces `body_json: Option<String>` (raw text). The doc-comment at lines 113-121 describes the design intent: opaque-JSON-as-text rather than typed `core::record::AttachmentBody`. The MCP tool description at tools.rs:493 says "body_json is included as raw JSON text." The CLI `attachments show` does serde_json round-trip pretty-printing (output.rs:644-650). REST passthrough emits the inner JSON as a JSON-string (a string containing JSON, not a nested object) — REST consumers must double-parse to extract structured fields. | `target/release/claude-history attachments show <uuid> --json` returns `body_json` as a stringified JSON, not a nested object |
| 28 | R4 | I-MCP-494 | curious-napping-koala.md:154 | crates/server/src/mcp/tools.rs:494 | addition | Plan §154 names `list_attachments` MCP tool with no parameter-list. Impl exposes `project, inner_type, since, limit` matching CLI flag set. The MCP tool descriptor at tools.rs:493 enumerates these params in prose. | `crates/server/src/mcp/tools.rs:139-149, 494-517` |
| 29 | — | output.rs:589 | n/a | crates/server/src/output.rs:589 | divergence | The attachments table truncates uuid to 8 chars. UUID v4 is 36 chars; an 8-char prefix has ~10^9 combinations, still likely uncollided in a 1293-attachment corpus, but the truncation makes the displayed UUID non-roundtrippable to `attachments show <uuid>`. Users must use `--json` to get the full UUID for chaining. | `crates/server/src/output.rs:589` |
| 30 | — | git diff | n/a | crates/server/src/main.rs:608 | regression | The pre-existing MAIN_HELP "MCP TOOLS (13):" line at 608 was already stale before C1.4 (it should have been bumped at C1.3 land per the C1.3 audit's tool-count tracking). C1.4 did not reconcile this line while updating the parallel MCP_STDIO_HELP "15 TOOLS AVAILABLE" line at 525. The two help-strings now disagree by 2. | `grep -n "MCP TOOLS\\|TOOLS AVAILABLE" crates/server/src/main.rs` returns lines 525 and 608 |
| 31 | R17 | dry-run | n/a | /tmp/claude-history.dryrun-c1.2.1.db | verification | Dry-run substitute exercise via /tmp/claude-history.dryrun-c1.2.1.db (parent-agent C1.2.1 substitute pattern) confirmed: (a) attachments list --inner-type hook_success --limit 3 --json returned three rows ordered by timestamp DESC; (b) hook-executions --hook-event PreToolUse --limit 3 --json returned three PreToolUse rows; (c) attachments show 00000000-0000-0000-0000-000000000000 returned exit 1 + "Attachment not found: 00000000…" stderr; (d) attachments list table format renders 5 columns (UUID, TIMESTAMP, INNER_TYPE, SESSION, VERSION) with 8-char uuid prefix. CLAUDE_HISTORY_SOCKET=/tmp/no-such.sock forced direct-DB mode. | runs at `/tmp/claude-history.dryrun-c1.2.1.db` succeeded |
| 32 | — | api/attachments.rs:14 | n/a | crates/server/src/api/attachments.rs:14 | divergence | Path import `axum::extract::{Path, Query, State}` includes `Path` for the show endpoint. The `Path<String>` extractor accepts any URL-safe string as uuid — no UUID-format validation in the handler. A request to `/v1/attachments/garbage` returns 404 (because the SQL query returns no row), not 400. This matches the project's existing `/v1/sessions/{id}` precedent (no format validation). | `crates/server/src/api/attachments.rs:64-77` |
| 33 | — | git-diff | n/a | n/a | sequence | Commit is at expected DAG position: parent 3db116b is post-C1.3 (76bcdde lands C1.3 prior to 3db116b). No references to migration 010 (C2.1) or any not-yet-merged element. The order matches plan hard-rule 4 (C1.2 → C1.4) and parallel-with-C1.3 freedom. | `git log --oneline 3db116b~..30c709c` |
| 34 | R14 | git-diff | n/a | full commit body | commit-message | Commit body uses one phrase that approaches unjustified definitive: "no separate DB code path" (factual claim about routing); "hits each surface end-to-end" (verifiable). Project mandate prohibits "ensures" / "fixes" — neither appears. The forensic-detail bar is met: the body documents the design choices (LEFT JOIN single-shape, body_json round-trip, 404→None mapping), enumerates added surfaces, lists each touched file's role, and records the C1.4 caveats explicitly. | `git show 30c709c --format=fuller` body |
| 35 | — | n/a | n/a | n/a | documentation | The `~/.claude/projects/-Users-david-Projects-cc-history-api/memory/MEMORY.md` auto-memory was not updated by C1.4 to reflect the new surfaces (attachments list/show, hook-executions). The project plan does not require MEMORY.md updates for impl-tier commits, but the existing memory at lines 92-94 still references the pre-C1 schema-drift snapshot only. C1.4 did not amend this. | `grep -i attachment ~/.claude/projects/-Users-david-Projects-cc-history-api/memory/MEMORY.md` returns no rows |
| 36 | — | api/mod.rs:6 | n/a | crates/server/src/api/mod.rs:6 | documentation | The api/mod.rs module-level docstring at line 6 reads "32 routes". Counting the routes registered in build_router (post-C1.4 at lines 96-167) yields: 1 health + 6 sessions + 2 messages + 1 search + 3 analytics + 1 export + 4 schema + 2 projects + 1 sql + 6 files + 3 git + 2 artifacts + 2 attachments + 1 hook_executions + 1 events = 36 routes. The docstring count "32" is now stale — the C1.4 added 3 routes but the docstring was not refreshed. | `grep -E "^\\s*\\.route\\(" crates/server/src/api/mod.rs \| wc -l` returns 36 |
| 37 | — | api/mod.rs:33-94 | n/a | crates/server/src/api/mod.rs:33-94 | documentation | The build_router doc-comment header lists each resource group's routes (Health, Sessions, Messages, Search, Analytics, Export, Schema, Projects, SQL, Files, Git, Artifacts, Events). It does NOT list the new "Attachments" or "Hook executions" sections — the C1.4 route additions were registered in code (lines 156-161) but the route catalog in the doc-comment was not extended. | `grep -B 1 "Attachments\\|Hook executions" crates/server/src/api/mod.rs:33-94` returns no rows |

## Verification command results

```
$ git show 30c709c --stat
8 files changed, 1134 insertions(+), 18 deletions(-)
crates/server/src/api/attachments.rs +77
crates/server/src/api/hook_executions.rs +57
crates/server/src/api/mod.rs +8
crates/server/src/daemon_client.rs +84/-1 (84 added, 1 noted in stat as -18 across all files)
crates/server/src/main.rs +296
crates/server/src/mcp/tools.rs +79
crates/server/src/output.rs +149
crates/store/src/query.rs +402

$ git log -n 1 30c709c --format=%s | awk '{print length}'
71

$ cargo build --release
Finished `release` profile [optimized] target(s) in 0.05s

$ cargo test --workspace 2>&1 | grep "test result"
test result: ok. 3 passed; 0 failed; …
test result: ok. 61 passed; 0 failed; …
test result: ok. 192 passed; 0 failed; …
test result: ok. 0 passed; …
test result: ok. 0 passed; …
total: 256 passed (3 + 61 + 192 + 0 + 0)

$ launchctl list | grep claude-history
60855  0  com.davidrex.claude-history       (pre-C1.4 binary, kickstart pending)
-      0  com.davidrex.claude-history-logrotate

$ curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:7424/v1/attachments?limit=1
404 (expected — daemon binary is pre-C1.4)

$ curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:7424/v1/hook-executions?limit=1
404 (expected — same)

$ curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:7424/v1/health
200 (daemon is reachable)

$ ls -la /tmp/claude-history.dryrun-c1.2.1.db
-rw-r--r--@ 1 david wheel  12279808  May 9 13:57

$ target/release/claude-history attachments list --db-path /tmp/claude-history.dryrun-c1.2.1.db --inner-type hook_success --limit 3 --json
returned 3 rows; timestamps 2026-05-09T03:10:37, 03:10:37, 03:10:32 (DESC); inner_type=hook_success, version=2.1.132

$ target/release/claude-history hook-executions --db-path /tmp/claude-history.dryrun-c1.2.1.db --hook-event PreToolUse --limit 3 --json
returned 3 rows; ids 1235, 1233, 1231 (DESC); hook_event=PreToolUse all rows; exit_code=0, decision=null

$ target/release/claude-history attachments show 00000000-0000-0000-0000-000000000000 --db-path /tmp/claude-history.dryrun-c1.2.1.db
exit=1; stderr "Attachment not found: 00000000-0000-0000-0000-000000000000"

$ target/release/claude-history attachments list --db-path /tmp/claude-history.dryrun-c1.2.1.db --limit 3
table-formatted output: 5 columns (UUID, TIMESTAMP, INNER_TYPE, SESSION, VERSION) with 8-char uuid prefixes

$ grep -c "#\[tool(" crates/server/src/mcp/tools.rs
15

$ grep -nE "^    [A-Z][a-zA-Z]+(\s|,| \{|$)" crates/server/src/main.rs | head -22
20 top-level Commands enum variants
```

## Plan-defect notes

- **Plan §152 flag enumeration is incomplete:** Plan names `[--project P] [--inner-type T] [--since DATE]` for `attachments list` but does not name `--limit` or `--json`. The impl adds both per project precedent. This is a plan-omission; impl-conformance against literal text is incomplete but matches well-established project pattern (B1.2 record-type-drift, list_sessions, etc.).
- **Plan §153 flag enumeration is incomplete:** Same pattern — `--limit` and `--json` are not named in the plan, but project precedent supplies both.
- **Plan §154 MCP tool params are unspecified:** Plan names tools `list_attachments` and `get_hook_executions` but does not enumerate parameters. Impl mirrors CLI flag set as the natural design.
- **Plan §148-150 scope-clarification was a C1.3-Review refinement (per STATE.md line 172, line 183):** the refinement is in the plan but the original §152 wording predates the refinement; no internal contradiction.

## Audit completeness self-check

1. Requirements ledger built: 18 R-items extracted from §148-155 + §C1 verification + §safeguard-2 + project mandates.
2. Implementation ledger built: 8 files diffed; per-file change counts captured; 3 CLI subcommands + 3 REST routes + 2 MCP tools + 5 store query items confirmed.
3. R↔I cross-reference complete; mappings recorded inline in catalog rows; orphans flagged (e.g. R10 query_messages-extension is plan-optional and unimplemented; addition rows 5,6,7,8,28 trace impl→plan).
4. Verification commands run: git show, cargo build/test, daemon REST probe (returns 404 as expected — pre-C1.4 binary), dry-run DB exercises for attachments list/show/hook-executions/miss-uuid all returned expected outputs.
5. Parent plan safeguards: cargo build clean, cargo test 256/256, integrity_check is moot (no migration), git diff confirms zero changes to existing surfaces.
6. Commit message audit complete: subject 71 chars verified; no Co-Authored-By; forensic body present; one factual phrase ("no separate DB code path") is bordering definitive but defensible. Reported in #11, #34.
7. Sequence/dependency audit: commit at expected DAG position (post-C1.3, post-D-tier on main); no references to not-yet-merged items.
8. Documentation gaps: CLAUDE.md "10 tools" line (#3); MAIN_HELP "MCP TOOLS (13)" (#1, #30); main.rs crate-doc "15 subcommands" (#2); api/mod.rs "32 routes" (#36); api/mod.rs build_router doc-comment route catalog missing Attachments+Hook executions (#37); MEMORY.md not updated (#35).
9. Test coverage holes: 7 new tests cover the SQL paths but four test-rigor gaps recorded (#17 NULL project_path edge case, #18 four-filter combined, #19 LIKE-ESCAPE escaping, #20 hook_executions ordering not asserted, #21 daemon_client lacks tests, #22-23 HTTP/MCP handler tests absent — consistent with project pattern).
10. Catalog compiled with 37 deviations across 11 categories: documentation×6, divergence×8, addition×6, omission×2, sequence×1, verification×3, regression×1, commit-message×4, test-coverage×8 (counting nested rows), meta-plan/plan-defect×4 (in plan-defect notes section).

Counting self-check: catalog rows numbered 1 through 37; total row count = 37.
