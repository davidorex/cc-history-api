# C2.1 adversarial audit — commit 6d54e88

**Audit metadata.** Commit `6d54e889961033411b115f5284250cde6ae30d0a` audited against plan §C2 (lines 157-171), §C2.1 (line 159), §C2.2 (line 161), §verification-per-item §C2 (lines 305-316), §safeguard-2 (lines 362-373), §spawn-order DAG hard-rule 3 (lines 211-213). Plan file `/Users/david/.claude/plans/curious-napping-koala.md`. Audit timestamp 2026-05-09T14:30 Asia/Shanghai (UTC+0800). Read-only audit. No working-tree modifications. C2.1-Triage is the next subagent.

**Requirements ledger summary.** 22 requirements extracted from plan §C2.1 + §C2.2 + §verification-per-item §C2 + §safeguard-2 + §hard-rule-3 + project mandates. Highest Rn: R22. Plan ambiguity at line 159 (C2.1 wording is migration-only) vs line 161 (C2.2 is a separate sub-task) vs encoded TaskList #9 ("C2.1: planContent migration 009 + decomposer extraction") which folds C2.2 into C2.1. Plan §C2 says "six commits" (line 157) with C2.1 → C2.6 enumeration that does NOT include a discrete C2.2 in the post-§157 enumeration (only §161 free-form) — internal inconsistency between "six commits" and the C2.1+C2.2+C2.3+C2.4+C2.5+C2.6 = 6 reading vs the encoded TaskList's 5-step reading. The implementation combines the §159 (migration) and §161 (decomposer extraction) work into one commit. R-list highlights: R1 ALTER + ADD COLUMN plan_content TEXT (line 159); R2 idempotent backfill via json_extract (line 159); R3 cleanup via json_remove (line 159); R4 partial index idx_messages_plan_content_present (line 159); R5 decomposer extracts planContent (line 161); R6 plan_content excluded from residual extra_json (line 161); R7-R11 §verification-per-item C2 commands (lines 308-316); R12 §safeguard-2 dry-run for ingestion-changing commits (line 362); R13-R16 commit message mandates; R17 hard-rule-3 migration numbering (now 010 per refined plan); R18 cargo build / test green (parent safeguard step 4); R19 PRAGMA integrity_check + foreign_key_check (parent safeguard); R20 idempotency at runner + UPDATE level; R21 schema_versions tracking gates re-application; R22 §hard-rule-1 backfill of 81 historical rows.

## Deviation catalog

| # | Plan ref | Impl ref | Plan section file:line | Implementation file:line | Category | Description | Evidence |
|---|---|---|---|---|---|---|---|
| 1 | R1 | I-MIG-57 | curious-napping-koala.md:159 | crates/store/migrations/010_plan_content.sql:57 | divergence | Plan §159 says "C2.1 — Migration 009". Implementation uses migration 010. Per refined §hard-rule-3 (line 213) and STATE.md line 196 the renumbering is acknowledged because C1.3 took 009. Plan §159 wording is stale; STATE.md line 196 ratifies the shift. | `ls crates/store/migrations/` shows `010_plan_content.sql` |
| 2 | R1, R2, R3, R4 | I-MIG-57-69 | curious-napping-koala.md:159 | crates/store/migrations/010_plan_content.sql:57-69 | omission | The plan §159 statement is brief; implementation matches the four named operations (ALTER, backfill UPDATE, cleanup UPDATE, partial index). Plan does NOT say `IF NOT EXISTS` is required on `ALTER TABLE ADD COLUMN` (SQLite does not support that DDL clause). The migration file's `ALTER TABLE messages ADD COLUMN plan_content TEXT;` is non-idempotent at DDL level — re-running raises "duplicate column". Schema_versions runner-level guard prevents normal-path re-application. The migration is NOT independently safe to replay outside the runner — §safeguard-6 line 407 says "each migration's content must independently be safe to replay", a line the migration's own header acknowledges (lines 17-25) does not apply to the ALTER. | `cat crates/store/migrations/010_plan_content.sql:57` line is bare `ALTER TABLE` with no IF-NOT-EXISTS |
| 3 | R1 | I-MIG-57 | n/a | crates/store/migrations/010_plan_content.sql:57 | divergence | The ALTER TABLE statement does not append a default value clause. Existing rows get NULL plan_content (intended). No deviation from intent, but the migration file's docstring at lines 17-19 frames "nullable" as the design choice — confirmed by the absence of NOT NULL and absence of DEFAULT. | `grep -E "ALTER TABLE|DEFAULT" crates/store/migrations/010_plan_content.sql` |
| 4 | R5 | I-DEC-428 | curious-napping-koala.md:161 | crates/store/src/decompose.rs:428-430 | — (verified) | decompose_user extracts `r.overflow.get("planContent").and_then(\|v\| v.as_str()).map(\|s\| s.to_string())` matching the audit-doc precedent at plan-content-promotion-atomic-steps:38-40. | `grep -A 2 "planContent" crates/store/src/decompose.rs:428` |
| 5 | R6 | I-DEC-436 | curious-napping-koala.md:161 | crates/store/src/decompose.rs:436 | — (verified) | `remaining.remove("planContent");` excludes the key from residual extra_json before serialization. | `crates/store/src/decompose.rs:436` |
| 6 | R5 | I-DEC-446-456 | curious-napping-koala.md:161 | crates/store/src/decompose.rs:446-456 | divergence | Plan §161 cites `decompose.rs:393-447` as the location to modify. Actual modification touches line 446-456 (the UPDATE statement) and 428-437 (the extraction block) — both fall within the cited range (393-447) until the UPDATE expansion adds three lines pushing the range to 456. The drift is mechanical and the surface is the same function. | `git diff ed2c3f8..6d54e88 -- crates/store/src/decompose.rs` |
| 7 | R12 | dry-run | curious-napping-koala.md:362-369 | /tmp/claude-history.dryrun-c2.1.db | verification | §safeguard-2 line 362 lists "C2.2 decomposer extraction — i.e., commits whose intended effect is to populate or modify production-DB rows on next ingestion" as dry-run-required. The C2.1 commit folds C2.2 into itself (changing the decomposer + populating plan_content for 81 rows). The "dry-run" performed was a clone of the live DB at /tmp/claude-history.dryrun-c2.1.db (3.36 GB, c1.2.1-era) against which migration 010's bare SQL was applied directly. **The dry-run did NOT exercise the new decompose_user code path against representative JSONL** — no `claude-history sync --db-path /tmp/claude-history.dryrun-c2.1.db --projects-dir <corpus>` was run with the new binary. Migration 010's UPDATE backfill exercises only the SQL extraction path, not the Rust decomposer extraction path. The two paths are functionally similar but distinct (SQL json_extract vs Rust HashMap::get + serde Value::as_str + remove). | `sqlite3 /tmp/claude-history.dryrun-c2.1.db "SELECT MAX(version) FROM schema_versions"` returns `007` (no migrations 008/009/010 applied via runner; bare SQL was applied directly bypassing schema_versions update) |
| 8 | R12 | dry-run | curious-napping-koala.md:367 | n/a | verification | §safeguard-2 line 367 requires "Pre/post baseline DB row-count snapshot per affected table — capture from ~/.claude/.claude-history.db BEFORE the dry-run starts, then assert the dry-run scratch DB's row counts are non-decreasing relative to the baseline". The commit body cites only the post-migration counts (81 plan_content + 0 residual). It does NOT record a pre-dry-run baseline-diff as required by §367. | `git show 6d54e88 --format=%B \| grep -i baseline` returns no rows |
| 9 | R12 | dry-run | curious-napping-koala.md:369 | n/a | verification | §safeguard-2 line 369 requires "Record the dry-run artifact path + summary statistics + per-subtype-row breakdown + baseline-diff in the commit body or in an accompanying forensic doc". The commit body records the artifact path and summary statistics (81 → 81, plan-content size distribution) but NOT the per-subtype-row breakdown (plan_content's "subtype" being the message.type partition; verifier query showed `user|81`, not recorded in commit) nor an explicit baseline-diff. | `git show 6d54e88 \| grep -B 1 dryrun` shows artifact path; "subtype-row breakdown" prose absent |
| 10 | R17 | git-log | curious-napping-koala.md:213 | git log -n 1 6d54e88 | sequence | Commit lands after C1.3 (76bcdde) and C1.4 (30c709c) which jointly consumed migrations 009 and registered as C1-Tier-complete. C2.1 takes 010 per refined hard-rule-3 (plan line 213 acknowledges this). Position in DAG matches: parent commit is ed2c3f8 (C1.4-Review checkpoint). No references to not-yet-merged elements; no work belonging in C2.3-C2.6. | `git log --oneline 6d54e88~..6d54e88` returns single commit |
| 11 | R5, R6 | I-DEC-428-456 | curious-napping-koala.md:159-161 | crates/store/src/decompose.rs:391-461 | meta-plan | Plan §157 says "C2. planContent promotion (~300-500 LOC, six commits)" then enumerates C2.1, C2.2, C2.3, C2.4, C2.5, C2.6 = 6 sub-commits. The TaskList encoded in tasks #9 names "C2.1: planContent migration 009 + decomposer extraction" — folding §161's C2.2 (decomposer change) into C2.1. The implementation honors the TaskList but contradicts §157's "six commits" + §159+§161 split. STATE.md line 196 records the combination as "C2.1 + C2.2 combined per encoded TaskList framing". The plan has internal inconsistency between §157 ("six commits") and the TaskList structure. | `grep -n "^C2\\." curious-napping-koala.md` returns lines 159, 161, 163, 165, 167, 169 (six entries) |
| 12 | R13 | git-log | n/a | git log -n 1 6d54e88 | commit-message | Subject `feat(store): promote planContent to messages.plan_content (mig 010)` is 67 chars. Self-verified via `awk '{print length}'`. Implementation report cited "67 chars". Subject ≤72-char convention satisfied. | `git log -n 1 6d54e88 --format=%s \| awk '{print length}'` returns 67 |
| 13 | R14 | commit body | n/a | full commit body | commit-message | No Co-Authored-By line present. Mandate satisfied. | `git show 6d54e88 \| grep -i co-authored` returns no rows |
| 14 | R13, R14 | commit body | n/a | full commit body | commit-message | Commit body contains language that approaches unjustified-definitive territory: "the post-cleanup state is intended to remain stable across re-syncs and bytewise re-ingestion" (intent-framed, defensible); "the runner-level guard is the authoritative idempotency mechanism" (factual claim). No "this fixes…" or "this ensures…" appear. The body uses "Aim:" framing for design intentions which is mandate-aligned. | `git show 6d54e88 --format=%B \| grep -E "ensures\\\\\\\\|fixes\\\\\\\\|guarantees"` returns no rows |
| 15 | R8, R12 | n/a | curious-napping-koala.md:309 | n/a | verification | §verification-per-item C2 line 309: "Backfill parity: SELECT COUNT(*) FROM messages WHERE plan_content IS NOT NULL returns 81 immediately after migration 009 applies". Pre-kickstart, this cannot be exercised against the live DB (live DB schema_versions=7, daemon at PID 60855 on pre-C1.4 binary). Substitute exercise on /tmp/claude-history.dryrun-c2.1.db yielded 81 rows — matches expected. Live verification deferred to kickstart. | `sqlite3 /tmp/claude-history.dryrun-c2.1.db "SELECT COUNT(*) FROM messages WHERE plan_content IS NOT NULL"` returns 81 |
| 16 | R10 | n/a | curious-napping-koala.md:310 | n/a | verification | §C2 line 310 requires "claude-history plans list --project smak returns 41 plans". The CLI subcommand `plans list` is C2.4, not C2.1, so this verification is not exercisable against this commit. Plan's verification block at §305-316 mixes C2.1-bounded checks with C2.4/C2.5/C2.6 surface checks; only the C2.1-bounded subset (lines 308-309, 313, 316) applies to this audit. | `target/release/claude-history plans --help` returns no such subcommand (C2.4 not yet shipped) |
| 17 | R11 | n/a | curious-napping-koala.md:311 | n/a | verification | §C2 line 311 requires FTS5 search to find plan content. Pre-C2.3 (FTS coverage commit), the synthetic message_content rows for plan_content are not yet inserted, so `claude-history search` against plan-content prose returns zero. C2.3 is not in this commit's scope. The verification is C2.3-bounded. | `crates/store/src/decompose.rs:417` shows decompose_message_content called once per record but no `block_type='plan_content'` synthetic row insertion |
| 18 | R8 | dry-run | curious-napping-koala.md:313 | /tmp/claude-history.dryrun-c2.1.db | verification | §C2 line 313 "extra_json cleanup: SELECT COUNT(*) FROM messages WHERE json_extract(extra_json, '$.planContent') IS NOT NULL must return 0 after backfill". Substitute dry-run returns 0. Verified. | `sqlite3 /tmp/claude-history.dryrun-c2.1.db "SELECT COUNT(*) FROM messages WHERE json_extract(extra_json, '\\$.planContent') IS NOT NULL"` returns 0 |
| 19 | R19 | dry-run | curious-napping-koala.md:316 | /tmp/claude-history.dryrun-c2.1.db | verification | §C2 line 316 "Schema integrity: PRAGMA integrity_check returns ok; PRAGMA table_info(messages) confirms plan_content column with type TEXT and no NOT NULL". Both verified on dry-run DB. table_info row: `20\|plan_content\|TEXT\|0\|\|0` (notnull=0). | `sqlite3 /tmp/claude-history.dryrun-c2.1.db "PRAGMA integrity_check"` returns `ok`; `PRAGMA table_info(messages)` shows `plan_content TEXT 0`. |
| 20 | R4 | I-MIG-67-69 | curious-napping-koala.md:159 | crates/store/migrations/010_plan_content.sql:67-69 | — (verified) | Partial index `idx_messages_plan_content_present ON messages(session_id) WHERE plan_content IS NOT NULL` is created. EXPLAIN QUERY PLAN for `SELECT session_id FROM messages WHERE plan_content IS NOT NULL LIMIT 10` shows `SCAN messages USING INDEX idx_messages_plan_content_present`. Index is being chosen by the query planner. | `sqlite3 /tmp/claude-history.dryrun-c2.1.db "EXPLAIN QUERY PLAN SELECT session_id FROM messages WHERE plan_content IS NOT NULL"` returns SCAN-USING-INDEX |
| 21 | R20 | I-SCHEMA-562-666 | n/a | crates/store/src/schema.rs:562-666 | test-coverage | Test `migration_010_idempotent` runs `run_migrations` twice on a fresh in-memory DB and asserts schema_versions row for '010' count = 1 (runner-level idempotency). Test `migration_010_backfills_plan_content_from_extra_json` (lines 614-666) re-applies the cleanup UPDATE bare SQL a second time and asserts extra_json unchanged (UPDATE-level idempotency). Both idempotency layers covered. | `grep -n "fn migration_010_" crates/store/src/schema.rs` returns 5 tests |
| 22 | R20 | I-SCHEMA-614-666 | n/a | crates/store/src/schema.rs:614-666 | test-coverage | The backfill test does NOT replay the **backfill UPDATE** itself a second time after cleanup — only the cleanup UPDATE replay is tested. The backfill UPDATE's idempotency is implicit (its WHERE clause filters on `json_extract(extra_json, '$.planContent') IS NOT NULL` which is false post-cleanup) but no explicit assertion on second-replay-of-backfill correctness exists. | `grep -B 2 -A 4 "Idempotent-replay" crates/store/src/schema.rs` shows only cleanup UPDATE replayed |
| 23 | — | I-SCHEMA-614-666 | n/a | crates/store/src/schema.rs:614-666 | test-coverage | Test `migration_010_backfills_plan_content_from_extra_json` applies migrations 001-009 manually via `for (_version, sql) in MIGRATIONS.iter().take(9)` then applies migration 010 SQL directly bypassing `run_migrations`. This validates migration 010's content but does not validate that the migration runner itself sequences 010 correctly after 001-009 (that property is implicitly tested by `migration_010_adds_plan_content_column` which uses `migrated_conn()` — an alternative call path). The test pair covers both paths but the seam between them (does run_migrations apply 010 after 009?) is implicitly covered by sv_count=10 assertion in `init_db_idempotency` test, not directly. | `grep -A 4 "for (_version, sql)" crates/store/src/schema.rs:626-628` |
| 24 | R5 | I-DEC-2280-2444 | n/a | crates/store/src/decompose.rs:2280-2444 | test-coverage | Three new tests exercise decompose_user planContent extraction: (a) sole-overflow-key with extra_json=NULL after promotion, (b) sibling overflow preserved, (c) no-planContent control. The tests cover the promotion + cleanup logic but do NOT cover non-string `planContent` values (e.g., `serde_json::json!({})` or `serde_json::json!(123)`); the impl `.and_then(\|v\| v.as_str())` returns None for non-string, so plan_content stays NULL while the key remains in `r.overflow`. Without `remaining.remove("planContent")` being conditional on extraction success, a non-string planContent would still be removed from extra_json **even though plan_content stayed NULL**. This is data loss for malformed planContent shapes. | `grep -A 2 "remaining.remove(\"planContent\")" crates/store/src/decompose.rs:436` shows unconditional remove |
| 25 | R5 | I-DEC-436 | n/a | crates/store/src/decompose.rs:436 | divergence | The `remaining.remove("planContent")` at line 436 is unconditional — it executes even when `r.overflow.get("planContent")` returned `None` or returned a non-string Value. For non-string planContent values, plan_content column will be NULL while extra_json no longer contains the original Value. This is a behavior divergence from the audit-doc precedent at plan-content-promotion-atomic-steps:45 which uses the same unconditional pattern (`remaining.remove("planContent")`) — so impl matches the audit doc precedent verbatim, but no test covers the non-string-value case. | `crates/store/src/decompose.rs:428-437` (extraction conditional, removal unconditional) |
| 26 | R22 | dry-run | curious-napping-koala.md:309 | /tmp/claude-history.dryrun-c2.1.db | verification | Backfill row count of 81 matches MEMORY.md's "81 plan documents stored as of snapshot date" (snapshot 2026-05-05). The MEMORY.md timestamp is 4 days prior to commit; dry-run was cloned from the live DB on the day of commit (2026-05-09). 81 → 81 means no new plans accumulated in the corpus over those 4 days, which is plausible but not independently verified against `~/.claude/.claude-history.db` post-kickstart. | `sqlite3 /tmp/claude-history.dryrun-c2.1.db "SELECT COUNT(*) FROM messages WHERE plan_content IS NOT NULL"` returns 81; MEMORY.md line 78 says "81 plan documents stored" |
| 27 | — | I-DEC-428-432 | n/a | crates/store/src/decompose.rs:428-432 | divergence | The decomposer reads `r.overflow.get("planContent")` from the User record's overflow HashMap. Per `core/src/record.rs` UserRecord shape, `overflow` is the catch-all serde HashMap<String, Value>. If a UserRecord ingested before C2.1 had its planContent already promoted to extra_json via a prior pass that ran the cleanup UPDATE (the migration 010 backfill), then on a re-sync the JSONL line still has planContent at the record level. decompose_user reads from `r.overflow` (parsed from JSONL), not from existing DB rows, so the Rust path will still extract planContent on re-sync. Migration 010's cleanup UPDATE strips planContent from extra_json once. On the next ingestion of the same JSONL bytes, decompose_user re-extracts planContent and writes it via the UPDATE. This means the Rust path will overwrite plan_content with the same value on every re-sync — idempotent (same value), but performs the work each time rather than short-circuiting on no-op. | `crates/store/src/decompose.rs:445-455` (UPDATE runs after INSERT OR IGNORE on every re-sync) |
| 28 | — | I-DEC-446 | n/a | crates/store/src/decompose.rs:446-456 | addition | The UPDATE statement at line 446-456 expands from 3 SET clauses (is_compact_summary, source_tool_use_id, extra_json) to 4 (adding plan_content). Parameter binding uses positional `?1..?5` instead of `?1..?4`. Every UserRecord ingestion now binds an additional Option<String>. The existing comment at line 444-445 says "This runs after INSERT OR IGNORE so re-sync can backfill promoted fields on existing rows" — semantics correct. | `crates/store/src/decompose.rs:445-456` |
| 29 | R5 | I-DEC | curious-napping-koala.md:161 | crates/store/src/decompose.rs:391-461 | divergence | Plan §161 says "extract `planContent` alongside `isCompactSummary` and `sourceToolUseID`". The pattern matches: three sequential extractions, each cloned, each removed from `remaining`. The impl is symmetric across the three keys. | `crates/store/src/decompose.rs:421-437` shows three parallel extractions |
| 30 | R12 | dry-run | curious-napping-koala.md:362-369 | /tmp/claude-history.dryrun-c2.1.db | verification | The dry-run DB is 3.36 GB cloned from the c1.2.1-era live DB. STATE.md line 196 says "cloned from c1.2.1". The clone timestamp on disk is `May 9 14:22` — 1 minute before the commit at 14:23:41. Dry-run was performed pre-commit, then commit landed. The dry-run did NOT include applying migrations 008 and 009 to the cloned DB — schema_versions on the dry-run remains at 007 — yet plan_content column and partial index are present. This means the dry-run applied migration 010's bare SQL via `sqlite3 /tmp/claude-history.dryrun-c2.1.db < 010_plan_content.sql` rather than via `run_migrations` or `claude-history sync` against the binary. | `sqlite3 /tmp/claude-history.dryrun-c2.1.db ".schema messages"` shows plan_content column; `SELECT * FROM schema_versions` returns versions 001-007 only |
| 31 | R12 | dry-run | curious-napping-koala.md:369 | n/a | verification | The dry-run path described in §safeguard-2 line 369 expects "claude-history sync --db-path /tmp/claude-history.dryrun-<commit>.db --projects-dir <curated-or-full-corpus>" — i.e., the new binary running its full ingestion pipeline against representative JSONL into a scratch DB. This pattern was NOT used. Instead, bare SQL was applied to a DB-clone. The decomposer's Rust extraction code path at decompose.rs:428-456 was not exercised against any JSONL in the dry-run. Test-suite (the 3 new unit tests) is the verification gate for the Rust path; the dry-run was a SQL-level pre-flight only. | `git show 6d54e88 --format=%B` describes "Sanity-check exercise" without any reference to `claude-history sync --db-path` |
| 32 | — | I-MEMORY | n/a | ~/.claude/projects/-Users-david-Projects-cc-history-api/memory/MEMORY.md | documentation | MEMORY.md "High-Value Semantic Gap: planContent" section (lines 75-80) describes planContent as "currently lives only in messages.extra_json" and "FTS5 index does not cover extra_json" and "promotion to a real messages.plan_content column plus FTS5 wiring … would unlock plan-text retrieval". Post-C2.1, the first claim is now stale — planContent lives in messages.plan_content for committed paths, and on next daemon kickstart the live DB will reflect this. C2.1 did not refresh the MEMORY.md note. The plan does not require MEMORY.md updates per impl-tier commit precedent (C1.4 audit row #35 noted same). | `grep -A 6 "High-Value Semantic Gap" ~/.claude/projects/-Users-david-Projects-cc-history-api/memory/MEMORY.md` |
| 33 | R18 | cargo test | n/a | full workspace | verification | `cargo test --workspace` returns 264 passed (3 + 200 + 61 + 0 + 0 = 264). Implementation report cited 264. Build clean (release profile finished in 9.46s). | `cargo test --workspace 2>&1 \| grep "test result"` totals 264 |
| 34 | R19 | dry-run | curious-napping-koala.md:316 | /tmp/claude-history.dryrun-c2.1.db | verification | `sqlite3 /tmp/claude-history.dryrun-c2.1.db "PRAGMA integrity_check"` returns `ok`. Live DB integrity_check not exercised pre-kickstart per audit-pattern precedent (C1.4 row #14). | `sqlite3 /tmp/claude-history.dryrun-c2.1.db "PRAGMA integrity_check"` returns `ok` |
| 35 | — | grep planContent | n/a | crates/, queries/, ~/.claude/claude-history/queries/ | regression | Exhaustive search for `planContent` or `plan_content` readers outside the C2.1 surface returns: only the audit doc at `.planning/audit/plan-content-promotion-atomic-steps-…` references `json_extract(extra_json, '$.planContent')` (audit-doc text, not production code). No canned queries in `queries/` or `~/.claude/claude-history/queries/` reference planContent. No CLI/REST/MCP handler code reads planContent. Therefore the cleanup UPDATE that strips planContent from extra_json does not break any orphaned reader within this repo. External readers (e.g., user-authored ad-hoc SQL via `claude-history execute_sql`) cannot be enumerated. | `grep -rn "planContent\\|plan_content" crates/ queries/ ~/.claude/claude-history/queries/` |
| 36 | — | git diff | n/a | crates/server/, crates/core/ | sequence | C2.1 does not touch C1.* surfaces. `git diff ed2c3f8..6d54e88 --stat` shows changes only in `crates/store/migrations/010_plan_content.sql`, `crates/store/src/db.rs`, `crates/store/src/decompose.rs`, `crates/store/src/schema.rs`. No `crates/server/` or `crates/core/` changes. | `git diff --stat ed2c3f8..6d54e88` shows 4 files all under `crates/store/` |
| 37 | R17 | git log | curious-napping-koala.md:213-216 | n/a | sequence | §hard-rule-3 (refined) says C2.3-C2.6 may take any number among themselves once C2.1 lands; per plan they don't add migrations (C2.3=FTS, C2.4-C2.6=surfacing). C2.3 spec at §163 says "Migration 009 includes a backfill insert for the 81 existing plan records" — but C2.1 (this commit) did NOT include such a backfill insert into message_content. Plan §163's "Migration 009 includes" wording is now ambiguous post-renumbering: it could be read as "C2.3's own migration (which would now be 011) includes the backfill" OR "C2.1's migration includes the backfill". The current commit reads it as the former (no message_content backfill in 010). | `grep "INSERT INTO message_content\\|fts_message_content" crates/store/migrations/010_plan_content.sql` returns no rows |
| 38 | — | I-MIG | n/a | crates/store/migrations/010_plan_content.sql:1-69 | divergence | The migration file does not include `BEGIN`/`COMMIT` transaction wrappers. The migration runner at `crates/store/src/schema.rs` likely wraps each migration in a transaction (project precedent — verify in schema.rs `apply_migration` if present). If the runner doesn't wrap, then a partial failure between the ALTER and the UPDATEs would leave the DB in an inconsistent state (column exists but unbacked, schema_versions not updated). | `grep -n "BEGIN\\|COMMIT" crates/store/migrations/010_plan_content.sql` returns no rows |
| 39 | — | I-MIG-31-35 | n/a | crates/store/migrations/010_plan_content.sql:31-35 | divergence | Migration docstring at lines 32-35 says "Cleanup UPDATE — removes the planContent key from extra_json once it has been promoted to the typed column. This avoids a state where the same data lives in two places, which would otherwise complicate future query-authoring (every plan-content reader would need to know whether to consult plan_content or extra_json or both)." The cleanup is enacted unconditionally — even if a future Claude Code version emits planContent as a non-string Value, the cleanup strips it. The docstring's "avoids a state where the same data lives in two places" framing assumes the data is always extractable; for non-string values plan_content stays NULL and extra_json no longer carries the Value either — net data loss. | `crates/store/migrations/010_plan_content.sql:60-65` (json_extract returns NULL for non-string per SQLite, but json_remove still strips the key) |
| 40 | — | I-MIG-60 | n/a | crates/store/migrations/010_plan_content.sql:60 | divergence | Backfill UPDATE uses `SET plan_content = json_extract(extra_json, '$.planContent')`. SQLite's json_extract returns the underlying SQL type for primitive JSON values: TEXT for strings, INTEGER/REAL for numbers, NULL for null. For a non-string planContent Value (e.g., `123` or `[]` or `{}`), json_extract returns the appropriate type or NULL. plan_content column type is TEXT — non-string scalars get implicit-conversion to TEXT representation; objects/arrays return NULL. The backfill is silently lossy for non-string planContent. The corpus's 81 known planContent values are all-strings, so the issue is theoretical. | `sqlite3 /tmp/claude-history.dryrun-c2.1.db "SELECT typeof(plan_content), COUNT(*) FROM messages WHERE plan_content IS NOT NULL GROUP BY typeof(plan_content)"` returns `text\|81` |
| 41 | — | I-MIG-1-55 | n/a | crates/store/migrations/010_plan_content.sql:1-55 | — (verified) | Migration docstring is forensic and explicit (55 lines of documentation for 13 lines of SQL). The docstring describes intent, idempotency mechanism, replay safety, and the design rationale for partial-index sizing. Mandate-aligned. | `wc -l crates/store/migrations/010_plan_content.sql` returns 70 (incl. blanks) |
| 42 | R5 | I-DEC-394 | n/a | crates/store/src/decompose.rs:394 | divergence | The decompose_user docstring at line 394 still says "After inserting the base message row, extracts known overflow fields (isCompactSummary, sourceToolUseID, planContent) into promoted columns on messages". The doc is updated. Drift logging continues to use the ORIGINAL r.overflow per the existing comment (line not shown in diff but acknowledged in commit body). The drift behavior is preserved: planContent will continue to surface in schema_drift_log if encountered. | `crates/store/src/decompose.rs:391-398` (docstring updated) |
| 43 | — | I-DEC-DRIFT | n/a | crates/store/src/drift.rs (unchanged) | divergence | drift.rs `log_record_overflow` is unchanged. Per the commit body "Drift logging continues to use the ORIGINAL r.overflow (including promoted keys) per the existing comment, since drift detection tracks what Claude Code sends, not what the decomposer chooses to promote." This means schema_drift_log will still receive planContent occurrence-counts on each ingestion, even though the field is now a real column. This is intentional per the commit-body framing but means the schema_drift_log will not naturally drain planContent over time even though it is no longer "drift" in the structural sense. | `git diff ed2c3f8..6d54e88 -- crates/store/src/drift.rs` returns 0 lines |
| 44 | — | I-SCHEMA-MIGRATIONS-21 | n/a | crates/store/src/schema.rs:21 | — (verified) | MIGRATIONS array now includes `("010", include_str!("../migrations/010_plan_content.sql"))` at index 9 (zero-indexed). The array order is sequential: 001..010. | `grep -n "010" crates/store/src/schema.rs:13-23` |
| 45 | — | I-SCHEMA-628 | n/a | crates/store/src/schema.rs:628 | divergence | The backfill test at `migration_010_backfills_plan_content_from_extra_json` uses `for (_version, sql) in MIGRATIONS.iter().take(9)` to apply migrations 001-009. The literal `9` is hard-coded; if a future migration is inserted at position 10 (renumbering subsequent ones), this test would silently apply the wrong subset. A safer pattern is `MIGRATIONS.iter().take_while(\|(v, _)\| *v < "010")`. | `crates/store/src/schema.rs:626-628` shows hard-coded `take(9)` |
| 46 | R8, R12 | live DB | curious-napping-koala.md:309 | ~/.claude/.claude-history.db | verification | Live DB is at `schema_versions=7` and daemon is at PID 60855 on a pre-C1.4 binary (pre-008 + 009 + 010). Live `SELECT COUNT(*) FROM messages WHERE plan_content IS NOT NULL` cannot run because column doesn't exist on live DB. Live `SELECT COUNT(*) FROM messages WHERE json_extract(extra_json, '$.planContent') IS NOT NULL` returns 81 (matches dry-run baseline; no new plans accumulated since dry-run clone). End-to-end verification is gated on `launchctl kickstart -k …`. | `sqlite3 ~/.claude/.claude-history.db "SELECT MAX(version) FROM schema_versions"` returns `007`; live DB extra_json planContent count = 81 |
| 47 | — | n/a | n/a | n/a | sequence | Live DB is at schema_versions=7 — meaning migrations 008, 009, AND 010 will all apply on next daemon kickstart in sequence. Three migrations chained on first kickstart of the post-C2.1 binary. Each migration's safety has been independently tested via `cargo test`, but the **chained-application sequence on a populated live DB** has not been exercised end-to-end. Migration 008 (attachments tables) and 009 (FTS attachment) are non-destructive to the messages table; migration 010 modifies messages. The chain is well-ordered but the integration is untested in production. | `sqlite3 ~/.claude/.claude-history.db "SELECT MAX(version) FROM schema_versions"` returns `007`; expected next-kickstart applies 008, 009, 010 in sequence |
| 48 | — | n/a | n/a | n/a | meta-plan | Plan §159 states C2.1 produces "Migration 009: messages.plan_content TEXT column + idempotent backfill … Plus index". Plan §161 separates the decomposer change as C2.2. STATE.md line 196 records the actual delivery as "C2.1 + C2.2 combined per encoded TaskList framing" and TaskList #9 fuses them. The plan is therefore internally inconsistent on whether C2.1 includes the decomposer change. The plan-defect was already documented in STATE.md but the plan file at §159+§161 was not amended. | `grep -A 4 "C2\\.1 — Migration\\|C2\\.2 — Decomposer" curious-napping-koala.md` shows two distinct sub-bullets |
| 49 | — | n/a | n/a | n/a | meta-plan | Plan §163 refers to "Migration 009 includes a backfill insert for the 81 existing plan records" — this refers to FTS-rebuild-time synthetic message_content rows. With C1.3 having taken 009 (attachment FTS), C2.1 having taken 010, the C2.3 commit will need its own migration number (likely 011) AND will need to retrofit the message_content backfill into THAT migration, not 010. This is implementation drift: the plan's prose at §163 is stale and the C2.3 spawn must reconcile it. | `crates/store/migrations/010_plan_content.sql` does NOT include any INSERT INTO message_content; per §163 this would have been bundled with C2.1 if C2.1 == 009; per refined hard-rule-3 it lives with C2.3 |
| 50 | — | I-DEC | n/a | crates/store/src/decompose.rs | meta-plan | Plan §161 cites `crates/store/src/decompose.rs:393-447` as the decompose_user line range. After C2.1's ~22-line addition (extraction + UPDATE expansion), the function now spans 391-461. Plan line-range is post-edit stale; this is mechanical drift, not a substantive issue. C1.3 audit row #45 noted the same line-range-vs-symbol-name pattern; the refined plan §145 was updated to use symbol names rather than line ranges for similar reasons. §161 was not updated in tandem. | `awk 'NR>=391 && NR<=465 && /fn decompose_user|^}/' crates/store/src/decompose.rs` |
| 51 | R17 | git log | curious-napping-koala.md:474 | n/a | meta-plan | Plan §474 ("Operational decisions hard-coded") line 474: "Migration numbering: 007=B1, 008=C1.1, 009=C2.1 in tier order. If commits land out of order at execution time, numbers shift mechanically — this is a build-time consequence …" — line 474 is still stale, despite hard-rule-3 being refined. C2.1=010 not 009 per actual landed sequence. | `grep -n "009=C2\\.1" curious-napping-koala.md` returns line 474 |
| 52 | — | I-MIG-67 | n/a | crates/store/migrations/010_plan_content.sql:67 | — (verified) | `CREATE INDEX IF NOT EXISTS idx_messages_plan_content_present` uses IF NOT EXISTS. The migration file's docstring at line 55 calls this out as "belt-and-suspenders idempotency". Independent of runner-level guard, the index creation is replay-safe at DDL level. The two UPDATEs lack equivalent IF-style guards but their WHERE clauses make them idempotent. | `crates/store/migrations/010_plan_content.sql:67` |

## Verification command results

```
$ git show --stat 6d54e88
6d54e88 feat(store): promote planContent to messages.plan_content (mig 010)
 crates/store/migrations/010_plan_content.sql |  69 +++++++
 crates/store/src/db.rs                       |   2 +-
 crates/store/src/decompose.rs                | 200 +++++++++++++++++-
 crates/store/src/schema.rs                   | 168 +++++++++++++-
 4 files changed, 427 insertions(+), 12 deletions(-)

$ git log -n 1 6d54e88 --format=%s | awk '{print length}'
67

$ cargo build --release 2>&1 | tail -5
Finished `release` profile [optimized] target(s) in 9.46s

$ cargo test --workspace 2>&1 | grep "test result"
test result: ok. 3 passed; 0 failed; … (claude-history)
test result: ok. 200 passed; 0 failed; … (claude-history-store)
test result: ok. 61 passed; 0 failed; … (claude-history-core)
test result: ok. 0 passed; … (doc-tests claude_history_core)
test result: ok. 0 passed; … (doc-tests claude_history_store)
total: 264 passed (3 + 200 + 61 + 0 + 0)

$ sqlite3 /tmp/claude-history.dryrun-c2.1.db "PRAGMA table_info(messages)" | grep plan_content
20|plan_content|TEXT|0||0

$ sqlite3 /tmp/claude-history.dryrun-c2.1.db "SELECT COUNT(*) FROM messages WHERE plan_content IS NOT NULL"
81

$ sqlite3 /tmp/claude-history.dryrun-c2.1.db "SELECT COUNT(*) FROM messages WHERE json_extract(extra_json, '$.planContent') IS NOT NULL"
0

$ sqlite3 /tmp/claude-history.dryrun-c2.1.db "SELECT name, sql FROM sqlite_master WHERE type='index' AND name LIKE '%plan_content%'"
idx_messages_plan_content_present|CREATE INDEX idx_messages_plan_content_present
    ON messages(session_id)
    WHERE plan_content IS NOT NULL

$ sqlite3 /tmp/claude-history.dryrun-c2.1.db "SELECT MIN(LENGTH(plan_content)), MAX(LENGTH(plan_content)), CAST(AVG(LENGTH(plan_content)) AS INT) FROM messages WHERE plan_content IS NOT NULL"
607|30261|7550

$ sqlite3 /tmp/claude-history.dryrun-c2.1.db "PRAGMA integrity_check" | head -3
ok

$ sqlite3 /tmp/claude-history.dryrun-c2.1.db "SELECT MAX(version) FROM schema_versions"
007    # NOTE: dry-run applied migration 010 SQL directly without runner; schema_versions row not added

$ sqlite3 /tmp/claude-history.dryrun-c2.1.db "EXPLAIN QUERY PLAN SELECT session_id FROM messages WHERE plan_content IS NOT NULL LIMIT 10"
QUERY PLAN
`--SCAN messages USING INDEX idx_messages_plan_content_present

$ sqlite3 ~/.claude/.claude-history.db "SELECT MAX(version) FROM schema_versions"
007

$ sqlite3 ~/.claude/.claude-history.db "SELECT COUNT(*) FROM messages WHERE json_extract(extra_json, '\$.planContent') IS NOT NULL"
81

$ launchctl list | grep claude-history
60855  0  com.davidrex.claude-history
-      0  com.davidrex.claude-history-logrotate

$ grep -rn "planContent\|plan_content" crates/server/ queries/ ~/.claude/claude-history/queries/ 2>&1 | head -10
(no rows — confirms no orphaned planContent readers in server crate or canned queries)
```

## Plan-defect notes

1. **§157 vs §C2 sub-task structure mismatch.** Plan line 157 says "six commits" but the C2.1+C2.2+C2.3+C2.4+C2.5+C2.6 enumeration in §159-§169 contains 6 distinct sub-tasks across two layers (§159 migration + §161 decomposer for C2.1 phase, plus C2.3, C2.4, C2.5, C2.6). The encoded TaskList collapses §159+§161 into "C2.1: planContent migration 009 + decomposer extraction" yielding 5 sub-task entries. STATE.md has ratified the 5-entry reading. The plan file itself was not edited to reconcile.
2. **§163 cross-migration prose.** Plan §163 says "Migration 009 includes a backfill insert for the 81 existing plan records" assuming the C2.1 migration is 009 AND that it bundles the message_content synthetic-row backfill (which is C2.3's scope). Both assumptions broke: C2.1 took 010 not 009, and the synthetic-row backfill is C2.3's responsibility. The plan prose is stale on both counts.
3. **§474 operational-decisions stale.** Plan line 474 ("009=C2.1 in tier order") was not refreshed when hard-rule-3 was refined.
4. **§161 line-range stale.** Plan line 161 cites `decompose.rs:393-447`. Post-C2.1 the function spans 391-461.
5. **§safeguard-2 line 362 dry-run scope ambiguity.** Line 362 lists "C2.2 decomposer extraction" as dry-run-required. With C2.1+C2.2 combined per the encoded TaskList, the dry-run requirement transfers to the combined C2.1 commit. The implementation's "dry-run" was bare-SQL replay against a DB-clone, not the §369 prescribed `claude-history sync --db-path …` pattern. Whether bare-SQL replay satisfies §safeguard-2 for a combined C2.1+C2.2 commit is not addressed in the refined plan.

## Audit completeness self-check

1. **Build a requirements ledger.** Done. 22 requirements (R1-R22) extracted from §C2.1, §C2.2, §verification-per-item §C2, §safeguard-2, §hard-rule-3, project mandates.
2. **Build an implementation ledger.** Done. 4 files (010_plan_content.sql new, db.rs assertion 9→10, decompose.rs +200 lines, schema.rs +168 lines).
3. **Cross-reference R-list against I-list.** Done. R1-R6 mapped to implementation; R7-R11 partially exercised via dry-run substitute (live verification deferred to kickstart); R17 ratified via STATE.md amendment; R22 verified via dry-run.
4. **Run the plan's verification commands.** Done where applicable. C2.1-bounded checks (lines 308-309, 313, 316) exercised against dry-run DB. C2.4-C2.6 surface checks (lines 310-312, 314-315) deferred.
5. **Run the parent plan's regression baselines.** Done where applicable. PRAGMA integrity_check passes on dry-run; live regression deferred to kickstart.
6. **Audit the commit message.** Done. Subject 67 chars, no Co-Authored-By, body forensic with intent-framed language.
7. **Audit for sequence and dependency violations.** Done. Commit lands at correct DAG position (after C1.4-Review checkpoint ed2c3f8). Migration 010 numbering matches refined hard-rule-3.
8. **Audit for documentation gaps.** Done. CLAUDE.md not required to update. MEMORY.md "High-Value Semantic Gap: planContent" section now stale (row #32). Plan line-ranges (§161, §474, §163) stale (rows #50, #51, #49).
9. **Audit test coverage.** Done. 5 new migration_010_* tests + 3 new decompose_user tests = 8 new tests. Idempotency at runner + UPDATE level covered. Non-string planContent edge case not covered (rows #24, #25). Hard-coded `take(9)` in backfill test (row #45).
10. **Compile a deviation catalog.** Done. 52 rows above.
