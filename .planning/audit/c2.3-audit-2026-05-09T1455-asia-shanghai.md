# C2.3 adversarial audit — commit 7c6048b

**Audit metadata.** Commit `7c6048b1742a33bcc32507d0c86f36d8280f6af3` audited against plan §C2 (line 157), §C2.3 (line 163), §verification-per-item §C2 (lines 305-316; especially line 311 FTS5 search), §safeguard-2 (lines 351-373), §spawn-order DAG hard-rule 5 (line 215), §hard-rule-3 (line 213). Plan file `/Users/david/.claude/plans/curious-napping-koala.md`. Audit timestamp 2026-05-09T14:55 Asia/Shanghai (UTC+0800). Read-only audit. No working-tree modifications. C2.3-Triage is the next subagent.

**Requirements ledger summary.** 24 requirements extracted from plan §C2.3 + §verification-per-item §C2 (lines 311-313, 316) + §hard-rule-5 + §hard-rule-3 + §safeguard-2 + project mandates. Highest Rn: R24. Plan §163 prose at line 163 still contains stale wording inherited from pre-renumbering ("Migration 009 includes a backfill insert for the 81 existing plan records" — both `009` and `81` are now stale: 011 actual; live count may have drifted since the corpus survey). The plan was refined per STATE.md line 196 noting `future C2.3 takes 011` but §163 itself was not refreshed in tandem. R-list highlights: R1 next-available migration number (now 011 per refined §hard-rule-3 footnote in STATE.md); R2 backfill insert with synthetic message_content rows for the 81 existing plan records (line 163); R3 block_index = -1 sentinel (line 163); R4 block_type = 'plan_content' (line 163); R5 text_content = plan_content (line 163); R6 existing FTS5 index automatically picks them up via standard rebuild path (line 163); R7 §C2 line 311 FTS5 search returns plan content matches; R8 §safeguard-2 dry-run for ingestion-changing commits (line 362); R9-R12 commit message mandates; R13 §hard-rule-5 C2.3 may commit any order vs C2.4-C2.6 once C2.1 lands; R14 cargo build / test green; R15 PRAGMA integrity_check; R16-R18 schema_versions tracking + idempotency at runner + UPDATE level; R19 corpus survey count 81 (line 163, "live count may have grown"); R20 §C2 line 313 extra_json cleanup invariant; R21 decompose-time parity (implied by FTS-currency on live ingestion — derived from §163 "automatically picks them up" + §305-316 live-ingestion verification); R22 schema integrity (line 316); R23 idempotent re-decompose; R24 idempotent migration replay.

## Deviation catalog

| # | Plan ref | Impl ref | Plan section file:line | Implementation file:line | Category | Description | Evidence |
|---|---|---|---|---|---|---|---|
| 1 | R1 | I-MIG-FILE | curious-napping-koala.md:163 | crates/store/migrations/011_plan_content_fts.sql | divergence | Plan §163 says "C2.3 takes its own migration number (likely 011 — verify next-available)". Implementation uses 011. STATE.md line 196 ratifies the assignment. Plan §163's parenthetical literal `Migration 009 includes a backfill insert` is stale; the actual landed numbering is 011. | `ls crates/store/migrations/011_*.sql` returns `011_plan_content_fts.sql` |
| 2 | R2, R3, R4, R5 | I-MIG-70-73 | curious-napping-koala.md:163 | crates/store/migrations/011_plan_content_fts.sql:70-73 | — (verified) | Backfill SQL: `INSERT OR IGNORE INTO message_content (message_uuid, block_index, block_type, text_content) SELECT uuid, -1, 'plan_content', plan_content FROM messages WHERE plan_content IS NOT NULL`. Sentinel triple matches plan §163 exactly. | `cat crates/store/migrations/011_plan_content_fts.sql:70-73` |
| 3 | R6 | I-MIG-only | curious-napping-koala.md:163 | crates/store/migrations/011_plan_content_fts.sql | — (verified) | Migration does NOT add an FTS5 table or alter `fts_message_content`. Existing external-content FTS5 table at migration 002 (`content='message_content', content_rowid='id'`) externalizes message_content as a whole; rebuild path INSERT INTO fts_message_content('rebuild') re-derives from message_content rowid, picking up synthetic rows automatically. | `grep "fts_message_content\|CREATE\\|VIRTUAL" crates/store/migrations/011_plan_content_fts.sql` returns no rows; migration 002 confirms `content='message_content'` |
| 4 | R19 | dry-run | curious-napping-koala.md:163 | /tmp/claude-history.dryrun-c2.3.db | — (verified) | Backfill row count of 81 matches plan §163 "corpus survey count was 81" and matches the source `messages` row count of 81. 1:1 parity between source plan_content rows and synthetic message_content rows verified via JOIN. | `sqlite3 .../dryrun-c2.3.db "SELECT COUNT(*) FROM message_content WHERE block_type='plan_content'"` returns 81; `... WHERE plan_content IS NOT NULL` returns 81 |
| 5 | R3, R4 | I-DEC-477-485 | curious-napping-koala.md:163 | crates/store/src/decompose.rs:466-486 | addition | Plan §163 prose mentions only the migration backfill ("the existing FTS5 index ... automatically picks them up via the standard rebuild path"). It does NOT explicitly require a decompose-time INSERT for live ingestion. Implementation adds `decompose_user` step 4b that mirrors the migration shape on every newly-decomposed plan-bearing user record. Per the verification block at §305-316 line 312 ("on plan-mode submission … within ~2 s, the user record's plan_content column is populated and the corresponding synthetic message_content row exists"), the live-ingestion behavior is plan-required, but plan §163 does not name decompose_user explicitly as the locus. Defensible-looking alternative; flagged per audit constraint. | `crates/store/src/decompose.rs:466-486` |
| 6 | R20 | dry-run | curious-napping-koala.md:313 | /tmp/claude-history.dryrun-c2.3.db | verification | §C2 line 313 "extra_json cleanup: SELECT COUNT(*) FROM messages WHERE json_extract(extra_json, '$.planContent') IS NOT NULL must return 0 after backfill" is a C2.1 invariant, not C2.3-introduced. C2.3 does not change extra_json shape; cleanup state is preserved. Verified on dry-run DB. | `sqlite3 .../dryrun-c2.3.db "SELECT COUNT(*) FROM messages WHERE json_extract(extra_json, '\$.planContent') IS NOT NULL"` returns 0 |
| 7 | R7 | dry-run | curious-napping-koala.md:311 | /tmp/claude-history.dryrun-c2.3.db | — (verified) | §C2 line 311 "claude-history search '<phrase>' finds the plan content. Pre-C2 this query returns zero; post-C2 it returns the plan(s)." After `INSERT INTO fts_message_content('rebuild')`, MATCH on `"JSON-First Artifact Invariant"` returns 193 total hits, of which 2 are in `block_type='plan_content'` (was 0 pre-rebuild). Other matches: `text|75`, `tool_result|116`. | `sqlite3 .../dryrun-c2.3.db "SELECT mc.block_type, COUNT(*) FROM fts_message_content f JOIN message_content mc ON mc.id=f.rowid WHERE f.fts_message_content MATCH '\"JSON-First Artifact Invariant\"' GROUP BY mc.block_type"` |
| 8 | R7 | dry-run | curious-napping-koala.md:311 | n/a | regression | Pre-C2.3, FTS MATCH on `"JSON-First Artifact Invariant"` returned 191 hits (75 text + 116 tool_result, with no plan_content variants). Post-C2.3 with rebuild, same query returns 193 hits — i.e. existing search totals INCREASE by exactly the new plan_content matches. Existing CLI/MCP/REST search-result consumers that paginate or aggregate on per-query totals will see different totals on identical FTS queries after the next FTS rebuild. The new `block_type` value 'plan_content' is one previous consumers have never observed. | Pre-rebuild MATCH = 191 (`SELECT COUNT(*) FROM fts_message_content WHERE fts_message_content MATCH '"JSON-First Artifact Invariant"'` before rebuild); post-rebuild = 193 |
| 9 | R8 | dry-run | curious-napping-koala.md:362-369 | /tmp/claude-history.dryrun-c2.3.db | — (verified) | §safeguard-2 dry-run conducted via the Rust code path per refined §369 (`claude-history sync --db-path … --projects-dir <empty-tmp>` exercises init_db → run_migrations end-to-end). Commit body acknowledges this addresses the C2.1 audit gap (C2.1 used a bare sqlite3 shell). The dry-run DB carries schema_versions=11 with full integrity_check=ok. | Commit body §369 reference; `sqlite3 .../dryrun-c2.3.db "PRAGMA integrity_check"` returns `ok` |
| 10 | R8 | dry-run | curious-napping-koala.md:362-369 | n/a | divergence | Commit body mentions: "The dry-run DB had its schema_versions row for 010 inserted manually before the sync invocation because C2.1's bare-sqlite3 application of 010 left the column present without a runner-tracking row". This means 011's runner-driven application was tested only against a hand-edited schema_versions sequence, not against a clean lineage 001→011 produced by `init_db` against a fresh DB. The unit test `migration_011_idempotent` covers the clean lineage on in-memory DB; the dry-run is the production-mass exercise but on a hand-edited base. | Commit body §safeguard-2 paragraph; `sqlite3 .../dryrun-c2.3.db "SELECT * FROM schema_versions ORDER BY version"` shows 001..011 present including 010 with a possibly-non-runner `applied_at` |
| 11 | R10 | n/a | curious-napping-koala.md:310 | n/a | verification | §C2 line 310 "claude-history plans list --project smak returns 41 plans" is a C2.4-bounded check (CLI plans subcommand is C2.4). Not exercisable against this commit. Plan's verification block at §305-316 mixes C2.1+C2.3-bounded checks with C2.4/C2.5/C2.6 surface checks; only the C2.3-bounded subset (lines 311-312) plus C2.1 invariants (309, 313, 316) apply to this audit. | `target/release/claude-history plans --help` returns no such subcommand (C2.4 not yet shipped) |
| 12 | R12 | dry-run | curious-napping-koala.md:312 | n/a | verification | §C2 line 312 "Live ingestion: trigger a fresh plan-mode session in any project … within ~2 s, the user record's plan_content column is populated and the corresponding synthetic message_content row exists." Pre-kickstart, this cannot be exercised against the live DB (live DB schema_versions=7, daemon at PID 60855 on pre-C1.4 binary). The 4 unit tests + the 81-row backfill in dry-run substitute. Live verification deferred to kickstart. | `launchctl list \| grep claude-history` returns PID 60855; `sqlite3 ~/.claude/.claude-history.db "SELECT MAX(version) FROM schema_versions"` returns `007` |
| 13 | R13 | git-log | curious-napping-koala.md:215 | n/a | sequence | §hard-rule-5 says C2.3 may commit in any order vs C2.4/C2.5/C2.6 once C2.1 lands. C2.1 (6d54e88) is upstream of 7c6048b. Parent commit is 0b37e00 (C2.1-Review checkpoint). Position in DAG matches. No references to not-yet-merged elements; no work belonging in C2.4-C2.6. | `git log --oneline 6d54e88..7c6048b` shows: 60bd7f4 (C2.1.1 fix), ed2c3f8/0b37e00 (review checkpoint), 7c6048b |
| 14 | R9 | git-log | n/a | git log -n 1 7c6048b | commit-message | Subject `feat(store,decompose): synthetic message_content for plan_content FTS` is 69 chars (verified via `awk '{print length}'`). Implementation report cited "69 chars". Subject ≤72-char convention satisfied. | `git log -n 1 7c6048b --format=%s \| awk '{print length}'` returns 69 |
| 15 | R10 | commit body | n/a | full commit body | commit-message | No Co-Authored-By line present. Mandate satisfied. | `git show 7c6048b \| grep -i co-authored` returns no rows |
| 16 | R11 | commit body | n/a | full commit body | commit-message | Commit body uses intent-framed prose ("aims to", "mirrors", "absorbs", "keeps") and explicit Aim/empirical-check framing. No "this fixes…" or "this ensures…" appear. Forensic detail extensive (~95-line body documenting both surfaces, dry-run shape, sentinel rationale, FTS rebuild expectation, test-surface enumeration, and "Open considerations not addressed in this commit"). Mandate-aligned. | `git show 7c6048b --format=%B \| grep -E "ensures\\\\\|fixes\\\\\|guarantees"` returns no rows |
| 17 | R14 | cargo test | n/a | full workspace | verification | `cargo test --workspace` returns 269 passed (3 + 205 + 61 + 0 + 0 = 269). Implementation report cited 269/269. Build clean (release profile finished in 0.23s incremental). | `cargo test --workspace 2>&1 \| grep "^test result"` totals 269 passed 0 failed |
| 18 | R15 | dry-run | n/a | /tmp/claude-history.dryrun-c2.3.db | verification | `PRAGMA integrity_check` returns `ok` on dry-run DB. Live DB integrity_check not exercised pre-kickstart per audit-pattern precedent. | `sqlite3 /tmp/claude-history.dryrun-c2.3.db "PRAGMA integrity_check"` returns `ok` |
| 19 | R16, R17 | I-SCHEMA-723-743 | n/a | crates/store/src/schema.rs:723-743 | test-coverage | `migration_011_idempotent` runs `run_migrations` twice on a fresh in-memory DB and asserts the schema_versions row for `'011'` is recorded exactly once (count == 1). Runner-level idempotency covered. | `grep -A 12 "fn migration_011_idempotent" crates/store/src/schema.rs` |
| 20 | R18, R24 | I-SCHEMA-746-857 | n/a | crates/store/src/schema.rs:746-857 | test-coverage | `migration_011_backfills_synthetic_message_content_rows` seeds three messages (two with plan_content, one without), applies migration 011 SQL directly, asserts: (a) exactly two synthetic rows, (b) text_content matches verbatim, (c) no row for the no-plan message, (d) replay-stable (re-applying SQL leaves count at 2). UPDATE-level idempotency via INSERT OR IGNORE covered. | `crates/store/src/schema.rs:746-857` |
| 21 | R7, R6 | I-SCHEMA-859-918 | n/a | crates/store/src/schema.rs:859-918 | test-coverage | `migration_011_synthetic_row_visible_to_fts_after_rebuild` seeds plan_content, runs backfill SQL inline, runs `INSERT INTO fts_message_content('rebuild')`, MATCHes a distinctive token (`zilliqotic-marker`) and asserts hit count >= 1. End-to-end FTS gate covered. | `crates/store/src/schema.rs:859-918` |
| 22 | R23 | I-DEC-2398-2444 | n/a | crates/store/src/decompose.rs:2398-2444 | test-coverage | `test_decompose_user_plan_content_synthetic_row_idempotent` decomposes the same UserRecord twice and asserts synthetic row count remains 1. INSERT OR IGNORE covers re-sync race. | `crates/store/src/decompose.rs:2398-2444` |
| 23 | — | I-DEC-2364-2384 | n/a | crates/store/src/decompose.rs:2364-2384 | test-coverage | Existing `test_decompose_user_plan_content` augmented with synthetic-row assertion: post-decompose, message_content carries the projected row keyed by (message_uuid, -1, 'plan_content') with text_content matching plan_content verbatim. | `git diff 0b37e00..7c6048b -- crates/store/src/decompose.rs \| grep -A 16 "C2.3: synthetic"` |
| 24 | — | I-DEC-no-test | n/a | crates/store/src/decompose.rs (gap) | test-coverage | No test exercises the case where `plan_content` extraction returns `None` (overflow.get("planContent") missing or non-string Value). The new `if let Some(ref pc) = plan_content` branch is short-circuited in those paths; existing `test_decompose_user_preserves_non_string_plan_content` indirectly covers the non-string case (it asserts plan_content stays None) but does NOT explicitly assert the absence of a synthetic message_content row in that case. | `grep -B 1 -A 5 "non_string_plan_content\\|preserves_non_string" crates/store/src/decompose.rs` shows no message_content COUNT assertion in that test |
| 25 | R21 | I-DEC-477-485 | curious-napping-koala.md:163 | crates/store/src/decompose.rs:477-485 | divergence | `result.rows_inserted += inserted` adds the synthetic-row insert count to `DecomposedRowCount`. The synthetic row is now counted as a "decomposed row" in the same metric used by sync_all to gate FTS rebuild (`files_synced > 0`). The metric semantics shift slightly: a record whose only insertable surface was the synthetic plan_content row now contributes 1 to rows_inserted. Functionally net-positive (FTS rebuild gating logic stays correct), but the metric's meaning is now "rows inserted across all decompose tables including synthetic FTS-projection rows". | `crates/store/src/decompose.rs:481-484` |
| 26 | — | I-DEC-block_type | n/a | crates/server/src/main.rs:627 | documentation | `QUERIES_SCHEMA_HELP` at `crates/server/src/main.rs:627` enumerates `block_type(text\|thinking\|tool_use\|tool_result)`. Post-C2.3 there is a fifth value `plan_content` available in production data, but the CLI `claude-history queries --help` schema reference does not list it. Discoverability surface for ad-hoc SQL authors omits the new value. | `crates/server/src/main.rs:627` shows 4-value enumeration |
| 27 | — | I-MCP | n/a | crates/server/src/mcp/mod.rs (block_type) | documentation | The MCP server instructions at `crates/server/src/mcp/mod.rs` and the README/CLAUDE.md tool-list both reference 10 tools; C2.3 adds no new MCP tool. No tool-count drift expected. C2.3 does not require a new MCP route. Verified: `grep -c "MCP" crates/server/src/mcp/mod.rs` returns multiple but tool count unchanged. | `git diff 0b37e00..7c6048b -- crates/server/` returns 0 lines |
| 28 | — | I-DEC-block_index | n/a | crates/store/src/query.rs:1000-1019, 1150-1169, 1380-1389 | divergence | Three `query.rs` query paths build `ExportContentBlock` lists via `SELECT block_index, block_type, text_content, … FROM message_content WHERE message_uuid = ?1 ORDER BY block_index ASC`. Synthetic block_index = -1 sorts FIRST in ASC ordering, BEFORE all real content blocks. Markdown export and any consumer iterating in returned order will see the plan_content block before the user's actual `text` block on plan-bearing user messages. Plan §163 does not specify ordering for the synthetic row vs real blocks. | `grep -A 4 "ORDER BY block_index ASC" crates/store/src/query.rs` returns three sites |
| 29 | — | I-EXPORT-258-291 | n/a | crates/server/src/export.rs:258-291 | addition | `write_markdown_block` at `crates/server/src/export.rs:258` matches on `block_type` with arms for text/thinking, tool_use, tool_result, and an `_` fallthrough that renders unknown block_types as `*[<block_type>]* <text>`. Synthetic `plan_content` rows hit the fallthrough arm and render as `*[plan_content]* <markdown>` in the markdown export. Combined with the ASC ordering at #28, every plan-bearing user message in the markdown export now begins with a `*[plan_content]*` prefix line followed by the actual user text. Plan §163 does not specify export-rendering behavior for the synthetic row. | `crates/server/src/export.rs:285-291` is the `_` arm; `crates/server/src/query.rs:1003` supplies sort order |
| 30 | — | I-FIRST-TEXT | n/a | crates/server/src/export.rs:311-320 | divergence | `first_text_content` at `crates/server/src/export.rs:311-320` filters on `block.block_type == "text" \|\| block.block_type == "thinking"`. Synthetic `plan_content` rows are excluded from first-text extraction. Plan-bearing user messages in summary contexts therefore continue to surface the original `text` block, not the plan markdown. Filter is incidentally compatible with the synthetic row. | `crates/server/src/export.rs:313` |
| 31 | — | I-VIEW-FILTER | n/a | crates/store/migrations/004_modeling.sql:101, 161; 006_version_monitoring.sql:129, 191 | — (verified) | Four analytical views (`v_file_conversation_context`, `v_git_commit_context`, plus two from migration 006) JOIN message_content with `mc.block_type = 'text'` filters AND `m.type = 'assistant'` filters. Synthetic plan_content rows live on `user`-typed messages and use `block_type = 'plan_content'`. Both filters individually exclude synthetic rows. Views are immune. | `grep -B 2 -A 2 "block_type = 'text'" crates/store/migrations/004_modeling.sql crates/store/migrations/006_version_monitoring.sql` |
| 32 | — | I-CANNED-QUERIES | n/a | queries/, ~/.claude/claude-history/queries/ | — (verified) | Canned queries do not filter on `block_index` ranges. `message-search-context.sql` and `fts-search.sql` JOIN message_content and select block_type but apply no filter that would exclude block_index = -1; results would include synthetic rows post-rebuild. Plan §163 does not specify canned-query behavior. No canned query has a `block_index >= 0` or `BETWEEN` filter that would produce off-by-one drift. | `grep -rn "block_index\|BETWEEN\|>= 0" queries/ ~/.claude/claude-history/queries/` returns no matching filters |
| 33 | — | I-MEMORY | n/a | ~/.claude/projects/-Users-david-Projects-cc-history-api/memory/MEMORY.md | documentation | MEMORY.md "High-Value Semantic Gap: planContent" section frames planContent as "FTS5 index does not cover extra_json, so claude-history search cannot find phrases that exist only inside plan markdown" and "Promotion to a real messages.plan_content column plus FTS5 wiring (likely via a synthetic message_content row with block_type='plan_content')". Post-C2.3 (and post-kickstart), both the column and the FTS wiring exist, satisfying the gap. C2.3 did not refresh the MEMORY.md note (consistent with C2.1 precedent — no MEMORY.md edits in tier-impl commits). | `grep -A 8 "High-Value Semantic Gap" ~/.claude/projects/-Users-david-Projects-cc-history-api/memory/MEMORY.md` |
| 34 | — | I-DRIFT | n/a | crates/store/src/drift.rs (unchanged) | — (verified) | `drift.rs` is not modified. Drift logging continues to track planContent occurrences in `schema_drift_log` per the C2.1 contract. C2.3 does not change drift behavior. | `git diff 0b37e00..7c6048b -- crates/store/src/drift.rs` returns 0 lines |
| 35 | R16 | I-SCHEMA-22 | n/a | crates/store/src/schema.rs:22 | — (verified) | MIGRATIONS array now includes `("011", include_str!("../migrations/011_plan_content_fts.sql"))` at index 10 (zero-indexed). Sequential ordering 001..011. | `grep -n '"011"' crates/store/src/schema.rs` returns line 22 |
| 36 | R16 | I-DB-220, I-SCHEMA-274-279, 488 | n/a | crates/store/src/db.rs:223; crates/store/src/schema.rs:275-279, 488 | — (verified) | sv_count idempotency assertion bumped 10→11 in three sites: `db.rs::tests` line 223 ("Should still have exactly 11 migration versions (001-011) after second init"), `schema.rs::tests` line 275-279 ("11 entries"), `schema.rs::tests` line 488 ("all 11 migrations should be recorded post-011"). | `git diff 0b37e00..7c6048b -- crates/store/src/db.rs crates/store/src/schema.rs \| grep -E "10\\\\\|11"` |
| 37 | R8 | dry-run | curious-napping-koala.md:367 | n/a | verification | §safeguard-2 line 367 requires "Pre/post baseline DB row-count snapshot per affected table". Commit body records: "Pre: message_content = 739123, plan_content rows = 81, synthetic block_index=-1 rows = 0, schema_versions = 9. Post: message_content = 739204 (delta 81), synthetic rows = 81, … schema_versions = 11". The pre snapshot at schema_versions=9 (rather than the dry-run-pre-state of schema_versions=10 after C2.1 was applied) is unusual — see #10 for context. The pre-row-count of 739123 + delta 81 = 739204 matches the recorded post. | Commit body Dry-run section |
| 38 | R8 | dry-run | curious-napping-koala.md:369 | n/a | — (verified) | §safeguard-2 line 369 requires "Record the dry-run artifact path + summary statistics + per-subtype-row breakdown + baseline-diff in the commit body". Commit body records artifact path (`/tmp/claude-history.dryrun-c2.3.db`), summary stats (pre/post counts), per-block_type breakdown (193 total / 2 plan_content), and the FTS gate result (was 0, now 2 in plan_content). | Commit body §safeguard-2 paragraph |
| 39 | R22 | dry-run | curious-napping-koala.md:316 | /tmp/claude-history.dryrun-c2.3.db | — (verified) | §C2 line 316 "Schema integrity: PRAGMA integrity_check returns ok". Verified on dry-run. PRAGMA table_info(message_content) shows existing columns unchanged (no schema mutation in 011). | `sqlite3 /tmp/claude-history.dryrun-c2.3.db "PRAGMA integrity_check"` returns `ok` |
| 40 | R7 | dry-run | curious-napping-koala.md:311 | n/a | — (verified) | Pre-rebuild on dry-run: MATCH count = 191 across 75 text + 116 tool_result + 0 plan_content. Post-rebuild: 193 with 2 plan_content. The 2 plan_content hits validate that the synthetic rows tokenize correctly under FTS5 and that BM25 ranking surfaces them alongside other matches. | `sqlite3 .../dryrun-c2.3.db "SELECT COUNT(*) FROM fts_message_content WHERE fts_message_content MATCH '\"JSON-First Artifact Invariant\"'"` returns 191 pre-rebuild, 193 post-rebuild |
| 41 | — | I-MIG-1-69 | n/a | crates/store/migrations/011_plan_content_fts.sql:1-69 | — (verified) | Migration docstring is forensic and explicit (69 lines of documentation for 4 lines of SQL). Documents sentinel rationale, UNIQUE-constraint reuse, replay safety, FTS rebuild expectation, decomposer parity. Mandate-aligned. | `wc -l crates/store/migrations/011_plan_content_fts.sql` returns 73 (incl. blanks) |
| 42 | — | I-MIG-67 | n/a | crates/store/migrations/011_plan_content_fts.sql:70-73 | divergence | Migration uses `INSERT OR IGNORE` against `UNIQUE(message_uuid, block_index)` for replay idempotency. No `BEGIN`/`COMMIT` transaction wrapper. Migration runner at `crates/store/src/schema.rs::run_migrations` likely wraps each migration in a transaction (project precedent). For a single-statement migration the wrapping is moot; the INSERT statement is atomic at the storage layer. | `grep -n "BEGIN\\|COMMIT" crates/store/migrations/011_plan_content_fts.sql` returns no rows |
| 43 | R6 | dry-run | curious-napping-koala.md:163 | n/a | verification | Plan §163 says "the existing FTS5 index at crates/store/src/fts.rs automatically picks them up via the standard rebuild path". Verified by direct rebuild on dry-run DB: synthetic rows produce indexable tokens; BM25 ranks include them in match results. The rebuild is gated on the watcher's 30s loop OR sync_all's `files_synced > 0` check (`crates/store/src/sync.rs:426`). Until the next gated rebuild, synthetic rows exist in message_content but are NOT yet in the FTS index. The migration alone does NOT trigger a rebuild. | Commit body "Open considerations": "tokens land in the FTS5 index on rebuild. The watcher's existing 30 s loop carries this within a bounded window after deployment." `crates/store/src/sync.rs:418-427` shows rebuild_fts_index call gated on `files_synced > 0` |
| 44 | R6 | sync-gating | n/a | crates/store/src/sync.rs:418-427 | divergence | `sync_all` rebuilds FTS only when `files_synced > 0`. After the migration applies on first daemon kickstart (post-deploy), if no JSONL files have been modified between then and the next sync_all invocation, sync_all skips the rebuild — leaving the 81 backfilled synthetic rows un-tokenized in the FTS index until either (a) the watcher's 30s timer fires and rebuilds (verifiable in watcher.rs:734) or (b) some JSONL file is modified. The watcher loop in `crates/server/src/watcher.rs:734` is the load-bearing post-deploy rebuild trigger. The migration does NOT directly call rebuild_fts_index. | `grep -B 4 -A 8 "rebuild_fts_index" crates/store/src/sync.rs` shows files_synced gate; `crates/server/src/watcher.rs:734` shows unconditional 30s rebuild |
| 45 | — | I-DAEMON | n/a | live system | sequence | Live DB at `~/.claude/.claude-history.db` is at schema_versions=7, daemon at PID 60855 on pre-C1.4 binary. On next `launchctl kickstart -k`, migrations 008+009+010+011 will all apply in sequence. Each individual migration's safety has been independently tested via cargo test; the chained-application sequence on a populated live DB has not been exercised end-to-end. Same observation as C2.1 audit row #47. | `launchctl list \| grep claude-history` returns PID 60855; `sqlite3 ~/.claude/.claude-history.db "SELECT MAX(version) FROM schema_versions"` returns `007` |
| 46 | — | n/a | n/a | n/a | sequence | C2.3 does not touch C1.* surfaces, C2.1 surfaces (no edits to migration 010 or messages.plan_content shape), C2.1.1 surfaces (no edits to non-string preservation in decompose_user). `git diff 0b37e00..7c6048b --stat` shows changes only in: `crates/store/migrations/011_plan_content_fts.sql` (new), `crates/store/src/db.rs`, `crates/store/src/decompose.rs`, `crates/store/src/schema.rs`. No `crates/server/`, `crates/core/`, `queries/`, or `mcpb/` changes. | `git diff --stat 0b37e00..7c6048b` shows 4 files all under `crates/store/` |
| 47 | — | I-DEC-468-484 | n/a | crates/store/src/decompose.rs:468-484 | divergence | The decompose-time INSERT OR IGNORE fires unconditionally when plan_content is `Some`, on every JSONL line decompose. Per the existing comment at line 444-445 ("This runs after INSERT OR IGNORE so re-sync can backfill promoted fields on existing rows"), re-sync of the same JSONL bytes triggers another INSERT OR IGNORE. The IGNORE absorbs the duplicate; the work is performed (constraint check + ignore) every time rather than short-circuiting. Same idempotency-via-IGNORE pattern as C2.1 audit row #27. | `crates/store/src/decompose.rs:468-484` |
| 48 | — | n/a | n/a | n/a | meta-plan | Plan §163 prose still contains stale literal `Migration 009 includes a backfill insert for the 81 existing plan records (corpus survey count was 81; live count may have grown).` After C1.3 took 009 and C2.1 took 010, C2.3 actually used 011. STATE.md line 196 records the renumbering acknowledgment but plan §163 itself was not amended. The implementation correctly reads §163 as "C2.3 takes its own migration number (likely 011 — verify next-available)" elsewhere in §163, but the earlier "Migration 009 includes a backfill insert" sentence is unreconciled. | `grep -n "Migration 009 includes" /Users/david/.claude/plans/curious-napping-koala.md` returns line 163 |
| 49 | — | I-MIG-block_index_doc | n/a | crates/store/src/schema.rs:746-857 | test-coverage | The backfill test at `migration_011_backfills_synthetic_message_content_rows` uses `MIGRATIONS.iter().take_while(\|(v, _)\| *v < "011")` — adopting the predicate-based pattern called out as safer in C2.1 audit row #45 (which had used hard-coded `take(9)`). Pattern improvement applied. | `grep "take_while\|take(" crates/store/src/schema.rs:746-790` |
| 50 | R19 | live DB | curious-napping-koala.md:163 | ~/.claude/.claude-history.db | verification | Plan §163 says "corpus survey count was 81; live count may have grown". Live DB cannot run `WHERE plan_content IS NOT NULL` (column not yet present at schema_versions=7). Live `SELECT COUNT(*) FROM messages WHERE json_extract(extra_json, '$.planContent') IS NOT NULL` was 81 at C2.1 audit time; not re-verified at C2.3 audit time but the dry-run base (clone of c2.1 dry-run, itself cloned from a pre-C2.1 live DB) shows 81 plan_content rows post-migration-010. Drift between MEMORY.md snapshot (81 plans), C2.1 dry-run (81), and current live (≥81 with potential drift) not reconciled by either commit. | `sqlite3 /tmp/claude-history.dryrun-c2.3.db "SELECT COUNT(*) FROM messages WHERE plan_content IS NOT NULL"` returns 81 |
| 51 | — | I-DEC-rows_inserted | n/a | crates/store/src/decompose.rs:484 | divergence | The decompose-time synthetic insert returns `inserted: usize` (0 or 1) and is added to `result.rows_inserted`. On re-decompose of the same UserRecord, the second invocation's `inserted` is 0 (UNIQUE constraint absorbed); `result.rows_inserted` does not double-count. The metric is consistent. | `crates/store/src/decompose.rs:478-485` |
| 52 | — | I-DEC-block_index-collision | n/a | crates/store/src/decompose.rs (no defensive check) | divergence | The decompose-time insert hardcodes `block_index = -1` literal. There is no defensive runtime check that would detect a future Claude Code emission of a record with multiple plan_content blocks (which is impossible by current shape — `plan_content` is a single Option<String>) or a future migration that allows block_index < 0 from another source. Sentinel is currently unique by structural argument; no runtime guard. | `crates/store/src/decompose.rs:482-484` |

## Verification command results

```
$ git show --stat 7c6048b
 crates/store/migrations/011_plan_content_fts.sql |  73 ++++++++
 crates/store/src/db.rs                           |   2 +-
 crates/store/src/decompose.rs                    |  99 +++++++++++
 crates/store/src/schema.rs                       | 209 ++++++++++++++++++++++-
 4 files changed, 378 insertions(+), 5 deletions(-)

$ git log -n 1 7c6048b --format=%s | awk '{print length}'
69

$ cargo build --release 2>&1 | tail -5
    Finished `release` profile [optimized] target(s) in 0.23s

$ cargo test --workspace 2>&1 | grep -E "^test result"
test result: ok. 3 passed; 0 failed; 0 ignored; ...
test result: ok. 205 passed; 0 failed; 0 ignored; ...
test result: ok. 61 passed; 0 failed; 0 ignored; ...
test result: ok. 0 passed; 0 failed; 0 ignored; ...
test result: ok. 0 passed; 0 failed; 0 ignored; ...
total: 269 passed; 0 failed

$ sqlite3 /tmp/claude-history.dryrun-c2.3.db "SELECT COUNT(*) FROM message_content WHERE block_type='plan_content'"
81

$ sqlite3 /tmp/claude-history.dryrun-c2.3.db "SELECT COUNT(*) FROM messages WHERE plan_content IS NOT NULL"
81

$ sqlite3 /tmp/claude-history.dryrun-c2.3.db "SELECT MIN(block_index), MAX(block_index) FROM message_content"
-1|7

$ sqlite3 /tmp/claude-history.dryrun-c2.3.db "PRAGMA integrity_check"
ok

$ sqlite3 .../dryrun-c2.3.db "SELECT COUNT(*) FROM fts_message_content WHERE fts_message_content MATCH '\"JSON-First Artifact Invariant\"'"
193  (post-rebuild; 191 pre-rebuild — the +2 are the synthetic plan_content matches)

$ sqlite3 .../dryrun-c2.3.db "SELECT mc.block_type, COUNT(*) FROM fts_message_content f JOIN message_content mc ON mc.id=f.rowid WHERE f.fts_message_content MATCH '\"JSON-First Artifact Invariant\"' GROUP BY mc.block_type"
plan_content|2
text|75
tool_result|116

$ launchctl list | grep claude-history
60855	0	com.davidrex.claude-history
-	0	com.davidrex.claude-history-logrotate

$ sqlite3 ~/.claude/.claude-history.db "SELECT MAX(version) FROM schema_versions"
007

$ grep -rn "block_index" --include='*.rs' crates/ | grep -E ">= 0|< 0|>= 1|BETWEEN"
(no rows — no production-code filter on block_index ranges)
```

## Plan-defect notes

- §163 line 163 in `/Users/david/.claude/plans/curious-napping-koala.md` retains stale literal `Migration 009 includes a backfill insert for the 81 existing plan records`. Both `009` (now 011) and `81` (potentially drifted) are unreconciled. STATE.md line 196 records the renumbering ("future C2.3 takes 011") but the plan file was not amended. (#48)
- §305-316 verification block mixes C2.1-bounded, C2.3-bounded, and C2.4/C2.5/C2.6-bounded checks without per-sub-task partition. Lines 310 (plans list — C2.4), 314 (MCP list_plans — C2.5), 315 (REST /v1/plans — C2.6) are not exercisable against C2.1 or C2.3 alone. (#11)
- §163 prose specifies migration backfill but is silent on the decompose-time mirror. Implementation adds decompose_user step 4b based on §312's live-ingestion 2s requirement; plan does not name decompose.rs as the locus for that requirement. (#5)

## Audit completeness self-check

- Step 1 (requirements ledger): R1–R24 extracted, recorded in summary above.
- Step 2 (implementation ledger): four-file diff enumerated; per-file change shape recorded.
- Step 3 (cross-reference): R1–R24 → Im mapped via 52 catalog rows; orphan additions (decompose-time insert #5, rows_inserted accounting #25, schema-help omission #26) flagged.
- Step 4 (verification commands): subject 69 chars, cargo build OK, cargo test 269/269, dry-run integrity ok, FTS5 MATCH count 191→193 with 2 plan_content hits.
- Step 5 (regression safeguards): integrity_check, foreign_key shape (no FK changes by 011), no canned-query regression, view-filter immunity verified.
- Step 6 (commit message): subject ≤72, no Co-Authored-By, no banned definitives, ~95-line forensic body.
- Step 7 (sequence/dependency): position downstream of 0b37e00, no references to not-yet-merged elements, no C2.4-C2.6 work pulled forward.
- Step 8 (documentation): MEMORY.md gap-section unrefreshed (consistent with C2.1 precedent); QUERIES_SCHEMA_HELP block_type enumeration omits 'plan_content'.
- Step 9 (test coverage): 4 new tests cover migration runner-level idempotency, backfill correctness + replay, FTS rebuild gate, decompose-time idempotency. Augmented existing test asserts synthetic-row mirror. Gap: no explicit assertion of synthetic-row absence on non-string plan_content path (#24).
- Step 10 (compile): 52-row deviation catalog produced; no severity language; all rows carry evidence.
