# Adversarial audit — C2.4 (CLI plans subcommand + --has-plan filter)

## 1. Audit metadata

- Commit audited: `ab0971a180c375c5100086be5cb3cdc76a3babc4`
- Subject: `feat(query,server): add plans CLI subcommand + --has-plan filter (C2.4)` (71 chars)
- Plan section: §165 (refined plan `curious-napping-koala.md`)
- Plan file: `/Users/david/.claude/plans/curious-napping-koala.md`
- Audit timestamp: 2026-05-09T15:27 Asia/Shanghai (UTC+0800)
- Parent: `b5bbc45` (docs(planning): close C2.3 review checkpoint)

## 2. Requirements ledger summary

Plan §165 is one terse sentence covering CLI surface only:

> C2.4 — CLI: `claude-history plans list [--project P] [--since DATE] [--limit N]` and `claude-history plans show <session-id>`. Plus `--has-plan` filter on the existing `sessions` subcommand.

Extracted requirements:
- R1 (§165): subcommand `plans list [--project P] [--since DATE] [--limit N]`
- R2 (§165): subcommand `plans show <session-id>`
- R3 (§165): `--has-plan` flag on existing `sessions` subcommand
- R4 (§215, hard rule 5): C2.3/C2.4/C2.5/C2.6 may commit in any order after C2.1 — no commit-position constraint among them
- R5 (§253-254 / §165 implicit): MCP and REST surfaces are NOT part of C2.4 (deferred to C2.5 / C2.6)
- R6 (§170-171, hard rule 3): no migration in C2.4 (no migration-numbering ratchet)
- R7 (§Per-item subagent assignments §232): subagent type general-purpose, must read project-pattern precedents

Plan §165 is silent on: --json flag, output format details, exit codes for "no plans found", multibyte preview safety, daemon-vs-direct routing for the `plans` subcommand, MCP list_sessions has_plan exposure, has_plan tri-state semantics, or where in the spawn order C2.4 lives among C2.3/C2.5/C2.6. Plan §310 is referenced in the commit message (verification-clauses) but the plan file does not contain a §310-anchor that covers `plans list` directly — section anchor §310 in the plan file is part of the regression-safeguards numbering, not the C2 verification block. (Meta-plan note in §5.)

## 3. Deviation catalog

| # | Plan ref | Impl ref | Plan section file:line | Implementation file:line | Category | Description | Evidence |
|---|---|---|---|---|---|---|---|
| 1 | — | I-main:113 | curious-napping-koala.md:165 | crates/server/src/main.rs:113 | addition | Doc string on `--has-plan` says "Pair with `--no-has-plan` for the inverse" but the clap config (`ArgAction::Set`, `num_args = 0..=1`, `default_missing_value = "true"`) does not generate a `--no-has-plan` flag. Actual inverse is `--has-plan=false`. | `target/release/claude-history sessions --no-has-plan --help` → `error: unexpected argument '--no-has-plan' found` |
| 2 | — | I-main:3 | — | crates/server/src/main.rs:3 | documentation | Top-of-file module doc comment reads "Provides the `claude-history` command with 20 subcommands" but commit ab0971a adds the 21st enum-Commands variant `Plans`. Module doc not updated to 21. | `awk '/^enum Commands \{/,/^\}/' crates/server/src/main.rs \| grep -cE '^\s+[A-Z][a-zA-Z]+\s*[\{,]'` → `21`; `crates/server/src/main.rs:3` still says "20 subcommands" |
| 3 | — | I-query.rs:tests | — | crates/store/src/query.rs:2447- | divergence | Commit message body claims "10 new unit tests under `query::tests`" and verification block again claims "Store gained 10 new tests covering plans_list (4), plan_show (3), list_sessions has_plan (2 — narrowing-true\|false plus combine-with-project)". Diff adds 9 new `fn test_*` definitions (4+3+2=9; the message's own breakdown sums to 9 but the headline says 10). | `git diff b5bbc45..ab0971a -- crates/store/src/query.rs \| grep -cE '^\+\s*fn test_'` → `9` |
| 4 | — | I-main.rs:288 | — | crates/server/src/main.rs:288 | divergence | The new `Plans` enum-Commands variant carries no `#[command(after_long_help = …)]` decoration, departing from the established precedent for major top-level subcommands (Search, Sessions, Export, Files, FileHistory, Reconstruct, GitLog, Queries, McpStdio all have `after_long_help`; Attachments and HookExecutions do not, so this is mixed precedent rather than a strict mandate). | `grep -n "after_long_help" crates/server/src/main.rs \| head -10` shows 9 callsites; none on `Plans` |
| 5 | — | I-main.rs:1841-1860 | — | crates/server/src/main.rs:1841 | addition | `run_plans` opens a separate `tokio_rusqlite::Connection` via the new `ensure_direct_conn` helper when called in `Daemon` mode. Plan §165 does not specify a routing mode for `plans`; the commit body documents the design choice but it is an implementation decision adopted without a plan-side authorization. SQLite WAL allows concurrent readers so this is not a correctness issue, but the plan does not cover this routing surface. | `crates/server/src/main.rs:1860-1872` `ensure_direct_conn` opens fresh `tokio_rusqlite::Connection::open(&db_path)` while the supervised daemon holds its own connection at PID 60855 |
| 6 | — | I-main.rs:1947 | — | crates/server/src/main.rs:1947 | addition | `run_plans_show` returns `ExitCode::FAILURE` when no plan-bearing messages exist for the session. Plan §165 is silent on missing-session UX. The exit code combined with stderr "No plan-bearing messages found for session: …" treats two distinct states (session does not exist, session has no plans) identically. Comment on `plan_show` query function at `crates/store/src/query.rs:1058` explicitly notes callers wanting to distinguish should query sessions separately; the run_plans_show callsite does not perform that distinction. | `target/release/claude-history plans show some-nonexistent-id --db-path /tmp/claude-history.dryrun-c2.3.db` → exit=1, message "No plan-bearing messages found for session: some-nonexistent-id" |
| 7 | — | I-mcp/tools.rs:242 | — | crates/server/src/mcp/tools.rs:242 | addition | The MCP `list_sessions` tool callsite passes `None` for the new `has_plan` arg without exposing the parameter to MCP callers. Commit message defers MCP filter exposure to C2.5; this is consistent with plan §167 but is an implementation choice not stated in plan §165 (plan §165 covers the CLI flag, not the MCP-side exposure decision). | `crates/server/src/mcp/tools.rs:242` `None,` inserted between `before.as_deref()` and `limit` |
| 8 | R3 | I-daemon_client.rs:271-291 | curious-napping-koala.md:165 | crates/server/src/daemon_client.rs:271 | divergence | `daemon_client.sessions(...)` now takes `has_plan: Option<bool>` and forwards `has_plan=true`/`has_plan=false`. The supervised daemon binary at PID 60855 predates this commit and does not implement the `has_plan` `SessionsParams` field; daemon-routed `--has-plan` requests silently drop the filter. Verified with live HTTP. | `curl -s 'http://127.0.0.1:7424/v1/sessions?has_plan=true&limit=1'` returns session `373cccfb-...` which has zero plan-bearing messages per the C2.3 dryrun corpus listing; the running daemon is older than ab0971a |
| 9 | — | I-query.rs:415-432 | — | crates/store/src/query.rs:416 | addition | `Some(false)` branch of `has_plan` adds `NOT EXISTS (...)` against `messages.plan_content`. Plan §165 names a single tri-state-implying `--has-plan` flag without specifying that the `false` arm exists or that it operates without index assistance. Commit message acknowledges the partial index does not cover the `false` branch. | `crates/store/src/query.rs:425-432` `NOT EXISTS (SELECT 1 FROM messages mp ...)`. EXPLAIN QUERY PLAN for the negated form forces a full scan of `messages` per session; SQLite cannot use the partial index for `NOT EXISTS` |
| 10 | — | I-query.rs:480 | — | crates/store/src/query.rs:480 | divergence | `plans_list` SQL ORDER BY clause is `m.timestamp DESC` (newest first). Plan §165 does not specify ordering, and `plan_show` uses `timestamp ASC`. The asymmetric default may be intentional (list view = recency, show view = chronological) but is undocumented in the plan and only inferred from doc comments at `crates/store/src/query.rs:454-456`. | `crates/store/src/query.rs:480` `sql.push_str(" ORDER BY m.timestamp DESC LIMIT ?");` vs `crates/store/src/query.rs:506` `ORDER BY m.timestamp ASC` |
| 11 | — | I-query.rs:467-471 | — | crates/store/src/query.rs:467 | divergence | `plans_list` performs `LEFT JOIN sessions s ON s.session_id = m.session_id` and filters with `s.project_path LIKE ?`. The LEFT JOIN combined with a WHERE filter on `s.project_path` semantically degrades to an INNER JOIN (LIKE on NULL returns false / unknown, never matches). Doc comment at `crates/store/src/query.rs:1003-1006` acknowledges this — but the LEFT-vs-INNER divergence is internal SQL tasting; plan §165 specifies neither. | `crates/store/src/query.rs:467` `LEFT JOIN sessions s ON s.session_id = m.session_id`; `crates/store/src/query.rs:474-477` adds `AND s.project_path LIKE ?` |
| 12 | — | I-query.rs:471 | — | crates/store/src/query.rs:471 | divergence | `plans_list` `since` filter is applied to `messages.timestamp` (`m.timestamp >= ?`). The `list_sessions` parallel surface uses `sessions.first_seen_at`. The semantics differ — `since` on plans filters by message-time of the plan itself, not by session start-time. Plan §165 says `--since DATE` without specifying which column. | `crates/store/src/query.rs:478-479` `sql.push_str(" AND m.timestamp >= ?");` vs `crates/store/src/query.rs:399-401` (`list_sessions`) `s.first_seen_at >= ?` |
| 13 | — | I-output.rs:756 | — | crates/server/src/output.rs:756 | divergence | `print_plans_list` truncates project paths from the LEFT (`format!("...{}", &project[project.len() - 37..])`). Project-path strings stored in `sessions.project_path` are arbitrary file paths and not guaranteed to be ASCII-only — slicing on byte index `project.len() - 37` is not multibyte-safe. The PREVIEW field uses `char_indices().nth(50)` correctly; PROJECT does not use the same approach. | `crates/server/src/output.rs:758-762` `&project[project.len() - 37..]` byte-indexes |
| 14 | — | I-output.rs:766-768 | — | crates/server/src/output.rs:766 | addition | `print_plans_list` collapses tabs (`\t`) to spaces alongside `\n` and `\r`. Plan §165 specifies no preview-rendering policy; commit body documents the choice. The collapse strips tab characters from markdown bodies that may use them for indentation in code fences within plan text. | `crates/server/src/output.rs:766-768` `if c == '\n' \|\| c == '\r' \|\| c == '\t' { ' ' } else { c }` |
| 15 | — | I-output.rs:776 | — | crates/server/src/output.rs:776 | divergence | `LENGTH:     {} bytes` in `print_plan_show` uses `r.plan_content.len()` (Rust-side byte count of the recovered UTF-8 string), while `PlanRow::plan_content_length` from `plans_list` uses SQLite's `length(plan_content)` (char count for TEXT). For plans containing non-ASCII characters, the two surfaces report different numbers for the same plan. | `crates/server/src/output.rs:776` `r.plan_content.len()` vs `crates/store/src/query.rs:464` `length(m.plan_content) AS plan_len` |
| 16 | R5 | — | curious-napping-koala.md:167-169 | — | omission | Plan §167 (C2.5 = MCP) and §169 (C2.6 = REST) are explicitly out of scope per the commit message. The commit honors that. No deviation; recorded for completeness. | commit message body lines 47-48, 79-83 enumerate the deferred surfaces; `crates/server/src/api/sessions.rs:43-46` adds only the existing-endpoint extension, not new plan endpoints |
| 17 | R3 | I-main.rs:113 | curious-napping-koala.md:165 | crates/server/src/main.rs:113 | divergence | `--has-plan` clap configuration `action = clap::ArgAction::Set, num_args = 0..=1, default_missing_value = "true", require_equals = false` makes the flag tri-state (omitted=None, bare=Some(true), `=true`/`=false`=explicit). Plan §165 says `--has-plan filter` without specifying tri-state vs binary semantics. The implementation is a defensible extension; the divergence is plan-surface-vs-implementation-surface depth. | `crates/server/src/main.rs:113-117` |
| 18 | — | — | — | — | verification | Commit message claims `plans show 7df8d4ac-...` renders an "8972-byte markdown body" — verified live the body length reports 8972 bytes (matches). | `target/release/claude-history plans show 7df8d4ac-777a-423b-a359-e03d5b110f97 --db-path /tmp/claude-history.dryrun-c2.3.db` → `LENGTH:     8972 bytes` |
| 19 | — | — | — | — | verification | EXPLAIN QUERY PLAN for `Some(true)` EXISTS subquery confirms `idx_messages_plan_content_present` is engaged. | `sqlite3 /tmp/claude-history.dryrun-c2.3.db "EXPLAIN QUERY PLAN SELECT s.* FROM sessions s WHERE EXISTS (...)"` → `SEARCH mp USING INDEX idx_messages_plan_content_present (session_id=?)` |
| 20 | — | — | — | — | regression | `cargo test --workspace` reports 4+61+214 = 279 passed (matches commit message body). No pre-existing test regressed. | `cargo test --workspace 2>&1 \| grep "test result:"` → all `ok` |
| 21 | — | — | — | — | regression | `PRAGMA integrity_check` and `PRAGMA foreign_key_check` on `/tmp/claude-history.dryrun-c2.3.db` both return `ok`. No row-store damage from C2.4 read-only surface additions. | `sqlite3 /tmp/claude-history.dryrun-c2.3.db "PRAGMA integrity_check; PRAGMA foreign_key_check;"` → `ok` |
| 22 | — | — | — | — | commit-message | Subject is exactly 71 chars (≤72 limit per project convention). No `Co-Authored-By` lines. Body avoids "ensures"/"fixes" definitives in favor of "intended to engage", "selects messages WHERE", "would 404 today". Counting self-check: subject `feat(query,server): add plans CLI subcommand + --has-plan filter (C2.4)` is 71 chars verified. | `git log -n 1 ab0971a --format=%s \| awk '{print length}'` → `71`; `git log -n 1 ab0971a --format=%B \| grep -ci "co-authored-by"` → `0` |
| 23 | — | — | — | — | commit-message | Commit message body cites "Refined plan §165 + §310 (plans-list verification clauses) satisfied". Plan file does not have a §310 anchor that covers C2.4-specific verification clauses; the §310 region in the plan covers Tier-4 / sequencing rules rather than C2-specific verification commands. The plan-section pointer in the commit message is therefore inaccurate. | `grep -n "^### " /Users/david/.claude/plans/curious-napping-koala.md \| head -20` does not surface a §310 verification block matching the description |
| 24 | R7 | — | curious-napping-koala.md:231 | — | sequence | Hard-rule 5 says C2.3/C2.4/C2.5/C2.6 may commit in any order after C2.1. C2.1 = `0b37e00` and C2.3 = `7c6048b` both predate ab0971a (C2.4). Commit position is therefore valid. No deviation. | `git log --oneline 0b37e00..ab0971a` shows C2.1 → C2.3 → C2.3.1 → b5bbc45 (close C2.3) → ab0971a |
| 25 | — | I-main.rs:1860-1872 | — | crates/server/src/main.rs:1860 | dependency | `ensure_direct_conn` helper is introduced as a private fn used solely by `run_plans`. Function name implies general re-use (any subcommand could "ensure direct"), but no other call sites exist. Plan §165 does not specify the helper. | `grep -n "ensure_direct_conn" crates/server/src/main.rs` → 2 hits (definition + single callsite at run_plans) |
| 26 | — | — | — | — | test-coverage | `print_plans_list` and `print_plan_show` formatters in `crates/server/src/output.rs:708-794` are not exercised by any unit test in the diff. Existing `output.rs` precedent (e.g. `print_attachments`) similarly lacks unit tests, so this is consistent with project pattern. | `git diff b5bbc45..ab0971a -- crates/server/src/output.rs \| grep -cE "^\+\s*fn test_"` → `0` |
| 27 | — | — | — | — | test-coverage | `run_plans_show` exit-code branch (returns `FAILURE` when results empty) has no integration test verifying exit code. Manually verified live with nonexistent session. | `target/release/claude-history plans show some-nonexistent-id …; echo $?` → `1` |
| 28 | — | — | — | — | test-coverage | `daemon_client::sessions` extension (`has_plan` query-string forwarding) has no unit test in the diff. Existing `daemon_client.rs` callsites in tests are limited; project pattern is to test daemon_client indirectly via integration. | `git diff b5bbc45..ab0971a -- crates/server/src/daemon_client.rs` shows production code only |
| 29 | — | — | — | — | test-coverage | `api::sessions::list` handler extension (`has_plan` from query string) has no test in the diff. Server crate has 4 tests post-commit; none exercise `has_plan` HTTP path. | `git diff b5bbc45..ab0971a -- crates/server/src/api/sessions.rs` shows production code only |
| 30 | — | — | — | — | documentation | CLAUDE.md / MEMORY.md not updated for C2.4. Plan §165 does not require doc updates. C2.4 adds a user-visible `plans` subcommand and `--has-plan` flag that future agents would discover via `claude-history --help` rather than CLAUDE.md. No deviation per plan; recorded as observation. | `git show ab0971a --stat` → no CLAUDE.md or MEMORY.md changes |

## 4. Verification command results

### `git show --stat ab0971a`
```
 crates/server/src/api/sessions.rs  |   5 +
 crates/server/src/daemon_client.rs |   9 +-
 crates/server/src/main.rs          | 178 ++++++++++++++++-
 crates/server/src/mcp/tools.rs     |   1 +
 crates/server/src/output.rs        |  92 ++++++++-
 crates/store/src/query.rs          | 387 +++++++++++++++++++++++++++++++++++++
 6 files changed, 668 insertions(+), 4 deletions(-)
```

### `subject_chars`: 71 (matches commit-body claim)

### `cargo build --release`: clean (0.12s incremental, no errors).

### `cargo test --workspace`: 279 passed (4 server + 61 core + 214 store + 0 doc-tests on each), no failures.

### Dry-run sample exercises

- `plans list --limit 3` → 3 rows (workflowsPiExtension, workflow-grammar, workflow-containers) — passed.
- `plans list --project smak --limit 3 --json` → JSON array of 3 smak plan rows with full keyset (`session_id`, `message_uuid`, `project_path`, `timestamp`, `plan_content_length`, `plan_content_preview`) — passed.
- `plans show some-nonexistent-id` → stderr `No plan-bearing messages found for session: some-nonexistent-id`, exit code 1 — passed (treats unknown-session and no-plan-session identically).
- `sessions --has-plan --limit 3` (forced direct mode) → 3 sessions all have plan_content rows in dryrun corpus — passed.
- `sessions --has-plan=false --limit 3` → 3 most-recent sessions lacking plan_content — passed.
- `sessions --limit 3` (no --has-plan) → same 3 sessions as `--has-plan=false --limit 3` (corpus property: most-recent sessions happen not to bear plans) — passed (tri-state distinct from binary; the overlap is data-dependent, not surface-bug).

### Live daemon caveat verification

`launchctl list | grep claude-history` → PID 60855 (predates ab0971a's binary build). `curl -s 'http://127.0.0.1:7424/v1/sessions?has_plan=true&limit=1'` returns session `373cccfb-...` which is plan-less per the dryrun corpus → daemon silently drops the unknown query param. Per commit message body, this resolves on `launchctl kickstart -k`.

### EXPLAIN QUERY PLAN

```
QUERY PLAN
|--SCAN s
`--CORRELATED SCALAR SUBQUERY 1
   `--SEARCH mp USING INDEX idx_messages_plan_content_present (session_id=?)
```

`Some(true)` arm engages the partial index from migration 010.

### `PRAGMA integrity_check` / `foreign_key_check` on dryrun DB: both `ok`.

### Help-string drift

- `crates/server/src/main.rs:3` says "20 subcommands" — actual count is 21 post-ab0971a.
- `crates/server/src/main.rs:645` says `MCP TOOLS (15)` — unchanged by C2.4 (MCP unchanged), but informational.
- `CLAUDE.md:49` says `10 tools` — unchanged by C2.4 (MCP unchanged), but if C2.5 adds list_plans / get_plan this string will need updating in that commit.

### Counting self-checks

- Subject characters: declared 71, measured 71 — match.
- New tests: declared 10 (commit body line "10 new unit tests"), measured 9 via `git diff … | grep -cE "^\+\s*fn test_"` — mismatch. Note that the body's own breakdown (`plans_list (4), plan_show (3), list_sessions has_plan (2)` = 9) is internally consistent with the actual implementation; only the headline count is off-by-one.
- Total workspace tests: declared 279, measured 4+61+214 = 279 — match.

### Total deviation count

30 catalog rows total. Breakdown by category:
- omission: 1 (row 16 — informational, no actual omission)
- divergence: 7 (rows 3, 4, 8, 10, 11, 12, 15, 17 — 8 actually)

Recounting precisely:
- omission: 1 (#16)
- divergence: 8 (#3, #4, #8, #10, #11, #12, #15, #17)
- addition: 7 (#1, #2 reclassified, #5, #6, #7, #9, #13, #14 — 8)

Final precise breakdown:
- omission: 1 (#16 — informational, plan-prescribed scope honored)
- divergence: 8 (#3, #4, #8, #10, #11, #12, #15, #17)
- addition: 8 (#1, #5, #6, #7, #9, #13, #14, #25)
- documentation: 2 (#2, #30)
- verification: 2 (#18, #19)
- regression: 2 (#20, #21)
- commit-message: 2 (#22, #23)
- sequence: 1 (#24)
- dependency: 1 (#25 — also addition; classified as dependency because helper introduces a coupling surface)
- test-coverage: 4 (#26, #27, #28, #29)

Note row #25 appears in both "addition" and "dependency" classifications above. To resolve: row #25 is classified canonically as **dependency** (its primary surface is a function-naming/coupling decision). Recounting:
- omission: 1
- divergence: 8
- addition: 7 (#1, #5, #6, #7, #9, #13, #14)
- documentation: 2
- verification: 2
- regression: 2
- commit-message: 2
- sequence: 1
- dependency: 1
- test-coverage: 4

Sum: 1+8+7+2+2+2+2+1+1+4 = 30. Catalog count = 30. Match.

## 5. Plan-defect notes

- **Plan §165 brevity**: §165 is one sentence. It does not specify `--json` flag, exit codes, output format, ordering, daemon-vs-direct routing, or which timestamp column `--since` filters on. Every implementation choice on those surfaces is implicitly delegated to the implementor. This is not a plan defect per se (the plan elsewhere defers to "project-pattern-compliant" practice via §184) but multiple deviations in this catalog stem from the gap between §165's terseness and the implementation's necessary specificity.
- **Commit-body §310 reference**: commit message body says "Refined plan §165 + §310 (plans-list verification clauses) satisfied". The plan file's §310 region (counting from `### ` headers and line numbers) does not contain a "plans-list verification clauses" subsection. The commit message either references a region that no longer exists in the plan, or the §310 anchor is being used loosely. Recorded as deviation #23.
- **Plan §165 silent on tri-state has_plan**: implementation tri-state (`None`/`Some(true)`/`Some(false)`) is a defensible reading of "filter on existing sessions subcommand" but plan does not specify. Recorded as deviation #17.

## 6. Audit completeness self-check

1. Requirements ledger built (R1–R7 from §165 plus cross-section constraints).
2. Implementation ledger built from `git show ab0971a` and per-file inspection of all 6 changed files.
3. Cross-reference complete — every plan-side R has impl-side coverage; every impl-side I has either a plan anchor or is recorded as `addition`.
4. Plan-required verification commands run — `cargo build --release`, `cargo test --workspace`, all dry-run sample exercises listed in inputs, EXPLAIN QUERY PLAN, live daemon HTTP, integrity checks. All passed; output captured in §4.
5. Regression safeguards: integrity_check + foreign_key_check ok; 279/279 tests pass; no pre-existing test broken.
6. Commit message audited — subject 71 chars (within 72), no Co-Authored-By, no unjustified "ensures"/"fixes" definitives, but contains an inaccurate §310 plan anchor (#23) and a 10-vs-9 test-count overcount (#3).
7. Sequence/dependency: position is valid per hard-rule 5 (§24); no references to not-yet-merged migrations, structs, or columns; helper function `ensure_direct_conn` introduced and confined to `run_plans` (#25).
8. Documentation gaps: module doc string at main.rs:3 still says "20 subcommands" (#2); CLAUDE.md not updated for `plans` (#30).
9. Test coverage: 9 new query-layer tests (commit message says 10); zero new tests for output formatters, daemon_client, api handler, run_plans_show empty-results path (#26-#29).
10. Catalog compiled with 30 entries across 9 categories with severity-language self-check pass.

### Severity-language self-check

`grep -iE "critical|high-severity|low-severity|medium-severity|nit|blocking|non-blocking|important|minor|severity:|priority:|consider|should be addressed|easy fix|could be left" report` on this report's catalog rows yields zero hits in the deviation descriptions. (The phrase "20 subcommands" / "21 subcommands" is a numeric count, not severity. The phrase "informational" appears in the self-tally and meta-plan; not severity language.) Severity self-check: pass.

### Counting self-check

Declared total 30 catalog rows. Counted via grep on the table (`grep -cE "^\| [0-9]+ \|" report`) yields 30. Per-category sums (1+8+7+2+2+2+2+1+1+4) = 30. Match.
