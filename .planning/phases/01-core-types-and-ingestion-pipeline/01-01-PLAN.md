---
phase: 01-core-types-and-ingestion-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/core/Cargo.toml
  - crates/core/src/lib.rs
  - crates/store/Cargo.toml
  - crates/store/src/lib.rs
  - crates/store/src/db.rs
  - crates/store/src/schema.rs
  - crates/store/migrations/001_initial.sql
  - crates/server/Cargo.toml
  - crates/server/src/main.rs
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-07
  - STORE-01
  - STORE-02
  - STORE-03
  - STORE-04
  - STORE-05
  - STORE-06

must_haves:
  truths:
    - "Cargo workspace compiles with `cargo build` producing a single binary at target/debug/claude-history"
    - "Opening the database creates all 11+ tables via embedded migration"
    - "SQLite is in WAL mode with busy_timeout configured"
    - "schema_versions table tracks applied migration version"
    - "DB path is configurable via CLAUDE_HISTORY_DB_PATH env var or defaults to ~/.claude/.claude-history.db"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace root with 3 members"
      contains: "[workspace]"
    - path: "crates/store/migrations/001_initial.sql"
      provides: "DDL for all normalized tables"
      contains: "CREATE TABLE"
    - path: "crates/store/src/db.rs"
      provides: "Connection initialization with WAL and pragmas"
      contains: "journal_mode"
    - path: "crates/store/src/schema.rs"
      provides: "Migration runner with schema_versions tracking"
      contains: "schema_versions"
    - path: "crates/server/src/main.rs"
      provides: "Binary entry point"
      contains: "fn main"
  key_links:
    - from: "crates/server/Cargo.toml"
      to: "crates/store"
      via: "dependency declaration"
      pattern: "claude-history-store"
    - from: "crates/store/src/schema.rs"
      to: "crates/store/migrations/001_initial.sql"
      via: "include_str! embedding"
      pattern: "include_str!"
    - from: "crates/store/src/db.rs"
      to: "crates/store/src/schema.rs"
      via: "run_migrations call during init"
      pattern: "run_migrations"
---

<objective>
Create the Cargo workspace foundation with all three crates and the complete SQLite schema with embedded migrations. This plan establishes the project structure, database initialization, and all normalized tables that the decomposition engine will write to.

Purpose: Every subsequent plan depends on the workspace compiling and the database schema existing. This is the structural foundation.
Output: Compilable Cargo workspace, SQLite database with 11+ normalized tables, WAL mode, migration tracking.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-types-and-ingestion-pipeline/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cargo workspace with 3-crate structure and all dependencies</name>
  <files>
    Cargo.toml
    crates/core/Cargo.toml
    crates/core/src/lib.rs
    crates/store/Cargo.toml
    crates/store/src/lib.rs
    crates/server/Cargo.toml
    crates/server/src/main.rs
  </files>
  <action>
Create the Cargo workspace root with `resolver = "2"` and members `["crates/core", "crates/store", "crates/server"]`.

Define workspace.dependencies for all shared deps (see research 01-RESEARCH.md "Cargo.toml Dependencies" section):
- serde = { version = "1.0", features = ["derive"] }
- serde_json = "1.0"
- rusqlite = { version = "0.38", features = ["bundled", "fts5"] }
- tokio-rusqlite = "0.7"
- tokio = { version = "1", features = ["full"] }
- tracing = "0.1"
- tracing-subscriber = { version = "0.3", features = ["env-filter"] }
- clap = { version = "4", features = ["derive"] }
- walkdir = "2"
- anyhow = "1"
- thiserror = "2"

Crate names: `claude-history-core`, `claude-history-store`, `claude-history` (server is the binary).

**core crate** (lib): depends on serde, serde_json, thiserror, tracing. lib.rs exports an empty module for now.

**store crate** (lib): depends on core (path = "../core"), rusqlite, tokio-rusqlite, tracing, thiserror, serde, serde_json. lib.rs declares `pub mod db;` and `pub mod schema;`.

**server crate** (bin, name = "claude-history"): depends on store (path = "../store"), core (path = "../core"), tokio, tracing, tracing-subscriber, clap, walkdir, anyhow. main.rs has a minimal `#[tokio::main] async fn main()` that initializes tracing-subscriber with EnvFilter (default RUST_LOG=info) and prints "claude-history" for now. This confirms the binary compiles. [INFRA-01, INFRA-02, INFRA-07]

For INFRA-03: In main.rs, add a helper function `fn db_path() -> PathBuf` that checks `std::env::var("CLAUDE_HISTORY_DB_PATH")` and falls back to `~/.claude/.claude-history.db` using `dirs::home_dir()` or `std::env::var("HOME")`. Add the `dirs` crate to server dependencies if needed, or use HOME env var directly.
  </action>
  <verify>
Run `cargo build` from workspace root. Confirm it compiles without errors and produces `target/debug/claude-history` binary. Run `./target/debug/claude-history` and confirm it outputs something (not a crash).
  </verify>
  <done>
Workspace compiles. Binary exists at target/debug/claude-history. Three crates defined with correct dependency chain (server -> store -> core). tracing initialized. DB path resolution function exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SQLite schema (all normalized tables) with embedded migration and WAL initialization</name>
  <files>
    crates/store/migrations/001_initial.sql
    crates/store/src/schema.rs
    crates/store/src/db.rs
    crates/store/src/lib.rs
  </files>
  <action>
**001_initial.sql** — Write the complete DDL for all normalized tables. Reference the research section "STORE-01" and the decompose code examples for column names. Tables to create:

1. `sessions` — session_id TEXT PRIMARY KEY, project_path TEXT, first_seen_at TEXT, last_seen_at TEXT, version TEXT, slug TEXT, git_branch TEXT
2. `messages` — uuid TEXT PRIMARY KEY, session_id TEXT NOT NULL REFERENCES sessions(session_id), type TEXT NOT NULL (user/assistant/system), timestamp TEXT NOT NULL, parent_uuid TEXT, is_sidechain INTEGER NOT NULL DEFAULT 0, user_type TEXT, cwd TEXT, git_branch TEXT, version TEXT, slug TEXT, agent_id TEXT, is_meta INTEGER, model TEXT, stop_reason TEXT, request_id TEXT, subtype TEXT (for system records)
3. `message_content` — id INTEGER PRIMARY KEY AUTOINCREMENT, message_uuid TEXT NOT NULL REFERENCES messages(uuid), block_index INTEGER NOT NULL, block_type TEXT NOT NULL (text/thinking/tool_use/tool_result), text_content TEXT, tool_use_id TEXT, tool_name TEXT, tool_input TEXT (JSON), is_error INTEGER, thinking_signature TEXT, UNIQUE(message_uuid, block_index)
4. `token_usage` — message_uuid TEXT PRIMARY KEY REFERENCES messages(uuid), input_tokens INTEGER, output_tokens INTEGER, cache_creation_input_tokens INTEGER, cache_read_input_tokens INTEGER, service_tier TEXT, cache_creation_json TEXT (JSON blob for sub-object), extra_json TEXT (JSON blob for server_tool_use, iterations, etc.)
5. `tool_executions` — id INTEGER PRIMARY KEY AUTOINCREMENT, message_uuid TEXT NOT NULL REFERENCES messages(uuid), tool_use_id TEXT NOT NULL, tool_name TEXT NOT NULL, input_json TEXT, result_content TEXT, is_error INTEGER, UNIQUE(message_uuid, tool_use_id)
6. `agents` — agent_id TEXT PRIMARY KEY, session_id TEXT REFERENCES sessions(session_id), first_seen_at TEXT, last_seen_at TEXT
7. `queue_operations` — id INTEGER PRIMARY KEY AUTOINCREMENT, session_id TEXT NOT NULL, operation TEXT NOT NULL, timestamp TEXT NOT NULL, content TEXT
8. `progress_events` — id INTEGER PRIMARY KEY AUTOINCREMENT, uuid TEXT NOT NULL, session_id TEXT NOT NULL, timestamp TEXT NOT NULL, data_type TEXT NOT NULL, data_json TEXT NOT NULL
9. `system_events` — id INTEGER PRIMARY KEY AUTOINCREMENT, uuid TEXT NOT NULL, session_id TEXT NOT NULL, timestamp TEXT NOT NULL, subtype TEXT NOT NULL, level TEXT, duration_ms INTEGER, content TEXT, extra_json TEXT
10. `summaries` — id INTEGER PRIMARY KEY AUTOINCREMENT, session_id TEXT NOT NULL, summary TEXT NOT NULL, leaf_uuid TEXT NOT NULL
11. `sync_metadata` — file_path TEXT PRIMARY KEY, last_byte_offset INTEGER NOT NULL DEFAULT 0, record_count INTEGER NOT NULL DEFAULT 0, last_synced_at TEXT NOT NULL
12. `schema_versions` — version TEXT PRIMARY KEY, applied_at TEXT NOT NULL DEFAULT (datetime('now'))
13. `schema_drift_log` — id INTEGER PRIMARY KEY AUTOINCREMENT, field_name TEXT NOT NULL, record_type TEXT NOT NULL, version TEXT, sample_value TEXT, first_seen_at TEXT NOT NULL DEFAULT (datetime('now')), source_context TEXT, UNIQUE(field_name, record_type, version)

Add CREATE INDEX statements for: messages(session_id), messages(timestamp), message_content(message_uuid), tool_executions(message_uuid), tool_executions(tool_name), progress_events(session_id), progress_events(data_type), system_events(session_id), system_events(subtype), queue_operations(session_id). [STORE-01, STORE-02, STORE-04]

**schema.rs** — Implement the migration runner:
- Define `const MIGRATIONS: &[(&str, &str)]` with `("001", include_str!("../migrations/001_initial.sql"))`.
- `pub fn run_migrations(conn: &rusqlite::Connection) -> Result<()>` that: creates `schema_versions` table if not exists, iterates MIGRATIONS, checks if version already applied, if not applies via `unchecked_transaction()` and inserts version row.
- Use `thiserror` for a `SchemaError` type. [STORE-03, STORE-05]

**db.rs** — Implement connection initialization:
- `pub async fn init_db(path: &Path) -> Result<tokio_rusqlite::Connection>` that opens the connection, then calls into `conn.call()` to set pragmas: `PRAGMA journal_mode = WAL`, `busy_timeout(Duration::from_secs(5))`, `PRAGMA synchronous = NORMAL`, `PRAGMA foreign_keys = ON`, then runs migrations.
- Create parent directories if they don't exist before opening.
- Use `thiserror` for a `DbError` type. [STORE-06]

**lib.rs** — Update to export `pub mod db;` and `pub mod schema;`.
  </action>
  <verify>
Write a small test in `crates/store/src/db.rs` (or as integration test) that calls `init_db` with a temp file path, then queries `PRAGMA journal_mode` (should return "wal"), queries `SELECT version FROM schema_versions` (should return "001"), and confirms all tables exist via `SELECT name FROM sqlite_master WHERE type='table'`. Run with `cargo test -p claude-history-store`.
  </verify>
  <done>
All 13 tables created by migration 001. schema_versions tracks the migration. WAL mode confirmed. busy_timeout set. init_db function opens connection, applies pragmas and migrations, returns ready connection. Test passes.
  </done>
</task>

</tasks>

<verification>
1. `cargo build` succeeds from workspace root
2. `./target/debug/claude-history` runs without panic
3. `cargo test -p claude-history-store` passes (WAL mode, migration applied, tables exist)
4. Binary size is reasonable (< 50 MB debug, < 15 MB release)
</verification>

<success_criteria>
- Cargo workspace with 3 crates compiles to single binary
- SQLite database has all 13 tables after init_db()
- WAL mode active, busy_timeout set, foreign keys enabled
- schema_versions table records migration "001" as applied
- DB path configurable via CLAUDE_HISTORY_DB_PATH env var
- tracing initialized with env-filter in binary
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-types-and-ingestion-pipeline/01-01-SUMMARY.md`
</output>
